# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:5ea013ca8025bbeecf9dea45959c97fd951303a30f3a830991e36be618a88d24
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule TreeSitterLanguagePack.PackConfig do
  @moduledoc """
  Configuration for the tree-sitter language pack.

  Controls cache directory and which languages to pre-download.
  Can be loaded from a TOML file, constructed programmatically,
  or passed as a dict/object from language bindings.

  # Example

  ```no_run
  use tree_sitter_language_pack::PackConfig;

  let config = PackConfig {
      cache_dir: None,
      languages: Some(vec!["python".to_string(), "rust".to_string()]),
      groups: None,
  };
  ```
  """

  @typedoc "Configuration for the tree-sitter language pack."
  @type t :: %__MODULE__{
          cache_dir: String.t() | nil,
          languages: [String.t()] | nil,
          groups: [String.t()] | nil
        }

  defstruct cache_dir: nil,
            languages: nil,
            groups: nil

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
