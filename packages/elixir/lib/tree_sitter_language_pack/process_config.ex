# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:3de0dc74b8d75abc9645f668b829dd4c055eed907cb4457ead0c65bc03cc2fe4
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule TreeSitterLanguagePack.ProcessConfig do
  @moduledoc """
  Configuration for the `process()` function.

  Controls which analysis features are enabled and whether chunking is performed.

  # Examples

  ```
  use tree_sitter_language_pack::ProcessConfig;

  // Defaults: structure + imports + exports enabled
  let config = ProcessConfig::new("python");

  // With chunking
  let config = ProcessConfig::new("python").with_chunking(1000);

  // Everything enabled
  let config = ProcessConfig::new("python").all();
  ```
  """

  @typedoc "Configuration for the `process()` function."
  @type t :: %__MODULE__{
          language: String.t() | nil,
          structure: boolean(),
          imports: boolean(),
          exports: boolean(),
          comments: boolean(),
          docstrings: boolean(),
          symbols: boolean(),
          diagnostics: boolean(),
          chunk_max_size: non_neg_integer() | nil,
          data_extraction: boolean(),
          max_source_bytes: non_neg_integer() | nil,
          parse_timeout_ms: non_neg_integer() | nil
        }

  defstruct language: "",
            structure: true,
            imports: true,
            exports: true,
            comments: false,
            docstrings: false,
            symbols: false,
            diagnostics: false,
            chunk_max_size: nil,
            data_extraction: false,
            max_source_bytes: nil,
            parse_timeout_ms: nil

  defimpl Jason.Encoder do
    @doc false
    def encode(value, opts) do
      value
      |> Map.from_struct()
      |> Enum.reject(fn {_k, v} -> v == nil end)
      |> Enum.into(%{})
      |> Jason.Encoder.encode(opts)
    end
  end
end
