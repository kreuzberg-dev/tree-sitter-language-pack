// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:489967ed25415d58cc96491972adb858342dd7f9cfadd132bc71602a83dd79de
// alef:handle-abi:1
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.treesitterlanguagepack;

import java.lang.foreign.Arena;
import java.lang.foreign.FunctionDescriptor;
import java.lang.foreign.Linker;
import java.lang.foreign.SymbolLookup;
import java.lang.foreign.ValueLayout;
import java.lang.invoke.MethodHandle;
import java.io.File;
import java.net.URL;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.nio.file.StandardCopyOption;
import java.util.ArrayList;
import java.util.Enumeration;
import java.util.List;
import java.util.jar.JarEntry;
import java.util.jar.JarFile;

@SuppressWarnings({"PMD.AvoidCatchingGenericException", "PMD"})
final class NativeLib {
    private static final System.Logger LOGGER = System.getLogger(NativeLib.class.getName());
    private static final Linker LINKER = Linker.nativeLinker();
    private static SymbolLookup LIB;
    private static final String NATIVES_RESOURCE_ROOT = "/natives";
    private static final String LIBRARY_PATH_PROPERTY = "ts_pack_core_ffi.library.path";
    private static final String LIBRARY_DIRECTORY_PROPERTY = "ts_pack_core_ffi.library.dir";
    private static final String SKIP_BUNDLED_PROPERTY = "ts_pack_core_ffi.skipBundled";
    private static final String LIBRARY_PATH_ENV = "TS_PACK_CORE_FFI_LIB_PATH";
    private static final String LIBRARY_DIRECTORY_ENV = "TS_PACK_CORE_FFI_LIB_DIR";
    private static final String SKIP_BUNDLED_ENV = "TS_PACK_CORE_FFI_SKIP_BUNDLED";
    private static final String[] REQUIRED_SYMBOLS = {
        "ts_pack_available_languages",
        "ts_pack_byte_range_free",
        "ts_pack_byte_range_from_json",
        "ts_pack_byte_range_to_json",
        "ts_pack_cache_dir",
        "ts_pack_cache_dir_len",
        "ts_pack_chunk_context_free",
        "ts_pack_chunk_context_from_json",
        "ts_pack_clean_cache",
        "ts_pack_code_chunk_free",
        "ts_pack_code_chunk_from_json",
        "ts_pack_comment_info_free",
        "ts_pack_comment_info_from_json",
        "ts_pack_configure",
        "ts_pack_data_attribute_free",
        "ts_pack_data_attribute_from_json",
        "ts_pack_data_node_free",
        "ts_pack_data_node_from_json",
        "ts_pack_detect_language",
        "ts_pack_detect_language_from_content",
        "ts_pack_detect_language_from_content_len",
        "ts_pack_detect_language_from_extension",
        "ts_pack_detect_language_from_extension_len",
        "ts_pack_detect_language_from_path",
        "ts_pack_detect_language_from_path_len",
        "ts_pack_detect_language_len",
        "ts_pack_diagnostic_free",
        "ts_pack_diagnostic_from_json",
        "ts_pack_doc_section_free",
        "ts_pack_doc_section_from_json",
        "ts_pack_docstring_info_free",
        "ts_pack_docstring_info_from_json",
        "ts_pack_download",
        "ts_pack_download_all",
        "ts_pack_download_group",
        "ts_pack_download_manager_clean_cache",
        "ts_pack_download_manager_download_all_best_effort",
        "ts_pack_download_manager_free",
        "ts_pack_download_manager_installed_languages",
        "ts_pack_download_manager_new",
        "ts_pack_downloaded_languages",
        "ts_pack_export_info_free",
        "ts_pack_export_info_from_json",
        "ts_pack_file_metrics_free",
        "ts_pack_file_metrics_from_json",
        "ts_pack_free_string",
        "ts_pack_get_folds_query",
        "ts_pack_get_folds_query_len",
        "ts_pack_get_highlights_query",
        "ts_pack_get_highlights_query_len",
        "ts_pack_get_indents_query",
        "ts_pack_get_indents_query_len",
        "ts_pack_get_injections_query",
        "ts_pack_get_injections_query_len",
        "ts_pack_get_language",
        "ts_pack_get_locals_query",
        "ts_pack_get_locals_query_len",
        "ts_pack_get_parser",
        "ts_pack_get_tags_query",
        "ts_pack_get_tags_query_len",
        "ts_pack_has_language",
        "ts_pack_import_info_free",
        "ts_pack_import_info_from_json",
        "ts_pack_init",
        "ts_pack_language_count",
        "ts_pack_language_free",
        "ts_pack_language_registry_available_languages",
        "ts_pack_language_registry_free",
        "ts_pack_language_registry_get_language",
        "ts_pack_language_registry_has_language",
        "ts_pack_language_registry_has_parser",
        "ts_pack_language_registry_language_count",
        "ts_pack_language_registry_new",
        "ts_pack_language_registry_process",
        "ts_pack_last_error_code",
        "ts_pack_last_error_context",
        "ts_pack_manifest_groups",
        "ts_pack_manifest_languages",
        "ts_pack_node_byte_range",
        "ts_pack_node_child",
        "ts_pack_node_child_by_field_name",
        "ts_pack_node_child_count",
        "ts_pack_node_end_byte",
        "ts_pack_node_end_position",
        "ts_pack_node_free",
        "ts_pack_node_has_error",
        "ts_pack_node_is_error",
        "ts_pack_node_is_extra",
        "ts_pack_node_is_missing",
        "ts_pack_node_is_named",
        "ts_pack_node_kind",
        "ts_pack_node_kind_id",
        "ts_pack_node_named_child",
        "ts_pack_node_named_child_count",
        "ts_pack_node_parent",
        "ts_pack_node_start_byte",
        "ts_pack_node_start_position",
        "ts_pack_node_to_sexp",
        "ts_pack_node_walk",
        "ts_pack_pack_config_free",
        "ts_pack_pack_config_from_json",
        "ts_pack_parser_free",
        "ts_pack_parser_new",
        "ts_pack_parser_parse",
        "ts_pack_parser_parse_bytes",
        "ts_pack_parser_reset",
        "ts_pack_parser_set_language",
        "ts_pack_parser_set_max_source_bytes",
        "ts_pack_parser_set_parse_timeout_ms",
        "ts_pack_point_free",
        "ts_pack_point_from_json",
        "ts_pack_point_to_json",
        "ts_pack_prefetch",
        "ts_pack_process",
        "ts_pack_process_config_free",
        "ts_pack_process_config_from_json",
        "ts_pack_process_result_free",
        "ts_pack_process_result_from_json",
        "ts_pack_process_result_to_json",
        "ts_pack_span_free",
        "ts_pack_span_from_json",
        "ts_pack_structure_item_free",
        "ts_pack_structure_item_from_json",
        "ts_pack_symbol_info_free",
        "ts_pack_symbol_info_from_json",
        "ts_pack_tree_cursor_field_name",
        "ts_pack_tree_cursor_free",
        "ts_pack_tree_cursor_goto_first_child",
        "ts_pack_tree_cursor_goto_next_sibling",
        "ts_pack_tree_cursor_goto_parent",
        "ts_pack_tree_cursor_node",
        "ts_pack_tree_free",
        "ts_pack_tree_root_node",
        "ts_pack_tree_walk"
    };
    private static final Object NATIVE_EXTRACT_LOCK = new Object();
    private static String cachedExtractKey;
    private static Path cachedExtractDir;

    static {
        Path loadedLibraryPath = loadNativeLibrary();
        try {
            Arena arena = Arena.ofShared();
            // Try the loaded library path (for System.load() with absolute path)
            if (loadedLibraryPath != null) {
                try {
                    LIB = SymbolLookup.libraryLookup(loadedLibraryPath, arena);
                } catch (RuntimeException | Error inner) {
                    // If Path variant fails, fallback to defaultLookup
                    LIB = LINKER.defaultLookup();
                }
            } else {
                LIB = LINKER.defaultLookup();
            }
            validateRequiredSymbols(loadedLibraryPath);
        } catch (RuntimeException | Error e) {
            ExceptionInInitializerError error = new ExceptionInInitializerError(
                "Failed to initialize library symbols: " + e.getMessage());
            error.initCause(e);
            throw error;
        }
    }

    private static Path loadNativeLibrary() {
        Path loadedLibraryPath = null;
        String osName = System.getProperty("os.name", "").toLowerCase(java.util.Locale.ROOT);
        String osArch = System.getProperty("os.arch", "").toLowerCase(java.util.Locale.ROOT);

        String libName;
        String libExt;
        if (osName.contains("mac") || osName.contains("darwin")) {
            libName = "libts_pack_core_ffi";
            libExt = ".dylib";
        } else if (osName.contains("win")) {
            libName = "ts_pack_core_ffi";
            libExt = ".dll";
        } else {
            libName = "libts_pack_core_ffi";
            libExt = ".so";
        }

        String nativesRid = resolveNativesRid(osName, osArch);
        String nativesDir = NATIVES_RESOURCE_ROOT + "/" + nativesRid;

        String explicitPath = firstNonBlank(System.getProperty(LIBRARY_PATH_PROPERTY), System.getenv(LIBRARY_PATH_ENV));
        if (explicitPath != null) {
            return loadAbsolutePath(Paths.get(explicitPath));
        }

        String explicitDirectory = firstNonBlank(
                System.getProperty(LIBRARY_DIRECTORY_PROPERTY),
                System.getenv(LIBRARY_DIRECTORY_ENV));
        if (explicitDirectory != null) {
            return loadAbsolutePath(Paths.get(explicitDirectory, libName + libExt));
        }

        String skipBundledValue = firstNonBlank(
                System.getProperty(SKIP_BUNDLED_PROPERTY),
                System.getenv(SKIP_BUNDLED_ENV));
        if (!Boolean.parseBoolean(skipBundledValue)) {
            Path extracted = tryExtractAndLoadFromResources(nativesDir, libName, libExt);
            if (extracted != null) {
                return extracted;
            }
        }

        try {
            System.loadLibrary("ts_pack_core_ffi");
            // Find the full path by searching java.library.path
            Path libPath = findLoadedLibraryPath(libName, libExt);
            if (libPath != null) {
                loadedLibraryPath = libPath;
            }
        } catch (UnsatisfiedLinkError e) {
            String msg = "Failed to load ts_pack_core_ffi native library. Expected resource: " + nativesDir + "/" + libName
                    + libExt + " (RID: " + nativesRid + "). "
                    + "Ensure the library is bundled in the JAR under natives/{os-arch}/, "
                    + "place it on java.library.path, set -D" + LIBRARY_PATH_PROPERTY
                    + "=/absolute/path, or set " + LIBRARY_PATH_ENV + ".";
            UnsatisfiedLinkError out = new UnsatisfiedLinkError(msg + " Original error: " + e.getMessage());
            out.initCause(e);
            throw out;
        }
        return loadedLibraryPath;
    }

    private static String firstNonBlank(String preferred, String fallback) {
        if (preferred != null && !preferred.isBlank()) {
            return preferred;
        }
        if (fallback != null && !fallback.isBlank()) {
            return fallback;
        }
        return null;
    }

    private static Path loadAbsolutePath(Path configuredPath) {
        Path absolutePath = configuredPath.toAbsolutePath().normalize();
        if (!Files.isRegularFile(absolutePath)) {
            throw new UnsatisfiedLinkError("Configured native library does not exist: " + absolutePath);
        }
        System.load(absolutePath.toString());
        return absolutePath;
    }

    private static void validateRequiredSymbols(Path loadedLibraryPath) {
        List<String> missing = new ArrayList<>();
        for (String symbol : REQUIRED_SYMBOLS) {
            if (LIB.find(symbol).isEmpty() && LIB.find("_" + symbol).isEmpty()) {
                missing.add(symbol);
            }
        }
        if (missing.isEmpty()) {
            return;
        }

        int exportedCount = REQUIRED_SYMBOLS.length - missing.size();
        String loadedFrom = loadedLibraryPath == null
                ? "System.loadLibrary(ts_pack_core_ffi) / java.library.path"
                : loadedLibraryPath.toString();
        throw new UnsatisfiedLinkError(
                "The loaded ts_pack_core_ffi native library is stale relative to this binding: it exports "
                        + exportedCount + " of " + REQUIRED_SYMBOLS.length + " required symbols. Missing: "
                        + String.join(", ", missing) + ". Loaded from: " + loadedFrom
                        + ". Rebuild the native library, point this binding at a current one with -D"
                        + LIBRARY_PATH_PROPERTY + "=/absolute/path, or bypass bundled resources with -D"
                        + SKIP_BUNDLED_PROPERTY + "=true.");
    }

    private static Path tryExtractAndLoadFromResources(String nativesDir, String libName, String libExt) {
        String resourcePath = nativesDir + "/" + libName + libExt;
        URL resource = NativeLib.class.getResource(resourcePath);
        if (resource == null) {
            return null;
        }

        try {
            Path tempDir = extractOrReuseNativeDirectory(nativesDir);
            Path libPath = tempDir.resolve(libName + libExt);
            if (!Files.exists(libPath)) {
                throw new UnsatisfiedLinkError("Missing extracted native library: " + libPath);
            }
            Path absPath = libPath.toAbsolutePath();
            System.load(absPath.toString());
            return absPath;
        } catch (Exception | Error e) {
            LOGGER.log(System.Logger.Level.WARNING, "Failed to extract and load native library from resources", e);
            return null;
        }
    }

    private static Path extractOrReuseNativeDirectory(String nativesDir) throws Exception {
        URL location = NativeLib.class.getProtectionDomain().getCodeSource().getLocation();
        if (location == null) {
            throw new IllegalStateException("Missing code source location for ts_pack_core_ffi JAR");
        }

        Path codePath = Path.of(location.toURI());
        String key = codePath.toAbsolutePath() + "::" + nativesDir;

        synchronized (NATIVE_EXTRACT_LOCK) {
            if (cachedExtractDir != null && key.equals(cachedExtractKey)) {
                return cachedExtractDir;
            }
            Path tempDir = Files.createTempDirectory("ts_pack_core_ffi_native");
            tempDir.toFile().deleteOnExit();
            List<Path> extracted = extractNativeDirectory(codePath, nativesDir, tempDir);
            if (extracted.isEmpty()) {
                throw new IllegalStateException("No native files extracted from resources dir: " + nativesDir);
            }
            cachedExtractKey = key;
            cachedExtractDir = tempDir;
            return tempDir;
        }
    }

    private static List<Path> extractNativeDirectory(Path codePath, String nativesDir, Path destDir) throws Exception {
        if (!Files.exists(destDir) || !Files.isDirectory(destDir)) {
            throw new IllegalArgumentException("Destination directory does not exist: " + destDir);
        }

        String prefix = nativesDir.startsWith("/") ? nativesDir.substring(1) : nativesDir;
        if (!prefix.endsWith("/")) {
            prefix = prefix + "/";
        }

        if (Files.isDirectory(codePath)) {
            Path nativesPath = codePath.resolve(prefix);
            if (!Files.exists(nativesPath) || !Files.isDirectory(nativesPath)) {
                return List.of();
            }
            return copyDirectory(nativesPath, destDir);
        }

        List<Path> extracted = new ArrayList<>();
        try (JarFile jar = new JarFile(codePath.toFile())) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (!name.startsWith(prefix) || entry.isDirectory()) {
                    continue;
                }
                String relative = name.substring(prefix.length());
                Path out = safeResolve(destDir, relative);
                Files.createDirectories(out.getParent());
                try (var in = jar.getInputStream(entry)) {
                    Files.copy(in, out, StandardCopyOption.REPLACE_EXISTING);
                }
                out.toFile().deleteOnExit();
                extracted.add(out);
            }
        }
        return extracted;
    }

    private static List<Path> copyDirectory(Path srcDir, Path destDir) throws Exception {
        List<Path> copied = new ArrayList<>();
        try (var paths = Files.walk(srcDir)) {
            for (Path src : (Iterable<Path>) paths::iterator) {
                if (Files.isDirectory(src)) {
                    continue;
                }
                Path relative = srcDir.relativize(src);
                Path out = safeResolve(destDir, relative.toString());
                Files.createDirectories(out.getParent());
                Files.copy(src, out, StandardCopyOption.REPLACE_EXISTING);
                out.toFile().deleteOnExit();
                copied.add(out);
            }
        }
        return copied;
    }

    private static Path safeResolve(Path destDir, String relative) throws Exception {
        Path normalizedDest = destDir.toAbsolutePath().normalize();
        Path out = normalizedDest.resolve(relative).normalize();
        if (!out.startsWith(normalizedDest)) {
            throw new SecurityException("Blocked extracting native file outside destination directory: " + relative);
        }
        return out;
    }

    private static String resolveNativesRid(String osName, String osArch) {
        // Classifier names match go_java_platform(): `linux-aarch64`, `macos-arm64`,
        // `windows-x86_64`, etc. macOS distinguishes arm64 (ARM-based), while other
        // platforms use aarch64. Align with src/publish/platform.rs::go_java_platform().
        boolean isMac = osName.contains("mac") || osName.contains("darwin");
        boolean isWindows = osName.contains("win");

        String arch;
        if (osArch.contains("aarch64") || osArch.contains("arm64")) {
            // macOS uses "arm64" for aarch64; Linux uses "aarch64"
            arch = isMac ? "arm64" : "aarch64";
        } else if (osArch.contains("x86_64") || osArch.contains("amd64")) {
            arch = "x86_64";
        } else {
            arch = osArch.replaceAll("[^a-z0-9_]+", "");
        }

        String os;
        if (isMac) {
            os = "macos";
        } else if (isWindows) {
            os = "windows";
        } else {
            os = "linux";
        }

        return os + "-" + arch;
    }

    private static Path findLoadedLibraryPath(String fullLibName, String libExt) {
        // Search java.library.path for the library file
        String javaLibPath = System.getProperty("java.library.path");
        if (javaLibPath != null) {
            for (String path : javaLibPath.split(File.pathSeparator)) {
                Path libPath = Paths.get(path, fullLibName + libExt);
                if (Files.exists(libPath)) {
                    try {
                        return libPath.toRealPath();
                    } catch (java.io.IOException e) {
                        return libPath.toAbsolutePath();
                    }
                }
            }
        }
        // Library not found in java.library.path
        return null;
    }


    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_EXTENSION = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_extension")
            .or(() -> LIB.find("_ts_pack_detect_language_from_extension"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_extension"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_extension"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_detect_language_from_extension" +
                " (tried: ts_pack_detect_language_from_extension, _ts_pack_detect_language_from_extension). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_EXTENSION_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_extension_len")
            .or(() -> LIB.find("_ts_pack_detect_language_from_extension_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_extension_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_extension_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_PATH = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_path")
            .or(() -> LIB.find("_ts_pack_detect_language_from_path"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_path"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_path"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_detect_language_from_path" +
                " (tried: ts_pack_detect_language_from_path, _ts_pack_detect_language_from_path). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_PATH_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_path_len")
            .or(() -> LIB.find("_ts_pack_detect_language_from_path_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_path_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_path_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_CONTENT = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_content")
            .or(() -> LIB.find("_ts_pack_detect_language_from_content"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_content"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_content"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_detect_language_from_content" +
                " (tried: ts_pack_detect_language_from_content, _ts_pack_detect_language_from_content). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_DETECT_LANGUAGE_FROM_CONTENT_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_from_content_len")
            .or(() -> LIB.find("_ts_pack_detect_language_from_content_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_from_content_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_from_content_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_HIGHLIGHTS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_highlights_query")
            .or(() -> LIB.find("_ts_pack_get_highlights_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_highlights_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_highlights_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_highlights_query" +
                " (tried: ts_pack_get_highlights_query, _ts_pack_get_highlights_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_HIGHLIGHTS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_highlights_query_len")
            .or(() -> LIB.find("_ts_pack_get_highlights_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_highlights_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_highlights_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_INJECTIONS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_injections_query")
            .or(() -> LIB.find("_ts_pack_get_injections_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_injections_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_injections_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_injections_query" +
                " (tried: ts_pack_get_injections_query, _ts_pack_get_injections_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_INJECTIONS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_injections_query_len")
            .or(() -> LIB.find("_ts_pack_get_injections_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_injections_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_injections_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_LOCALS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_locals_query")
            .or(() -> LIB.find("_ts_pack_get_locals_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_locals_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_locals_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_locals_query" +
                " (tried: ts_pack_get_locals_query, _ts_pack_get_locals_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_LOCALS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_locals_query_len")
            .or(() -> LIB.find("_ts_pack_get_locals_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_locals_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_locals_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_TAGS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_tags_query")
            .or(() -> LIB.find("_ts_pack_get_tags_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_tags_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_tags_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_tags_query" +
                " (tried: ts_pack_get_tags_query, _ts_pack_get_tags_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_TAGS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_tags_query_len")
            .or(() -> LIB.find("_ts_pack_get_tags_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_tags_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_tags_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_INDENTS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_indents_query")
            .or(() -> LIB.find("_ts_pack_get_indents_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_indents_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_indents_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_indents_query" +
                " (tried: ts_pack_get_indents_query, _ts_pack_get_indents_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_INDENTS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_indents_query_len")
            .or(() -> LIB.find("_ts_pack_get_indents_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_indents_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_indents_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_FOLDS_QUERY = LINKER.downcallHandle(
        LIB.find("ts_pack_get_folds_query")
            .or(() -> LIB.find("_ts_pack_get_folds_query"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_folds_query"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_folds_query"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_folds_query" +
                " (tried: ts_pack_get_folds_query, _ts_pack_get_folds_query). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_GET_FOLDS_QUERY_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_get_folds_query_len")
            .or(() -> LIB.find("_ts_pack_get_folds_query_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_folds_query_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_folds_query_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_get_language")
            .or(() -> LIB.find("_ts_pack_get_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_language" +
                " (tried: ts_pack_get_language, _ts_pack_get_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_GET_PARSER = LINKER.downcallHandle(
        LIB.find("ts_pack_get_parser")
            .or(() -> LIB.find("_ts_pack_get_parser"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_get_parser"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_get_parser"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_get_parser" +
                " (tried: ts_pack_get_parser, _ts_pack_get_parser). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DETECT_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language")
            .or(() -> LIB.find("_ts_pack_detect_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_detect_language" +
                " (tried: ts_pack_detect_language, _ts_pack_detect_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_DETECT_LANGUAGE_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_detect_language_len")
            .or(() -> LIB.find("_ts_pack_detect_language_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_detect_language_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_detect_language_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_AVAILABLE_LANGUAGES = LINKER.downcallHandle(
        LIB.find("ts_pack_available_languages")
            .or(() -> LIB.find("_ts_pack_available_languages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_available_languages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_available_languages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_available_languages" +
                " (tried: ts_pack_available_languages, _ts_pack_available_languages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_HAS_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_has_language")
            .or(() -> LIB.find("_ts_pack_has_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_has_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_has_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_has_language" +
                " (tried: ts_pack_has_language, _ts_pack_has_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_COUNT = LINKER.downcallHandle(
        LIB.find("ts_pack_language_count")
            .or(() -> LIB.find("_ts_pack_language_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_count" +
                " (tried: ts_pack_language_count, _ts_pack_language_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_PROCESS = LINKER.downcallHandle(
        LIB.find("ts_pack_process")
            .or(() -> LIB.find("_ts_pack_process"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_process"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_process"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_process" +
                " (tried: ts_pack_process, _ts_pack_process). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_INIT = LINKER.downcallHandle(
        LIB.find("ts_pack_init")
            .or(() -> LIB.find("_ts_pack_init"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_init"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_init"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_init" +
                " (tried: ts_pack_init, _ts_pack_init). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CONFIGURE = LINKER.downcallHandle(
        LIB.find("ts_pack_configure")
            .or(() -> LIB.find("_ts_pack_configure"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_configure"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_configure"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_configure" +
                " (tried: ts_pack_configure, _ts_pack_configure). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD = LINKER.downcallHandle(
        LIB.find("ts_pack_download")
            .or(() -> LIB.find("_ts_pack_download"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download" +
                " (tried: ts_pack_download, _ts_pack_download). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PREFETCH = LINKER.downcallHandle(
        LIB.find("ts_pack_prefetch")
            .or(() -> LIB.find("_ts_pack_prefetch"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_prefetch"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_prefetch"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_prefetch" +
                " (tried: ts_pack_prefetch, _ts_pack_prefetch). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_ALL = LINKER.downcallHandle(
        LIB.find("ts_pack_download_all")
            .or(() -> LIB.find("_ts_pack_download_all"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_all"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_all"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_all" +
                " (tried: ts_pack_download_all, _ts_pack_download_all). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_GROUP = LINKER.downcallHandle(
        LIB.find("ts_pack_download_group")
            .or(() -> LIB.find("_ts_pack_download_group"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_group"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_group"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_group" +
                " (tried: ts_pack_download_group, _ts_pack_download_group). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_MANIFEST_LANGUAGES = LINKER.downcallHandle(
        LIB.find("ts_pack_manifest_languages")
            .or(() -> LIB.find("_ts_pack_manifest_languages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_manifest_languages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_manifest_languages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_manifest_languages" +
                " (tried: ts_pack_manifest_languages, _ts_pack_manifest_languages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_MANIFEST_GROUPS = LINKER.downcallHandle(
        LIB.find("ts_pack_manifest_groups")
            .or(() -> LIB.find("_ts_pack_manifest_groups"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_manifest_groups"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_manifest_groups"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_manifest_groups" +
                " (tried: ts_pack_manifest_groups, _ts_pack_manifest_groups). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOADED_LANGUAGES = LINKER.downcallHandle(
        LIB.find("ts_pack_downloaded_languages")
            .or(() -> LIB.find("_ts_pack_downloaded_languages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_downloaded_languages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_downloaded_languages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_downloaded_languages" +
                " (tried: ts_pack_downloaded_languages, _ts_pack_downloaded_languages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CLEAN_CACHE = LINKER.downcallHandle(
        LIB.find("ts_pack_clean_cache")
            .or(() -> LIB.find("_ts_pack_clean_cache"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_clean_cache"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_clean_cache"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_clean_cache" +
                " (tried: ts_pack_clean_cache, _ts_pack_clean_cache). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid()
    );


    static final MethodHandle TS_PACK_CACHE_DIR = LINKER.downcallHandle(
        LIB.find("ts_pack_cache_dir")
            .or(() -> LIB.find("_ts_pack_cache_dir"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_cache_dir"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_cache_dir"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_cache_dir" +
                " (tried: ts_pack_cache_dir, _ts_pack_cache_dir). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );

    static final MethodHandle TS_PACK_CACHE_DIR_LEN = LINKER.downcallHandle(
        LIB.find("ts_pack_cache_dir_len")
            .or(() -> LIB.find("_ts_pack_cache_dir_len"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_cache_dir_len"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_cache_dir_len"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_FREE_STRING = LINKER.downcallHandle(
        LIB.find("ts_pack_free_string")
            .or(() -> LIB.find("_ts_pack_free_string"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_free_string"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_free_string"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PARSER_NEW = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_new")
            .or(() -> LIB.find("_ts_pack_parser_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_new" +
                " (tried: ts_pack_parser_new, _ts_pack_parser_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PARSER_SET_MAX_SOURCE_BYTES = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_set_max_source_bytes")
            .or(() -> LIB.find("_ts_pack_parser_set_max_source_bytes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_set_max_source_bytes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_set_max_source_bytes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_set_max_source_bytes" +
                " (tried: ts_pack_parser_set_max_source_bytes, _ts_pack_parser_set_max_source_bytes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_PARSER_SET_PARSE_TIMEOUT_MS = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_set_parse_timeout_ms")
            .or(() -> LIB.find("_ts_pack_parser_set_parse_timeout_ms"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_set_parse_timeout_ms"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_set_parse_timeout_ms"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_set_parse_timeout_ms" +
                " (tried: ts_pack_parser_set_parse_timeout_ms, _ts_pack_parser_set_parse_timeout_ms). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_PARSER_SET_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_set_language")
            .or(() -> LIB.find("_ts_pack_parser_set_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_set_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_set_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_set_language" +
                " (tried: ts_pack_parser_set_language, _ts_pack_parser_set_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PARSER_PARSE = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_parse")
            .or(() -> LIB.find("_ts_pack_parser_parse"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_parse"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_parse"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_parse" +
                " (tried: ts_pack_parser_parse, _ts_pack_parser_parse). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PARSER_PARSE_BYTES = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_parse_bytes")
            .or(() -> LIB.find("_ts_pack_parser_parse_bytes"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_parse_bytes"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_parse_bytes"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_parse_bytes" +
                " (tried: ts_pack_parser_parse_bytes, _ts_pack_parser_parse_bytes). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_PARSER_RESET = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_reset")
            .or(() -> LIB.find("_ts_pack_parser_reset"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_reset"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_reset"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_parser_reset" +
                " (tried: ts_pack_parser_reset, _ts_pack_parser_reset). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_ROOT_NODE = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_root_node")
            .or(() -> LIB.find("_ts_pack_tree_root_node"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_root_node"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_root_node"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_root_node" +
                " (tried: ts_pack_tree_root_node, _ts_pack_tree_root_node). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_WALK = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_walk")
            .or(() -> LIB.find("_ts_pack_tree_walk"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_walk"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_walk"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_walk" +
                " (tried: ts_pack_tree_walk, _ts_pack_tree_walk). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_KIND = LINKER.downcallHandle(
        LIB.find("ts_pack_node_kind")
            .or(() -> LIB.find("_ts_pack_node_kind"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_kind"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_kind"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_kind" +
                " (tried: ts_pack_node_kind, _ts_pack_node_kind). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_KIND_ID = LINKER.downcallHandle(
        LIB.find("ts_pack_node_kind_id")
            .or(() -> LIB.find("_ts_pack_node_kind_id"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_kind_id"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_kind_id"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_kind_id" +
                " (tried: ts_pack_node_kind_id, _ts_pack_node_kind_id). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_START_BYTE = LINKER.downcallHandle(
        LIB.find("ts_pack_node_start_byte")
            .or(() -> LIB.find("_ts_pack_node_start_byte"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_start_byte"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_start_byte"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_start_byte" +
                " (tried: ts_pack_node_start_byte, _ts_pack_node_start_byte). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_END_BYTE = LINKER.downcallHandle(
        LIB.find("ts_pack_node_end_byte")
            .or(() -> LIB.find("_ts_pack_node_end_byte"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_end_byte"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_end_byte"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_end_byte" +
                " (tried: ts_pack_node_end_byte, _ts_pack_node_end_byte). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_BYTE_RANGE = LINKER.downcallHandle(
        LIB.find("ts_pack_node_byte_range")
            .or(() -> LIB.find("_ts_pack_node_byte_range"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_byte_range"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_byte_range"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_byte_range" +
                " (tried: ts_pack_node_byte_range, _ts_pack_node_byte_range). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_START_POSITION = LINKER.downcallHandle(
        LIB.find("ts_pack_node_start_position")
            .or(() -> LIB.find("_ts_pack_node_start_position"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_start_position"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_start_position"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_start_position" +
                " (tried: ts_pack_node_start_position, _ts_pack_node_start_position). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_END_POSITION = LINKER.downcallHandle(
        LIB.find("ts_pack_node_end_position")
            .or(() -> LIB.find("_ts_pack_node_end_position"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_end_position"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_end_position"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_end_position" +
                " (tried: ts_pack_node_end_position, _ts_pack_node_end_position). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_IS_NAMED = LINKER.downcallHandle(
        LIB.find("ts_pack_node_is_named")
            .or(() -> LIB.find("_ts_pack_node_is_named"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_is_named"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_is_named"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_is_named" +
                " (tried: ts_pack_node_is_named, _ts_pack_node_is_named). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_IS_ERROR = LINKER.downcallHandle(
        LIB.find("ts_pack_node_is_error")
            .or(() -> LIB.find("_ts_pack_node_is_error"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_is_error"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_is_error"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_is_error" +
                " (tried: ts_pack_node_is_error, _ts_pack_node_is_error). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_IS_MISSING = LINKER.downcallHandle(
        LIB.find("ts_pack_node_is_missing")
            .or(() -> LIB.find("_ts_pack_node_is_missing"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_is_missing"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_is_missing"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_is_missing" +
                " (tried: ts_pack_node_is_missing, _ts_pack_node_is_missing). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_IS_EXTRA = LINKER.downcallHandle(
        LIB.find("ts_pack_node_is_extra")
            .or(() -> LIB.find("_ts_pack_node_is_extra"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_is_extra"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_is_extra"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_is_extra" +
                " (tried: ts_pack_node_is_extra, _ts_pack_node_is_extra). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_HAS_ERROR = LINKER.downcallHandle(
        LIB.find("ts_pack_node_has_error")
            .or(() -> LIB.find("_ts_pack_node_has_error"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_has_error"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_has_error"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_has_error" +
                " (tried: ts_pack_node_has_error, _ts_pack_node_has_error). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_PARENT = LINKER.downcallHandle(
        LIB.find("ts_pack_node_parent")
            .or(() -> LIB.find("_ts_pack_node_parent"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_parent"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_parent"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_parent" +
                " (tried: ts_pack_node_parent, _ts_pack_node_parent). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_CHILD = LINKER.downcallHandle(
        LIB.find("ts_pack_node_child")
            .or(() -> LIB.find("_ts_pack_node_child"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_child"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_child"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_child" +
                " (tried: ts_pack_node_child, _ts_pack_node_child). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_NODE_CHILD_COUNT = LINKER.downcallHandle(
        LIB.find("ts_pack_node_child_count")
            .or(() -> LIB.find("_ts_pack_node_child_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_child_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_child_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_child_count" +
                " (tried: ts_pack_node_child_count, _ts_pack_node_child_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_NAMED_CHILD = LINKER.downcallHandle(
        LIB.find("ts_pack_node_named_child")
            .or(() -> LIB.find("_ts_pack_node_named_child"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_named_child"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_named_child"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_named_child" +
                " (tried: ts_pack_node_named_child, _ts_pack_node_named_child). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.JAVA_LONG)
    );


    static final MethodHandle TS_PACK_NODE_NAMED_CHILD_COUNT = LINKER.downcallHandle(
        LIB.find("ts_pack_node_named_child_count")
            .or(() -> LIB.find("_ts_pack_node_named_child_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_named_child_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_named_child_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_named_child_count" +
                " (tried: ts_pack_node_named_child_count, _ts_pack_node_named_child_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_CHILD_BY_FIELD_NAME = LINKER.downcallHandle(
        LIB.find("ts_pack_node_child_by_field_name")
            .or(() -> LIB.find("_ts_pack_node_child_by_field_name"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_child_by_field_name"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_child_by_field_name"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_child_by_field_name" +
                " (tried: ts_pack_node_child_by_field_name, _ts_pack_node_child_by_field_name). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_TO_SEXP = LINKER.downcallHandle(
        LIB.find("ts_pack_node_to_sexp")
            .or(() -> LIB.find("_ts_pack_node_to_sexp"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_to_sexp"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_to_sexp"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_to_sexp" +
                " (tried: ts_pack_node_to_sexp, _ts_pack_node_to_sexp). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_WALK = LINKER.downcallHandle(
        LIB.find("ts_pack_node_walk")
            .or(() -> LIB.find("_ts_pack_node_walk"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_walk"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_walk"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_node_walk" +
                " (tried: ts_pack_node_walk, _ts_pack_node_walk). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_NODE = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_node")
            .or(() -> LIB.find("_ts_pack_tree_cursor_node"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_node"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_node"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_cursor_node" +
                " (tried: ts_pack_tree_cursor_node, _ts_pack_tree_cursor_node). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_GOTO_FIRST_CHILD = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_goto_first_child")
            .or(() -> LIB.find("_ts_pack_tree_cursor_goto_first_child"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_goto_first_child"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_goto_first_child"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_cursor_goto_first_child" +
                " (tried: ts_pack_tree_cursor_goto_first_child, _ts_pack_tree_cursor_goto_first_child). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_GOTO_PARENT = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_goto_parent")
            .or(() -> LIB.find("_ts_pack_tree_cursor_goto_parent"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_goto_parent"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_goto_parent"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_cursor_goto_parent" +
                " (tried: ts_pack_tree_cursor_goto_parent, _ts_pack_tree_cursor_goto_parent). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_GOTO_NEXT_SIBLING = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_goto_next_sibling")
            .or(() -> LIB.find("_ts_pack_tree_cursor_goto_next_sibling"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_goto_next_sibling"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_goto_next_sibling"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_cursor_goto_next_sibling" +
                " (tried: ts_pack_tree_cursor_goto_next_sibling, _ts_pack_tree_cursor_goto_next_sibling). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_FIELD_NAME = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_field_name")
            .or(() -> LIB.find("_ts_pack_tree_cursor_field_name"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_field_name"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_field_name"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_tree_cursor_field_name" +
                " (tried: ts_pack_tree_cursor_field_name, _ts_pack_tree_cursor_field_name). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_NEW = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_new")
            .or(() -> LIB.find("_ts_pack_language_registry_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_new" +
                " (tried: ts_pack_language_registry_new, _ts_pack_language_registry_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_GET_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_get_language")
            .or(() -> LIB.find("_ts_pack_language_registry_get_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_get_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_get_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_get_language" +
                " (tried: ts_pack_language_registry_get_language, _ts_pack_language_registry_get_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_AVAILABLE_LANGUAGES = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_available_languages")
            .or(() -> LIB.find("_ts_pack_language_registry_available_languages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_available_languages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_available_languages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_available_languages" +
                " (tried: ts_pack_language_registry_available_languages, _ts_pack_language_registry_available_languages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_HAS_PARSER = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_has_parser")
            .or(() -> LIB.find("_ts_pack_language_registry_has_parser"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_has_parser"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_has_parser"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_has_parser" +
                " (tried: ts_pack_language_registry_has_parser, _ts_pack_language_registry_has_parser). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_HAS_LANGUAGE = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_has_language")
            .or(() -> LIB.find("_ts_pack_language_registry_has_language"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_has_language"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_has_language"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_has_language" +
                " (tried: ts_pack_language_registry_has_language, _ts_pack_language_registry_has_language). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_LANGUAGE_COUNT = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_language_count")
            .or(() -> LIB.find("_ts_pack_language_registry_language_count"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_language_count"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_language_count"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_language_count" +
                " (tried: ts_pack_language_registry_language_count, _ts_pack_language_registry_language_count). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_PROCESS = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_process")
            .or(() -> LIB.find("_ts_pack_language_registry_process"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_process"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_process"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_language_registry_process" +
                " (tried: ts_pack_language_registry_process, _ts_pack_language_registry_process). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_MANAGER_NEW = LINKER.downcallHandle(
        LIB.find("ts_pack_download_manager_new")
            .or(() -> LIB.find("_ts_pack_download_manager_new"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_manager_new"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_manager_new"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_manager_new" +
                " (tried: ts_pack_download_manager_new, _ts_pack_download_manager_new). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_MANAGER_INSTALLED_LANGUAGES = LINKER.downcallHandle(
        LIB.find("ts_pack_download_manager_installed_languages")
            .or(() -> LIB.find("_ts_pack_download_manager_installed_languages"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_manager_installed_languages"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_manager_installed_languages"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_manager_installed_languages" +
                " (tried: ts_pack_download_manager_installed_languages, _ts_pack_download_manager_installed_languages). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_MANAGER_DOWNLOAD_ALL_BEST_EFFORT = LINKER.downcallHandle(
        LIB.find("ts_pack_download_manager_download_all_best_effort")
            .or(() -> LIB.find("_ts_pack_download_manager_download_all_best_effort"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_manager_download_all_best_effort"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_manager_download_all_best_effort"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_manager_download_all_best_effort" +
                " (tried: ts_pack_download_manager_download_all_best_effort, _ts_pack_download_manager_download_all_best_effort). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_MANAGER_CLEAN_CACHE = LINKER.downcallHandle(
        LIB.find("ts_pack_download_manager_clean_cache")
            .or(() -> LIB.find("_ts_pack_download_manager_clean_cache"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_manager_clean_cache"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_manager_clean_cache"))
            // Fallback underscore variant
            .orElseThrow(() -> new ExceptionInInitializerError(
                "Native symbol not found: " + "ts_pack_download_manager_clean_cache" +
                " (tried: ts_pack_download_manager_clean_cache, _ts_pack_download_manager_clean_cache). " +
                "Ensure the native library was compiled with this function exported. " +
                "If this is an optional feature, check that all required features are enabled.")),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LAST_ERROR_CODE = LINKER.downcallHandle(
        LIB.find("ts_pack_last_error_code").orElse(null),
        FunctionDescriptor.of(ValueLayout.JAVA_LONG)
    );

    static final MethodHandle TS_PACK_LAST_ERROR_CONTEXT = LINKER.downcallHandle(
        LIB.find("ts_pack_last_error_context").orElse(null),
        FunctionDescriptor.of(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DATA_ATTRIBUTE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_data_attribute_from_json")
            .or(() -> LIB.find("_ts_pack_data_attribute_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_data_attribute_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_data_attribute_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DATA_ATTRIBUTE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_data_attribute_free")
            .or(() -> LIB.find("_ts_pack_data_attribute_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_data_attribute_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_data_attribute_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DATA_NODE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_data_node_from_json")
            .or(() -> LIB.find("_ts_pack_data_node_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_data_node_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_data_node_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DATA_NODE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_data_node_free")
            .or(() -> LIB.find("_ts_pack_data_node_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_data_node_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_data_node_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_SPAN_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_span_from_json")
            .or(() -> LIB.find("_ts_pack_span_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_span_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_span_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_SPAN_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_span_free")
            .or(() -> LIB.find("_ts_pack_span_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_span_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_span_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PROCESS_RESULT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_process_result_from_json")
            .or(() -> LIB.find("_ts_pack_process_result_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_process_result_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_process_result_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PROCESS_RESULT_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_process_result_free")
            .or(() -> LIB.find("_ts_pack_process_result_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_process_result_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_process_result_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_FILE_METRICS_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_file_metrics_from_json")
            .or(() -> LIB.find("_ts_pack_file_metrics_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_file_metrics_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_file_metrics_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_FILE_METRICS_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_file_metrics_free")
            .or(() -> LIB.find("_ts_pack_file_metrics_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_file_metrics_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_file_metrics_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_STRUCTURE_ITEM_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_structure_item_from_json")
            .or(() -> LIB.find("_ts_pack_structure_item_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_structure_item_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_structure_item_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_STRUCTURE_ITEM_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_structure_item_free")
            .or(() -> LIB.find("_ts_pack_structure_item_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_structure_item_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_structure_item_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_COMMENT_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_comment_info_from_json")
            .or(() -> LIB.find("_ts_pack_comment_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_comment_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_comment_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_COMMENT_INFO_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_comment_info_free")
            .or(() -> LIB.find("_ts_pack_comment_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_comment_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_comment_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOCSTRING_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_docstring_info_from_json")
            .or(() -> LIB.find("_ts_pack_docstring_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_docstring_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_docstring_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOCSTRING_INFO_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_docstring_info_free")
            .or(() -> LIB.find("_ts_pack_docstring_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_docstring_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_docstring_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOC_SECTION_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_doc_section_from_json")
            .or(() -> LIB.find("_ts_pack_doc_section_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_doc_section_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_doc_section_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOC_SECTION_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_doc_section_free")
            .or(() -> LIB.find("_ts_pack_doc_section_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_doc_section_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_doc_section_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_IMPORT_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_import_info_from_json")
            .or(() -> LIB.find("_ts_pack_import_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_import_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_import_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_IMPORT_INFO_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_import_info_free")
            .or(() -> LIB.find("_ts_pack_import_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_import_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_import_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_EXPORT_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_export_info_from_json")
            .or(() -> LIB.find("_ts_pack_export_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_export_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_export_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_EXPORT_INFO_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_export_info_free")
            .or(() -> LIB.find("_ts_pack_export_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_export_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_export_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_SYMBOL_INFO_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_symbol_info_from_json")
            .or(() -> LIB.find("_ts_pack_symbol_info_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_symbol_info_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_symbol_info_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_SYMBOL_INFO_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_symbol_info_free")
            .or(() -> LIB.find("_ts_pack_symbol_info_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_symbol_info_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_symbol_info_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DIAGNOSTIC_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_diagnostic_from_json")
            .or(() -> LIB.find("_ts_pack_diagnostic_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_diagnostic_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_diagnostic_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DIAGNOSTIC_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_diagnostic_free")
            .or(() -> LIB.find("_ts_pack_diagnostic_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_diagnostic_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_diagnostic_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CODE_CHUNK_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_code_chunk_from_json")
            .or(() -> LIB.find("_ts_pack_code_chunk_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_code_chunk_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_code_chunk_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CODE_CHUNK_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_code_chunk_free")
            .or(() -> LIB.find("_ts_pack_code_chunk_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_code_chunk_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_code_chunk_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CHUNK_CONTEXT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_chunk_context_from_json")
            .or(() -> LIB.find("_ts_pack_chunk_context_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_chunk_context_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_chunk_context_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_CHUNK_CONTEXT_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_chunk_context_free")
            .or(() -> LIB.find("_ts_pack_chunk_context_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_chunk_context_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_chunk_context_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PACK_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_pack_config_from_json")
            .or(() -> LIB.find("_ts_pack_pack_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_pack_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_pack_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PACK_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_pack_config_free")
            .or(() -> LIB.find("_ts_pack_pack_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_pack_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_pack_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_POINT_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_point_from_json")
            .or(() -> LIB.find("_ts_pack_point_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_point_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_point_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_POINT_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_point_free")
            .or(() -> LIB.find("_ts_pack_point_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_point_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_point_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_BYTE_RANGE_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_byte_range_from_json")
            .or(() -> LIB.find("_ts_pack_byte_range_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_byte_range_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_byte_range_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_BYTE_RANGE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_byte_range_free")
            .or(() -> LIB.find("_ts_pack_byte_range_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_byte_range_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_byte_range_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PARSER_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_parser_free")
            .or(() -> LIB.find("_ts_pack_parser_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_parser_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_parser_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_free")
            .or(() -> LIB.find("_ts_pack_tree_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_NODE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_node_free")
            .or(() -> LIB.find("_ts_pack_node_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_node_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_node_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_TREE_CURSOR_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_tree_cursor_free")
            .or(() -> LIB.find("_ts_pack_tree_cursor_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_tree_cursor_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_tree_cursor_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PROCESS_CONFIG_FROM_JSON = LINKER.downcallHandle(
        LIB.find("ts_pack_process_config_from_json")
            .or(() -> LIB.find("_ts_pack_process_config_from_json"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_process_config_from_json"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_process_config_from_json"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PROCESS_CONFIG_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_process_config_free")
            .or(() -> LIB.find("_ts_pack_process_config_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_process_config_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_process_config_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_REGISTRY_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_language_registry_free")
            .or(() -> LIB.find("_ts_pack_language_registry_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_registry_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_registry_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_DOWNLOAD_MANAGER_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_download_manager_free")
            .or(() -> LIB.find("_ts_pack_download_manager_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_download_manager_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_download_manager_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_LANGUAGE_FREE = LINKER.downcallHandle(
        LIB.find("ts_pack_language_free")
            .or(() -> LIB.find("_ts_pack_language_free"))
            // Try underscore-prefixed variant for macOS
            .or(() -> LINKER.defaultLookup().find("ts_pack_language_free"))
            // Fallback to default lookup
            .or(() -> LINKER.defaultLookup().find("_ts_pack_language_free"))
            // Fallback underscore variant
            .orElseThrow(),
        FunctionDescriptor.ofVoid(ValueLayout.ADDRESS)
    );


    static final MethodHandle TS_PACK_PROCESS_RESULT_TO_JSON = LIB.find("ts_pack_process_result_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle TS_PACK_BYTE_RANGE_TO_JSON = LIB.find("ts_pack_byte_range_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


    static final MethodHandle TS_PACK_POINT_TO_JSON = LIB.find("ts_pack_point_to_json")
        .map(s -> LINKER.downcallHandle(s, FunctionDescriptor.of(ValueLayout.ADDRESS, ValueLayout.ADDRESS)))
        .orElse(null);


}
