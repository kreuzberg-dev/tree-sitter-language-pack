/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:03ca1422bd12a963e699aeecdcc04227696432779a23341d86aaee5412eb7965
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: data_extraction */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "ts_pack.h"
#include "test_runner.h"

void test_data_extraction_caddy_directives(void) {
    /* Data extraction: Caddyfile directives as KeyValue nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"caddy\"}");
    TS_PACKAlefHandle result = ts_pack_process("localhost\nroot * /var/www\nfile_server\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_csv_rows(void) {
    /* Data extraction: CSV rows are positional Sequence nodes with indexed cells */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"csv\"}");
    TS_PACKAlefHandle result = ts_pack_process("a,b,c\n1,2,3\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "Sequence") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_csv_single_row(void) {
    /* Data extraction: CSV single row three cells */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"csv\"}");
    TS_PACKAlefHandle result = ts_pack_process("x,y,z\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_cue_fields(void) {
    /* Data extraction: CUE field definitions as KeyValue nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"cue\"}");
    TS_PACKAlefHandle result = ts_pack_process("host: \"localhost\"\nport: 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_dtd_element_decl(void) {
    /* Data extraction: DTD element declarations as KeyValue nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"dtd\"}");
    TS_PACKAlefHandle result = ts_pack_process("<!ELEMENT server (host, port)>\n<!ELEMENT host (#PCDATA)>\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_editorconfig_section(void) {
    /* Data extraction: .editorconfig INI dialect sections and settings */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"editorconfig\"}");
    TS_PACKAlefHandle result = ts_pack_process("[*.rs]\nindent_style = space\nindent_size = 4\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_hcl_attribute(void) {
    /* Data extraction: HCL attribute key-value pair */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"hcl\"}");
    TS_PACKAlefHandle result = ts_pack_process("region = \"us-east-1\"\ncount  = 3\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_hcl_block(void) {
    /* Data extraction: HCL block becomes a container KeyValue node */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"hcl\"}");
    TS_PACKAlefHandle result = ts_pack_process("resource \"aws_instance\" \"web\" {\n  ami = \"ami-123\"\n  instance_type = \"t2.micro\"\n}\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_hjson_flat(void) {
    /* Data extraction: HJSON flat object (JSON superset with comments) */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"hjson\"}");
    TS_PACKAlefHandle result = ts_pack_process("{\n  host: \"localhost\"\n  port: 8080\n}\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_hocon_flat(void) {
    /* Data extraction: HOCON (HCL dialect) flat key-value pairs */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"hocon\"}");
    TS_PACKAlefHandle result = ts_pack_process("host = \"localhost\"\nport = 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_ini_flat(void) {
    /* Data extraction: INI flat key-value settings */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"ini\"}");
    TS_PACKAlefHandle result = ts_pack_process("host=localhost\nport=8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_ini_section(void) {
    /* Data extraction: INI section becomes a container with children */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"ini\"}");
    TS_PACKAlefHandle result = ts_pack_process("[database]\nhost=localhost\nport=5432\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_json5_flat(void) {
    /* Data extraction: JSON5 flat object (JSON superset with trailing commas and comments) */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"json5\"}");
    TS_PACKAlefHandle result = ts_pack_process("{\n  host: \"localhost\",\n  port: 8080,\n}\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_json_array(void) {
    /* Data extraction: JSON top-level array emits Sequence nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"json\"}");
    TS_PACKAlefHandle result = ts_pack_process("[1, 2, 3]", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "Sequence") == 0 && "equals assertion failed");
    {
        /* count_equals: count elements in array */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count == 3 && "expected 3 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_json_empty_object(void) {
    /* Data extraction: JSON empty object produces zero children */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"json\"}");
    TS_PACKAlefHandle result = ts_pack_process("{}", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert((data_children == NULL || strlen(data_children) == 0) && "expected empty value");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_json_flat(void) {
    /* Data extraction: JSON flat key-value object */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"json\"}");
    TS_PACKAlefHandle result = ts_pack_process("{\"host\": \"localhost\", \"port\": 8080}", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_json_nested(void) {
    /* Data extraction: JSON nested object preserves hierarchy */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"json\"}");
    TS_PACKAlefHandle result = ts_pack_process("{\"server\": {\"host\": \"x\", \"port\": 8080}}", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_kdl_flat(void) {
    /* Data extraction: KDL flat nodes with scalar values */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"kdl\"}");
    TS_PACKAlefHandle result = ts_pack_process("host \"localhost\"\nport 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_kdl_nested(void) {
    /* Data extraction: KDL node with children block */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"kdl\"}");
    TS_PACKAlefHandle result = ts_pack_process("server {\n  host \"localhost\"\n  port 8080\n}\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_nginx_directives(void) {
    /* Data extraction: nginx config directives as KeyValue nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"nginx\"}");
    TS_PACKAlefHandle result = ts_pack_process("worker_processes 4;\nerror_log /var/log/nginx/error.log;\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_po_message(void) {
    /* Data extraction: PO gettext message pairs with msgid as key and msgstr as value */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"po\"}");
    TS_PACKAlefHandle result = ts_pack_process("msgid \"Hello\"\nmsgstr \"Hallo\"\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_properties_dotted_key(void) {
    /* Data extraction: .properties dotted key preserved verbatim */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"properties\"}");
    TS_PACKAlefHandle result = ts_pack_process("server.host=localhost\nserver.port=8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_properties_empty(void) {
    /* Data extraction: empty .properties file produces zero children */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"properties\"}");
    TS_PACKAlefHandle result = ts_pack_process("", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert((data_children == NULL || strlen(data_children) == 0) && "expected empty value");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_properties_flat(void) {
    /* Data extraction: .properties flat key=value pairs (closes issue #136) */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"properties\"}");
    TS_PACKAlefHandle result = ts_pack_process("host=localhost\nport=8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_psv_rows(void) {
    /* Data extraction: PSV (pipe-separated values) rows as Sequence nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"psv\"}");
    TS_PACKAlefHandle result = ts_pack_process("a|b|c\n1|2|3\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "Sequence") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_toml_array(void) {
    /* Data extraction: TOML inline array emits Sequence node */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"toml\"}");
    TS_PACKAlefHandle result = ts_pack_process("ports = [8080, 8081, 8082]\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_toml_flat(void) {
    /* Data extraction: TOML flat key-value pairs */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"toml\"}");
    TS_PACKAlefHandle result = ts_pack_process("host = \"localhost\"\nport = 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_toml_table(void) {
    /* Data extraction: TOML table section becomes a container KeyValue node */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"toml\"}");
    TS_PACKAlefHandle result = ts_pack_process("[server]\nhost = \"localhost\"\nport = 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_xml_element(void) {
    /* Data extraction: XML element with attributes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"xml\"}");
    TS_PACKAlefHandle result = ts_pack_process("<server id=\"main\"><host>localhost</host></server>", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "Element") == 0 && "equals assertion failed");
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_xml_empty_element(void) {
    /* Data extraction: XML self-closing element produces an Element node */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"xml\"}");
    TS_PACKAlefHandle result = ts_pack_process("<br/>", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    assert(data);
    ts_pack_data_node_free(data);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_xml_nested(void) {
    /* Data extraction: XML nested elements produce child nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"xml\"}");
    TS_PACKAlefHandle result = ts_pack_process("<config><host>localhost</host><port>8080</port></config>", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    assert(data);
    ts_pack_data_node_free(data);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_yaml_flat(void) {
    /* Data extraction: YAML flat mapping */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"yaml\"}");
    TS_PACKAlefHandle result = ts_pack_process("host: localhost\nport: 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_kind = ts_pack_data_node_kind(data_handle);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    assert(data_kind != NULL && strcmp(data_kind, "KeyValue") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_kind);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_yaml_nested(void) {
    /* Data extraction: YAML nested block mapping preserves hierarchy */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"yaml\"}");
    TS_PACKAlefHandle result = ts_pack_process("server:\n  host: localhost\n  port: 8080\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    TS_PACKAlefHandle data_handle = ts_pack_process_result_data(result);
    assert(data_handle != 0);
    char* data_children = ts_pack_data_node_children(data_handle);
    assert(data);
    {
        /* count_min: count top-level JSON array elements */
        assert(data_children != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(data_children);
        assert(elem_count >= 1 && "expected at least 1 elements");
    }
    ts_pack_data_node_free(data);
    ts_pack_free_string(data_children);
    ts_pack_data_node_free(data_handle);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_data_extraction_yaml_sequence(void) {
    /* Data extraction: YAML block sequence emits Sequence nodes */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"data_extraction\":true,\"language\":\"yaml\"}");
    TS_PACKAlefHandle result = ts_pack_process("ports:\n  - 8080\n  - 8081\n", config_handle);
    assert(result != 0 && "expected call to succeed");
    TS_PACKAlefHandle data = ts_pack_process_result_data(result);
    assert(data);
    ts_pack_data_node_free(data);
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}
