/*
 * This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ac66af7986fb8435de6b2aae02a3cee5fd656cc3e341084dc611fefc31de4d54
 * To regenerate: alef generate
 * To verify freshness: alef verify --exit-code
 * Issues & docs: https://github.com/kreuzberg-dev/alef
 */
/* E2e tests for category: extraction */

#include <assert.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "tree_sitter_language_pack.h"
#include "test_runner.h"

void test_extract_invalid_byte_range(void) {
    /* extract_patterns() with out-of-bounds byte_range returns empty results */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{\"funcs\":{\"byte_range\":[9999,99999],\"capture_output\":\"Full\",\"query\":\"(expression_statement) @e\"}}}");
    TS_PACKExtractPatterns* result = extract_patterns("x = 1\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    TS_PACKResults* results_handle = ts_pack_extract_patterns_results(result);
    assert(results_handle != NULL);
    TS_PACKFuncs* funcs_handle = ts_pack_results_funcs(results_handle);
    assert(funcs_handle != NULL);
    char* results_funcs_matches = ts_pack_funcs_matches(funcs_handle);
    {
        /* count_equals: count elements in array */
        assert(results_funcs_matches != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(results_funcs_matches);
        assert(elem_count == 0 && "expected 0 elements");
    }
    ts_pack_free_string(results_funcs_matches);
    ts_pack_funcs_free(funcs_handle);
    ts_pack_results_free(results_handle);
    ts_pack_conversion_options_free(options_handle);
    ts_pack_extract_patterns_free(result);
}

void test_extract_patterns_no_matches(void) {
    /* Extraction query that matches nothing returns empty results */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{\"classes\":{\"capture_output\":\"Full\",\"query\":\"(class_definition name: (identifier) @name)\"}}}");
    TS_PACKExtractPatterns* result = extract_patterns("x = 1\ny = 2\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    TS_PACKResults* results_handle = ts_pack_extract_patterns_results(result);
    assert(results_handle != NULL);
    TS_PACKClasses* classes_handle = ts_pack_results_classes(results_handle);
    assert(classes_handle != NULL);
    char* results_classes_matches = ts_pack_classes_matches(classes_handle);
    {
        /* count_equals: count elements in array */
        assert(results_classes_matches != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(results_classes_matches);
        assert(elem_count == 0 && "expected 0 elements");
    }
    ts_pack_free_string(results_classes_matches);
    ts_pack_classes_free(classes_handle);
    ts_pack_results_free(results_handle);
    ts_pack_conversion_options_free(options_handle);
    ts_pack_extract_patterns_free(result);
}

void test_extract_patterns_python_functions(void) {
    /* Extract function definitions from Python source using tree-sitter query */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{\"functions\":{\"capture_output\":\"Full\",\"query\":\"(function_definition name: (identifier) @name)\"}}}");
    TS_PACKExtractPatterns* result = extract_patterns("def hello():\n    pass\n\ndef world(x):\n    return x * 2\n", options_handle);
    assert(result != NULL && "expected call to succeed");
    char* language = ts_pack_extract_patterns_language(result);
    TS_PACKResults* results_handle = ts_pack_extract_patterns_results(result);
    assert(results_handle != NULL);
    TS_PACKFunctions* functions_handle = ts_pack_results_functions(results_handle);
    assert(functions_handle != NULL);
    char* results_functions_matches = ts_pack_functions_matches(functions_handle);
    assert(str_trim_eq(language, "python") == 0 && "equals assertion failed");
    {
        /* count_min: count top-level JSON array elements */
        assert(results_functions_matches != NULL && "expected non-null collection JSON");
        int elem_count = alef_json_array_count(results_functions_matches);
        assert(elem_count >= 2 && "expected at least 2 elements");
    }
    ts_pack_free_string(language);
    ts_pack_free_string(results_functions_matches);
    ts_pack_functions_free(functions_handle);
    ts_pack_results_free(results_handle);
    ts_pack_conversion_options_free(options_handle);
    ts_pack_extract_patterns_free(result);
}

void test_validate_empty_patterns(void) {
    /* validate_extraction() with empty patterns returns valid */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{}}");
    TS_PACKValidateExtraction* result = validate_extraction(options_handle);
    assert(result != NULL && "expected call to succeed");
    char* valid = ts_pack_validate_extraction_valid(result);
    assert(strcmp(valid, 1) == 0 && "equals assertion failed");
    ts_pack_free_string(valid);
    ts_pack_conversion_options_free(options_handle);
    ts_pack_validate_extraction_free(result);
}

void test_validate_extraction_invalid_query(void) {
    /* Invalid query syntax fails validation */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{\"broken\":{\"capture_output\":\"Full\",\"query\":\"(this_is_not_a_valid_node @x\"}}}");
    TS_PACKValidateExtraction* result = validate_extraction(options_handle);
    ts_pack_conversion_options_free(options_handle);
    assert(result == NULL && "expected call to fail");
}

void test_validate_extraction_valid_config(void) {
    /* Valid extraction config passes validation */
    TS_PACKConversionOptions* options_handle = ts_pack_conversion_options_from_json("{\"language\":\"python\",\"patterns\":{\"functions\":{\"capture_output\":\"Full\",\"query\":\"(function_definition name: (identifier) @name)\"}}}");
    TS_PACKValidateExtraction* result = validate_extraction(options_handle);
    assert(result != NULL && "expected call to succeed");
    char* valid = ts_pack_validate_extraction_valid(result);
    assert(strcmp(valid, 1) == 0 && "equals assertion failed");
    ts_pack_free_string(valid);
    ts_pack_conversion_options_free(options_handle);
    ts_pack_validate_extraction_free(result);
}
