# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:6db3ea0f92ed16da2477a398ef810873d0f2c25ee91c1026dabae89bad485e19
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: data_extraction
defmodule E2e.DataExtractionTest do
  use ExUnit.Case, async: false

  describe "data_extraction_caddy_directives" do
    test "data_extraction_caddy_directives" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "caddy"}
      {:ok, result} = TreeSitterLanguagePack.process("localhost\nroot * /var/www\nfile_server\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_csv_rows" do
    test "data_extraction_csv_rows" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "csv"}
      {:ok, result} = TreeSitterLanguagePack.process("a,b,c\n1,2,3\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "Sequence"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_csv_single_row" do
    test "data_extraction_csv_single_row" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "csv"}
      {:ok, result} = TreeSitterLanguagePack.process("x,y,z\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_cue_fields" do
    test "data_extraction_cue_fields" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "cue"}
      {:ok, result} = TreeSitterLanguagePack.process("host: \"localhost\"\nport: 8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_dtd_element_decl" do
    test "data_extraction_dtd_element_decl" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "dtd"}
      {:ok, result} = TreeSitterLanguagePack.process("<!ELEMENT server (host, port)>\n<!ELEMENT host (\#PCDATA)>\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_editorconfig_section" do
    test "data_extraction_editorconfig_section" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "editorconfig"}
      {:ok, result} = TreeSitterLanguagePack.process("[*.rs]\nindent_style = space\nindent_size = 4\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_hcl_attribute" do
    test "data_extraction_hcl_attribute" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "hcl"}
      {:ok, result} = TreeSitterLanguagePack.process("region = \"us-east-1\"\ncount  = 3\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_hcl_block" do
    test "data_extraction_hcl_block" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "hcl"}

      {:ok, result} =
        TreeSitterLanguagePack.process(
          "resource \"aws_instance\" \"web\" {\n  ami = \"ami-123\"\n  instance_type = \"t2.micro\"\n}\n",
          config_value
        )

      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_hjson_flat" do
    test "data_extraction_hjson_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "hjson"}
      {:ok, result} = TreeSitterLanguagePack.process("{\n  host: \"localhost\"\n  port: 8080\n}\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_hocon_flat" do
    test "data_extraction_hocon_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "hocon"}
      {:ok, result} = TreeSitterLanguagePack.process("host = \"localhost\"\nport = 8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_ini_flat" do
    test "data_extraction_ini_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "ini"}
      {:ok, result} = TreeSitterLanguagePack.process("host=localhost\nport=8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_ini_section" do
    test "data_extraction_ini_section" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "ini"}
      {:ok, result} = TreeSitterLanguagePack.process("[database]\nhost=localhost\nport=5432\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_json5_flat" do
    test "data_extraction_json5_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "json5"}
      {:ok, result} = TreeSitterLanguagePack.process("{\n  host: \"localhost\",\n  port: 8080,\n}\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_json_array" do
    test "data_extraction_json_array" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "json"}
      {:ok, result} = TreeSitterLanguagePack.process("[1, 2, 3]", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "Sequence"
      assert length(result.data.children) == 3
    end
  end

  describe "data_extraction_json_empty_object" do
    test "data_extraction_json_empty_object" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "json"}
      {:ok, result} = TreeSitterLanguagePack.process("{}", config_value)
      refute is_nil(result.data)
      assert result.data.children in [nil, "", [], %{}]
    end
  end

  describe "data_extraction_json_flat" do
    test "data_extraction_json_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "json"}
      {:ok, result} = TreeSitterLanguagePack.process("{\"host\": \"localhost\", \"port\": 8080}", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_json_nested" do
    test "data_extraction_json_nested" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "json"}
      {:ok, result} = TreeSitterLanguagePack.process("{\"server\": {\"host\": \"x\", \"port\": 8080}}", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_kdl_flat" do
    test "data_extraction_kdl_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "kdl"}
      {:ok, result} = TreeSitterLanguagePack.process("host \"localhost\"\nport 8080\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_kdl_nested" do
    test "data_extraction_kdl_nested" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "kdl"}
      {:ok, result} = TreeSitterLanguagePack.process("server {\n  host \"localhost\"\n  port 8080\n}\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_nginx_directives" do
    test "data_extraction_nginx_directives" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "nginx"}
      {:ok, result} = TreeSitterLanguagePack.process("worker_processes 4;\nerror_log /var/log/nginx/error.log;\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
    end
  end

  describe "data_extraction_po_message" do
    test "data_extraction_po_message" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "po"}
      {:ok, result} = TreeSitterLanguagePack.process("msgid \"Hello\"\nmsgstr \"Hallo\"\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_properties_dotted_key" do
    test "data_extraction_properties_dotted_key" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "properties"}
      {:ok, result} = TreeSitterLanguagePack.process("server.host=localhost\nserver.port=8080\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_properties_empty" do
    test "data_extraction_properties_empty" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "properties"}
      {:ok, result} = TreeSitterLanguagePack.process("", config_value)
      refute is_nil(result.data)
      assert result.data.children in [nil, "", [], %{}]
    end
  end

  describe "data_extraction_properties_flat" do
    test "data_extraction_properties_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "properties"}
      {:ok, result} = TreeSitterLanguagePack.process("host=localhost\nport=8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_psv_rows" do
    test "data_extraction_psv_rows" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "psv"}
      {:ok, result} = TreeSitterLanguagePack.process("a|b|c\n1|2|3\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "Sequence"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_toml_array" do
    test "data_extraction_toml_array" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "toml"}
      {:ok, result} = TreeSitterLanguagePack.process("ports = [8080, 8081, 8082]\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_toml_flat" do
    test "data_extraction_toml_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "toml"}
      {:ok, result} = TreeSitterLanguagePack.process("host = \"localhost\"\nport = 8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_toml_table" do
    test "data_extraction_toml_table" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "toml"}
      {:ok, result} = TreeSitterLanguagePack.process("[server]\nhost = \"localhost\"\nport = 8080\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_xml_element" do
    test "data_extraction_xml_element" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "xml"}
      {:ok, result} = TreeSitterLanguagePack.process("<server id=\"main\"><host>localhost</host></server>", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "Element"
    end
  end

  describe "data_extraction_xml_empty_element" do
    test "data_extraction_xml_empty_element" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "xml"}
      {:ok, result} = TreeSitterLanguagePack.process("<br/>", config_value)
      refute is_nil(result.data)
    end
  end

  describe "data_extraction_xml_nested" do
    test "data_extraction_xml_nested" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "xml"}
      {:ok, result} = TreeSitterLanguagePack.process("<config><host>localhost</host><port>8080</port></config>", config_value)
      refute is_nil(result.data)
    end
  end

  describe "data_extraction_yaml_flat" do
    test "data_extraction_yaml_flat" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "yaml"}
      {:ok, result} = TreeSitterLanguagePack.process("host: localhost\nport: 8080\n", config_value)
      refute is_nil(result.data)
      assert TreeSitterLanguagePack.DataNodeKind.wire_value(result.data.kind) == "KeyValue"
      assert length(result.data.children) >= 2
    end
  end

  describe "data_extraction_yaml_nested" do
    test "data_extraction_yaml_nested" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "yaml"}
      {:ok, result} = TreeSitterLanguagePack.process("server:\n  host: localhost\n  port: 8080\n", config_value)
      refute is_nil(result.data)
      assert length(result.data.children) >= 1
    end
  end

  describe "data_extraction_yaml_sequence" do
    test "data_extraction_yaml_sequence" do
      config_value = %TreeSitterLanguagePack.ProcessConfig{data_extraction: true, language: "yaml"}
      {:ok, result} = TreeSitterLanguagePack.process("ports:\n  - 8080\n  - 8081\n", config_value)
      refute is_nil(result.data)
    end
  end
end
