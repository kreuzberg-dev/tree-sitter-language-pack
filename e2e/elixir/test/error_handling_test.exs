# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:975aee80e787d5784565356ea79fdbc3bcf331796330baaa0ad24bd4656dbcce
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: error-handling
defmodule E2e.ErrorHandlingTest do
  use ExUnit.Case, async: true

  describe "error_detect_content_empty" do
    test "Detect language from empty content returns null" do
      {:ok, result} = TreeSitterLanguagePack.detect_language_from_content("", nil)
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "error_detect_extension_empty" do
    test "Detect language from empty extension returns null" do
      {:ok, result} = TreeSitterLanguagePack.detect_language_from_extension("", nil)
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "error_detect_path_empty" do
    test "Detect language from empty path returns null" do
      {:ok, result} = TreeSitterLanguagePack.detect_language_from_path("", nil)
      assert is_nil(result) or String.trim(result) == ""
    end
  end

  describe "error_empty_language_name" do
    test "Parsing with empty language name should error" do
      assert {:error, _} = TreeSitterLanguagePack.parse_string("hello", nil)
    end
  end

  describe "error_extract_unknown_language" do
    test "Extract patterns with unknown language returns error" do
      assert {:error, _} = TreeSitterLanguagePack.extract_patterns("x = 1", %{"language" => "nonexistent_language_xyz", "patterns" => %{}})
    end
  end

  describe "error_handling_empty_source" do
    test "Parsing an empty string should still produce a tree." do
      {:ok, tree} = TreeSitterLanguagePack.parse_string("", nil)
    end
  end

  describe "error_handling_invalid_syntax" do
    test "Parsing invalid syntax should produce a tree with error nodes." do
      {:ok, tree} = TreeSitterLanguagePack.parse_string("function function function @@@ %%%", nil)
      assert TreeSitterLanguagePack.tree_has_error_nodes(tree) == true
    end
  end

  describe "error_handling_unknown_language" do
    test "Loading a nonexistent language should produce an error." do
      assert {:error, _} = TreeSitterLanguagePack.parse_string("", nil)
    end
  end

  describe "error_process_empty_source" do
    test "Process empty source code succeeds with zero metrics" do
      {:ok, result} = TreeSitterLanguagePack.process("", %{"language" => "python"})
      assert result.metrics.total_lines == 0
    end
  end

  describe "error_run_query_invalid_syntax" do
    test "Run query with invalid S-expression syntax produces error" do
      {:ok, tree} = TreeSitterLanguagePack.parse_string("x = 1", nil)
      assert_raise RuntimeError, fn -> TreeSitterLanguagePack.run_query(tree, "python", "(((", source) end
    end
  end

  describe "parse_empty_language" do
    test "parse_string() returns error with empty language name" do
      assert {:error, _} = TreeSitterLanguagePack.parse_string("x = 1", nil)
    end
  end

  describe "process_unknown_language" do
    test "process() returns error when config language is unknown" do
      assert {:error, _} = TreeSitterLanguagePack.process("x = 1", %{"language" => "nonexistent_xyz"})
    end
  end
end
