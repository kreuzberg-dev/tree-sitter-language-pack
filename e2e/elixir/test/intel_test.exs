# Code generated by e2e-generator. DO NOT EDIT.

defmodule E2eTests.IntelTest do
  use ExUnit.Case, async: true

  @tag :skip_unless_c
  test "c_function_intel" do
    # Intel: C function with include
    unless TreeSitterLanguagePack.has_language("c") do
      IO.puts("Skipping: language 'c' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "\#include <stdio.h>\n\nint main() {\n    printf(\"hello\");\n    return 0;\n}\n",
          ~s({"language":"c","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "c"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert intel["metrics"]["total_lines"] >= 6
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_python
  test "config_all_python" do
    # Intel: process with all features enabled
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "\# A comment\ndef greet(name):\n    \"\"\"Say hello.\"\"\"\n    return f'Hi {name}'\n\nimport os\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert length(intel["imports"]) >= 1
      assert intel["metrics"]["total_lines"] >= 6
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_python
  test "config_minimal_python" do
    # Intel: process with minimal config - only metrics
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def hello():\n    pass\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert intel["metrics"]["total_lines"] >= 2
    end
  end

  @tag :skip_unless_go
  test "go_function_intel" do
    # Intel: extract structure from Go function definition
    unless TreeSitterLanguagePack.has_language("go") do
      IO.puts("Skipping: language 'go' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "package main\n\nimport \"fmt\"\n\nfunc main() {\n\tfmt.Println(\"hello\")\n}\n",
          ~s({"language":"go","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "go"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert length(intel["imports"]) >= 1
      assert intel["metrics"]["total_lines"] >= 7
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_java
  test "java_class_intel" do
    # Intel: Java class with methods and imports
    unless TreeSitterLanguagePack.has_language("java") do
      IO.puts("Skipping: language 'java' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return \"Hello \" + name;\n    }\n}\n",
          ~s({"language":"java","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "java"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Class" end)
      assert length(intel["imports"]) >= 1
      assert intel["metrics"]["total_lines"] >= 7
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_javascript
  test "javascript_multi_import_intel" do
    # Intel: detect multiple imports and function in JavaScript
    unless TreeSitterLanguagePack.has_language("javascript") do
      IO.puts("Skipping: language 'javascript' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n",
          ~s({"language":"javascript","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "javascript"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert length(intel["imports"]) >= 2
      assert intel["metrics"]["total_lines"] >= 6
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_python
  test "python_chunking_intel" do
    # Intel: chunk multi-function Python source into multiple pieces
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n",
          ~s({"language":"python","chunk_max_size":30})
        )

      chunks = intel["chunks"]
      assert intel["language"] == "python"
      assert intel["metrics"]["total_lines"] >= 8
      assert length(chunks) >= 2
    end
  end

  @tag :skip_unless_python
  test "python_chunking_medium" do
    # Intel: Python code with medium chunk size
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n",
          ~s({"language":"python","chunk_max_size":50})
        )

      chunks = intel["chunks"]
      assert intel["language"] == "python"
      assert length(intel["structure"]) >= 3
      assert intel["metrics"]["error_count"] == 0
      assert length(chunks) >= 2
    end
  end

  @tag :skip_unless_python
  test "python_class_with_methods_intel" do
    # Intel: extract nested structure from Python class with methods
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Class" end)
      assert intel["metrics"]["total_lines"] >= 6
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_python
  test "python_error_diagnostics" do
    # Intel: Python code with syntax errors should report diagnostics
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def broken(\n    pass\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert intel["metrics"]["error_count"] == 1
      assert length(intel["diagnostics"]) > 0
    end
  end

  @tag :skip_unless_python
  test "python_function_intel" do
    # Intel: extract structure from Python function definition
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def greet(name):\n    return f'Hello, {name}!'\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert intel["metrics"]["total_lines"] >= 2
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_python
  test "python_malformed_code_intel" do
    # Intel: detect diagnostics in malformed Python code
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "def broken(\n    return\nclass",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert length(intel["diagnostics"]) > 0
    end
  end

  @tag :skip_unless_python
  test "python_multi_import_intel" do
    # Intel: detect multiple Python imports
    unless TreeSitterLanguagePack.has_language("python") do
      IO.puts("Skipping: language 'python' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n",
          ~s({"language":"python","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "python"
      assert length(intel["structure"]) >= 1
      assert length(intel["imports"]) >= 3
      assert intel["metrics"]["total_lines"] >= 5
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_ruby
  test "ruby_class_intel" do
    # Intel: Ruby class with method
    unless TreeSitterLanguagePack.has_language("ruby") do
      IO.puts("Skipping: language 'ruby' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello \#{name}\"\n  end\nend\n",
          ~s({"language":"ruby","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "ruby"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Class" end)
      assert intel["metrics"]["total_lines"] >= 7
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_rust
  test "rust_chunking_intel" do
    # Intel: chunk multi-function Rust source into pieces
    unless TreeSitterLanguagePack.has_language("rust") do
      IO.puts("Skipping: language 'rust' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n",
          ~s({"language":"rust","chunk_max_size":30})
        )

      chunks = intel["chunks"]
      assert intel["language"] == "rust"
      assert intel["metrics"]["total_lines"] >= 7
      assert length(chunks) >= 2
    end
  end

  @tag :skip_unless_rust
  test "rust_function_intel" do
    # Intel: extract structure from Rust function definition
    unless TreeSitterLanguagePack.has_language("rust") do
      IO.puts("Skipping: language 'rust' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n",
          ~s({"language":"rust","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "rust"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert intel["metrics"]["total_lines"] >= 3
      assert intel["metrics"]["error_count"] == 0
    end
  end

  @tag :skip_unless_typescript
  test "typescript_function_intel" do
    # Intel: extract structure from TypeScript function
    unless TreeSitterLanguagePack.has_language("typescript") do
      IO.puts("Skipping: language 'typescript' not available")
    else
      intel =
        TreeSitterLanguagePack.process(
          "import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return `Hello, ${name}!`;\n}\n",
          ~s({"language":"typescript","structure":true,"imports":true,"exports":true,"comments":true,"docstrings":true,"symbols":true,"diagnostics":true})
        )

      assert intel["language"] == "typescript"
      assert length(intel["structure"]) >= 1
      assert Enum.any?(intel["structure"], fn s -> s["kind"] == "Function" end)
      assert length(intel["imports"]) >= 1
      assert intel["metrics"]["total_lines"] >= 5
      assert intel["metrics"]["error_count"] == 0
    end
  end
end
