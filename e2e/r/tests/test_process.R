# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:70b8311e006513b9afb209d4b7b66ef73ca90768f54bde5a5ba9fa5d332e9d03
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: process

test_that("process_javascript_exports_count: JavaScript with multiple exports, verify export count", {
  result <- process(source = "export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", config = list("language" = "javascript"))
  expect_equal(trimws(result$language), "javascript")
  expect_true(length(result$exports) >= 2)
})

test_that("process_javascript_exports_detail: JavaScript with exports, verify export count", {
  result <- process(source = "export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", config = list("language" = "javascript"))
  expect_equal(trimws(result$language), "javascript")
  expect_true(length(result$exports) >= 1)
})

test_that("process_python_all_features: Python comprehensive source with all feature extraction enabled", {
  result <- process(source = "import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", config = list("comments" = TRUE, "docstrings" = TRUE, "imports" = TRUE, "language" = "python", "structure" = TRUE, "symbols" = TRUE))
  expect_equal(trimws(result$language), "python")
  expect_true(length(result$structure) >= 2)
  expect_true(length(result$imports) >= 1)
  expect_true(length(result$comments) >= 1)
  expect_true(result$metrics$total_lines >= 10)
})

test_that("process_python_comments: Python with comments, verify comment count", {
  result <- process(source = "# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", config = list("comments" = TRUE, "language" = "python"))
  expect_equal(trimws(result$language), "python")
  expect_true(length(result$comments) >= 1)
})

test_that("process_python_docstrings: Python with function docstring, verify docstring count", {
  result <- process(source = "def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", config = list("docstrings" = TRUE, "language" = "python"))
  expect_equal(trimws(result$language), "python")
  expect_true(result$metrics$total_lines >= 3)
})

test_that("process_python_imports_detail: Python with multiple imports, verify imports contain specific source", {
  result <- process(source = "import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", config = list("language" = "python"))
  expect_equal(trimws(result$language), "python")
  expect_true(length(result$imports) >= 2)
  expect_true(grepl("os", result$import_sources, fixed = TRUE))
})

test_that("process_python_metrics_detail: Python code with metrics assertions", {
  result <- process(source = "# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", config = list("language" = "python"))
  expect_equal(trimws(result$language), "python")
  expect_true(result$metrics$code_lines >= 4)
  expect_true(result$metrics$comment_lines >= 1)
  expect_true(result$metrics$max_depth >= 1)
})

test_that("process_python_symbols: Python with class and functions, verify symbol count", {
  result <- process(source = "my_const = 42\ndef helper(): pass\nclass widget: pass\n", config = list("language" = "python", "symbols" = TRUE))
  expect_equal(trimws(result$language), "python")
  expect_true(length(result$symbols) >= 1)
})

test_that("process_rust_structure_name: Rust struct with name, verify structure name contains value", {
  result <- process(source = "pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", config = list("language" = "rust"))
  expect_equal(trimws(result$language), "rust")
  expect_true(length(result$structure) >= 1)
  expect_true(grepl("MyConfig", result$structure_names, fixed = TRUE))
})
