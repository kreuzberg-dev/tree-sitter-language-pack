# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c29b47d37172da4e8067b4490b7044a66bdf1cec93e51ddfa6d8359331bca49d
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: smoke

test_that("smoke_abl: Smoke test: load abl and parse a simple snippet", {
  tree <- parse_string(language = "abl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_actionscript: Smoke test: load actionscript and parse a simple snippet", {
  tree <- parse_string(language = "actionscript", source = "var x:int = 1;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ada: Smoke test: load ada and parse a simple snippet", {
  tree <- parse_string(language = "ada", source = "procedure Main is begin null; end Main;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_agda: Smoke test: load agda and parse a simple snippet", {
  tree <- parse_string(language = "agda", source = "module Main where")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_al: Smoke test: load al and parse a simple snippet", {
  tree <- parse_string(language = "al", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_angular: Smoke test: load angular and parse a simple snippet", {
  tree <- parse_string(language = "angular", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_apex: Smoke test: load apex and parse a simple snippet", {
  tree <- parse_string(language = "apex", source = "public class Main {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_arduino: Smoke test: load arduino and parse a simple snippet", {
  tree <- parse_string(language = "arduino", source = "void setup() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_asciidoc: Smoke test: load asciidoc and parse a simple snippet", {
  tree <- parse_string(language = "asciidoc", source = "= Title\n\nParagraph.")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_asm: Smoke test: load asm and parse a simple snippet", {
  tree <- parse_string(language = "asm", source = "mov eax, 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_astro: Smoke test: load astro and parse a simple snippet", {
  tree <- parse_string(language = "astro", source = "---\n---\n<p>hello</p>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_awk: Smoke test: load awk and parse a simple snippet", {
  tree <- parse_string(language = "awk", source = "begin { print \"hello\" }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bash: Smoke test: load bash and parse a simple snippet", {
  tree <- parse_string(language = "bash", source = "echo hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bass: Smoke test: load bass and parse a simple snippet", {
  tree <- parse_string(language = "bass", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_batch: Smoke test: load batch and parse a simple snippet", {
  tree <- parse_string(language = "batch", source = "@echo off\necho hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_beancount: Smoke test: load beancount and parse a simple snippet", {
  tree <- parse_string(language = "beancount", source = "2024-01-01 open Assets:Bank USD")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bibtex: Smoke test: load bibtex and parse a simple snippet", {
  tree <- parse_string(language = "bibtex", source = "@article{key, title={A}}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bicep: Smoke test: load bicep and parse a simple snippet", {
  tree <- parse_string(language = "bicep", source = "param name string")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bitbake: Smoke test: load bitbake and parse a simple snippet", {
  tree <- parse_string(language = "bitbake", source = "description = \"hello\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_blade: Smoke test: load blade and parse a simple snippet", {
  tree <- parse_string(language = "blade", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_brightscript: Smoke test: load brightscript and parse a simple snippet", {
  tree <- parse_string(language = "brightscript", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_bsl: Smoke test: load bsl and parse a simple snippet", {
  tree <- parse_string(language = "bsl", source = "procedure main() endprocedure")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_c: Smoke test: load c and parse a simple snippet", {
  tree <- parse_string(language = "c", source = "int main() { return 0; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_c3: Smoke test: load c3 and parse a simple snippet", {
  tree <- parse_string(language = "c3", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_caddy: Smoke test: load caddy and parse a simple snippet", {
  tree <- parse_string(language = "caddy", source = ":8080 {\n\trespond \"Hello\"\n}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cairo: Smoke test: load cairo and parse a simple snippet", {
  tree <- parse_string(language = "cairo", source = "fn main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_capnp: Smoke test: load capnp and parse a simple snippet", {
  tree <- parse_string(language = "capnp", source = "@0xabcdef1234567890;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cedar: Smoke test: load cedar and parse a simple snippet", {
  tree <- parse_string(language = "cedar", source = "permit(principal, action, resource);")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cedarschema: Smoke test: load cedarschema and parse a simple snippet", {
  tree <- parse_string(language = "cedarschema", source = "entity User;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cel: Smoke test: load cel and parse a simple snippet", {
  tree <- parse_string(language = "cel", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cfml: Smoke test: load cfml and parse a simple snippet", {
  tree <- parse_string(language = "cfml", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_chatito: Smoke test: load chatito and parse a simple snippet", {
  tree <- parse_string(language = "chatito", source = "%[greeting]\n    hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_chuck: Smoke test: load chuck and parse a simple snippet", {
  tree <- parse_string(language = "chuck", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_circom: Smoke test: load circom and parse a simple snippet", {
  tree <- parse_string(language = "circom", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_clarity: Smoke test: load clarity and parse a simple snippet", {
  tree <- parse_string(language = "clarity", source = "(define-public (hello) (ok true))")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_clojure: Smoke test: load clojure and parse a simple snippet", {
  tree <- parse_string(language = "clojure", source = "(def x 1)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cmake: Smoke test: load cmake and parse a simple snippet", {
  tree <- parse_string(language = "cmake", source = "cmake_minimum_required(VERSION 3.0)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cobol: Smoke test: load cobol and parse a simple snippet", {
  tree <- parse_string(language = "cobol", source = "       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_comment: Smoke test: load comment and parse a simple snippet", {
  tree <- parse_string(language = "comment", source = "todo: fix this")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_commonlisp: Smoke test: load commonlisp and parse a simple snippet", {
  tree <- parse_string(language = "commonlisp", source = "(defun hello () (print \"hello\"))")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cooklang: Smoke test: load cooklang and parse a simple snippet", {
  tree <- parse_string(language = "cooklang", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_corn: Smoke test: load corn and parse a simple snippet", {
  tree <- parse_string(language = "corn", source = "{ key = \"value\" }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cpon: Smoke test: load cpon and parse a simple snippet", {
  tree <- parse_string(language = "cpon", source = "{\"key\": 1}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cpp: Smoke test: load cpp and parse a simple snippet", {
  tree <- parse_string(language = "cpp", source = "int main() { return 0; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_crystal: Smoke test: load crystal and parse a simple snippet", {
  tree <- parse_string(language = "crystal", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_csharp: Smoke test: load csharp and parse a simple snippet", {
  tree <- parse_string(language = "csharp", source = "class Main {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_css: Smoke test: load css and parse a simple snippet", {
  tree <- parse_string(language = "css", source = "body { color: red; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cst: Smoke test: load cst and parse a simple snippet", {
  tree <- parse_string(language = "cst", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_csv: Smoke test: load csv and parse a simple snippet", {
  tree <- parse_string(language = "csv", source = "a,b,c\n1,2,3")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cuda: Smoke test: load cuda and parse a simple snippet", {
  tree <- parse_string(language = "cuda", source = "__global__ void kernel() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cue: Smoke test: load cue and parse a simple snippet", {
  tree <- parse_string(language = "cue", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_cylc: Smoke test: load cylc and parse a simple snippet", {
  tree <- parse_string(language = "cylc", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_d: Smoke test: load d and parse a simple snippet", {
  tree <- parse_string(language = "d", source = "void main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_dart: Smoke test: load dart and parse a simple snippet", {
  tree <- parse_string(language = "dart", source = "void main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_desktop: Smoke test: load desktop and parse a simple snippet", {
  tree <- parse_string(language = "desktop", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_devicetree: Smoke test: load devicetree and parse a simple snippet", {
  tree <- parse_string(language = "devicetree", source = "/dts-v1/;\n/ { };")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_dhall: Smoke test: load dhall and parse a simple snippet", {
  tree <- parse_string(language = "dhall", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_diff: Smoke test: load diff and parse a simple snippet", {
  tree <- parse_string(language = "diff", source = "--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_djot: Smoke test: load djot and parse a simple snippet", {
  tree <- parse_string(language = "djot", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_dockerfile: Smoke test: load dockerfile and parse a simple snippet", {
  tree <- parse_string(language = "dockerfile", source = "from alpine")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_dot: Smoke test: load dot and parse a simple snippet", {
  tree <- parse_string(language = "dot", source = "digraph G { A -> B; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_doxygen: Smoke test: load doxygen and parse a simple snippet", {
  tree <- parse_string(language = "doxygen", source = "/** @brief A function */")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_dtd: Smoke test: load dtd and parse a simple snippet", {
  tree <- parse_string(language = "dtd", source = "<!ELEMENT note (body)>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_earthfile: Smoke test: load earthfile and parse a simple snippet", {
  tree <- parse_string(language = "earthfile", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ebnf: Smoke test: load ebnf and parse a simple snippet", {
  tree <- parse_string(language = "ebnf", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_editorconfig: Smoke test: load editorconfig and parse a simple snippet", {
  tree <- parse_string(language = "editorconfig", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_eds: Smoke test: load eds and parse a simple snippet", {
  tree <- parse_string(language = "eds", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_eex: Smoke test: load eex and parse a simple snippet", {
  tree <- parse_string(language = "eex", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_elisp: Smoke test: load elisp and parse a simple snippet", {
  tree <- parse_string(language = "elisp", source = "(defun hello () (message \"hello\"))")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_elixir: Smoke test: load elixir and parse a simple snippet", {
  tree <- parse_string(language = "elixir", source = "io.puts(\"hello\")")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_elm: Smoke test: load elm and parse a simple snippet", {
  tree <- parse_string(language = "elm", source = "module Main exposing (..)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_elsa: Smoke test: load elsa and parse a simple snippet", {
  tree <- parse_string(language = "elsa", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_elvish: Smoke test: load elvish and parse a simple snippet", {
  tree <- parse_string(language = "elvish", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_embeddedtemplate: Smoke test: load embeddedtemplate and parse a simple snippet", {
  tree <- parse_string(language = "embeddedtemplate", source = "<%= value %>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_enforce: Smoke test: load enforce and parse a simple snippet", {
  tree <- parse_string(language = "enforce", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_erlang: Smoke test: load erlang and parse a simple snippet", {
  tree <- parse_string(language = "erlang", source = "main() -> ok.")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_facility: Smoke test: load facility and parse a simple snippet", {
  tree <- parse_string(language = "facility", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_faust: Smoke test: load faust and parse a simple snippet", {
  tree <- parse_string(language = "faust", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fennel: Smoke test: load fennel and parse a simple snippet", {
  tree <- parse_string(language = "fennel", source = "(fn hello [] (print :hello))")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fidl: Smoke test: load fidl and parse a simple snippet", {
  tree <- parse_string(language = "fidl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_firrtl: Smoke test: load firrtl and parse a simple snippet", {
  tree <- parse_string(language = "firrtl", source = "circuit Main :")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fish: Smoke test: load fish and parse a simple snippet", {
  tree <- parse_string(language = "fish", source = "echo hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_foam: Smoke test: load foam and parse a simple snippet", {
  tree <- parse_string(language = "foam", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_forth: Smoke test: load forth and parse a simple snippet", {
  tree <- parse_string(language = "forth", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fortran: Smoke test: load fortran and parse a simple snippet", {
  tree <- parse_string(language = "fortran", source = "program main\nend program main")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fsharp: Smoke test: load fsharp and parse a simple snippet", {
  tree <- parse_string(language = "fsharp", source = "let x = 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_fsharp_signature: Smoke test: load fsharp_signature and parse a simple snippet", {
  tree <- parse_string(language = "fsharp_signature", source = "val x: int")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_func: Smoke test: load func and parse a simple snippet", {
  tree <- parse_string(language = "func", source = "() recv_internal() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gap: Smoke test: load gap and parse a simple snippet", {
  tree <- parse_string(language = "gap", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gdscript: Smoke test: load gdscript and parse a simple snippet", {
  tree <- parse_string(language = "gdscript", source = "extends Node\nfunc _ready():\n\tpass")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gdshader: Smoke test: load gdshader and parse a simple snippet", {
  tree <- parse_string(language = "gdshader", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_git_config: Smoke test: load git_config and parse a simple snippet", {
  tree <- parse_string(language = "git_config", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_git_rebase: Smoke test: load git_rebase and parse a simple snippet", {
  tree <- parse_string(language = "git_rebase", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gitattributes: Smoke test: load gitattributes and parse a simple snippet", {
  tree <- parse_string(language = "gitattributes", source = "*.txt text")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gitcommit: Smoke test: load gitcommit and parse a simple snippet", {
  tree <- parse_string(language = "gitcommit", source = "feat: add feature\n\nBody text")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gitignore: Smoke test: load gitignore and parse a simple snippet", {
  tree <- parse_string(language = "gitignore", source = "*.o\n*.log")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gleam: Smoke test: load gleam and parse a simple snippet", {
  tree <- parse_string(language = "gleam", source = "pub fn main() { }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_glimmer: Smoke test: load glimmer and parse a simple snippet", {
  tree <- parse_string(language = "glimmer", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_glsl: Smoke test: load glsl and parse a simple snippet", {
  tree <- parse_string(language = "glsl", source = "void main() { gl_Position = vec4(0.0); }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gn: Smoke test: load gn and parse a simple snippet", {
  tree <- parse_string(language = "gn", source = "group(\"hello\") {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gnuplot: Smoke test: load gnuplot and parse a simple snippet", {
  tree <- parse_string(language = "gnuplot", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_go: Smoke test: load go and parse a simple snippet", {
  tree <- parse_string(language = "go", source = "package main")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_godot_resource: Smoke test: load godot_resource and parse a simple snippet", {
  tree <- parse_string(language = "godot_resource", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gomod: Smoke test: load gomod and parse a simple snippet", {
  tree <- parse_string(language = "gomod", source = "module example.com/hello\n\ngo 1.21")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gosum: Smoke test: load gosum and parse a simple snippet", {
  tree <- parse_string(language = "gosum", source = "example.com/pkg v1.0.0 h1:abc=")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gotmpl: Smoke test: load gotmpl and parse a simple snippet", {
  tree <- parse_string(language = "gotmpl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gowork: Smoke test: load gowork and parse a simple snippet", {
  tree <- parse_string(language = "gowork", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gpg: Smoke test: load gpg and parse a simple snippet", {
  tree <- parse_string(language = "gpg", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_graphql: Smoke test: load graphql and parse a simple snippet", {
  tree <- parse_string(language = "graphql", source = "type Query { hello: String }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gren: Smoke test: load gren and parse a simple snippet", {
  tree <- parse_string(language = "gren", source = "module Main exposing (..)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_groovy: Smoke test: load groovy and parse a simple snippet", {
  tree <- parse_string(language = "groovy", source = "def x = 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_gstlaunch: Smoke test: load gstlaunch and parse a simple snippet", {
  tree <- parse_string(language = "gstlaunch", source = "fakesrc ! fakesink")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hack: Smoke test: load hack and parse a simple snippet", {
  tree <- parse_string(language = "hack", source = "<?hh\nfunction main(): void {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hare: Smoke test: load hare and parse a simple snippet", {
  tree <- parse_string(language = "hare", source = "export fn main() void = void;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_haskell: Smoke test: load haskell and parse a simple snippet", {
  tree <- parse_string(language = "haskell", source = "main = putStrLn \"hello\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_haxe: Smoke test: load haxe and parse a simple snippet", {
  tree <- parse_string(language = "haxe", source = "class Main { static function main() {} }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hcl: Smoke test: load hcl and parse a simple snippet", {
  tree <- parse_string(language = "hcl", source = "variable \"name\" { type = string }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_heex: Smoke test: load heex and parse a simple snippet", {
  tree <- parse_string(language = "heex", source = "<%= @greeting %>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hjson: Smoke test: load hjson and parse a simple snippet", {
  tree <- parse_string(language = "hjson", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hlsl: Smoke test: load hlsl and parse a simple snippet", {
  tree <- parse_string(language = "hlsl", source = "float4 main() : SV_Target { return 0; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hocon: Smoke test: load hocon and parse a simple snippet", {
  tree <- parse_string(language = "hocon", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hoon: Smoke test: load hoon and parse a simple snippet", {
  tree <- parse_string(language = "hoon", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_html: Smoke test: load html and parse a simple snippet", {
  tree <- parse_string(language = "html", source = "<p>hello</p>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_htmldjango: Smoke test: load htmldjango and parse a simple snippet", {
  tree <- parse_string(language = "htmldjango", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_http: Smoke test: load http and parse a simple snippet", {
  tree <- parse_string(language = "http", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hurl: Smoke test: load hurl and parse a simple snippet", {
  tree <- parse_string(language = "hurl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_hyprlang: Smoke test: load hyprlang and parse a simple snippet", {
  tree <- parse_string(language = "hyprlang", source = "general { border_size = 1 }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_idris: Smoke test: load idris and parse a simple snippet", {
  tree <- parse_string(language = "idris", source = "module Main")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ini: Smoke test: load ini and parse a simple snippet", {
  tree <- parse_string(language = "ini", source = "[section]\nkey = value")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ispc: Smoke test: load ispc and parse a simple snippet", {
  tree <- parse_string(language = "ispc", source = "export void main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_jai: Smoke test: load jai and parse a simple snippet", {
  tree <- parse_string(language = "jai", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_janet: Smoke test: load janet and parse a simple snippet", {
  tree <- parse_string(language = "janet", source = "(print \"hello\")")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_java: Smoke test: load java and parse a simple snippet", {
  tree <- parse_string(language = "java", source = "class Main {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_javadoc: Smoke test: load javadoc and parse a simple snippet", {
  tree <- parse_string(language = "javadoc", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_javascript: Smoke test: load javascript and parse a simple snippet", {
  tree <- parse_string(language = "javascript", source = "console.log('hello');")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_jinja2: Smoke test: load jinja2 and parse a simple snippet", {
  tree <- parse_string(language = "jinja2", source = "{{ variable }}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_jq: Smoke test: load jq and parse a simple snippet", {
  tree <- parse_string(language = "jq", source = ".[] | select(.key)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_jsdoc: Smoke test: load jsdoc and parse a simple snippet", {
  tree <- parse_string(language = "jsdoc", source = "/** @param {string} name */")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_json: Smoke test: load json and parse a simple snippet", {
  tree <- parse_string(language = "json", source = "{\"key\": \"value\"}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_json5: Smoke test: load json5 and parse a simple snippet", {
  tree <- parse_string(language = "json5", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_jsonnet: Smoke test: load jsonnet and parse a simple snippet", {
  tree <- parse_string(language = "jsonnet", source = "{ key: 'value' }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_julia: Smoke test: load julia and parse a simple snippet", {
  tree <- parse_string(language = "julia", source = "function main() end")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_just: Smoke test: load just and parse a simple snippet", {
  tree <- parse_string(language = "just", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_kcl: Smoke test: load kcl and parse a simple snippet", {
  tree <- parse_string(language = "kcl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_kconfig: Smoke test: load kconfig and parse a simple snippet", {
  tree <- parse_string(language = "kconfig", source = "config FOO\n\tbool \"Enable foo\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_kdl: Smoke test: load kdl and parse a simple snippet", {
  tree <- parse_string(language = "kdl", source = "node \"value\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_kotlin: Smoke test: load kotlin and parse a simple snippet", {
  tree <- parse_string(language = "kotlin", source = "fun main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_latex: Smoke test: load latex and parse a simple snippet", {
  tree <- parse_string(language = "latex", source = "\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_lean: Smoke test: load lean and parse a simple snippet", {
  tree <- parse_string(language = "lean", source = "def main : IO Unit := pure ()")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ledger: Smoke test: load ledger and parse a simple snippet", {
  tree <- parse_string(language = "ledger", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_less: Smoke test: load less and parse a simple snippet", {
  tree <- parse_string(language = "less", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_linkerscript: Smoke test: load linkerscript and parse a simple snippet", {
  tree <- parse_string(language = "linkerscript", source = "sections { .text : { *(.text) } }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_liquid: Smoke test: load liquid and parse a simple snippet", {
  tree <- parse_string(language = "liquid", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_llvm: Smoke test: load llvm and parse a simple snippet", {
  tree <- parse_string(language = "llvm", source = "define i32 @main() { ret i32 0 }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_lua: Smoke test: load lua and parse a simple snippet", {
  tree <- parse_string(language = "lua", source = "print('hello')")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_luadoc: Smoke test: load luadoc and parse a simple snippet", {
  tree <- parse_string(language = "luadoc", source = "---@param name string")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_luap: Smoke test: load luap and parse a simple snippet", {
  tree <- parse_string(language = "luap", source = "[a-z]+")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_luau: Smoke test: load luau and parse a simple snippet", {
  tree <- parse_string(language = "luau", source = "local x: number = 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_magik: Smoke test: load magik and parse a simple snippet", {
  tree <- parse_string(language = "magik", source = "_method object.hello\n_endmethod")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_make: Smoke test: load make and parse a simple snippet", {
  tree <- parse_string(language = "make", source = "all:\n\techo hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_markdown: Smoke test: load markdown and parse a simple snippet", {
  tree <- parse_string(language = "markdown", source = "# Hello\n\nWorld")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_markdown_inline: Smoke test: load markdown_inline and parse a simple snippet", {
  tree <- parse_string(language = "markdown_inline", source = "**bold** and *italic*")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_matlab: Smoke test: load matlab and parse a simple snippet", {
  tree <- parse_string(language = "matlab", source = "function y = hello(x)\ny = x;\nend")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_mermaid: Smoke test: load mermaid and parse a simple snippet", {
  tree <- parse_string(language = "mermaid", source = "graph TD\nA --> B")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_meson: Smoke test: load meson and parse a simple snippet", {
  tree <- parse_string(language = "meson", source = "project('hello', 'c')")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_mlir: Smoke test: load mlir and parse a simple snippet", {
  tree <- parse_string(language = "mlir", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_mojo: Smoke test: load mojo and parse a simple snippet", {
  tree <- parse_string(language = "mojo", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_move: Smoke test: load move and parse a simple snippet", {
  tree <- parse_string(language = "move", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nasm: Smoke test: load nasm and parse a simple snippet", {
  tree <- parse_string(language = "nasm", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_netlinx: Smoke test: load netlinx and parse a simple snippet", {
  tree <- parse_string(language = "netlinx", source = "program_name='hello'")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nginx: Smoke test: load nginx and parse a simple snippet", {
  tree <- parse_string(language = "nginx", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nickel: Smoke test: load nickel and parse a simple snippet", {
  tree <- parse_string(language = "nickel", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nim: Smoke test: load nim and parse a simple snippet", {
  tree <- parse_string(language = "nim", source = "echo \"hello\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ninja: Smoke test: load ninja and parse a simple snippet", {
  tree <- parse_string(language = "ninja", source = "rule cc\n  command = cc $in -o $out")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nix: Smoke test: load nix and parse a simple snippet", {
  tree <- parse_string(language = "nix", source = "{ pkgs ? import <nixpkgs> {} }: pkgs.hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_norg: Smoke test: load norg and parse a simple snippet", {
  tree <- parse_string(language = "norg", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_norg_meta: Smoke test: load norg_meta and parse a simple snippet", {
  tree <- parse_string(language = "norg_meta", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nqc: Smoke test: load nqc and parse a simple snippet", {
  tree <- parse_string(language = "nqc", source = "task main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_nushell: Smoke test: load nushell and parse a simple snippet", {
  tree <- parse_string(language = "nushell", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_objc: Smoke test: load objc and parse a simple snippet", {
  tree <- parse_string(language = "objc", source = "@interface Main @end")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ocaml: Smoke test: load ocaml and parse a simple snippet", {
  tree <- parse_string(language = "ocaml", source = "let () = print_endline \"hello\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ocaml_interface: Smoke test: load ocaml_interface and parse a simple snippet", {
  tree <- parse_string(language = "ocaml_interface", source = "val x : int")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ocamllex: Smoke test: load ocamllex and parse a simple snippet", {
  tree <- parse_string(language = "ocamllex", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_odin: Smoke test: load odin and parse a simple snippet", {
  tree <- parse_string(language = "odin", source = "package main")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_openscad: Smoke test: load openscad and parse a simple snippet", {
  tree <- parse_string(language = "openscad", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_org: Smoke test: load org and parse a simple snippet", {
  tree <- parse_string(language = "org", source = "* Hello\nWorld")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pascal: Smoke test: load pascal and parse a simple snippet", {
  tree <- parse_string(language = "pascal", source = "program Hello; begin end.")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pem: Smoke test: load pem and parse a simple snippet", {
  tree <- parse_string(language = "pem", source = "-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_perl: Smoke test: load perl and parse a simple snippet", {
  tree <- parse_string(language = "perl", source = "print 'hello';")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pgn: Smoke test: load pgn and parse a simple snippet", {
  tree <- parse_string(language = "pgn", source = "1. e4 e5 *")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_php: Smoke test: load php and parse a simple snippet", {
  tree <- parse_string(language = "php", source = "<?php echo 'hello'; ?>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_phpdoc: Smoke test: load phpdoc and parse a simple snippet", {
  tree <- parse_string(language = "phpdoc", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pkl: Smoke test: load pkl and parse a simple snippet", {
  tree <- parse_string(language = "pkl", source = "name = \"hello\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_po: Smoke test: load po and parse a simple snippet", {
  tree <- parse_string(language = "po", source = "msgid \"hello\"\nmsgstr \"world\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_poe_filter: Smoke test: load poe_filter and parse a simple snippet", {
  tree <- parse_string(language = "poe_filter", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pony: Smoke test: load pony and parse a simple snippet", {
  tree <- parse_string(language = "pony", source = "actor Main\n  new create(env: Env) => None")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_postscript: Smoke test: load postscript and parse a simple snippet", {
  tree <- parse_string(language = "postscript", source = "/hello { (Hello) show } def")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_powershell: Smoke test: load powershell and parse a simple snippet", {
  tree <- parse_string(language = "powershell", source = "write-host 'hello'")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_printf: Smoke test: load printf and parse a simple snippet", {
  tree <- parse_string(language = "printf", source = "%d %s")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_prisma: Smoke test: load prisma and parse a simple snippet", {
  tree <- parse_string(language = "prisma", source = "model User { id Int @id }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_prolog: Smoke test: load prolog and parse a simple snippet", {
  tree <- parse_string(language = "prolog", source = "hello :- write('hello'), nl.")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_promql: Smoke test: load promql and parse a simple snippet", {
  tree <- parse_string(language = "promql", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_properties: Smoke test: load properties and parse a simple snippet", {
  tree <- parse_string(language = "properties", source = "key=value")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_proto: Smoke test: load proto and parse a simple snippet", {
  tree <- parse_string(language = "proto", source = "syntax = \"proto3\";")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_prql: Smoke test: load prql and parse a simple snippet", {
  tree <- parse_string(language = "prql", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_psv: Smoke test: load psv and parse a simple snippet", {
  tree <- parse_string(language = "psv", source = "a|b|c\n1|2|3")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pug: Smoke test: load pug and parse a simple snippet", {
  tree <- parse_string(language = "pug", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_puppet: Smoke test: load puppet and parse a simple snippet", {
  tree <- parse_string(language = "puppet", source = "notify { 'hello': }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_purescript: Smoke test: load purescript and parse a simple snippet", {
  tree <- parse_string(language = "purescript", source = "module Main where")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_pymanifest: Smoke test: load pymanifest and parse a simple snippet", {
  tree <- parse_string(language = "pymanifest", source = "include *.txt")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_python: Smoke test: load python and parse a simple snippet", {
  tree <- parse_string(language = "python", source = "print('hello')")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ql: Smoke test: load ql and parse a simple snippet", {
  tree <- parse_string(language = "ql", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_qmldir: Smoke test: load qmldir and parse a simple snippet", {
  tree <- parse_string(language = "qmldir", source = "module Example")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_qmljs: Smoke test: load qmljs and parse a simple snippet", {
  tree <- parse_string(language = "qmljs", source = "import QtQuick 2.0\nItem {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_query: Smoke test: load query and parse a simple snippet", {
  tree <- parse_string(language = "query", source = "(identifier) @name")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_r: Smoke test: load r and parse a simple snippet", {
  tree <- parse_string(language = "r", source = "print('hello')")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_racket: Smoke test: load racket and parse a simple snippet", {
  tree <- parse_string(language = "racket", source = "#lang racket\n(define x 1)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rasi: Smoke test: load rasi and parse a simple snippet", {
  tree <- parse_string(language = "rasi", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_razor: Smoke test: load razor and parse a simple snippet", {
  tree <- parse_string(language = "razor", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rbs: Smoke test: load rbs and parse a simple snippet", {
  tree <- parse_string(language = "rbs", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_re2c: Smoke test: load re2c and parse a simple snippet", {
  tree <- parse_string(language = "re2c", source = "/*!re2c\n  [a-z]+ { return; }\n*/")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_readline: Smoke test: load readline and parse a simple snippet", {
  tree <- parse_string(language = "readline", source = "set editing-mode vi")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_regex: Smoke test: load regex and parse a simple snippet", {
  tree <- parse_string(language = "regex", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rego: Smoke test: load rego and parse a simple snippet", {
  tree <- parse_string(language = "rego", source = "package main\ndefault allow = false")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_requirements: Smoke test: load requirements and parse a simple snippet", {
  tree <- parse_string(language = "requirements", source = "flask>=2.0")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rescript: Smoke test: load rescript and parse a simple snippet", {
  tree <- parse_string(language = "rescript", source = "let x = 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_robot: Smoke test: load robot and parse a simple snippet", {
  tree <- parse_string(language = "robot", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_roc: Smoke test: load roc and parse a simple snippet", {
  tree <- parse_string(language = "roc", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ron: Smoke test: load ron and parse a simple snippet", {
  tree <- parse_string(language = "ron", source = "(key: \"value\")")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rst: Smoke test: load rst and parse a simple snippet", {
  tree <- parse_string(language = "rst", source = "hello\n=====\n\nworld")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rtf: Smoke test: load rtf and parse a simple snippet", {
  tree <- parse_string(language = "rtf", source = "{\\rtf1 hello}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ruby: Smoke test: load ruby and parse a simple snippet", {
  tree <- parse_string(language = "ruby", source = "puts 'hello'")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_rust: Smoke test: load rust and parse a simple snippet", {
  tree <- parse_string(language = "rust", source = "fn main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_scala: Smoke test: load scala and parse a simple snippet", {
  tree <- parse_string(language = "scala", source = "object Main")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_scheme: Smoke test: load scheme and parse a simple snippet", {
  tree <- parse_string(language = "scheme", source = "(define x 1)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_scss: Smoke test: load scss and parse a simple snippet", {
  tree <- parse_string(language = "scss", source = "$color: red;\nbody { color: $color; }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_slang: Smoke test: load slang and parse a simple snippet", {
  tree <- parse_string(language = "slang", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_smali: Smoke test: load smali and parse a simple snippet", {
  tree <- parse_string(language = "smali", source = ".class public LMain;\n.super Ljava/lang/Object;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_smalltalk: Smoke test: load smalltalk and parse a simple snippet", {
  tree <- parse_string(language = "smalltalk", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_smithy: Smoke test: load smithy and parse a simple snippet", {
  tree <- parse_string(language = "smithy", source = "namespace example\nstring MyString")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sml: Smoke test: load sml and parse a simple snippet", {
  tree <- parse_string(language = "sml", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_snakemake: Smoke test: load snakemake and parse a simple snippet", {
  tree <- parse_string(language = "snakemake", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_solidity: Smoke test: load solidity and parse a simple snippet", {
  tree <- parse_string(language = "solidity", source = "pragma solidity ^0.8.0;\ncontract Main {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_souffle: Smoke test: load souffle and parse a simple snippet", {
  tree <- parse_string(language = "souffle", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sourcepawn: Smoke test: load sourcepawn and parse a simple snippet", {
  tree <- parse_string(language = "sourcepawn", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sparql: Smoke test: load sparql and parse a simple snippet", {
  tree <- parse_string(language = "sparql", source = "select ?s where { ?s ?p ?o }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sql: Smoke test: load sql and parse a simple snippet", {
  tree <- parse_string(language = "sql", source = "select 1;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sql_bigquery: Smoke test: load sql_bigquery and parse a simple snippet", {
  tree <- parse_string(language = "sql_bigquery", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_squirrel: Smoke test: load squirrel and parse a simple snippet", {
  tree <- parse_string(language = "squirrel", source = "function main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ssh_config: Smoke test: load ssh_config and parse a simple snippet", {
  tree <- parse_string(language = "ssh_config", source = "host example\n  hostname example.com")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_stan: Smoke test: load stan and parse a simple snippet", {
  tree <- parse_string(language = "stan", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_starlark: Smoke test: load starlark and parse a simple snippet", {
  tree <- parse_string(language = "starlark", source = "def hello(): pass")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_superhtml: Smoke test: load superhtml and parse a simple snippet", {
  tree <- parse_string(language = "superhtml", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_svelte: Smoke test: load svelte and parse a simple snippet", {
  tree <- parse_string(language = "svelte", source = "<script>let x = 1;</script>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_sway: Smoke test: load sway and parse a simple snippet", {
  tree <- parse_string(language = "sway", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_swift: Smoke test: load swift and parse a simple snippet", {
  tree <- parse_string(language = "swift", source = "print(\"hello\")")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_systemverilog: Smoke test: load systemverilog and parse a simple snippet", {
  tree <- parse_string(language = "systemverilog", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tablegen: Smoke test: load tablegen and parse a simple snippet", {
  tree <- parse_string(language = "tablegen", source = "def Hello : Base {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tact: Smoke test: load tact and parse a simple snippet", {
  tree <- parse_string(language = "tact", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tcl: Smoke test: load tcl and parse a simple snippet", {
  tree <- parse_string(language = "tcl", source = "puts hello")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_teal: Smoke test: load teal and parse a simple snippet", {
  tree <- parse_string(language = "teal", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_templ: Smoke test: load templ and parse a simple snippet", {
  tree <- parse_string(language = "templ", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tera: Smoke test: load tera and parse a simple snippet", {
  tree <- parse_string(language = "tera", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_terraform: Smoke test: load terraform and parse a simple snippet", {
  tree <- parse_string(language = "terraform", source = "resource \"null_resource\" \"main\" {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_test: Smoke test: load test and parse a simple snippet", {
  tree <- parse_string(language = "test", source = "===========\nTest\n===========\n---\n(node)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_textproto: Smoke test: load textproto and parse a simple snippet", {
  tree <- parse_string(language = "textproto", source = "key: \"value\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_thrift: Smoke test: load thrift and parse a simple snippet", {
  tree <- parse_string(language = "thrift", source = "service HelloService {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tlaplus: Smoke test: load tlaplus and parse a simple snippet", {
  tree <- parse_string(language = "tlaplus", source = "---- MODULE Main ----\n====")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tmux: Smoke test: load tmux and parse a simple snippet", {
  tree <- parse_string(language = "tmux", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_todotxt: Smoke test: load todotxt and parse a simple snippet", {
  tree <- parse_string(language = "todotxt", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_toml: Smoke test: load toml and parse a simple snippet", {
  tree <- parse_string(language = "toml", source = "key = \"value\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tsv: Smoke test: load tsv and parse a simple snippet", {
  tree <- parse_string(language = "tsv", source = "a\tb\tc\n1\t2\t3")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_tsx: Smoke test: load tsx and parse a simple snippet", {
  tree <- parse_string(language = "tsx", source = "const App = () => <div />;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_turtle: Smoke test: load turtle and parse a simple snippet", {
  tree <- parse_string(language = "turtle", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_twig: Smoke test: load twig and parse a simple snippet", {
  tree <- parse_string(language = "twig", source = "{{ variable }}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_typescript: Smoke test: load typescript and parse a simple snippet", {
  tree <- parse_string(language = "typescript", source = "const x: number = 42;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_typespec: Smoke test: load typespec and parse a simple snippet", {
  tree <- parse_string(language = "typespec", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_typoscript: Smoke test: load typoscript and parse a simple snippet", {
  tree <- parse_string(language = "typoscript", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_typst: Smoke test: load typst and parse a simple snippet", {
  tree <- parse_string(language = "typst", source = "#let x = 1")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_udev: Smoke test: load udev and parse a simple snippet", {
  tree <- parse_string(language = "udev", source = "action==\"add\", kernel==\"sd*\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ungrammar: Smoke test: load ungrammar and parse a simple snippet", {
  tree <- parse_string(language = "ungrammar", source = "root = item*\nitem = 'token'")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_uxntal: Smoke test: load uxntal and parse a simple snippet", {
  tree <- parse_string(language = "uxntal", source = "|0100 LIT 01")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_v: Smoke test: load v and parse a simple snippet", {
  tree <- parse_string(language = "v", source = "fn main() {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vb: Smoke test: load vb and parse a simple snippet", {
  tree <- parse_string(language = "vb", source = "module main\nend module")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_verilog: Smoke test: load verilog and parse a simple snippet", {
  tree <- parse_string(language = "verilog", source = "module main; endmodule")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vhdl: Smoke test: load vhdl and parse a simple snippet", {
  tree <- parse_string(language = "vhdl", source = "entity main is end main;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vhs: Smoke test: load vhs and parse a simple snippet", {
  tree <- parse_string(language = "vhs", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vim: Smoke test: load vim and parse a simple snippet", {
  tree <- parse_string(language = "vim", source = "echo 'hello'")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vimdoc: Smoke test: load vimdoc and parse a simple snippet", {
  tree <- parse_string(language = "vimdoc", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vrl: Smoke test: load vrl and parse a simple snippet", {
  tree <- parse_string(language = "vrl", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_vue: Smoke test: load vue and parse a simple snippet", {
  tree <- parse_string(language = "vue", source = "<template><div>hello</div></template>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wast: Smoke test: load wast and parse a simple snippet", {
  tree <- parse_string(language = "wast", source = "(module)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wat: Smoke test: load wat and parse a simple snippet", {
  tree <- parse_string(language = "wat", source = "(module)")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wgsl: Smoke test: load wgsl and parse a simple snippet", {
  tree <- parse_string(language = "wgsl", source = "@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wgsl_bevy: Smoke test: load wgsl_bevy and parse a simple snippet", {
  tree <- parse_string(language = "wgsl_bevy", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wit: Smoke test: load wit and parse a simple snippet", {
  tree <- parse_string(language = "wit", source = "package example:pkg;")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_wolfram: Smoke test: load wolfram and parse a simple snippet", {
  tree <- parse_string(language = "wolfram", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_x86asm: Smoke test: load x86asm and parse a simple snippet", {
  tree <- parse_string(language = "x86asm", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_xcompose: Smoke test: load xcompose and parse a simple snippet", {
  tree <- parse_string(language = "xcompose", source = "<Multi_key> <a> : \"a\"")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_xml: Smoke test: load xml and parse a simple snippet", {
  tree <- parse_string(language = "xml", source = "<?xml version=\"1.0\"?>\n<root>hello</root>")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_yaml: Smoke test: load yaml and parse a simple snippet", {
  tree <- parse_string(language = "yaml", source = "key: value")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_yuck: Smoke test: load yuck and parse a simple snippet", {
  tree <- parse_string(language = "yuck", source = "(defwidget main [] (label :text \"hi\"))")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_zig: Smoke test: load zig and parse a simple snippet", {
  tree <- parse_string(language = "zig", source = "pub fn main() void {}")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ziggy: Smoke test: load ziggy and parse a simple snippet", {
  tree <- parse_string(language = "ziggy", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_ziggy_schema: Smoke test: load ziggy_schema and parse a simple snippet", {
  tree <- parse_string(language = "ziggy_schema", source = "x")
  expect_true(tree$root_child_count() >= 1)
})

test_that("smoke_zsh: Smoke test: load zsh and parse a simple snippet", {
  tree <- parse_string(language = "zsh", source = "echo hello")
  expect_true(tree$root_child_count() >= 1)
})
