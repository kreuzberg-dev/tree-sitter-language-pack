/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:0af180097940f325f0e84745767562fee934c6e686a876b01f8a8a57c5564ffc
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: parsing */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "ts_pack.h"
#include "test_runner.h"

void test_parsing_go_function(void) {
    /* Parse a Go function declaration and assert node type */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"go\"}");
    TS_PACKAlefHandle result = ts_pack_process("package main\nfunc main() {}", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_html_element(void) {
    /* Parse an HTML element and assert node type */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"html\"}");
    TS_PACKAlefHandle result = ts_pack_process("<div>hello</div>", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_javascript_class(void) {
    /* Parse a JavaScript class declaration. */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKAlefHandle result = ts_pack_process("class Foo { bar() {} }", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_javascript_variable(void) {
    /* Parse a JavaScript variable declaration and assert node type */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"javascript\"}");
    TS_PACKAlefHandle result = ts_pack_process("const x = 1;", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_python_function(void) {
    /* Parse a Python function definition and assert node type */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"python\"}");
    TS_PACKAlefHandle result = ts_pack_process("def hello(): pass", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_rust_function(void) {
    /* Parse a Rust function definition and assert node type */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"rust\"}");
    TS_PACKAlefHandle result = ts_pack_process("fn main() {}", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_rust_struct(void) {
    /* Parse a Rust struct definition. */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"rust\"}");
    TS_PACKAlefHandle result = ts_pack_process("struct Point { x: f64, y: f64 }", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}

void test_parsing_typescript_function(void) {
    /* Parse a TypeScript function with type annotations and template literal return */
    TS_PACKAlefHandle config_handle = ts_pack_process_config_from_json("{\"language\":\"typescript\"}");
    TS_PACKAlefHandle result = ts_pack_process("function greet(name: string): string { return `hi ${name}`; }", config_handle);
    assert(result != 0 && "expected call to succeed");
    ts_pack_process_config_free(config_handle);
    ts_pack_process_result_free(result);
}
