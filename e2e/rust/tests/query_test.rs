// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: query

use tree_sitter_language_pack::process;

#[test]
fn test_highlights_query_python() {
    // get_highlights_query returns non-empty string for python
    let source = String::new();
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[test]
fn test_highlights_query_unknown_language() {
    // get_highlights_query returns None for unknown language
    let source = String::new();
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(result.is_empty(), "expected empty value");
}

#[test]
fn test_injections_query_javascript() {
    // get_injections_query returns non-empty for javascript (has embedded languages)
    let source = String::new();
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[test]
fn test_locals_query_python() {
    // get_locals_query returns non-empty for python
    let source = String::new();
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(!result.is_empty(), "expected non-empty value");
}

#[test]
fn test_run_query_python_functions() {
    // Parse Python and run a query to find function definitions
    let source = r#"def hello():
    pass

def world():
    return 42
"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result
            .run_query(
                serde_json::from_str(
                    r#"{"language":"python","query_source":"(function_definition name: (identifier) @name)"}"#
                )
                .unwrap()
            )
            .len()
            >= 2,
        "expected at least 2 elements, got {}",
        result
            .run_query(
                serde_json::from_str(
                    r#"{"language":"python","query_source":"(function_definition name: (identifier) @name)"}"#
                )
                .unwrap()
            )
            .len()
    );
}
