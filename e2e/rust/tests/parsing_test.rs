// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: parsing

use tree_sitter_language_pack::process;

#[test]
fn test_parsing_go_function() {
    // Parse a Go function declaration and assert node type
    let source = r#"package main
func main() {}"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"function_declaration"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_html_element() {
    // Parse an HTML element and assert node type
    let source = r#"<div>hello</div>"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"element"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_javascript_class() {
    // Parse a JavaScript class declaration.
    let source = r#"class Foo { bar() {} }"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(!result.root_child_count.is_empty(), "expected >= 1");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"class_declaration"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_javascript_variable() {
    // Parse a JavaScript variable declaration and assert node type
    let source = r#"const x = 1;"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"lexical_declaration"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_python_function() {
    // Parse a Python function definition and assert node type
    let source = r#"def hello(): pass"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"function_definition"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_rust_function() {
    // Parse a Rust function definition and assert node type
    let source = r#"fn main() {}"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(
        result.contains_node_type(serde_json::from_str(r#"{"node_type":"function_item"}"#).unwrap()),
        "method_result is_true assertion failed"
    );
}

#[test]
fn test_parsing_rust_struct() {
    // Parse a Rust struct definition.
    let source = r#"struct Point { x: f64, y: f64 }"#;
    let config = None;
    let result = process(&source, config).expect("should succeed");
    assert!(!result.root_child_count.is_empty(), "expected >= 1");
}
