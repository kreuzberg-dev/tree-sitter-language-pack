// Code generated by e2e-generator. DO NOT EDIT.
#![allow(clippy::too_many_lines)]

#[test]
fn smoke_abl() {
    if !tree_sitter_language_pack::has_language("abl") {
        eprintln!("Skipping: language 'abl' not available");
        return;
    }
    // Smoke test: load abl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("abl").expect("Failed to get parser for 'abl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_actionscript() {
    if !tree_sitter_language_pack::has_language("actionscript") {
        eprintln!("Skipping: language 'actionscript' not available");
        return;
    }
    // Smoke test: load actionscript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("actionscript").expect("Failed to get parser for 'actionscript'");
    let tree = parser.parse("var x:int = 1;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ada() {
    if !tree_sitter_language_pack::has_language("ada") {
        eprintln!("Skipping: language 'ada' not available");
        return;
    }
    // Smoke test: load ada and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ada").expect("Failed to get parser for 'ada'");
    let tree = parser.parse("procedure Main is begin null; end Main;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_agda() {
    if !tree_sitter_language_pack::has_language("agda") {
        eprintln!("Skipping: language 'agda' not available");
        return;
    }
    // Smoke test: load agda and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("agda").expect("Failed to get parser for 'agda'");
    let tree = parser.parse("module Main where", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_al() {
    if !tree_sitter_language_pack::has_language("al") {
        eprintln!("Skipping: language 'al' not available");
        return;
    }
    // Smoke test: load al and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("al").expect("Failed to get parser for 'al'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_angular() {
    if !tree_sitter_language_pack::has_language("angular") {
        eprintln!("Skipping: language 'angular' not available");
        return;
    }
    // Smoke test: load angular and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("angular").expect("Failed to get parser for 'angular'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_apex() {
    if !tree_sitter_language_pack::has_language("apex") {
        eprintln!("Skipping: language 'apex' not available");
        return;
    }
    // Smoke test: load apex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("apex").expect("Failed to get parser for 'apex'");
    let tree = parser.parse("public class Main {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_arduino() {
    if !tree_sitter_language_pack::has_language("arduino") {
        eprintln!("Skipping: language 'arduino' not available");
        return;
    }
    // Smoke test: load arduino and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("arduino").expect("Failed to get parser for 'arduino'");
    let tree = parser.parse("void setup() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_asciidoc() {
    if !tree_sitter_language_pack::has_language("asciidoc") {
        eprintln!("Skipping: language 'asciidoc' not available");
        return;
    }
    // Smoke test: load asciidoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("asciidoc").expect("Failed to get parser for 'asciidoc'");
    let tree = parser.parse("= Title\n\nParagraph.", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_asm() {
    if !tree_sitter_language_pack::has_language("asm") {
        eprintln!("Skipping: language 'asm' not available");
        return;
    }
    // Smoke test: load asm and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("asm").expect("Failed to get parser for 'asm'");
    let tree = parser.parse("mov eax, 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_astro() {
    if !tree_sitter_language_pack::has_language("astro") {
        eprintln!("Skipping: language 'astro' not available");
        return;
    }
    // Smoke test: load astro and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("astro").expect("Failed to get parser for 'astro'");
    let tree = parser.parse("---\n---\n<p>hello</p>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_awk() {
    if !tree_sitter_language_pack::has_language("awk") {
        eprintln!("Skipping: language 'awk' not available");
        return;
    }
    // Smoke test: load awk and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("awk").expect("Failed to get parser for 'awk'");
    let tree = parser.parse("BEGIN { print \"hello\" }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bash() {
    if !tree_sitter_language_pack::has_language("bash") {
        eprintln!("Skipping: language 'bash' not available");
        return;
    }
    // Smoke test: load bash and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bash").expect("Failed to get parser for 'bash'");
    let tree = parser.parse("echo hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bass() {
    if !tree_sitter_language_pack::has_language("bass") {
        eprintln!("Skipping: language 'bass' not available");
        return;
    }
    // Smoke test: load bass and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bass").expect("Failed to get parser for 'bass'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_batch() {
    if !tree_sitter_language_pack::has_language("batch") {
        eprintln!("Skipping: language 'batch' not available");
        return;
    }
    // Smoke test: load batch and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("batch").expect("Failed to get parser for 'batch'");
    let tree = parser.parse("@echo off\necho hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_beancount() {
    if !tree_sitter_language_pack::has_language("beancount") {
        eprintln!("Skipping: language 'beancount' not available");
        return;
    }
    // Smoke test: load beancount and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("beancount").expect("Failed to get parser for 'beancount'");
    let tree = parser.parse("2024-01-01 open Assets:Bank USD", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bibtex() {
    if !tree_sitter_language_pack::has_language("bibtex") {
        eprintln!("Skipping: language 'bibtex' not available");
        return;
    }
    // Smoke test: load bibtex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bibtex").expect("Failed to get parser for 'bibtex'");
    let tree = parser.parse("@article{key, title={A}}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bicep() {
    if !tree_sitter_language_pack::has_language("bicep") {
        eprintln!("Skipping: language 'bicep' not available");
        return;
    }
    // Smoke test: load bicep and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bicep").expect("Failed to get parser for 'bicep'");
    let tree = parser.parse("param name string", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bitbake() {
    if !tree_sitter_language_pack::has_language("bitbake") {
        eprintln!("Skipping: language 'bitbake' not available");
        return;
    }
    // Smoke test: load bitbake and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bitbake").expect("Failed to get parser for 'bitbake'");
    let tree = parser.parse("DESCRIPTION = \"hello\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_blade() {
    if !tree_sitter_language_pack::has_language("blade") {
        eprintln!("Skipping: language 'blade' not available");
        return;
    }
    // Smoke test: load blade and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("blade").expect("Failed to get parser for 'blade'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_brightscript() {
    if !tree_sitter_language_pack::has_language("brightscript") {
        eprintln!("Skipping: language 'brightscript' not available");
        return;
    }
    // Smoke test: load brightscript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("brightscript").expect("Failed to get parser for 'brightscript'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_bsl() {
    if !tree_sitter_language_pack::has_language("bsl") {
        eprintln!("Skipping: language 'bsl' not available");
        return;
    }
    // Smoke test: load bsl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("bsl").expect("Failed to get parser for 'bsl'");
    let tree = parser.parse("Procedure Main() EndProcedure", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_c() {
    if !tree_sitter_language_pack::has_language("c") {
        eprintln!("Skipping: language 'c' not available");
        return;
    }
    // Smoke test: load c and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("c").expect("Failed to get parser for 'c'");
    let tree = parser.parse("int main() { return 0; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_c3() {
    if !tree_sitter_language_pack::has_language("c3") {
        eprintln!("Skipping: language 'c3' not available");
        return;
    }
    // Smoke test: load c3 and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("c3").expect("Failed to get parser for 'c3'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_caddy() {
    if !tree_sitter_language_pack::has_language("caddy") {
        eprintln!("Skipping: language 'caddy' not available");
        return;
    }
    // Smoke test: load caddy and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("caddy").expect("Failed to get parser for 'caddy'");
    let tree = parser.parse(":8080 {\n\trespond \"Hello\"\n}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cairo() {
    if !tree_sitter_language_pack::has_language("cairo") {
        eprintln!("Skipping: language 'cairo' not available");
        return;
    }
    // Smoke test: load cairo and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cairo").expect("Failed to get parser for 'cairo'");
    let tree = parser.parse("fn main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_capnp() {
    if !tree_sitter_language_pack::has_language("capnp") {
        eprintln!("Skipping: language 'capnp' not available");
        return;
    }
    // Smoke test: load capnp and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("capnp").expect("Failed to get parser for 'capnp'");
    let tree = parser.parse("@0xabcdef1234567890;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cedar() {
    if !tree_sitter_language_pack::has_language("cedar") {
        eprintln!("Skipping: language 'cedar' not available");
        return;
    }
    // Smoke test: load cedar and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cedar").expect("Failed to get parser for 'cedar'");
    let tree = parser.parse("permit(principal, action, resource);", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cedarschema() {
    if !tree_sitter_language_pack::has_language("cedarschema") {
        eprintln!("Skipping: language 'cedarschema' not available");
        return;
    }
    // Smoke test: load cedarschema and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("cedarschema").expect("Failed to get parser for 'cedarschema'");
    let tree = parser.parse("entity User;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cel() {
    if !tree_sitter_language_pack::has_language("cel") {
        eprintln!("Skipping: language 'cel' not available");
        return;
    }
    // Smoke test: load cel and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cel").expect("Failed to get parser for 'cel'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cfml() {
    if !tree_sitter_language_pack::has_language("cfml") {
        eprintln!("Skipping: language 'cfml' not available");
        return;
    }
    // Smoke test: load cfml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cfml").expect("Failed to get parser for 'cfml'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_chatito() {
    if !tree_sitter_language_pack::has_language("chatito") {
        eprintln!("Skipping: language 'chatito' not available");
        return;
    }
    // Smoke test: load chatito and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("chatito").expect("Failed to get parser for 'chatito'");
    let tree = parser.parse("%[greeting]\n    hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_chuck() {
    if !tree_sitter_language_pack::has_language("chuck") {
        eprintln!("Skipping: language 'chuck' not available");
        return;
    }
    // Smoke test: load chuck and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("chuck").expect("Failed to get parser for 'chuck'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_circom() {
    if !tree_sitter_language_pack::has_language("circom") {
        eprintln!("Skipping: language 'circom' not available");
        return;
    }
    // Smoke test: load circom and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("circom").expect("Failed to get parser for 'circom'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_clarity() {
    if !tree_sitter_language_pack::has_language("clarity") {
        eprintln!("Skipping: language 'clarity' not available");
        return;
    }
    // Smoke test: load clarity and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("clarity").expect("Failed to get parser for 'clarity'");
    let tree = parser.parse("(define-public (hello) (ok true))", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_clojure() {
    if !tree_sitter_language_pack::has_language("clojure") {
        eprintln!("Skipping: language 'clojure' not available");
        return;
    }
    // Smoke test: load clojure and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("clojure").expect("Failed to get parser for 'clojure'");
    let tree = parser.parse("(def x 1)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cmake() {
    if !tree_sitter_language_pack::has_language("cmake") {
        eprintln!("Skipping: language 'cmake' not available");
        return;
    }
    // Smoke test: load cmake and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cmake").expect("Failed to get parser for 'cmake'");
    let tree = parser.parse("cmake_minimum_required(VERSION 3.0)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cobol() {
    if !tree_sitter_language_pack::has_language("cobol") {
        eprintln!("Skipping: language 'cobol' not available");
        return;
    }
    // Smoke test: load cobol and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cobol").expect("Failed to get parser for 'cobol'");
    let tree = parser.parse("       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_comment() {
    if !tree_sitter_language_pack::has_language("comment") {
        eprintln!("Skipping: language 'comment' not available");
        return;
    }
    // Smoke test: load comment and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("comment").expect("Failed to get parser for 'comment'");
    let tree = parser.parse("TODO: fix this", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_commonlisp() {
    if !tree_sitter_language_pack::has_language("commonlisp") {
        eprintln!("Skipping: language 'commonlisp' not available");
        return;
    }
    // Smoke test: load commonlisp and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("commonlisp").expect("Failed to get parser for 'commonlisp'");
    let tree = parser.parse("(defun hello () (print \"hello\"))", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cooklang() {
    if !tree_sitter_language_pack::has_language("cooklang") {
        eprintln!("Skipping: language 'cooklang' not available");
        return;
    }
    // Smoke test: load cooklang and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cooklang").expect("Failed to get parser for 'cooklang'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_corn() {
    if !tree_sitter_language_pack::has_language("corn") {
        eprintln!("Skipping: language 'corn' not available");
        return;
    }
    // Smoke test: load corn and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("corn").expect("Failed to get parser for 'corn'");
    let tree = parser.parse("{ key = \"value\" }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cpon() {
    if !tree_sitter_language_pack::has_language("cpon") {
        eprintln!("Skipping: language 'cpon' not available");
        return;
    }
    // Smoke test: load cpon and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cpon").expect("Failed to get parser for 'cpon'");
    let tree = parser.parse("{\"key\": 1}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cpp() {
    if !tree_sitter_language_pack::has_language("cpp") {
        eprintln!("Skipping: language 'cpp' not available");
        return;
    }
    // Smoke test: load cpp and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cpp").expect("Failed to get parser for 'cpp'");
    let tree = parser.parse("int main() { return 0; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_crystal() {
    if !tree_sitter_language_pack::has_language("crystal") {
        eprintln!("Skipping: language 'crystal' not available");
        return;
    }
    // Smoke test: load crystal and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("crystal").expect("Failed to get parser for 'crystal'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_csharp() {
    if !tree_sitter_language_pack::has_language("csharp") {
        eprintln!("Skipping: language 'csharp' not available");
        return;
    }
    // Smoke test: load csharp and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("csharp").expect("Failed to get parser for 'csharp'");
    let tree = parser.parse("class Main {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_css() {
    if !tree_sitter_language_pack::has_language("css") {
        eprintln!("Skipping: language 'css' not available");
        return;
    }
    // Smoke test: load css and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("css").expect("Failed to get parser for 'css'");
    let tree = parser.parse("body { color: red; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cst() {
    if !tree_sitter_language_pack::has_language("cst") {
        eprintln!("Skipping: language 'cst' not available");
        return;
    }
    // Smoke test: load cst and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cst").expect("Failed to get parser for 'cst'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_csv() {
    if !tree_sitter_language_pack::has_language("csv") {
        eprintln!("Skipping: language 'csv' not available");
        return;
    }
    // Smoke test: load csv and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("csv").expect("Failed to get parser for 'csv'");
    let tree = parser.parse("a,b,c\n1,2,3", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cuda() {
    if !tree_sitter_language_pack::has_language("cuda") {
        eprintln!("Skipping: language 'cuda' not available");
        return;
    }
    // Smoke test: load cuda and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cuda").expect("Failed to get parser for 'cuda'");
    let tree = parser.parse("__global__ void kernel() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cue() {
    if !tree_sitter_language_pack::has_language("cue") {
        eprintln!("Skipping: language 'cue' not available");
        return;
    }
    // Smoke test: load cue and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cue").expect("Failed to get parser for 'cue'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_cylc() {
    if !tree_sitter_language_pack::has_language("cylc") {
        eprintln!("Skipping: language 'cylc' not available");
        return;
    }
    // Smoke test: load cylc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("cylc").expect("Failed to get parser for 'cylc'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_d() {
    if !tree_sitter_language_pack::has_language("d") {
        eprintln!("Skipping: language 'd' not available");
        return;
    }
    // Smoke test: load d and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("d").expect("Failed to get parser for 'd'");
    let tree = parser.parse("void main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_dart() {
    if !tree_sitter_language_pack::has_language("dart") {
        eprintln!("Skipping: language 'dart' not available");
        return;
    }
    // Smoke test: load dart and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("dart").expect("Failed to get parser for 'dart'");
    let tree = parser.parse("void main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_desktop() {
    if !tree_sitter_language_pack::has_language("desktop") {
        eprintln!("Skipping: language 'desktop' not available");
        return;
    }
    // Smoke test: load desktop and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("desktop").expect("Failed to get parser for 'desktop'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_devicetree() {
    if !tree_sitter_language_pack::has_language("devicetree") {
        eprintln!("Skipping: language 'devicetree' not available");
        return;
    }
    // Smoke test: load devicetree and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("devicetree").expect("Failed to get parser for 'devicetree'");
    let tree = parser.parse("/dts-v1/;\n/ { };", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_dhall() {
    if !tree_sitter_language_pack::has_language("dhall") {
        eprintln!("Skipping: language 'dhall' not available");
        return;
    }
    // Smoke test: load dhall and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("dhall").expect("Failed to get parser for 'dhall'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_diff() {
    if !tree_sitter_language_pack::has_language("diff") {
        eprintln!("Skipping: language 'diff' not available");
        return;
    }
    // Smoke test: load diff and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("diff").expect("Failed to get parser for 'diff'");
    let tree = parser.parse("--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_djot() {
    if !tree_sitter_language_pack::has_language("djot") {
        eprintln!("Skipping: language 'djot' not available");
        return;
    }
    // Smoke test: load djot and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("djot").expect("Failed to get parser for 'djot'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_dockerfile() {
    if !tree_sitter_language_pack::has_language("dockerfile") {
        eprintln!("Skipping: language 'dockerfile' not available");
        return;
    }
    // Smoke test: load dockerfile and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("dockerfile").expect("Failed to get parser for 'dockerfile'");
    let tree = parser.parse("FROM alpine", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_dot() {
    if !tree_sitter_language_pack::has_language("dot") {
        eprintln!("Skipping: language 'dot' not available");
        return;
    }
    // Smoke test: load dot and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("dot").expect("Failed to get parser for 'dot'");
    let tree = parser.parse("digraph G { A -> B; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_doxygen() {
    if !tree_sitter_language_pack::has_language("doxygen") {
        eprintln!("Skipping: language 'doxygen' not available");
        return;
    }
    // Smoke test: load doxygen and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("doxygen").expect("Failed to get parser for 'doxygen'");
    let tree = parser.parse("/** @brief A function */", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_dtd() {
    if !tree_sitter_language_pack::has_language("dtd") {
        eprintln!("Skipping: language 'dtd' not available");
        return;
    }
    // Smoke test: load dtd and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("dtd").expect("Failed to get parser for 'dtd'");
    let tree = parser.parse("<!ELEMENT note (body)>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_earthfile() {
    if !tree_sitter_language_pack::has_language("earthfile") {
        eprintln!("Skipping: language 'earthfile' not available");
        return;
    }
    // Smoke test: load earthfile and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("earthfile").expect("Failed to get parser for 'earthfile'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ebnf() {
    if !tree_sitter_language_pack::has_language("ebnf") {
        eprintln!("Skipping: language 'ebnf' not available");
        return;
    }
    // Smoke test: load ebnf and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ebnf").expect("Failed to get parser for 'ebnf'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_editorconfig() {
    if !tree_sitter_language_pack::has_language("editorconfig") {
        eprintln!("Skipping: language 'editorconfig' not available");
        return;
    }
    // Smoke test: load editorconfig and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("editorconfig").expect("Failed to get parser for 'editorconfig'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_eds() {
    if !tree_sitter_language_pack::has_language("eds") {
        eprintln!("Skipping: language 'eds' not available");
        return;
    }
    // Smoke test: load eds and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("eds").expect("Failed to get parser for 'eds'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_eex() {
    if !tree_sitter_language_pack::has_language("eex") {
        eprintln!("Skipping: language 'eex' not available");
        return;
    }
    // Smoke test: load eex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("eex").expect("Failed to get parser for 'eex'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_elisp() {
    if !tree_sitter_language_pack::has_language("elisp") {
        eprintln!("Skipping: language 'elisp' not available");
        return;
    }
    // Smoke test: load elisp and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("elisp").expect("Failed to get parser for 'elisp'");
    let tree = parser.parse("(defun hello () (message \"hello\"))", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_elixir() {
    if !tree_sitter_language_pack::has_language("elixir") {
        eprintln!("Skipping: language 'elixir' not available");
        return;
    }
    // Smoke test: load elixir and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("elixir").expect("Failed to get parser for 'elixir'");
    let tree = parser.parse("IO.puts(\"hello\")", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_elm() {
    if !tree_sitter_language_pack::has_language("elm") {
        eprintln!("Skipping: language 'elm' not available");
        return;
    }
    // Smoke test: load elm and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("elm").expect("Failed to get parser for 'elm'");
    let tree = parser.parse("module Main exposing (..)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_elsa() {
    if !tree_sitter_language_pack::has_language("elsa") {
        eprintln!("Skipping: language 'elsa' not available");
        return;
    }
    // Smoke test: load elsa and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("elsa").expect("Failed to get parser for 'elsa'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_elvish() {
    if !tree_sitter_language_pack::has_language("elvish") {
        eprintln!("Skipping: language 'elvish' not available");
        return;
    }
    // Smoke test: load elvish and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("elvish").expect("Failed to get parser for 'elvish'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_embeddedtemplate() {
    if !tree_sitter_language_pack::has_language("embeddedtemplate") {
        eprintln!("Skipping: language 'embeddedtemplate' not available");
        return;
    }
    // Smoke test: load embeddedtemplate and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("embeddedtemplate").expect("Failed to get parser for 'embeddedtemplate'");
    let tree = parser.parse("<%= value %>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_enforce() {
    if !tree_sitter_language_pack::has_language("enforce") {
        eprintln!("Skipping: language 'enforce' not available");
        return;
    }
    // Smoke test: load enforce and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("enforce").expect("Failed to get parser for 'enforce'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_erlang() {
    if !tree_sitter_language_pack::has_language("erlang") {
        eprintln!("Skipping: language 'erlang' not available");
        return;
    }
    // Smoke test: load erlang and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("erlang").expect("Failed to get parser for 'erlang'");
    let tree = parser.parse("main() -> ok.", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_facility() {
    if !tree_sitter_language_pack::has_language("facility") {
        eprintln!("Skipping: language 'facility' not available");
        return;
    }
    // Smoke test: load facility and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("facility").expect("Failed to get parser for 'facility'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_faust() {
    if !tree_sitter_language_pack::has_language("faust") {
        eprintln!("Skipping: language 'faust' not available");
        return;
    }
    // Smoke test: load faust and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("faust").expect("Failed to get parser for 'faust'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fennel() {
    if !tree_sitter_language_pack::has_language("fennel") {
        eprintln!("Skipping: language 'fennel' not available");
        return;
    }
    // Smoke test: load fennel and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("fennel").expect("Failed to get parser for 'fennel'");
    let tree = parser.parse("(fn hello [] (print :hello))", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fidl() {
    if !tree_sitter_language_pack::has_language("fidl") {
        eprintln!("Skipping: language 'fidl' not available");
        return;
    }
    // Smoke test: load fidl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("fidl").expect("Failed to get parser for 'fidl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_firrtl() {
    if !tree_sitter_language_pack::has_language("firrtl") {
        eprintln!("Skipping: language 'firrtl' not available");
        return;
    }
    // Smoke test: load firrtl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("firrtl").expect("Failed to get parser for 'firrtl'");
    let tree = parser.parse("circuit Main :", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fish() {
    if !tree_sitter_language_pack::has_language("fish") {
        eprintln!("Skipping: language 'fish' not available");
        return;
    }
    // Smoke test: load fish and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("fish").expect("Failed to get parser for 'fish'");
    let tree = parser.parse("echo hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_foam() {
    if !tree_sitter_language_pack::has_language("foam") {
        eprintln!("Skipping: language 'foam' not available");
        return;
    }
    // Smoke test: load foam and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("foam").expect("Failed to get parser for 'foam'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_forth() {
    if !tree_sitter_language_pack::has_language("forth") {
        eprintln!("Skipping: language 'forth' not available");
        return;
    }
    // Smoke test: load forth and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("forth").expect("Failed to get parser for 'forth'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fortran() {
    if !tree_sitter_language_pack::has_language("fortran") {
        eprintln!("Skipping: language 'fortran' not available");
        return;
    }
    // Smoke test: load fortran and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("fortran").expect("Failed to get parser for 'fortran'");
    let tree = parser.parse("program main\nend program main", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fsharp() {
    if !tree_sitter_language_pack::has_language("fsharp") {
        eprintln!("Skipping: language 'fsharp' not available");
        return;
    }
    // Smoke test: load fsharp and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("fsharp").expect("Failed to get parser for 'fsharp'");
    let tree = parser.parse("let x = 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_fsharp_signature() {
    if !tree_sitter_language_pack::has_language("fsharp_signature") {
        eprintln!("Skipping: language 'fsharp_signature' not available");
        return;
    }
    // Smoke test: load fsharp_signature and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("fsharp_signature").expect("Failed to get parser for 'fsharp_signature'");
    let tree = parser.parse("val x: int", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_func() {
    if !tree_sitter_language_pack::has_language("func") {
        eprintln!("Skipping: language 'func' not available");
        return;
    }
    // Smoke test: load func and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("func").expect("Failed to get parser for 'func'");
    let tree = parser.parse("() recv_internal() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gap() {
    if !tree_sitter_language_pack::has_language("gap") {
        eprintln!("Skipping: language 'gap' not available");
        return;
    }
    // Smoke test: load gap and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gap").expect("Failed to get parser for 'gap'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gdscript() {
    if !tree_sitter_language_pack::has_language("gdscript") {
        eprintln!("Skipping: language 'gdscript' not available");
        return;
    }
    // Smoke test: load gdscript and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gdscript").expect("Failed to get parser for 'gdscript'");
    let tree = parser.parse("extends Node\nfunc _ready():\n\tpass", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gdshader() {
    if !tree_sitter_language_pack::has_language("gdshader") {
        eprintln!("Skipping: language 'gdshader' not available");
        return;
    }
    // Smoke test: load gdshader and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gdshader").expect("Failed to get parser for 'gdshader'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_git_config() {
    if !tree_sitter_language_pack::has_language("git_config") {
        eprintln!("Skipping: language 'git_config' not available");
        return;
    }
    // Smoke test: load git_config and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("git_config").expect("Failed to get parser for 'git_config'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_git_rebase() {
    if !tree_sitter_language_pack::has_language("git_rebase") {
        eprintln!("Skipping: language 'git_rebase' not available");
        return;
    }
    // Smoke test: load git_rebase and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("git_rebase").expect("Failed to get parser for 'git_rebase'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gitattributes() {
    if !tree_sitter_language_pack::has_language("gitattributes") {
        eprintln!("Skipping: language 'gitattributes' not available");
        return;
    }
    // Smoke test: load gitattributes and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("gitattributes").expect("Failed to get parser for 'gitattributes'");
    let tree = parser.parse("*.txt text", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gitcommit() {
    if !tree_sitter_language_pack::has_language("gitcommit") {
        eprintln!("Skipping: language 'gitcommit' not available");
        return;
    }
    // Smoke test: load gitcommit and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gitcommit").expect("Failed to get parser for 'gitcommit'");
    let tree = parser.parse("feat: add feature\n\nBody text", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gitignore() {
    if !tree_sitter_language_pack::has_language("gitignore") {
        eprintln!("Skipping: language 'gitignore' not available");
        return;
    }
    // Smoke test: load gitignore and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gitignore").expect("Failed to get parser for 'gitignore'");
    let tree = parser.parse("*.o\n*.log", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gleam() {
    if !tree_sitter_language_pack::has_language("gleam") {
        eprintln!("Skipping: language 'gleam' not available");
        return;
    }
    // Smoke test: load gleam and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gleam").expect("Failed to get parser for 'gleam'");
    let tree = parser.parse("pub fn main() { }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_glimmer() {
    if !tree_sitter_language_pack::has_language("glimmer") {
        eprintln!("Skipping: language 'glimmer' not available");
        return;
    }
    // Smoke test: load glimmer and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("glimmer").expect("Failed to get parser for 'glimmer'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_glsl() {
    if !tree_sitter_language_pack::has_language("glsl") {
        eprintln!("Skipping: language 'glsl' not available");
        return;
    }
    // Smoke test: load glsl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("glsl").expect("Failed to get parser for 'glsl'");
    let tree = parser.parse("void main() { gl_Position = vec4(0.0); }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gn() {
    if !tree_sitter_language_pack::has_language("gn") {
        eprintln!("Skipping: language 'gn' not available");
        return;
    }
    // Smoke test: load gn and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gn").expect("Failed to get parser for 'gn'");
    let tree = parser.parse("group(\"hello\") {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gnuplot() {
    if !tree_sitter_language_pack::has_language("gnuplot") {
        eprintln!("Skipping: language 'gnuplot' not available");
        return;
    }
    // Smoke test: load gnuplot and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gnuplot").expect("Failed to get parser for 'gnuplot'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_go() {
    if !tree_sitter_language_pack::has_language("go") {
        eprintln!("Skipping: language 'go' not available");
        return;
    }
    // Smoke test: load go and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("go").expect("Failed to get parser for 'go'");
    let tree = parser.parse("package main", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_godot_resource() {
    if !tree_sitter_language_pack::has_language("godot_resource") {
        eprintln!("Skipping: language 'godot_resource' not available");
        return;
    }
    // Smoke test: load godot_resource and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("godot_resource").expect("Failed to get parser for 'godot_resource'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gomod() {
    if !tree_sitter_language_pack::has_language("gomod") {
        eprintln!("Skipping: language 'gomod' not available");
        return;
    }
    // Smoke test: load gomod and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gomod").expect("Failed to get parser for 'gomod'");
    let tree = parser.parse("module example.com/hello\n\ngo 1.21", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gosum() {
    if !tree_sitter_language_pack::has_language("gosum") {
        eprintln!("Skipping: language 'gosum' not available");
        return;
    }
    // Smoke test: load gosum and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gosum").expect("Failed to get parser for 'gosum'");
    let tree = parser.parse("example.com/pkg v1.0.0 h1:abc=", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gotmpl() {
    if !tree_sitter_language_pack::has_language("gotmpl") {
        eprintln!("Skipping: language 'gotmpl' not available");
        return;
    }
    // Smoke test: load gotmpl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gotmpl").expect("Failed to get parser for 'gotmpl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gowork() {
    if !tree_sitter_language_pack::has_language("gowork") {
        eprintln!("Skipping: language 'gowork' not available");
        return;
    }
    // Smoke test: load gowork and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gowork").expect("Failed to get parser for 'gowork'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gpg() {
    if !tree_sitter_language_pack::has_language("gpg") {
        eprintln!("Skipping: language 'gpg' not available");
        return;
    }
    // Smoke test: load gpg and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gpg").expect("Failed to get parser for 'gpg'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_graphql() {
    if !tree_sitter_language_pack::has_language("graphql") {
        eprintln!("Skipping: language 'graphql' not available");
        return;
    }
    // Smoke test: load graphql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("graphql").expect("Failed to get parser for 'graphql'");
    let tree = parser.parse("type Query { hello: String }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_groovy() {
    if !tree_sitter_language_pack::has_language("groovy") {
        eprintln!("Skipping: language 'groovy' not available");
        return;
    }
    // Smoke test: load groovy and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("groovy").expect("Failed to get parser for 'groovy'");
    let tree = parser.parse("def x = 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_gstlaunch() {
    if !tree_sitter_language_pack::has_language("gstlaunch") {
        eprintln!("Skipping: language 'gstlaunch' not available");
        return;
    }
    // Smoke test: load gstlaunch and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("gstlaunch").expect("Failed to get parser for 'gstlaunch'");
    let tree = parser.parse("fakesrc ! fakesink", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hack() {
    if !tree_sitter_language_pack::has_language("hack") {
        eprintln!("Skipping: language 'hack' not available");
        return;
    }
    // Smoke test: load hack and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hack").expect("Failed to get parser for 'hack'");
    let tree = parser.parse("<?hh\nfunction main(): void {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hare() {
    if !tree_sitter_language_pack::has_language("hare") {
        eprintln!("Skipping: language 'hare' not available");
        return;
    }
    // Smoke test: load hare and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hare").expect("Failed to get parser for 'hare'");
    let tree = parser.parse("export fn main() void = void;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_haskell() {
    if !tree_sitter_language_pack::has_language("haskell") {
        eprintln!("Skipping: language 'haskell' not available");
        return;
    }
    // Smoke test: load haskell and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("haskell").expect("Failed to get parser for 'haskell'");
    let tree = parser.parse("main = putStrLn \"hello\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_haxe() {
    if !tree_sitter_language_pack::has_language("haxe") {
        eprintln!("Skipping: language 'haxe' not available");
        return;
    }
    // Smoke test: load haxe and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("haxe").expect("Failed to get parser for 'haxe'");
    let tree = parser.parse("class Main { static function main() {} }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hcl() {
    if !tree_sitter_language_pack::has_language("hcl") {
        eprintln!("Skipping: language 'hcl' not available");
        return;
    }
    // Smoke test: load hcl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hcl").expect("Failed to get parser for 'hcl'");
    let tree = parser.parse("variable \"name\" { type = string }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_heex() {
    if !tree_sitter_language_pack::has_language("heex") {
        eprintln!("Skipping: language 'heex' not available");
        return;
    }
    // Smoke test: load heex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("heex").expect("Failed to get parser for 'heex'");
    let tree = parser.parse("<%= @greeting %>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hjson() {
    if !tree_sitter_language_pack::has_language("hjson") {
        eprintln!("Skipping: language 'hjson' not available");
        return;
    }
    // Smoke test: load hjson and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hjson").expect("Failed to get parser for 'hjson'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hlsl() {
    if !tree_sitter_language_pack::has_language("hlsl") {
        eprintln!("Skipping: language 'hlsl' not available");
        return;
    }
    // Smoke test: load hlsl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hlsl").expect("Failed to get parser for 'hlsl'");
    let tree = parser.parse("float4 main() : SV_Target { return 0; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hocon() {
    if !tree_sitter_language_pack::has_language("hocon") {
        eprintln!("Skipping: language 'hocon' not available");
        return;
    }
    // Smoke test: load hocon and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hocon").expect("Failed to get parser for 'hocon'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hoon() {
    if !tree_sitter_language_pack::has_language("hoon") {
        eprintln!("Skipping: language 'hoon' not available");
        return;
    }
    // Smoke test: load hoon and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hoon").expect("Failed to get parser for 'hoon'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_html() {
    if !tree_sitter_language_pack::has_language("html") {
        eprintln!("Skipping: language 'html' not available");
        return;
    }
    // Smoke test: load html and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("html").expect("Failed to get parser for 'html'");
    let tree = parser.parse("<p>hello</p>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_htmldjango() {
    if !tree_sitter_language_pack::has_language("htmldjango") {
        eprintln!("Skipping: language 'htmldjango' not available");
        return;
    }
    // Smoke test: load htmldjango and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("htmldjango").expect("Failed to get parser for 'htmldjango'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_http() {
    if !tree_sitter_language_pack::has_language("http") {
        eprintln!("Skipping: language 'http' not available");
        return;
    }
    // Smoke test: load http and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("http").expect("Failed to get parser for 'http'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hurl() {
    if !tree_sitter_language_pack::has_language("hurl") {
        eprintln!("Skipping: language 'hurl' not available");
        return;
    }
    // Smoke test: load hurl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hurl").expect("Failed to get parser for 'hurl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_hyprlang() {
    if !tree_sitter_language_pack::has_language("hyprlang") {
        eprintln!("Skipping: language 'hyprlang' not available");
        return;
    }
    // Smoke test: load hyprlang and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("hyprlang").expect("Failed to get parser for 'hyprlang'");
    let tree = parser.parse("general { border_size = 1 }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_idris() {
    if !tree_sitter_language_pack::has_language("idris") {
        eprintln!("Skipping: language 'idris' not available");
        return;
    }
    // Smoke test: load idris and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("idris").expect("Failed to get parser for 'idris'");
    let tree = parser.parse("module Main", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ini() {
    if !tree_sitter_language_pack::has_language("ini") {
        eprintln!("Skipping: language 'ini' not available");
        return;
    }
    // Smoke test: load ini and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ini").expect("Failed to get parser for 'ini'");
    let tree = parser.parse("[section]\nkey = value", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ispc() {
    if !tree_sitter_language_pack::has_language("ispc") {
        eprintln!("Skipping: language 'ispc' not available");
        return;
    }
    // Smoke test: load ispc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ispc").expect("Failed to get parser for 'ispc'");
    let tree = parser.parse("export void main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_jai() {
    if !tree_sitter_language_pack::has_language("jai") {
        eprintln!("Skipping: language 'jai' not available");
        return;
    }
    // Smoke test: load jai and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("jai").expect("Failed to get parser for 'jai'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_janet() {
    if !tree_sitter_language_pack::has_language("janet") {
        eprintln!("Skipping: language 'janet' not available");
        return;
    }
    // Smoke test: load janet and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("janet").expect("Failed to get parser for 'janet'");
    let tree = parser.parse("(print \"hello\")", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_java() {
    if !tree_sitter_language_pack::has_language("java") {
        eprintln!("Skipping: language 'java' not available");
        return;
    }
    // Smoke test: load java and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("java").expect("Failed to get parser for 'java'");
    let tree = parser.parse("class Main {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_javadoc() {
    if !tree_sitter_language_pack::has_language("javadoc") {
        eprintln!("Skipping: language 'javadoc' not available");
        return;
    }
    // Smoke test: load javadoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("javadoc").expect("Failed to get parser for 'javadoc'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_javascript() {
    if !tree_sitter_language_pack::has_language("javascript") {
        eprintln!("Skipping: language 'javascript' not available");
        return;
    }
    // Smoke test: load javascript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("javascript").expect("Failed to get parser for 'javascript'");
    let tree = parser.parse("console.log('hello');", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_jinja2() {
    if !tree_sitter_language_pack::has_language("jinja2") {
        eprintln!("Skipping: language 'jinja2' not available");
        return;
    }
    // Smoke test: load jinja2 and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("jinja2").expect("Failed to get parser for 'jinja2'");
    let tree = parser.parse("{{ variable }}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_jq() {
    if !tree_sitter_language_pack::has_language("jq") {
        eprintln!("Skipping: language 'jq' not available");
        return;
    }
    // Smoke test: load jq and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("jq").expect("Failed to get parser for 'jq'");
    let tree = parser.parse(".[] | select(.key)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_jsdoc() {
    if !tree_sitter_language_pack::has_language("jsdoc") {
        eprintln!("Skipping: language 'jsdoc' not available");
        return;
    }
    // Smoke test: load jsdoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("jsdoc").expect("Failed to get parser for 'jsdoc'");
    let tree = parser.parse("/** @param {string} name */", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_json() {
    if !tree_sitter_language_pack::has_language("json") {
        eprintln!("Skipping: language 'json' not available");
        return;
    }
    // Smoke test: load json and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("json").expect("Failed to get parser for 'json'");
    let tree = parser.parse("{\"key\": \"value\"}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_json5() {
    if !tree_sitter_language_pack::has_language("json5") {
        eprintln!("Skipping: language 'json5' not available");
        return;
    }
    // Smoke test: load json5 and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("json5").expect("Failed to get parser for 'json5'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_jsonnet() {
    if !tree_sitter_language_pack::has_language("jsonnet") {
        eprintln!("Skipping: language 'jsonnet' not available");
        return;
    }
    // Smoke test: load jsonnet and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("jsonnet").expect("Failed to get parser for 'jsonnet'");
    let tree = parser.parse("{ key: 'value' }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_julia() {
    if !tree_sitter_language_pack::has_language("julia") {
        eprintln!("Skipping: language 'julia' not available");
        return;
    }
    // Smoke test: load julia and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("julia").expect("Failed to get parser for 'julia'");
    let tree = parser.parse("function main() end", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_just() {
    if !tree_sitter_language_pack::has_language("just") {
        eprintln!("Skipping: language 'just' not available");
        return;
    }
    // Smoke test: load just and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("just").expect("Failed to get parser for 'just'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_kcl() {
    if !tree_sitter_language_pack::has_language("kcl") {
        eprintln!("Skipping: language 'kcl' not available");
        return;
    }
    // Smoke test: load kcl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("kcl").expect("Failed to get parser for 'kcl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_kconfig() {
    if !tree_sitter_language_pack::has_language("kconfig") {
        eprintln!("Skipping: language 'kconfig' not available");
        return;
    }
    // Smoke test: load kconfig and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("kconfig").expect("Failed to get parser for 'kconfig'");
    let tree = parser.parse("config FOO\n\tbool \"Enable foo\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_kdl() {
    if !tree_sitter_language_pack::has_language("kdl") {
        eprintln!("Skipping: language 'kdl' not available");
        return;
    }
    // Smoke test: load kdl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("kdl").expect("Failed to get parser for 'kdl'");
    let tree = parser.parse("node \"value\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_kotlin() {
    if !tree_sitter_language_pack::has_language("kotlin") {
        eprintln!("Skipping: language 'kotlin' not available");
        return;
    }
    // Smoke test: load kotlin and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("kotlin").expect("Failed to get parser for 'kotlin'");
    let tree = parser.parse("fun main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_latex() {
    if !tree_sitter_language_pack::has_language("latex") {
        eprintln!("Skipping: language 'latex' not available");
        return;
    }
    // Smoke test: load latex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("latex").expect("Failed to get parser for 'latex'");
    let tree = parser.parse(
        "\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}",
        None,
    );
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_lean() {
    if !tree_sitter_language_pack::has_language("lean") {
        eprintln!("Skipping: language 'lean' not available");
        return;
    }
    // Smoke test: load lean and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("lean").expect("Failed to get parser for 'lean'");
    let tree = parser.parse("def main : IO Unit := pure ()", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ledger() {
    if !tree_sitter_language_pack::has_language("ledger") {
        eprintln!("Skipping: language 'ledger' not available");
        return;
    }
    // Smoke test: load ledger and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ledger").expect("Failed to get parser for 'ledger'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_less() {
    if !tree_sitter_language_pack::has_language("less") {
        eprintln!("Skipping: language 'less' not available");
        return;
    }
    // Smoke test: load less and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("less").expect("Failed to get parser for 'less'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_linkerscript() {
    if !tree_sitter_language_pack::has_language("linkerscript") {
        eprintln!("Skipping: language 'linkerscript' not available");
        return;
    }
    // Smoke test: load linkerscript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("linkerscript").expect("Failed to get parser for 'linkerscript'");
    let tree = parser.parse("SECTIONS { .text : { *(.text) } }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_liquid() {
    if !tree_sitter_language_pack::has_language("liquid") {
        eprintln!("Skipping: language 'liquid' not available");
        return;
    }
    // Smoke test: load liquid and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("liquid").expect("Failed to get parser for 'liquid'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_llvm() {
    if !tree_sitter_language_pack::has_language("llvm") {
        eprintln!("Skipping: language 'llvm' not available");
        return;
    }
    // Smoke test: load llvm and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("llvm").expect("Failed to get parser for 'llvm'");
    let tree = parser.parse("define i32 @main() { ret i32 0 }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_lua() {
    if !tree_sitter_language_pack::has_language("lua") {
        eprintln!("Skipping: language 'lua' not available");
        return;
    }
    // Smoke test: load lua and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("lua").expect("Failed to get parser for 'lua'");
    let tree = parser.parse("print('hello')", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_luadoc() {
    if !tree_sitter_language_pack::has_language("luadoc") {
        eprintln!("Skipping: language 'luadoc' not available");
        return;
    }
    // Smoke test: load luadoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("luadoc").expect("Failed to get parser for 'luadoc'");
    let tree = parser.parse("---@param name string", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_luap() {
    if !tree_sitter_language_pack::has_language("luap") {
        eprintln!("Skipping: language 'luap' not available");
        return;
    }
    // Smoke test: load luap and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("luap").expect("Failed to get parser for 'luap'");
    let tree = parser.parse("[a-z]+", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_luau() {
    if !tree_sitter_language_pack::has_language("luau") {
        eprintln!("Skipping: language 'luau' not available");
        return;
    }
    // Smoke test: load luau and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("luau").expect("Failed to get parser for 'luau'");
    let tree = parser.parse("local x: number = 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_magik() {
    if !tree_sitter_language_pack::has_language("magik") {
        eprintln!("Skipping: language 'magik' not available");
        return;
    }
    // Smoke test: load magik and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("magik").expect("Failed to get parser for 'magik'");
    let tree = parser.parse("_method object.hello\n_endmethod", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_make() {
    if !tree_sitter_language_pack::has_language("make") {
        eprintln!("Skipping: language 'make' not available");
        return;
    }
    // Smoke test: load make and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("make").expect("Failed to get parser for 'make'");
    let tree = parser.parse("all:\n\techo hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_markdown() {
    if !tree_sitter_language_pack::has_language("markdown") {
        eprintln!("Skipping: language 'markdown' not available");
        return;
    }
    // Smoke test: load markdown and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("markdown").expect("Failed to get parser for 'markdown'");
    let tree = parser.parse("# Hello\n\nWorld", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_markdown_inline() {
    if !tree_sitter_language_pack::has_language("markdown_inline") {
        eprintln!("Skipping: language 'markdown_inline' not available");
        return;
    }
    // Smoke test: load markdown_inline and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("markdown_inline").expect("Failed to get parser for 'markdown_inline'");
    let tree = parser.parse("**bold** and *italic*", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_matlab() {
    if !tree_sitter_language_pack::has_language("matlab") {
        eprintln!("Skipping: language 'matlab' not available");
        return;
    }
    // Smoke test: load matlab and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("matlab").expect("Failed to get parser for 'matlab'");
    let tree = parser.parse("function y = hello(x)\ny = x;\nend", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_mermaid() {
    if !tree_sitter_language_pack::has_language("mermaid") {
        eprintln!("Skipping: language 'mermaid' not available");
        return;
    }
    // Smoke test: load mermaid and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("mermaid").expect("Failed to get parser for 'mermaid'");
    let tree = parser.parse("graph TD\nA --> B", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_meson() {
    if !tree_sitter_language_pack::has_language("meson") {
        eprintln!("Skipping: language 'meson' not available");
        return;
    }
    // Smoke test: load meson and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("meson").expect("Failed to get parser for 'meson'");
    let tree = parser.parse("project('hello', 'c')", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_mlir() {
    if !tree_sitter_language_pack::has_language("mlir") {
        eprintln!("Skipping: language 'mlir' not available");
        return;
    }
    // Smoke test: load mlir and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("mlir").expect("Failed to get parser for 'mlir'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_mojo() {
    if !tree_sitter_language_pack::has_language("mojo") {
        eprintln!("Skipping: language 'mojo' not available");
        return;
    }
    // Smoke test: load mojo and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("mojo").expect("Failed to get parser for 'mojo'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_move() {
    if !tree_sitter_language_pack::has_language("move") {
        eprintln!("Skipping: language 'move' not available");
        return;
    }
    // Smoke test: load move and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("move").expect("Failed to get parser for 'move'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nasm() {
    if !tree_sitter_language_pack::has_language("nasm") {
        eprintln!("Skipping: language 'nasm' not available");
        return;
    }
    // Smoke test: load nasm and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nasm").expect("Failed to get parser for 'nasm'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_netlinx() {
    if !tree_sitter_language_pack::has_language("netlinx") {
        eprintln!("Skipping: language 'netlinx' not available");
        return;
    }
    // Smoke test: load netlinx and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("netlinx").expect("Failed to get parser for 'netlinx'");
    let tree = parser.parse("PROGRAM_NAME='hello'", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nginx() {
    if !tree_sitter_language_pack::has_language("nginx") {
        eprintln!("Skipping: language 'nginx' not available");
        return;
    }
    // Smoke test: load nginx and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nginx").expect("Failed to get parser for 'nginx'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nickel() {
    if !tree_sitter_language_pack::has_language("nickel") {
        eprintln!("Skipping: language 'nickel' not available");
        return;
    }
    // Smoke test: load nickel and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nickel").expect("Failed to get parser for 'nickel'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nim() {
    if !tree_sitter_language_pack::has_language("nim") {
        eprintln!("Skipping: language 'nim' not available");
        return;
    }
    // Smoke test: load nim and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nim").expect("Failed to get parser for 'nim'");
    let tree = parser.parse("echo \"hello\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ninja() {
    if !tree_sitter_language_pack::has_language("ninja") {
        eprintln!("Skipping: language 'ninja' not available");
        return;
    }
    // Smoke test: load ninja and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ninja").expect("Failed to get parser for 'ninja'");
    let tree = parser.parse("rule cc\n  command = cc $in -o $out", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nix() {
    if !tree_sitter_language_pack::has_language("nix") {
        eprintln!("Skipping: language 'nix' not available");
        return;
    }
    // Smoke test: load nix and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nix").expect("Failed to get parser for 'nix'");
    let tree = parser.parse("{ pkgs ? import <nixpkgs> {} }: pkgs.hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_norg() {
    if !tree_sitter_language_pack::has_language("norg") {
        eprintln!("Skipping: language 'norg' not available");
        return;
    }
    // Smoke test: load norg and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("norg").expect("Failed to get parser for 'norg'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_norg_meta() {
    if !tree_sitter_language_pack::has_language("norg_meta") {
        eprintln!("Skipping: language 'norg_meta' not available");
        return;
    }
    // Smoke test: load norg_meta and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("norg_meta").expect("Failed to get parser for 'norg_meta'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nqc() {
    if !tree_sitter_language_pack::has_language("nqc") {
        eprintln!("Skipping: language 'nqc' not available");
        return;
    }
    // Smoke test: load nqc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nqc").expect("Failed to get parser for 'nqc'");
    let tree = parser.parse("task main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_nushell() {
    if !tree_sitter_language_pack::has_language("nushell") {
        eprintln!("Skipping: language 'nushell' not available");
        return;
    }
    // Smoke test: load nushell and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("nushell").expect("Failed to get parser for 'nushell'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_objc() {
    if !tree_sitter_language_pack::has_language("objc") {
        eprintln!("Skipping: language 'objc' not available");
        return;
    }
    // Smoke test: load objc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("objc").expect("Failed to get parser for 'objc'");
    let tree = parser.parse("@interface Main @end", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ocaml() {
    if !tree_sitter_language_pack::has_language("ocaml") {
        eprintln!("Skipping: language 'ocaml' not available");
        return;
    }
    // Smoke test: load ocaml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ocaml").expect("Failed to get parser for 'ocaml'");
    let tree = parser.parse("let () = print_endline \"hello\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ocaml_interface() {
    if !tree_sitter_language_pack::has_language("ocaml_interface") {
        eprintln!("Skipping: language 'ocaml_interface' not available");
        return;
    }
    // Smoke test: load ocaml_interface and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("ocaml_interface").expect("Failed to get parser for 'ocaml_interface'");
    let tree = parser.parse("val x : int", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ocamllex() {
    if !tree_sitter_language_pack::has_language("ocamllex") {
        eprintln!("Skipping: language 'ocamllex' not available");
        return;
    }
    // Smoke test: load ocamllex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ocamllex").expect("Failed to get parser for 'ocamllex'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_odin() {
    if !tree_sitter_language_pack::has_language("odin") {
        eprintln!("Skipping: language 'odin' not available");
        return;
    }
    // Smoke test: load odin and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("odin").expect("Failed to get parser for 'odin'");
    let tree = parser.parse("package main", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_openscad() {
    if !tree_sitter_language_pack::has_language("openscad") {
        eprintln!("Skipping: language 'openscad' not available");
        return;
    }
    // Smoke test: load openscad and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("openscad").expect("Failed to get parser for 'openscad'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_org() {
    if !tree_sitter_language_pack::has_language("org") {
        eprintln!("Skipping: language 'org' not available");
        return;
    }
    // Smoke test: load org and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("org").expect("Failed to get parser for 'org'");
    let tree = parser.parse("* Hello\nWorld", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pascal() {
    if !tree_sitter_language_pack::has_language("pascal") {
        eprintln!("Skipping: language 'pascal' not available");
        return;
    }
    // Smoke test: load pascal and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pascal").expect("Failed to get parser for 'pascal'");
    let tree = parser.parse("program Hello; begin end.", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pem() {
    if !tree_sitter_language_pack::has_language("pem") {
        eprintln!("Skipping: language 'pem' not available");
        return;
    }
    // Smoke test: load pem and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pem").expect("Failed to get parser for 'pem'");
    let tree = parser.parse("-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_perl() {
    if !tree_sitter_language_pack::has_language("perl") {
        eprintln!("Skipping: language 'perl' not available");
        return;
    }
    // Smoke test: load perl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("perl").expect("Failed to get parser for 'perl'");
    let tree = parser.parse("print 'hello';", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pgn() {
    if !tree_sitter_language_pack::has_language("pgn") {
        eprintln!("Skipping: language 'pgn' not available");
        return;
    }
    // Smoke test: load pgn and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pgn").expect("Failed to get parser for 'pgn'");
    let tree = parser.parse("1. e4 e5 *", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_php() {
    if !tree_sitter_language_pack::has_language("php") {
        eprintln!("Skipping: language 'php' not available");
        return;
    }
    // Smoke test: load php and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("php").expect("Failed to get parser for 'php'");
    let tree = parser.parse("<?php echo 'hello'; ?>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_phpdoc() {
    if !tree_sitter_language_pack::has_language("phpdoc") {
        eprintln!("Skipping: language 'phpdoc' not available");
        return;
    }
    // Smoke test: load phpdoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("phpdoc").expect("Failed to get parser for 'phpdoc'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pkl() {
    if !tree_sitter_language_pack::has_language("pkl") {
        eprintln!("Skipping: language 'pkl' not available");
        return;
    }
    // Smoke test: load pkl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pkl").expect("Failed to get parser for 'pkl'");
    let tree = parser.parse("name = \"hello\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_po() {
    if !tree_sitter_language_pack::has_language("po") {
        eprintln!("Skipping: language 'po' not available");
        return;
    }
    // Smoke test: load po and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("po").expect("Failed to get parser for 'po'");
    let tree = parser.parse("msgid \"hello\"\nmsgstr \"world\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_poe_filter() {
    if !tree_sitter_language_pack::has_language("poe_filter") {
        eprintln!("Skipping: language 'poe_filter' not available");
        return;
    }
    // Smoke test: load poe_filter and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("poe_filter").expect("Failed to get parser for 'poe_filter'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pony() {
    if !tree_sitter_language_pack::has_language("pony") {
        eprintln!("Skipping: language 'pony' not available");
        return;
    }
    // Smoke test: load pony and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pony").expect("Failed to get parser for 'pony'");
    let tree = parser.parse("actor Main\n  new create(env: Env) => None", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_postscript() {
    if !tree_sitter_language_pack::has_language("postscript") {
        eprintln!("Skipping: language 'postscript' not available");
        return;
    }
    // Smoke test: load postscript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("postscript").expect("Failed to get parser for 'postscript'");
    let tree = parser.parse("/hello { (Hello) show } def", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_powershell() {
    if !tree_sitter_language_pack::has_language("powershell") {
        eprintln!("Skipping: language 'powershell' not available");
        return;
    }
    // Smoke test: load powershell and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("powershell").expect("Failed to get parser for 'powershell'");
    let tree = parser.parse("Write-Host 'hello'", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_printf() {
    if !tree_sitter_language_pack::has_language("printf") {
        eprintln!("Skipping: language 'printf' not available");
        return;
    }
    // Smoke test: load printf and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("printf").expect("Failed to get parser for 'printf'");
    let tree = parser.parse("%d %s", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_prisma() {
    if !tree_sitter_language_pack::has_language("prisma") {
        eprintln!("Skipping: language 'prisma' not available");
        return;
    }
    // Smoke test: load prisma and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("prisma").expect("Failed to get parser for 'prisma'");
    let tree = parser.parse("model User { id Int @id }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_prolog() {
    if !tree_sitter_language_pack::has_language("prolog") {
        eprintln!("Skipping: language 'prolog' not available");
        return;
    }
    // Smoke test: load prolog and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("prolog").expect("Failed to get parser for 'prolog'");
    let tree = parser.parse("hello :- write('hello'), nl.", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_promql() {
    if !tree_sitter_language_pack::has_language("promql") {
        eprintln!("Skipping: language 'promql' not available");
        return;
    }
    // Smoke test: load promql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("promql").expect("Failed to get parser for 'promql'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_properties() {
    if !tree_sitter_language_pack::has_language("properties") {
        eprintln!("Skipping: language 'properties' not available");
        return;
    }
    // Smoke test: load properties and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("properties").expect("Failed to get parser for 'properties'");
    let tree = parser.parse("key=value", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_proto() {
    if !tree_sitter_language_pack::has_language("proto") {
        eprintln!("Skipping: language 'proto' not available");
        return;
    }
    // Smoke test: load proto and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("proto").expect("Failed to get parser for 'proto'");
    let tree = parser.parse("syntax = \"proto3\";", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_prql() {
    if !tree_sitter_language_pack::has_language("prql") {
        eprintln!("Skipping: language 'prql' not available");
        return;
    }
    // Smoke test: load prql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("prql").expect("Failed to get parser for 'prql'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_psv() {
    if !tree_sitter_language_pack::has_language("psv") {
        eprintln!("Skipping: language 'psv' not available");
        return;
    }
    // Smoke test: load psv and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("psv").expect("Failed to get parser for 'psv'");
    let tree = parser.parse("a|b|c\n1|2|3", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pug() {
    if !tree_sitter_language_pack::has_language("pug") {
        eprintln!("Skipping: language 'pug' not available");
        return;
    }
    // Smoke test: load pug and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("pug").expect("Failed to get parser for 'pug'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_puppet() {
    if !tree_sitter_language_pack::has_language("puppet") {
        eprintln!("Skipping: language 'puppet' not available");
        return;
    }
    // Smoke test: load puppet and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("puppet").expect("Failed to get parser for 'puppet'");
    let tree = parser.parse("notify { 'hello': }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_purescript() {
    if !tree_sitter_language_pack::has_language("purescript") {
        eprintln!("Skipping: language 'purescript' not available");
        return;
    }
    // Smoke test: load purescript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("purescript").expect("Failed to get parser for 'purescript'");
    let tree = parser.parse("module Main where", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_pymanifest() {
    if !tree_sitter_language_pack::has_language("pymanifest") {
        eprintln!("Skipping: language 'pymanifest' not available");
        return;
    }
    // Smoke test: load pymanifest and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("pymanifest").expect("Failed to get parser for 'pymanifest'");
    let tree = parser.parse("include *.txt", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_python() {
    if !tree_sitter_language_pack::has_language("python") {
        eprintln!("Skipping: language 'python' not available");
        return;
    }
    // Smoke test: load python and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("python").expect("Failed to get parser for 'python'");
    let tree = parser.parse("print('hello')", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ql() {
    if !tree_sitter_language_pack::has_language("ql") {
        eprintln!("Skipping: language 'ql' not available");
        return;
    }
    // Smoke test: load ql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ql").expect("Failed to get parser for 'ql'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_qmldir() {
    if !tree_sitter_language_pack::has_language("qmldir") {
        eprintln!("Skipping: language 'qmldir' not available");
        return;
    }
    // Smoke test: load qmldir and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("qmldir").expect("Failed to get parser for 'qmldir'");
    let tree = parser.parse("module Example", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_qmljs() {
    if !tree_sitter_language_pack::has_language("qmljs") {
        eprintln!("Skipping: language 'qmljs' not available");
        return;
    }
    // Smoke test: load qmljs and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("qmljs").expect("Failed to get parser for 'qmljs'");
    let tree = parser.parse("import QtQuick 2.0\nItem {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_query() {
    if !tree_sitter_language_pack::has_language("query") {
        eprintln!("Skipping: language 'query' not available");
        return;
    }
    // Smoke test: load query and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("query").expect("Failed to get parser for 'query'");
    let tree = parser.parse("(identifier) @name", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_r() {
    if !tree_sitter_language_pack::has_language("r") {
        eprintln!("Skipping: language 'r' not available");
        return;
    }
    // Smoke test: load r and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("r").expect("Failed to get parser for 'r'");
    let tree = parser.parse("print('hello')", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_racket() {
    if !tree_sitter_language_pack::has_language("racket") {
        eprintln!("Skipping: language 'racket' not available");
        return;
    }
    // Smoke test: load racket and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("racket").expect("Failed to get parser for 'racket'");
    let tree = parser.parse("#lang racket\n(define x 1)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rasi() {
    if !tree_sitter_language_pack::has_language("rasi") {
        eprintln!("Skipping: language 'rasi' not available");
        return;
    }
    // Smoke test: load rasi and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rasi").expect("Failed to get parser for 'rasi'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_razor() {
    if !tree_sitter_language_pack::has_language("razor") {
        eprintln!("Skipping: language 'razor' not available");
        return;
    }
    // Smoke test: load razor and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("razor").expect("Failed to get parser for 'razor'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rbs() {
    if !tree_sitter_language_pack::has_language("rbs") {
        eprintln!("Skipping: language 'rbs' not available");
        return;
    }
    // Smoke test: load rbs and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rbs").expect("Failed to get parser for 'rbs'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_re2c() {
    if !tree_sitter_language_pack::has_language("re2c") {
        eprintln!("Skipping: language 're2c' not available");
        return;
    }
    // Smoke test: load re2c and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("re2c").expect("Failed to get parser for 're2c'");
    let tree = parser.parse("/*!re2c\n  [a-z]+ { return; }\n*/", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_readline() {
    if !tree_sitter_language_pack::has_language("readline") {
        eprintln!("Skipping: language 'readline' not available");
        return;
    }
    // Smoke test: load readline and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("readline").expect("Failed to get parser for 'readline'");
    let tree = parser.parse("set editing-mode vi", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_regex() {
    if !tree_sitter_language_pack::has_language("regex") {
        eprintln!("Skipping: language 'regex' not available");
        return;
    }
    // Smoke test: load regex and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("regex").expect("Failed to get parser for 'regex'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rego() {
    if !tree_sitter_language_pack::has_language("rego") {
        eprintln!("Skipping: language 'rego' not available");
        return;
    }
    // Smoke test: load rego and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rego").expect("Failed to get parser for 'rego'");
    let tree = parser.parse("package main\ndefault allow = false", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_requirements() {
    if !tree_sitter_language_pack::has_language("requirements") {
        eprintln!("Skipping: language 'requirements' not available");
        return;
    }
    // Smoke test: load requirements and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("requirements").expect("Failed to get parser for 'requirements'");
    let tree = parser.parse("flask>=2.0", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rescript() {
    if !tree_sitter_language_pack::has_language("rescript") {
        eprintln!("Skipping: language 'rescript' not available");
        return;
    }
    // Smoke test: load rescript and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rescript").expect("Failed to get parser for 'rescript'");
    let tree = parser.parse("let x = 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_robot() {
    if !tree_sitter_language_pack::has_language("robot") {
        eprintln!("Skipping: language 'robot' not available");
        return;
    }
    // Smoke test: load robot and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("robot").expect("Failed to get parser for 'robot'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_roc() {
    if !tree_sitter_language_pack::has_language("roc") {
        eprintln!("Skipping: language 'roc' not available");
        return;
    }
    // Smoke test: load roc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("roc").expect("Failed to get parser for 'roc'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ron() {
    if !tree_sitter_language_pack::has_language("ron") {
        eprintln!("Skipping: language 'ron' not available");
        return;
    }
    // Smoke test: load ron and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ron").expect("Failed to get parser for 'ron'");
    let tree = parser.parse("(key: \"value\")", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rst() {
    if !tree_sitter_language_pack::has_language("rst") {
        eprintln!("Skipping: language 'rst' not available");
        return;
    }
    // Smoke test: load rst and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rst").expect("Failed to get parser for 'rst'");
    let tree = parser.parse("Hello\n=====\n\nWorld", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rtf() {
    if !tree_sitter_language_pack::has_language("rtf") {
        eprintln!("Skipping: language 'rtf' not available");
        return;
    }
    // Smoke test: load rtf and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rtf").expect("Failed to get parser for 'rtf'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ruby() {
    if !tree_sitter_language_pack::has_language("ruby") {
        eprintln!("Skipping: language 'ruby' not available");
        return;
    }
    // Smoke test: load ruby and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ruby").expect("Failed to get parser for 'ruby'");
    let tree = parser.parse("puts 'hello'", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_rust() {
    if !tree_sitter_language_pack::has_language("rust") {
        eprintln!("Skipping: language 'rust' not available");
        return;
    }
    // Smoke test: load rust and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("rust").expect("Failed to get parser for 'rust'");
    let tree = parser.parse("fn main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_scala() {
    if !tree_sitter_language_pack::has_language("scala") {
        eprintln!("Skipping: language 'scala' not available");
        return;
    }
    // Smoke test: load scala and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("scala").expect("Failed to get parser for 'scala'");
    let tree = parser.parse("object Main", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_scheme() {
    if !tree_sitter_language_pack::has_language("scheme") {
        eprintln!("Skipping: language 'scheme' not available");
        return;
    }
    // Smoke test: load scheme and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("scheme").expect("Failed to get parser for 'scheme'");
    let tree = parser.parse("(define x 1)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_scss() {
    if !tree_sitter_language_pack::has_language("scss") {
        eprintln!("Skipping: language 'scss' not available");
        return;
    }
    // Smoke test: load scss and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("scss").expect("Failed to get parser for 'scss'");
    let tree = parser.parse("$color: red;\nbody { color: $color; }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_slang() {
    if !tree_sitter_language_pack::has_language("slang") {
        eprintln!("Skipping: language 'slang' not available");
        return;
    }
    // Smoke test: load slang and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("slang").expect("Failed to get parser for 'slang'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_smali() {
    if !tree_sitter_language_pack::has_language("smali") {
        eprintln!("Skipping: language 'smali' not available");
        return;
    }
    // Smoke test: load smali and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("smali").expect("Failed to get parser for 'smali'");
    let tree = parser.parse(".class public LMain;\n.super Ljava/lang/Object;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_smalltalk() {
    if !tree_sitter_language_pack::has_language("smalltalk") {
        eprintln!("Skipping: language 'smalltalk' not available");
        return;
    }
    // Smoke test: load smalltalk and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("smalltalk").expect("Failed to get parser for 'smalltalk'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_smithy() {
    if !tree_sitter_language_pack::has_language("smithy") {
        eprintln!("Skipping: language 'smithy' not available");
        return;
    }
    // Smoke test: load smithy and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("smithy").expect("Failed to get parser for 'smithy'");
    let tree = parser.parse("namespace example\nstring MyString", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sml() {
    if !tree_sitter_language_pack::has_language("sml") {
        eprintln!("Skipping: language 'sml' not available");
        return;
    }
    // Smoke test: load sml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("sml").expect("Failed to get parser for 'sml'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_snakemake() {
    if !tree_sitter_language_pack::has_language("snakemake") {
        eprintln!("Skipping: language 'snakemake' not available");
        return;
    }
    // Smoke test: load snakemake and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("snakemake").expect("Failed to get parser for 'snakemake'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_solidity() {
    if !tree_sitter_language_pack::has_language("solidity") {
        eprintln!("Skipping: language 'solidity' not available");
        return;
    }
    // Smoke test: load solidity and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("solidity").expect("Failed to get parser for 'solidity'");
    let tree = parser.parse("pragma solidity ^0.8.0;\ncontract Main {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_souffle() {
    if !tree_sitter_language_pack::has_language("souffle") {
        eprintln!("Skipping: language 'souffle' not available");
        return;
    }
    // Smoke test: load souffle and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("souffle").expect("Failed to get parser for 'souffle'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sourcepawn() {
    if !tree_sitter_language_pack::has_language("sourcepawn") {
        eprintln!("Skipping: language 'sourcepawn' not available");
        return;
    }
    // Smoke test: load sourcepawn and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("sourcepawn").expect("Failed to get parser for 'sourcepawn'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sparql() {
    if !tree_sitter_language_pack::has_language("sparql") {
        eprintln!("Skipping: language 'sparql' not available");
        return;
    }
    // Smoke test: load sparql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("sparql").expect("Failed to get parser for 'sparql'");
    let tree = parser.parse("SELECT ?s WHERE { ?s ?p ?o }", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sql() {
    if !tree_sitter_language_pack::has_language("sql") {
        eprintln!("Skipping: language 'sql' not available");
        return;
    }
    // Smoke test: load sql and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("sql").expect("Failed to get parser for 'sql'");
    let tree = parser.parse("SELECT 1;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sql_bigquery() {
    if !tree_sitter_language_pack::has_language("sql_bigquery") {
        eprintln!("Skipping: language 'sql_bigquery' not available");
        return;
    }
    // Smoke test: load sql_bigquery and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("sql_bigquery").expect("Failed to get parser for 'sql_bigquery'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_squirrel() {
    if !tree_sitter_language_pack::has_language("squirrel") {
        eprintln!("Skipping: language 'squirrel' not available");
        return;
    }
    // Smoke test: load squirrel and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("squirrel").expect("Failed to get parser for 'squirrel'");
    let tree = parser.parse("function main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ssh_config() {
    if !tree_sitter_language_pack::has_language("ssh_config") {
        eprintln!("Skipping: language 'ssh_config' not available");
        return;
    }
    // Smoke test: load ssh_config and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("ssh_config").expect("Failed to get parser for 'ssh_config'");
    let tree = parser.parse("Host example\n  HostName example.com", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_stan() {
    if !tree_sitter_language_pack::has_language("stan") {
        eprintln!("Skipping: language 'stan' not available");
        return;
    }
    // Smoke test: load stan and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("stan").expect("Failed to get parser for 'stan'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_starlark() {
    if !tree_sitter_language_pack::has_language("starlark") {
        eprintln!("Skipping: language 'starlark' not available");
        return;
    }
    // Smoke test: load starlark and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("starlark").expect("Failed to get parser for 'starlark'");
    let tree = parser.parse("def hello(): pass", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_superhtml() {
    if !tree_sitter_language_pack::has_language("superhtml") {
        eprintln!("Skipping: language 'superhtml' not available");
        return;
    }
    // Smoke test: load superhtml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("superhtml").expect("Failed to get parser for 'superhtml'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_svelte() {
    if !tree_sitter_language_pack::has_language("svelte") {
        eprintln!("Skipping: language 'svelte' not available");
        return;
    }
    // Smoke test: load svelte and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("svelte").expect("Failed to get parser for 'svelte'");
    let tree = parser.parse("<script>let x = 1;</script>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_sway() {
    if !tree_sitter_language_pack::has_language("sway") {
        eprintln!("Skipping: language 'sway' not available");
        return;
    }
    // Smoke test: load sway and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("sway").expect("Failed to get parser for 'sway'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_swift() {
    if !tree_sitter_language_pack::has_language("swift") {
        eprintln!("Skipping: language 'swift' not available");
        return;
    }
    // Smoke test: load swift and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("swift").expect("Failed to get parser for 'swift'");
    let tree = parser.parse("print(\"hello\")", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_systemverilog() {
    if !tree_sitter_language_pack::has_language("systemverilog") {
        eprintln!("Skipping: language 'systemverilog' not available");
        return;
    }
    // Smoke test: load systemverilog and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("systemverilog").expect("Failed to get parser for 'systemverilog'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tablegen() {
    if !tree_sitter_language_pack::has_language("tablegen") {
        eprintln!("Skipping: language 'tablegen' not available");
        return;
    }
    // Smoke test: load tablegen and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tablegen").expect("Failed to get parser for 'tablegen'");
    let tree = parser.parse("def Hello : Base {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tact() {
    if !tree_sitter_language_pack::has_language("tact") {
        eprintln!("Skipping: language 'tact' not available");
        return;
    }
    // Smoke test: load tact and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tact").expect("Failed to get parser for 'tact'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tcl() {
    if !tree_sitter_language_pack::has_language("tcl") {
        eprintln!("Skipping: language 'tcl' not available");
        return;
    }
    // Smoke test: load tcl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tcl").expect("Failed to get parser for 'tcl'");
    let tree = parser.parse("puts hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_teal() {
    if !tree_sitter_language_pack::has_language("teal") {
        eprintln!("Skipping: language 'teal' not available");
        return;
    }
    // Smoke test: load teal and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("teal").expect("Failed to get parser for 'teal'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_templ() {
    if !tree_sitter_language_pack::has_language("templ") {
        eprintln!("Skipping: language 'templ' not available");
        return;
    }
    // Smoke test: load templ and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("templ").expect("Failed to get parser for 'templ'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tera() {
    if !tree_sitter_language_pack::has_language("tera") {
        eprintln!("Skipping: language 'tera' not available");
        return;
    }
    // Smoke test: load tera and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tera").expect("Failed to get parser for 'tera'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_terraform() {
    if !tree_sitter_language_pack::has_language("terraform") {
        eprintln!("Skipping: language 'terraform' not available");
        return;
    }
    // Smoke test: load terraform and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("terraform").expect("Failed to get parser for 'terraform'");
    let tree = parser.parse("resource \"null_resource\" \"main\" {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_test() {
    if !tree_sitter_language_pack::has_language("test") {
        eprintln!("Skipping: language 'test' not available");
        return;
    }
    // Smoke test: load test and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("test").expect("Failed to get parser for 'test'");
    let tree = parser.parse("===========\nTest\n===========\n---\n(node)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_textproto() {
    if !tree_sitter_language_pack::has_language("textproto") {
        eprintln!("Skipping: language 'textproto' not available");
        return;
    }
    // Smoke test: load textproto and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("textproto").expect("Failed to get parser for 'textproto'");
    let tree = parser.parse("key: \"value\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_thrift() {
    if !tree_sitter_language_pack::has_language("thrift") {
        eprintln!("Skipping: language 'thrift' not available");
        return;
    }
    // Smoke test: load thrift and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("thrift").expect("Failed to get parser for 'thrift'");
    let tree = parser.parse("service HelloService {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tlaplus() {
    if !tree_sitter_language_pack::has_language("tlaplus") {
        eprintln!("Skipping: language 'tlaplus' not available");
        return;
    }
    // Smoke test: load tlaplus and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tlaplus").expect("Failed to get parser for 'tlaplus'");
    let tree = parser.parse("---- MODULE Main ----\n====", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tmux() {
    if !tree_sitter_language_pack::has_language("tmux") {
        eprintln!("Skipping: language 'tmux' not available");
        return;
    }
    // Smoke test: load tmux and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tmux").expect("Failed to get parser for 'tmux'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_todotxt() {
    if !tree_sitter_language_pack::has_language("todotxt") {
        eprintln!("Skipping: language 'todotxt' not available");
        return;
    }
    // Smoke test: load todotxt and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("todotxt").expect("Failed to get parser for 'todotxt'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_toml() {
    if !tree_sitter_language_pack::has_language("toml") {
        eprintln!("Skipping: language 'toml' not available");
        return;
    }
    // Smoke test: load toml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("toml").expect("Failed to get parser for 'toml'");
    let tree = parser.parse("key = \"value\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tsv() {
    if !tree_sitter_language_pack::has_language("tsv") {
        eprintln!("Skipping: language 'tsv' not available");
        return;
    }
    // Smoke test: load tsv and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tsv").expect("Failed to get parser for 'tsv'");
    let tree = parser.parse("a\tb\tc\n1\t2\t3", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_tsx() {
    if !tree_sitter_language_pack::has_language("tsx") {
        eprintln!("Skipping: language 'tsx' not available");
        return;
    }
    // Smoke test: load tsx and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("tsx").expect("Failed to get parser for 'tsx'");
    let tree = parser.parse("const App = () => <div />;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_turtle() {
    if !tree_sitter_language_pack::has_language("turtle") {
        eprintln!("Skipping: language 'turtle' not available");
        return;
    }
    // Smoke test: load turtle and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("turtle").expect("Failed to get parser for 'turtle'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_twig() {
    if !tree_sitter_language_pack::has_language("twig") {
        eprintln!("Skipping: language 'twig' not available");
        return;
    }
    // Smoke test: load twig and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("twig").expect("Failed to get parser for 'twig'");
    let tree = parser.parse("{{ variable }}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_typescript() {
    if !tree_sitter_language_pack::has_language("typescript") {
        eprintln!("Skipping: language 'typescript' not available");
        return;
    }
    // Smoke test: load typescript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("typescript").expect("Failed to get parser for 'typescript'");
    let tree = parser.parse("const x: number = 42;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_typespec() {
    if !tree_sitter_language_pack::has_language("typespec") {
        eprintln!("Skipping: language 'typespec' not available");
        return;
    }
    // Smoke test: load typespec and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("typespec").expect("Failed to get parser for 'typespec'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_typoscript() {
    if !tree_sitter_language_pack::has_language("typoscript") {
        eprintln!("Skipping: language 'typoscript' not available");
        return;
    }
    // Smoke test: load typoscript and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("typoscript").expect("Failed to get parser for 'typoscript'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_typst() {
    if !tree_sitter_language_pack::has_language("typst") {
        eprintln!("Skipping: language 'typst' not available");
        return;
    }
    // Smoke test: load typst and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("typst").expect("Failed to get parser for 'typst'");
    let tree = parser.parse("#let x = 1", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_udev() {
    if !tree_sitter_language_pack::has_language("udev") {
        eprintln!("Skipping: language 'udev' not available");
        return;
    }
    // Smoke test: load udev and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("udev").expect("Failed to get parser for 'udev'");
    let tree = parser.parse("ACTION==\"add\", KERNEL==\"sd*\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ungrammar() {
    if !tree_sitter_language_pack::has_language("ungrammar") {
        eprintln!("Skipping: language 'ungrammar' not available");
        return;
    }
    // Smoke test: load ungrammar and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ungrammar").expect("Failed to get parser for 'ungrammar'");
    let tree = parser.parse("Root = Item*\nItem = 'token'", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_uxntal() {
    if !tree_sitter_language_pack::has_language("uxntal") {
        eprintln!("Skipping: language 'uxntal' not available");
        return;
    }
    // Smoke test: load uxntal and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("uxntal").expect("Failed to get parser for 'uxntal'");
    let tree = parser.parse("|0100 LIT 01", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_v() {
    if !tree_sitter_language_pack::has_language("v") {
        eprintln!("Skipping: language 'v' not available");
        return;
    }
    // Smoke test: load v and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("v").expect("Failed to get parser for 'v'");
    let tree = parser.parse("fn main() {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vb() {
    if !tree_sitter_language_pack::has_language("vb") {
        eprintln!("Skipping: language 'vb' not available");
        return;
    }
    // Smoke test: load vb and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vb").expect("Failed to get parser for 'vb'");
    let tree = parser.parse("Module Main\nEnd Module", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_verilog() {
    if !tree_sitter_language_pack::has_language("verilog") {
        eprintln!("Skipping: language 'verilog' not available");
        return;
    }
    // Smoke test: load verilog and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("verilog").expect("Failed to get parser for 'verilog'");
    let tree = parser.parse("module main; endmodule", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vhdl() {
    if !tree_sitter_language_pack::has_language("vhdl") {
        eprintln!("Skipping: language 'vhdl' not available");
        return;
    }
    // Smoke test: load vhdl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vhdl").expect("Failed to get parser for 'vhdl'");
    let tree = parser.parse("entity main is end main;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vhs() {
    if !tree_sitter_language_pack::has_language("vhs") {
        eprintln!("Skipping: language 'vhs' not available");
        return;
    }
    // Smoke test: load vhs and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vhs").expect("Failed to get parser for 'vhs'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vim() {
    if !tree_sitter_language_pack::has_language("vim") {
        eprintln!("Skipping: language 'vim' not available");
        return;
    }
    // Smoke test: load vim and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vim").expect("Failed to get parser for 'vim'");
    let tree = parser.parse("echo 'hello'", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vimdoc() {
    if !tree_sitter_language_pack::has_language("vimdoc") {
        eprintln!("Skipping: language 'vimdoc' not available");
        return;
    }
    // Smoke test: load vimdoc and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vimdoc").expect("Failed to get parser for 'vimdoc'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vrl() {
    if !tree_sitter_language_pack::has_language("vrl") {
        eprintln!("Skipping: language 'vrl' not available");
        return;
    }
    // Smoke test: load vrl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vrl").expect("Failed to get parser for 'vrl'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_vue() {
    if !tree_sitter_language_pack::has_language("vue") {
        eprintln!("Skipping: language 'vue' not available");
        return;
    }
    // Smoke test: load vue and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("vue").expect("Failed to get parser for 'vue'");
    let tree = parser.parse("<template><div>hello</div></template>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wast() {
    if !tree_sitter_language_pack::has_language("wast") {
        eprintln!("Skipping: language 'wast' not available");
        return;
    }
    // Smoke test: load wast and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wast").expect("Failed to get parser for 'wast'");
    let tree = parser.parse("(module)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wat() {
    if !tree_sitter_language_pack::has_language("wat") {
        eprintln!("Skipping: language 'wat' not available");
        return;
    }
    // Smoke test: load wat and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wat").expect("Failed to get parser for 'wat'");
    let tree = parser.parse("(module)", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wgsl() {
    if !tree_sitter_language_pack::has_language("wgsl") {
        eprintln!("Skipping: language 'wgsl' not available");
        return;
    }
    // Smoke test: load wgsl and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wgsl").expect("Failed to get parser for 'wgsl'");
    let tree = parser.parse(
        "@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }",
        None,
    );
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wgsl_bevy() {
    if !tree_sitter_language_pack::has_language("wgsl_bevy") {
        eprintln!("Skipping: language 'wgsl_bevy' not available");
        return;
    }
    // Smoke test: load wgsl_bevy and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wgsl_bevy").expect("Failed to get parser for 'wgsl_bevy'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wit() {
    if !tree_sitter_language_pack::has_language("wit") {
        eprintln!("Skipping: language 'wit' not available");
        return;
    }
    // Smoke test: load wit and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wit").expect("Failed to get parser for 'wit'");
    let tree = parser.parse("package example:pkg;", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_wolfram() {
    if !tree_sitter_language_pack::has_language("wolfram") {
        eprintln!("Skipping: language 'wolfram' not available");
        return;
    }
    // Smoke test: load wolfram and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("wolfram").expect("Failed to get parser for 'wolfram'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_x86asm() {
    if !tree_sitter_language_pack::has_language("x86asm") {
        eprintln!("Skipping: language 'x86asm' not available");
        return;
    }
    // Smoke test: load x86asm and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("x86asm").expect("Failed to get parser for 'x86asm'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_xcompose() {
    if !tree_sitter_language_pack::has_language("xcompose") {
        eprintln!("Skipping: language 'xcompose' not available");
        return;
    }
    // Smoke test: load xcompose and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("xcompose").expect("Failed to get parser for 'xcompose'");
    let tree = parser.parse("<Multi_key> <a> : \"a\"", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_xml() {
    if !tree_sitter_language_pack::has_language("xml") {
        eprintln!("Skipping: language 'xml' not available");
        return;
    }
    // Smoke test: load xml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("xml").expect("Failed to get parser for 'xml'");
    let tree = parser.parse("<?xml version=\"1.0\"?>\n<root>hello</root>", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_yaml() {
    if !tree_sitter_language_pack::has_language("yaml") {
        eprintln!("Skipping: language 'yaml' not available");
        return;
    }
    // Smoke test: load yaml and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("yaml").expect("Failed to get parser for 'yaml'");
    let tree = parser.parse("key: value", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_yuck() {
    if !tree_sitter_language_pack::has_language("yuck") {
        eprintln!("Skipping: language 'yuck' not available");
        return;
    }
    // Smoke test: load yuck and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("yuck").expect("Failed to get parser for 'yuck'");
    let tree = parser.parse("(defwidget main [] (label :text \"hi\"))", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_zig() {
    if !tree_sitter_language_pack::has_language("zig") {
        eprintln!("Skipping: language 'zig' not available");
        return;
    }
    // Smoke test: load zig and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("zig").expect("Failed to get parser for 'zig'");
    let tree = parser.parse("pub fn main() void {}", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ziggy() {
    if !tree_sitter_language_pack::has_language("ziggy") {
        eprintln!("Skipping: language 'ziggy' not available");
        return;
    }
    // Smoke test: load ziggy and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("ziggy").expect("Failed to get parser for 'ziggy'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_ziggy_schema() {
    if !tree_sitter_language_pack::has_language("ziggy_schema") {
        eprintln!("Skipping: language 'ziggy_schema' not available");
        return;
    }
    // Smoke test: load ziggy_schema and parse a simple snippet
    let mut parser =
        tree_sitter_language_pack::get_parser("ziggy_schema").expect("Failed to get parser for 'ziggy_schema'");
    let tree = parser.parse("x", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}

#[test]
fn smoke_zsh() {
    if !tree_sitter_language_pack::has_language("zsh") {
        eprintln!("Skipping: language 'zsh' not available");
        return;
    }
    // Smoke test: load zsh and parse a simple snippet
    let mut parser = tree_sitter_language_pack::get_parser("zsh").expect("Failed to get parser for 'zsh'");
    let tree = parser.parse("echo hello", None);
    assert!(tree.is_some(), "Parse tree should not be None");
    let tree = tree.unwrap();
    let root = tree.root_node();
    assert!(
        root.child_count() >= 1,
        "Root should have at least 1 child(ren), got {}",
        root.child_count()
    );
}
