// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d6354d64f0aa9c95577e25095f590fadf9b1c7393a4b5d84028b412247adf509
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import TreeSitterLanguagePack

/// E2e tests for category: error-handling.
final class ErrorHandlingTests: XCTestCase {
    func testErrorDetectContentEmpty() throws {
        // Detect language from empty content returns null
        let result = try detectLanguageFromContent("")
        XCTAssertTrue(result.isEmpty, "expected empty value")
    }

    func testErrorDetectExtensionEmpty() throws {
        // Detect language from empty extension returns null
        let result = try detectLanguageFromExtension("")
        XCTAssertTrue(result.isEmpty, "expected empty value")
    }

    func testErrorDetectPathEmpty() throws {
        // Detect language from empty path returns null
        let result = try detectLanguageFromPath("")
        XCTAssertTrue(result.isEmpty, "expected empty value")
    }

    func testErrorEmptyLanguageName() throws {
        // Parsing with empty language name should error
        XCTAssertThrowsError(try parseString("", "hello"))
    }

    func testErrorExtractUnknownLanguage() throws {
        // Extract patterns with unknown language returns error
        XCTAssertThrowsError(try extractPatterns("x = 1", "{\"language\":\"nonexistent_language_xyz\",\"patterns\":{}}"))
    }

    func testErrorHandlingEmptySource() throws {
        // Parsing an empty string should still produce a tree.
        let tree = try parseString("javascript", "")
    }

    func testErrorHandlingInvalidSyntax() throws {
        // Parsing invalid syntax should produce a tree with error nodes.
        let tree = try parseString("javascript", "function function function @@@ %%%")
        // method_result assertions not yet implemented for Swift
    }

    func testErrorHandlingUnknownLanguage() throws {
        // Loading a nonexistent language should produce an error.
        XCTAssertThrowsError(try parseString("nonexistent_xyz", ""))
    }

    func testErrorProcessEmptySource() throws {
        // Process empty source code succeeds with zero metrics
        let result = try process("", "{\"language\":\"python\"}")
        XCTAssertEqual(result.metrics.total_lines, 0)
    }

    func testErrorRunQueryInvalidSyntax() throws {
        // Run query with invalid S-expression syntax produces error
        let tree = try parseString("python", "x = 1")
        // method_result assertions not yet implemented for Swift
    }

    func testParseEmptyLanguage() throws {
        // parse_string() returns error with empty language name
        XCTAssertThrowsError(try parseString("", "x = 1"))
    }

    func testProcessUnknownLanguage() throws {
        // process() returns error when config language is unknown
        XCTAssertThrowsError(try process("x = 1", "{\"language\":\"nonexistent_xyz\"}"))
    }

}
