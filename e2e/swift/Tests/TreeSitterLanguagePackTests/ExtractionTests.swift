// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d6354d64f0aa9c95577e25095f590fadf9b1c7393a4b5d84028b412247adf509
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import XCTest
import TreeSitterLanguagePack

/// E2e tests for category: extraction.
final class ExtractionTests: XCTestCase {
    func testExtractInvalidByteRange() throws {
        // extract_patterns() with out-of-bounds byte_range returns empty results
        let result = try extractPatterns("x = 1\n", "{\"language\":\"python\",\"patterns\":{\"funcs\":{\"byte_range\":[9999,99999],\"capture_output\":\"Full\",\"query\":\"(expression_statement) @e\"}}}")
        XCTAssertEqual(result.results.funcs.matches.count, 0)
    }

    func testExtractPatternsNoMatches() throws {
        // Extraction query that matches nothing returns empty results
        let result = try extractPatterns("x = 1\ny = 2\n", "{\"language\":\"python\",\"patterns\":{\"classes\":{\"capture_output\":\"Full\",\"query\":\"(class_definition name: (identifier) @name)\"}}}")
        XCTAssertEqual(result.results.classes.matches.count, 0)
    }

    func testExtractPatternsPythonFunctions() throws {
        // Extract function definitions from Python source using tree-sitter query
        let result = try extractPatterns("def hello():\n    pass\n\ndef world(x):\n    return x * 2\n", "{\"language\":\"python\",\"patterns\":{\"functions\":{\"capture_output\":\"Full\",\"query\":\"(function_definition name: (identifier) @name)\"}}}")
        XCTAssertEqual(result.language.trimmingCharacters(in: .whitespaces), "python")
        XCTAssertGreaterThanOrEqual(result.results.functions.matches.count, 2)
    }

    func testValidateEmptyPatterns() throws {
        // validate_extraction() with empty patterns returns valid
        let result = try validateExtraction("{\"language\":\"python\",\"patterns\":{}}")
        XCTAssertEqual(result.valid, true)
    }

    func testValidateExtractionInvalidQuery() throws {
        // Invalid query syntax fails validation
        XCTAssertThrowsError(try validateExtraction("{\"language\":\"python\",\"patterns\":{\"broken\":{\"capture_output\":\"Full\",\"query\":\"(this_is_not_a_valid_node @x\"}}}"))
    }

    func testValidateExtractionValidConfig() throws {
        // Valid extraction config passes validation
        let result = try validateExtraction("{\"language\":\"python\",\"patterns\":{\"functions\":{\"capture_output\":\"Full\",\"query\":\"(function_definition name: (identifier) @name)\"}}}")
        XCTAssertEqual(result.valid, true)
    }

}
