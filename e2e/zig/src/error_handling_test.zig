// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:843650f11d4f9fc34099d6b1323b8e2d3781249d7335cc1e01350921cda40b1f
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const tree_sitter_language_pack = @import("tree_sitter_language_pack");

// E2e tests for category: error-handling

test "error_detect_content_empty" {
    // Detect language from empty content returns null
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_content("");
    try testing.expect(result.len == 0);
}

test "error_detect_extension_empty" {
    // Detect language from empty extension returns null
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("");
    try testing.expect(result.len == 0);
}

test "error_detect_path_empty" {
    // Detect language from empty path returns null
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("");
    try testing.expect(result.len == 0);
}

test "error_empty_language_name" {
    // Parsing with empty language name should error
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.parse_string("", "hello");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_extract_unknown_language" {
    // Extract patterns with unknown language returns error
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.extract_patterns("x = 1", "{\"language\":\"nonexistent_language_xyz\",\"patterns\":{}}");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_handling_empty_source" {
    // Parsing an empty string should still produce a tree.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const tree = tree_sitter_language_pack.parse_string("javascript", "");
}

test "error_handling_invalid_syntax" {
    // Parsing invalid syntax should produce a tree with error nodes.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const tree = tree_sitter_language_pack.parse_string("javascript", "function function function @@@ %%%");
    // method_result assertions not yet implemented for Zig
}

test "error_handling_unknown_language" {
    // Loading a nonexistent language should produce an error.
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.parse_string("nonexistent_xyz", "");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "error_process_empty_source" {
    // Process empty source code succeeds with zero metrics
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.process("", "{\"language\":\"python\"}");
    try testing.expectEqual(0, result.metrics.total_lines);
}

test "error_run_query_invalid_syntax" {
    // Run query with invalid S-expression syntax produces error
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const tree = tree_sitter_language_pack.parse_string("python", "x = 1");
    // method_result assertions not yet implemented for Zig
}

test "parse_empty_language" {
    // parse_string() returns error with empty language name
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.parse_string("", "x = 1");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

test "process_unknown_language" {
    // process() returns error when config language is unknown
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.process("x = 1", "{\"language\":\"nonexistent_xyz\"}");
    try testing.expect(@typeInfo(@TypeOf(result)) == .ErrorUnion);

