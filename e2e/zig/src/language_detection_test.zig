// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d6354d64f0aa9c95577e25095f590fadf9b1c7393a4b5d84028b412247adf509
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
const std = @import("std");
const testing = std.testing;
const tree_sitter_language_pack = @import("tree_sitter_language_pack");

// E2e tests for category: language-detection

test "ambiguity_h_extension" {
    // The .h extension is ambiguous (could be C, C++, or Objective-C)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.process("", null);
    try testing.expect(result.len > 0);
}

test "ambiguity_py_extension" {
    // The .py extension is NOT ambiguous (only maps to python)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.process("", null);
    try testing.expect(result.len == 0);
}

test "ambiguity_unknown_extension" {
    // Unknown extension returns empty (no ambiguity data)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.process("", null);
    try testing.expect(result.len == 0);
}

test "detect_content_bash_shebang" {
    // detect_language_from_content recognizes #!/bin/bash shebang
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_content("#!/bin/bash\necho hi");
    try testing.expectEqual("bash", result);
}

test "detect_content_no_shebang" {
    // detect_language_from_content returns None when no shebang present
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_content("no shebang here");
    try testing.expect(result.len == 0);
}

test "detect_content_python_shebang" {
    // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_content("#!/usr/bin/env python3\npass");
    try testing.expectEqual("python", result);
}

test "detect_ext_cpp" {
    // detect_language_from_extension recognizes .cpp as cpp
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("cpp");
    try testing.expectEqual("cpp", result);
}

test "detect_ext_go" {
    // detect_language_from_extension recognizes .go as go
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("go");
    try testing.expectEqual("go", result);
}

test "detect_ext_java" {
    // detect_language_from_extension recognizes .java as java
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("java");
    try testing.expectEqual("java", result);
}

test "detect_ext_javascript" {
    // detect_language_from_extension recognizes .js as javascript
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("js");
    try testing.expectEqual("javascript", result);
}

test "detect_ext_php" {
    // detect_language_from_extension recognizes .php as php
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("php");
    try testing.expectEqual("php", result);
}

test "detect_ext_python" {
    // detect_language_from_extension recognizes .py as python
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("py");
    try testing.expectEqual("python", result);
}

test "detect_ext_ruby" {
    // detect_language_from_extension recognizes .rb as ruby
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("rb");
    try testing.expectEqual("ruby", result);
}

test "detect_ext_rust" {
    // detect_language_from_extension recognizes .rs as rust
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("rs");
    try testing.expectEqual("rust", result);
}

test "detect_ext_typescript" {
    // detect_language_from_extension recognizes .ts as typescript
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("ts");
    try testing.expectEqual("typescript", result);
}

test "detect_ext_unknown" {
    // detect_language_from_extension returns None for unknown extension .xyz
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_extension("xyz");
    try testing.expect(result.len == 0);
}

test "detect_path_dotfile" {
    // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path(".gitignore");
    try testing.expect(result.len == 0);
}

test "detect_path_go_nested" {
    // detect_language_from_path extracts extension from nested path lib/server.go
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("lib/server.go");
    try testing.expectEqual("go", result);
}

test "detect_path_java_root" {
    // detect_language_from_path recognizes Main.java in root directory
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("Main.java");
    try testing.expectEqual("java", result);
}

test "detect_path_js_root" {
    // detect_language_from_path recognizes app.js in root directory
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("app.js");
    try testing.expectEqual("javascript", result);
}

test "detect_path_nested" {
    // detect_language_from_path extracts extension from nested path src/main.py
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("src/main.py");
    try testing.expectEqual("python", result);
}

test "detect_path_no_extension" {
    // detect_language_from_path returns None for extensionless file Makefile
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("Makefile");
    try testing.expect(result.len == 0);
}

test "detect_path_rust_src" {
    // detect_language_from_path extracts extension from nested path src/main.rs
    var gpa = std.heap.GeneralPurposeAllocator(.{}){}
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const result = tree_sitter_language_pack.detect_language_from_path("src/main.rs");
    try testing.expectEqual("rust", result);
}

