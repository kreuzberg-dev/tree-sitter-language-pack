// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:82af90950728f7e7423a0812417326d2de611822d0aecf8a029aa6cdc047df1a
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.tslp.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.tslp.android.TreeSitterLanguagePack
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.tslp.android.ProcessConfig

/** E2e tests for category: smoke. */
class SmokeTest {

    companion object {
        init {
            try {
                System.loadLibrary("ts_pack_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load ts_pack_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testSmokeAbl() = runBlocking {
        // Smoke test: load abl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"abl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_abl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAbnf() = runBlocking {
        // Smoke test: load abnf and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"abnf\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("a = \"b\"\r\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_abnf`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeActionscript() = runBlocking {
        // Smoke test: load actionscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"actionscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("var x:int = 1;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_actionscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAda() = runBlocking {
        // Smoke test: load ada and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ada\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("procedure Main is begin null; end Main;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ada`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAgda() = runBlocking {
        // Smoke test: load agda and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"agda\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Main where", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_agda`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAiken() = runBlocking {
        // Smoke test: load aiken and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"aiken\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main() {\n  1\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_aiken`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAl() = runBlocking {
        // Smoke test: load al and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"al\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_al`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAngular() = runBlocking {
        // Smoke test: load angular and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"angular\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_angular`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeApex() = runBlocking {
        // Smoke test: load apex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"apex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("public class Main {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_apex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeApplescript() = runBlocking {
        // Smoke test: load applescript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"applescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("set x to 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_applescript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeArduino() = runBlocking {
        // Smoke test: load arduino and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"arduino\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("void setup() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_arduino`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAsciidoc() = runBlocking {
        // Smoke test: load asciidoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"asciidoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("= Title\n\nParagraph.", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_asciidoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAsm() = runBlocking {
        // Smoke test: load asm and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"asm\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("mov eax, 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_asm`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAstro() = runBlocking {
        // Smoke test: load astro and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"astro\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("---\n---\n<p>hello</p>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_astro`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAvro() = runBlocking {
        // Smoke test: load avro and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"avro\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("protocol P {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_avro`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeAwk() = runBlocking {
        // Smoke test: load awk and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"awk\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("BEGIN { print \"hello\" }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_awk`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBallerina() = runBlocking {
        // Smoke test: load ballerina and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ballerina\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("public function main() {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ballerina`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBash() = runBlocking {
        // Smoke test: load bash and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bash\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("echo hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bash`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBass() = runBlocking {
        // Smoke test: load bass and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bass\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bass`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBatch() = runBlocking {
        // Smoke test: load batch and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"batch\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@echo off\necho hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_batch`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBeancount() = runBlocking {
        // Smoke test: load beancount and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"beancount\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("2024-01-01 open Assets:Bank USD", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_beancount`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBibtex() = runBlocking {
        // Smoke test: load bibtex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bibtex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@article{key, title={A}}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bibtex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBicep() = runBlocking {
        // Smoke test: load bicep and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bicep\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("param name string", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bicep`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBitbake() = runBlocking {
        // Smoke test: load bitbake and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bitbake\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("DESCRIPTION = \"hello\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bitbake`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBlade() = runBlocking {
        // Smoke test: load blade and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"blade\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_blade`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBpftrace() = runBlocking {
        // Smoke test: load bpftrace and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bpftrace\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("BEGIN { }\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bpftrace`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBrightscript() = runBlocking {
        // Smoke test: load brightscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"brightscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_brightscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeBsl() = runBlocking {
        // Smoke test: load bsl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"bsl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Procedure Main() EndProcedure", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_bsl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeC() = runBlocking {
        // Smoke test: load c and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"c\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("int main() { return 0; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_c`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeC3() = runBlocking {
        // Smoke test: load c3 and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"c3\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_c3`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCaddy() = runBlocking {
        // Smoke test: load caddy and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"caddy\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process(":8080 {\n\trespond \"Hello\"\n}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_caddy`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCairo() = runBlocking {
        // Smoke test: load cairo and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cairo\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cairo`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCapnp() = runBlocking {
        // Smoke test: load capnp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"capnp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@0xabcdef1234567890;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_capnp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCedar() = runBlocking {
        // Smoke test: load cedar and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cedar\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("permit(principal, action, resource);", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cedar`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCedarschema() = runBlocking {
        // Smoke test: load cedarschema and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cedarschema\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("entity User;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cedarschema`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCel() = runBlocking {
        // Smoke test: load cel and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cel\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cel`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCfml() = runBlocking {
        // Smoke test: load cfml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cfml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cfml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeChatito() = runBlocking {
        // Smoke test: load chatito and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"chatito\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("%[greeting]\n    hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_chatito`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeChuck() = runBlocking {
        // Smoke test: load chuck and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"chuck\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_chuck`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCircom() = runBlocking {
        // Smoke test: load circom and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"circom\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_circom`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeClarity() = runBlocking {
        // Smoke test: load clarity and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"clarity\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(define-public (hello) (ok true))", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_clarity`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeClojure() = runBlocking {
        // Smoke test: load clojure and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"clojure\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(def x 1)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_clojure`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCmake() = runBlocking {
        // Smoke test: load cmake and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cmake\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("cmake_minimum_required(VERSION 3.0)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cmake`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCobol() = runBlocking {
        // Smoke test: load cobol and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cobol\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cobol`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeComment() = runBlocking {
        // Smoke test: load comment and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"comment\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Review: handle edge case", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_comment`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCommonlisp() = runBlocking {
        // Smoke test: load commonlisp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"commonlisp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(defun hello () (print \"hello\"))", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_commonlisp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCooklang() = runBlocking {
        // Smoke test: load cooklang and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cooklang\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cooklang`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCorn() = runBlocking {
        // Smoke test: load corn and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"corn\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{ key = \"value\" }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_corn`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCpon() = runBlocking {
        // Smoke test: load cpon and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cpon\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{\"key\": 1}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cpon`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCpp() = runBlocking {
        // Smoke test: load cpp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cpp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("int main() { return 0; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cpp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCrystal() = runBlocking {
        // Smoke test: load crystal and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"crystal\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_crystal`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCsharp() = runBlocking {
        // Smoke test: load csharp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"csharp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Main {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_csharp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCss() = runBlocking {
        // Smoke test: load css and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"css\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("body { color: red; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_css`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCst() = runBlocking {
        // Smoke test: load cst and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cst\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cst`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCsv() = runBlocking {
        // Smoke test: load csv and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"csv\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("a,b,c\n1,2,3", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_csv`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCuda() = runBlocking {
        // Smoke test: load cuda and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cuda\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("__global__ void kernel() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cuda`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCue() = runBlocking {
        // Smoke test: load cue and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cue\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cue`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCylc() = runBlocking {
        // Smoke test: load cylc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cylc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cylc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCypher() = runBlocking {
        // Smoke test: load cypher and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cypher\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("MATCH (n) RETURN n\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cypher`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeCython() = runBlocking {
        // Smoke test: load cython and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"cython\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x = 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_cython`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeD() = runBlocking {
        // Smoke test: load d and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"d\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("void main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_d`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeD2() = runBlocking {
        // Smoke test: load d2 and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"d2\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("a -> b\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_d2`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDart() = runBlocking {
        // Smoke test: load dart and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dart\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("void main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dart`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDesktop() = runBlocking {
        // Smoke test: load desktop and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"desktop\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_desktop`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDevicetree() = runBlocking {
        // Smoke test: load devicetree and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"devicetree\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("/dts-v1/;\n/ { };", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_devicetree`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDhall() = runBlocking {
        // Smoke test: load dhall and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dhall\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dhall`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDiff() = runBlocking {
        // Smoke test: load diff and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"diff\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_diff`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDjot() = runBlocking {
        // Smoke test: load djot and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"djot\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_djot`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDockerfile() = runBlocking {
        // Smoke test: load dockerfile and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dockerfile\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("FROM alpine", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dockerfile`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDot() = runBlocking {
        // Smoke test: load dot and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dot\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("digraph G { A -> B; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dot`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDotenv() = runBlocking {
        // Smoke test: load dotenv and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dotenv\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("KEY=value\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dotenv`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDoxygen() = runBlocking {
        // Smoke test: load doxygen and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"doxygen\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("/** @brief A function */", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_doxygen`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeDtd() = runBlocking {
        // Smoke test: load dtd and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"dtd\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<!ELEMENT note (body)>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_dtd`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEarthfile() = runBlocking {
        // Smoke test: load earthfile and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"earthfile\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_earthfile`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEbnf() = runBlocking {
        // Smoke test: load ebnf and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ebnf\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ebnf`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEditorconfig() = runBlocking {
        // Smoke test: load editorconfig and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"editorconfig\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_editorconfig`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEdoc() = runBlocking {
        // Smoke test: load edoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"edoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@doc foo\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_edoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEds() = runBlocking {
        // Smoke test: load eds and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"eds\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_eds`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEex() = runBlocking {
        // Smoke test: load eex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"eex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_eex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEiffel() = runBlocking {
        // Smoke test: load eiffel and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"eiffel\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class FOO\nend\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_eiffel`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeElisp() = runBlocking {
        // Smoke test: load elisp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"elisp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(defun hello () (message \"hello\"))", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_elisp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeElixir() = runBlocking {
        // Smoke test: load elixir and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"elixir\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("IO.puts(\"hello\")", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_elixir`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeElm() = runBlocking {
        // Smoke test: load elm and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"elm\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Main exposing (..)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_elm`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeElsa() = runBlocking {
        // Smoke test: load elsa and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"elsa\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_elsa`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeElvish() = runBlocking {
        // Smoke test: load elvish and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"elvish\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_elvish`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEmbeddedtemplate() = runBlocking {
        // Smoke test: load embeddedtemplate and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"embeddedtemplate\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<%= value %>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_embeddedtemplate`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeEnforce() = runBlocking {
        // Smoke test: load enforce and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"enforce\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_enforce`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeErlang() = runBlocking {
        // Smoke test: load erlang and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"erlang\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("main() -> ok.", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_erlang`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFacility() = runBlocking {
        // Smoke test: load facility and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"facility\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_facility`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFaust() = runBlocking {
        // Smoke test: load faust and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"faust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_faust`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFennel() = runBlocking {
        // Smoke test: load fennel and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fennel\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(fn hello [] (print :hello))", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fennel`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFidl() = runBlocking {
        // Smoke test: load fidl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fidl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fidl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFirrtl() = runBlocking {
        // Smoke test: load firrtl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"firrtl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("circuit Main :", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_firrtl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFish() = runBlocking {
        // Smoke test: load fish and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fish\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("echo hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fish`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFlatbuffers() = runBlocking {
        // Smoke test: load flatbuffers and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"flatbuffers\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("table Foo {}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_flatbuffers`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFluent() = runBlocking {
        // Smoke test: load fluent and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fluent\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("hello = Hello\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fluent`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFoam() = runBlocking {
        // Smoke test: load foam and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"foam\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_foam`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeForth() = runBlocking {
        // Smoke test: load forth and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"forth\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_forth`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFortran() = runBlocking {
        // Smoke test: load fortran and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fortran\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("program main\nend program main", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fortran`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFsharp() = runBlocking {
        // Smoke test: load fsharp and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fsharp\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("let x = 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fsharp`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFsharpSignature() = runBlocking {
        // Smoke test: load fsharp_signature and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fsharp_signature\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("val x: int", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fsharp_signature`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFunc() = runBlocking {
        // Smoke test: load func and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"func\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("() recv_internal() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_func`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeFusion() = runBlocking {
        // Smoke test: load fusion and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"fusion\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("foo = 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_fusion`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGap() = runBlocking {
        // Smoke test: load gap and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gap\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gap`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGcode() = runBlocking {
        // Smoke test: load gcode and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gcode\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("G0 X0\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gcode`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGdscript() = runBlocking {
        // Smoke test: load gdscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gdscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("extends Node\nfunc _ready():\n\tpass", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gdscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGdshader() = runBlocking {
        // Smoke test: load gdshader and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gdshader\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gdshader`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGherkin() = runBlocking {
        // Smoke test: load gherkin and parse a simple feature
        val config = MAPPER.readValue("{\"language\":\"gherkin\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Feature: Calculator\n  Scenario: Add numbers\n    Given I have entered 1\n    When I add 2\n    Then the result should be 3\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gherkin`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGitConfig() = runBlocking {
        // Smoke test: load git_config and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"git_config\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_git_config`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGitRebase() = runBlocking {
        // Smoke test: load git_rebase and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"git_rebase\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_git_rebase`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGitattributes() = runBlocking {
        // Smoke test: load gitattributes and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gitattributes\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("*.txt text", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gitattributes`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGitcommit() = runBlocking {
        // Smoke test: load gitcommit and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gitcommit\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("feat: add feature\n\nBody text", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gitcommit`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGitignore() = runBlocking {
        // Smoke test: load gitignore and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gitignore\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("*.o\n*.log", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gitignore`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGleam() = runBlocking {
        // Smoke test: load gleam and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gleam\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("pub fn main() { }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gleam`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGlimmer() = runBlocking {
        // Smoke test: load glimmer and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"glimmer\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_glimmer`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGlsl() = runBlocking {
        // Smoke test: load glsl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"glsl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("void main() { gl_Position = vec4(0.0); }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_glsl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGn() = runBlocking {
        // Smoke test: load gn and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gn\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("group(\"hello\") {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gn`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGnuplot() = runBlocking {
        // Smoke test: load gnuplot and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gnuplot\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gnuplot`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGo() = runBlocking {
        // Smoke test: load go and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"go\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_go`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGodotResource() = runBlocking {
        // Smoke test: load godot_resource and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"godot_resource\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_godot_resource`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGomod() = runBlocking {
        // Smoke test: load gomod and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gomod\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module example.com/hello\n\ngo 1.21", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gomod`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGosum() = runBlocking {
        // Smoke test: load gosum and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gosum\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("example.com/pkg v1.0.0 h1:abc=", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gosum`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGotmpl() = runBlocking {
        // Smoke test: load gotmpl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gotmpl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gotmpl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGowork() = runBlocking {
        // Smoke test: load gowork and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gowork\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gowork`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGpg() = runBlocking {
        // Smoke test: load gpg and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gpg\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gpg`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGraphql() = runBlocking {
        // Smoke test: load graphql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"graphql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("type Query { hello: String }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_graphql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGren() = runBlocking {
        // Smoke test: load gren and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gren\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Main exposing (..)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gren`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGroovy() = runBlocking {
        // Smoke test: load groovy and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"groovy\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def x = 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_groovy`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeGstlaunch() = runBlocking {
        // Smoke test: load gstlaunch and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"gstlaunch\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fakesrc ! fakesink", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_gstlaunch`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHack() = runBlocking {
        // Smoke test: load hack and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hack\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<?hh\nfunction main(): void {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hack`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHaml() = runBlocking {
        // Smoke test: load haml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"haml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("%p hello\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_haml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHare() = runBlocking {
        // Smoke test: load hare and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hare\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("export fn main() void = void;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hare`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHaskell() = runBlocking {
        // Smoke test: load haskell and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"haskell\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("main = putStrLn \"hello\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_haskell`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHaskellPersistent() = runBlocking {
        // Smoke test: load haskell_persistent and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"haskell_persistent\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Person\n  name String\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_haskell_persistent`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHaxe() = runBlocking {
        // Smoke test: load haxe and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"haxe\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Main { static function main() {} }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_haxe`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHcl() = runBlocking {
        // Smoke test: load hcl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hcl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("variable \"name\" { type = string }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hcl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHeex() = runBlocking {
        // Smoke test: load heex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"heex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<%= @greeting %>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_heex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHjson() = runBlocking {
        // Smoke test: load hjson and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hjson\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hjson`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHlsl() = runBlocking {
        // Smoke test: load hlsl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hlsl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("float4 main() : SV_Target { return 0; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hlsl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHocon() = runBlocking {
        // Smoke test: load hocon and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hocon\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hocon`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHoon() = runBlocking {
        // Smoke test: load hoon and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hoon\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hoon`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHtml() = runBlocking {
        // Smoke test: load html and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"html\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<p>hello</p>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_html`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHtmldjango() = runBlocking {
        // Smoke test: load htmldjango and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"htmldjango\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_htmldjango`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHttp() = runBlocking {
        // Smoke test: load http and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"http\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_http`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHurl() = runBlocking {
        // Smoke test: load hurl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hurl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hurl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeHyprlang() = runBlocking {
        // Smoke test: load hyprlang and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"hyprlang\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("general { border_size = 1 }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_hyprlang`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeIdl() = runBlocking {
        // Smoke test: load idl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"idl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module M {\n};\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_idl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeIdris() = runBlocking {
        // Smoke test: load idris and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"idris\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Main", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_idris`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeIni() = runBlocking {
        // Smoke test: load ini and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ini\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("[section]\nkey = value", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ini`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeIspc() = runBlocking {
        // Smoke test: load ispc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ispc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("export void main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ispc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJai() = runBlocking {
        // Smoke test: load jai and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jai\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jai`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJanet() = runBlocking {
        // Smoke test: load janet and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"janet\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(print \"hello\")", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_janet`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJava() = runBlocking {
        // Smoke test: load java and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"java\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Main {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_java`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJavadoc() = runBlocking {
        // Smoke test: load javadoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"javadoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_javadoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJavascript() = runBlocking {
        // Smoke test: load javascript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"javascript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("console.log('hello');", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_javascript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJinja2() = runBlocking {
        // Smoke test: load jinja2 and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jinja2\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{{ variable }}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jinja2`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJjdescription() = runBlocking {
        // Smoke test: load jjdescription and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jjdescription\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("commit message\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jjdescription`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJq() = runBlocking {
        // Smoke test: load jq and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jq\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process(".[] | select(.key)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jq`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJsdoc() = runBlocking {
        // Smoke test: load jsdoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jsdoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("/** @param {string} name */", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jsdoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJson() = runBlocking {
        // Smoke test: load json and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"json\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{\"key\": \"value\"}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_json`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJson5() = runBlocking {
        // Smoke test: load json5 and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"json5\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_json5`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJsonnet() = runBlocking {
        // Smoke test: load jsonnet and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"jsonnet\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{ key: 'value' }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_jsonnet`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJulia() = runBlocking {
        // Smoke test: load julia and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"julia\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("function main() end", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_julia`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeJust() = runBlocking {
        // Smoke test: load just and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"just\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_just`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKcl() = runBlocking {
        // Smoke test: load kcl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kcl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kcl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKconfig() = runBlocking {
        // Smoke test: load kconfig and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kconfig\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("config FOO\n\tbool \"Enable foo\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kconfig`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKdl() = runBlocking {
        // Smoke test: load kdl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kdl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("node \"value\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kdl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKitty() = runBlocking {
        // Smoke test: load kitty and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kitty\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("font_size 12\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kitty`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKoka() = runBlocking {
        // Smoke test: load koka and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"koka\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fun main()\n  1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_koka`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKotlin() = runBlocking {
        // Smoke test: load kotlin and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kotlin\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fun main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kotlin`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKoto() = runBlocking {
        // Smoke test: load koto and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"koto\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x = 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_koto`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeKusto() = runBlocking {
        // Smoke test: load kusto and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"kusto\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("T | count\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_kusto`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLalrpop() = runBlocking {
        // Smoke test: load lalrpop and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"lalrpop\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("grammar;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_lalrpop`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLatex() = runBlocking {
        // Smoke test: load latex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"latex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_latex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLean() = runBlocking {
        // Smoke test: load lean and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"lean\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def main : IO Unit := pure ()", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_lean`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLedger() = runBlocking {
        // Smoke test: load ledger and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ledger\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ledger`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLeo() = runBlocking {
        // Smoke test: load leo and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"leo\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("program test.aleo {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_leo`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLess() = runBlocking {
        // Smoke test: load less and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"less\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_less`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLinkerscript() = runBlocking {
        // Smoke test: load linkerscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"linkerscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SECTIONS { .text : { *(.text) } }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_linkerscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLiquid() = runBlocking {
        // Smoke test: load liquid and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"liquid\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_liquid`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLlvm() = runBlocking {
        // Smoke test: load llvm and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"llvm\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("define i32 @main() { ret i32 0 }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_llvm`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLlvmMir() = runBlocking {
        // Smoke test: load llvm_mir and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"llvm_mir\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("---\nname: foo\n...\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_llvm_mir`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLua() = runBlocking {
        // Smoke test: load lua and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"lua\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("print('hello')", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_lua`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLuadoc() = runBlocking {
        // Smoke test: load luadoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"luadoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("---@param name string", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_luadoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLuap() = runBlocking {
        // Smoke test: load luap and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"luap\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("[a-z]+", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_luap`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeLuau() = runBlocking {
        // Smoke test: load luau and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"luau\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("local x: number = 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_luau`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeM68k() = runBlocking {
        // Smoke test: load m68k and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"m68k\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process(" move.l d0,d1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_m68k`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMagik() = runBlocking {
        // Smoke test: load magik and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"magik\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("_method object.hello\n_endmethod", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_magik`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMake() = runBlocking {
        // Smoke test: load make and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"make\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("all:\n\techo hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_make`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMarkdown() = runBlocking {
        // Smoke test: load markdown and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"markdown\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("# Hello\n\nWorld", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_markdown`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMarkdownInline() = runBlocking {
        // Smoke test: load markdown_inline and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"markdown_inline\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("**bold** and *italic*", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_markdown_inline`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMatlab() = runBlocking {
        // Smoke test: load matlab and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"matlab\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("function y = hello(x)\ny = x;\nend", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_matlab`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMenhir() = runBlocking {
        // Smoke test: load menhir and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"menhir\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("%token EOF\n%%\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_menhir`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMermaid() = runBlocking {
        // Smoke test: load mermaid and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"mermaid\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("graph TD\nA --> B", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_mermaid`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMeson() = runBlocking {
        // Smoke test: load meson and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"meson\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("project('hello', 'c')", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_meson`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMlir() = runBlocking {
        // Smoke test: load mlir and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"mlir\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_mlir`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMojo() = runBlocking {
        // Smoke test: load mojo and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"mojo\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_mojo`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMoonbit() = runBlocking {
        // Smoke test: load moonbit and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"moonbit\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_moonbit`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMotoko() = runBlocking {
        // Smoke test: load motoko and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"motoko\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("actor {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_motoko`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeMove() = runBlocking {
        // Smoke test: load move and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"move\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_move`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNasm() = runBlocking {
        // Smoke test: load nasm and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nasm\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nasm`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNetlinx() = runBlocking {
        // Smoke test: load netlinx and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"netlinx\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("PROGRAM_NAME='hello'", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_netlinx`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNginx() = runBlocking {
        // Smoke test: load nginx and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nginx\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nginx`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNickel() = runBlocking {
        // Smoke test: load nickel and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nickel\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nickel`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNim() = runBlocking {
        // Smoke test: load nim and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nim\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("echo \"hello\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nim`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNinja() = runBlocking {
        // Smoke test: load ninja and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ninja\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("rule cc\n  command = cc \$in -o \$out", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ninja`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNix() = runBlocking {
        // Smoke test: load nix and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nix\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{ pkgs ? import <nixpkgs> {} }: pkgs.hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nix`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNorg() = runBlocking {
        // Smoke test: load norg and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"norg\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_norg`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNorgMeta() = runBlocking {
        // Smoke test: load norg_meta and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"norg_meta\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_norg_meta`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNqc() = runBlocking {
        // Smoke test: load nqc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nqc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("task main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nqc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeNushell() = runBlocking {
        // Smoke test: load nushell and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"nushell\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_nushell`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeObjc() = runBlocking {
        // Smoke test: load objc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"objc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@interface Main @end", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_objc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOcaml() = runBlocking {
        // Smoke test: load ocaml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ocaml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("let () = print_endline \"hello\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ocaml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOcamlInterface() = runBlocking {
        // Smoke test: load ocaml_interface and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ocaml_interface\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("val x : int", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ocaml_interface`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOcamllex() = runBlocking {
        // Smoke test: load ocamllex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ocamllex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ocamllex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOdin() = runBlocking {
        // Smoke test: load odin and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"odin\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_odin`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOpenscad() = runBlocking {
        // Smoke test: load openscad and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"openscad\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_openscad`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeOrg() = runBlocking {
        // Smoke test: load org and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"org\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("* Hello\nWorld", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_org`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePascal() = runBlocking {
        // Smoke test: load pascal and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pascal\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("program Hello; begin end.", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pascal`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePem() = runBlocking {
        // Smoke test: load pem and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pem\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pem`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePenrose() = runBlocking {
        // Smoke test: load penrose and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"penrose\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("type Set\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_penrose`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePerl() = runBlocking {
        // Smoke test: load perl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"perl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("print 'hello';", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_perl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePgn() = runBlocking {
        // Smoke test: load pgn and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pgn\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("1. e4 e5 *", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pgn`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePhp() = runBlocking {
        // Smoke test: load php and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"php\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<?php echo 'hello'; ?>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_php`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePhpdoc() = runBlocking {
        // Smoke test: load phpdoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"phpdoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_phpdoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePicat() = runBlocking {
        // Smoke test: load picat and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"picat\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("main => true.\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_picat`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePkl() = runBlocking {
        // Smoke test: load pkl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pkl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("name = \"hello\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pkl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePlantuml() = runBlocking {
        // Smoke test: load plantuml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"plantuml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@startuml\n@enduml\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_plantuml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePo() = runBlocking {
        // Smoke test: load po and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"po\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("msgid \"hello\"\nmsgstr \"world\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_po`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePoeFilter() = runBlocking {
        // Smoke test: load poe_filter and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"poe_filter\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_poe_filter`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePony() = runBlocking {
        // Smoke test: load pony and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pony\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("actor Main\n  new create(env: Env) => None", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pony`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePostgres() = runBlocking {
        // Smoke test: load postgres and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"postgres\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SELECT 1;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_postgres`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePostscript() = runBlocking {
        // Smoke test: load postscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"postscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("/hello { (Hello) show } def", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_postscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePowershell() = runBlocking {
        // Smoke test: load powershell and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"powershell\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Write-Host 'hello'", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_powershell`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePrintf() = runBlocking {
        // Smoke test: load printf and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"printf\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("%d %s", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_printf`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePrisma() = runBlocking {
        // Smoke test: load prisma and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"prisma\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("model User { id Int @id }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_prisma`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeProlog() = runBlocking {
        // Smoke test: load prolog and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"prolog\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("hello :- write('hello'), nl.", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_prolog`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePromela() = runBlocking {
        // Smoke test: load promela and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"promela\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("init {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_promela`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePromql() = runBlocking {
        // Smoke test: load promql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"promql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_promql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeProperties() = runBlocking {
        // Smoke test: load properties and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"properties\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("key=value", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_properties`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeProto() = runBlocking {
        // Smoke test: load proto and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"proto\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("syntax = \"proto3\";", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_proto`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePrql() = runBlocking {
        // Smoke test: load prql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"prql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_prql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePsv() = runBlocking {
        // Smoke test: load psv and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"psv\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("a|b|c\n1|2|3", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_psv`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePug() = runBlocking {
        // Smoke test: load pug and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pug\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pug`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePuppet() = runBlocking {
        // Smoke test: load puppet and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"puppet\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("notify { 'hello': }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_puppet`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePurescript() = runBlocking {
        // Smoke test: load purescript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"purescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Main where", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_purescript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePymanifest() = runBlocking {
        // Smoke test: load pymanifest and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"pymanifest\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("include *.txt", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_pymanifest`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokePython() = runBlocking {
        // Smoke test: load python and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"python\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("print('hello')", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_python`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeQl() = runBlocking {
        // Smoke test: load ql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeQmldir() = runBlocking {
        // Smoke test: load qmldir and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"qmldir\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module Example", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_qmldir`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeQmljs() = runBlocking {
        // Smoke test: load qmljs and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"qmljs\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("import QtQuick 2.0\nItem {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_qmljs`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeQuery() = runBlocking {
        // Smoke test: load query and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"query\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(identifier) @name", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_query`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeR() = runBlocking {
        // Smoke test: load r and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"r\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("print('hello')", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_r`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRacket() = runBlocking {
        // Smoke test: load racket and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"racket\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("#lang racket\n(define x 1)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_racket`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRasi() = runBlocking {
        // Smoke test: load rasi and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rasi\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rasi`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRazor() = runBlocking {
        // Smoke test: load razor and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"razor\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_razor`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRbs() = runBlocking {
        // Smoke test: load rbs and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rbs\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rbs`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRe2c() = runBlocking {
        // Smoke test: load re2c and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"re2c\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("/*!re2c\n  [a-z]+ { return; }\n*/", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_re2c`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeReadline() = runBlocking {
        // Smoke test: load readline and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"readline\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("set editing-mode vi", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_readline`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeReason() = runBlocking {
        // Smoke test: load reason and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"reason\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("let x = 1;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_reason`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRegex() = runBlocking {
        // Smoke test: load regex and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"regex\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_regex`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRego() = runBlocking {
        // Smoke test: load rego and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rego\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package main\ndefault allow = false", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rego`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRequirements() = runBlocking {
        // Smoke test: load requirements and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"requirements\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("flask>=2.0", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_requirements`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRescript() = runBlocking {
        // Smoke test: load rescript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("let x = 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rescript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRobot() = runBlocking {
        // Smoke test: load robot and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"robot\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_robot`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRoc() = runBlocking {
        // Smoke test: load roc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"roc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_roc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRon() = runBlocking {
        // Smoke test: load ron and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ron\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(key: \"value\")", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ron`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRshtml() = runBlocking {
        // Smoke test: load rshtml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rshtml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<p>hi</p>\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rshtml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRst() = runBlocking {
        // Smoke test: load rst and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rst\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Hello\n=====\n\nWorld", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rst`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRtf() = runBlocking {
        // Smoke test: load rtf and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rtf\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{\\rtf1 hello}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rtf`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRuby() = runBlocking {
        // Smoke test: load ruby and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ruby\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("puts 'hello'", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ruby`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeRust() = runBlocking {
        // Smoke test: load rust and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"rust\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_rust`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSas() = runBlocking {
        // Smoke test: load sas and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sas\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("data _null_;\nrun;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sas`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeScala() = runBlocking {
        // Smoke test: load scala and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"scala\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("object Main", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_scala`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeScfg() = runBlocking {
        // Smoke test: load scfg and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"scfg\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("key value\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_scfg`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeScheme() = runBlocking {
        // Smoke test: load scheme and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"scheme\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(define x 1)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_scheme`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeScss() = runBlocking {
        // Smoke test: load scss and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"scss\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("\$color: red;\nbody { color: \$color; }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_scss`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSflog() = runBlocking {
        // Smoke test: load sflog and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sflog\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("37.0 APEX_CODE,DEBUG\n16:06:58.18 (1)|EXECUTION_STARTED\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sflog`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSlang() = runBlocking {
        // Smoke test: load slang and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"slang\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_slang`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSlim() = runBlocking {
        // Smoke test: load slim and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"slim\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("p hello\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_slim`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSlint() = runBlocking {
        // Smoke test: load slint and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"slint\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("export component Foo {}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_slint`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSmali() = runBlocking {
        // Smoke test: load smali and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"smali\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process(".class public LMain;\n.super Ljava/lang/Object;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_smali`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSmalltalk() = runBlocking {
        // Smoke test: load smalltalk and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"smalltalk\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_smalltalk`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSmithy() = runBlocking {
        // Smoke test: load smithy and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"smithy\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("namespace example\nstring MyString", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_smithy`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSml() = runBlocking {
        // Smoke test: load sml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSnakemake() = runBlocking {
        // Smoke test: load snakemake and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"snakemake\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_snakemake`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSnl() = runBlocking {
        // Smoke test: load snl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"snl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("program test\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_snl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSolidity() = runBlocking {
        // Smoke test: load solidity and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"solidity\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("pragma solidity ^0.8.0;\ncontract Main {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_solidity`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSoql() = runBlocking {
        // Smoke test: load soql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"soql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SELECT Id FROM Account\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_soql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSosl() = runBlocking {
        // Smoke test: load sosl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sosl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("FIND {test}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sosl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSouffle() = runBlocking {
        // Smoke test: load souffle and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"souffle\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_souffle`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSourcepawn() = runBlocking {
        // Smoke test: load sourcepawn and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sourcepawn\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sourcepawn`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSparql() = runBlocking {
        // Smoke test: load sparql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sparql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SELECT ?s WHERE { ?s ?p ?o }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sparql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSpicedb() = runBlocking {
        // Smoke test: load spicedb and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"spicedb\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("definition user {}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_spicedb`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSql() = runBlocking {
        // Smoke test: load sql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SELECT 1;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSqlBigquery() = runBlocking {
        // Smoke test: load sql_bigquery and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sql_bigquery\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sql_bigquery`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSquirrel() = runBlocking {
        // Smoke test: load squirrel and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"squirrel\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("function main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_squirrel`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSshConfig() = runBlocking {
        // Smoke test: load ssh_config and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ssh_config\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Host example\n  HostName example.com", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ssh_config`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeStan() = runBlocking {
        // Smoke test: load stan and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"stan\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_stan`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeStarlark() = runBlocking {
        // Smoke test: load starlark and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"starlark\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def hello(): pass", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_starlark`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeStrace() = runBlocking {
        // Smoke test: load strace and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"strace\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("open(\"/x\", O_RDONLY) = 3\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_strace`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeStyled() = runBlocking {
        // Smoke test: load styled and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"styled\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("color: red;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_styled`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSuperhtml() = runBlocking {
        // Smoke test: load superhtml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"superhtml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_superhtml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSvelte() = runBlocking {
        // Smoke test: load svelte and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"svelte\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<script>let x = 1;</script>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_svelte`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSway() = runBlocking {
        // Smoke test: load sway and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sway\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sway`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSwift() = runBlocking {
        // Smoke test: load swift and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"swift\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("print(\"hello\")", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_swift`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSxhkdrc() = runBlocking {
        // Smoke test: load sxhkdrc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sxhkdrc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("super + a\n\techo hi\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sxhkdrc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSysml() = runBlocking {
        // Smoke test: load sysml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"sysml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package P {}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_sysml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSystemtap() = runBlocking {
        // Smoke test: load systemtap and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"systemtap\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("probe begin {}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_systemtap`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeSystemverilog() = runBlocking {
        // Smoke test: load systemverilog and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"systemverilog\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_systemverilog`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeT32() = runBlocking {
        // Smoke test: load t32 and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"t32\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("PRINT 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_t32`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTablegen() = runBlocking {
        // Smoke test: load tablegen and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tablegen\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("def Hello : Base {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tablegen`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTact() = runBlocking {
        // Smoke test: load tact and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tact\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tact`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTask() = runBlocking {
        // Smoke test: load task and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"task\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("todo item\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_task`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTcl() = runBlocking {
        // Smoke test: load tcl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tcl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("puts hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tcl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTeal() = runBlocking {
        // Smoke test: load teal and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"teal\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_teal`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTempl() = runBlocking {
        // Smoke test: load templ and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"templ\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_templ`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTera() = runBlocking {
        // Smoke test: load tera and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tera\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tera`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTerraform() = runBlocking {
        // Smoke test: load terraform and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"terraform\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("resource \"null_resource\" \"main\" {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_terraform`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTest() = runBlocking {
        // Smoke test: load test and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"test\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("===========\nTest\n===========\n---\n(node)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_test`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTextproto() = runBlocking {
        // Smoke test: load textproto and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"textproto\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("key: \"value\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_textproto`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeThrift() = runBlocking {
        // Smoke test: load thrift and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"thrift\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("service HelloService {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_thrift`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTlaplus() = runBlocking {
        // Smoke test: load tlaplus and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tlaplus\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("---- MODULE Main ----\n====", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tlaplus`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTmux() = runBlocking {
        // Smoke test: load tmux and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tmux\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tmux`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTodotxt() = runBlocking {
        // Smoke test: load todotxt and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"todotxt\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_todotxt`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeToml() = runBlocking {
        // Smoke test: load toml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"toml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("key = \"value\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_toml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTsql() = runBlocking {
        // Smoke test: load tsql and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tsql\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("SELECT 1;\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tsql`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTsv() = runBlocking {
        // Smoke test: load tsv and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tsv\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("a\tb\tc\n1\t2\t3", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tsv`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTsx() = runBlocking {
        // Smoke test: load tsx and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"tsx\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("const App = () => <div />;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_tsx`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTurtle() = runBlocking {
        // Smoke test: load turtle and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"turtle\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_turtle`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTwig() = runBlocking {
        // Smoke test: load twig and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"twig\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("{{ variable }}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_twig`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTypescript() = runBlocking {
        // Smoke test: load typescript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"typescript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("const x: number = 42;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_typescript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTypespec() = runBlocking {
        // Smoke test: load typespec and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"typespec\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_typespec`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTyposcript() = runBlocking {
        // Smoke test: load typoscript and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"typoscript\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_typoscript`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeTypst() = runBlocking {
        // Smoke test: load typst and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"typst\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("#let x = 1", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_typst`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeUdev() = runBlocking {
        // Smoke test: load udev and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"udev\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("ACTION==\"add\", KERNEL==\"sd*\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_udev`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeUngrammar() = runBlocking {
        // Smoke test: load ungrammar and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ungrammar\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Root = Item*\nItem = 'token'", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ungrammar`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeUnison() = runBlocking {
        // Smoke test: load unison and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"unison\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x = 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_unison`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeUxntal() = runBlocking {
        // Smoke test: load uxntal and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"uxntal\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("|0100 LIT 01", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_uxntal`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeV() = runBlocking {
        // Smoke test: load v and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"v\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("fn main() {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_v`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVala() = runBlocking {
        // Smoke test: load vala and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vala\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("class Foo {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vala`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVb() = runBlocking {
        // Smoke test: load vb and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vb\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("Module Main\nEnd Module", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vb`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVento() = runBlocking {
        // Smoke test: load vento and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vento\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("hello\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vento`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVerilog() = runBlocking {
        // Smoke test: load verilog and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"verilog\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module main; endmodule", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_verilog`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVhdl() = runBlocking {
        // Smoke test: load vhdl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vhdl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("entity main is end main;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vhdl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVhs() = runBlocking {
        // Smoke test: load vhs and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vhs\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vhs`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVim() = runBlocking {
        // Smoke test: load vim and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vim\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("echo 'hello'", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vim`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVimdoc() = runBlocking {
        // Smoke test: load vimdoc and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vimdoc\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vimdoc`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVrl() = runBlocking {
        // Smoke test: load vrl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vrl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vrl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeVue() = runBlocking {
        // Smoke test: load vue and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"vue\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<template><div>hello</div></template>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_vue`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWast() = runBlocking {
        // Smoke test: load wast and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wast\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(module)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wast`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWat() = runBlocking {
        // Smoke test: load wat and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wat\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(module)", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wat`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWdl() = runBlocking {
        // Smoke test: load wdl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wdl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("version 1.0\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wdl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWgsl() = runBlocking {
        // Smoke test: load wgsl and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wgsl\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wgsl`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWgslBevy() = runBlocking {
        // Smoke test: load wgsl_bevy and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wgsl_bevy\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wgsl_bevy`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWit() = runBlocking {
        // Smoke test: load wit and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wit\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("package example:pkg;", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wit`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeWolfram() = runBlocking {
        // Smoke test: load wolfram and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"wolfram\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x = 1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_wolfram`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeX86asm() = runBlocking {
        // Smoke test: load x86asm and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"x86asm\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_x86asm`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeXcompose() = runBlocking {
        // Smoke test: load xcompose and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"xcompose\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<Multi_key> <a> : \"a\"", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_xcompose`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeXit() = runBlocking {
        // Smoke test: load xit and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"xit\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("[ ] todo\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_xit`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeXml() = runBlocking {
        // Smoke test: load xml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"xml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("<?xml version=\"1.0\"?>\n<root>hello</root>", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_xml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeXquery() = runBlocking {
        // Smoke test: load xquery and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"xquery\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("1\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_xquery`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeXresources() = runBlocking {
        // Smoke test: load xresources and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"xresources\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("*.foreground: #ffffff\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_xresources`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeYaml() = runBlocking {
        // Smoke test: load yaml and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"yaml\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("key: value", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_yaml`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeYang() = runBlocking {
        // Smoke test: load yang and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"yang\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("module m {\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_yang`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeYuck() = runBlocking {
        // Smoke test: load yuck and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"yuck\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("(defwidget main [] (label :text \"hi\"))", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_yuck`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeYul() = runBlocking {
        // Smoke test: load yul and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"yul\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("object \"C\" {\n  code {\n  }\n}\n", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_yul`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeZig() = runBlocking {
        // Smoke test: load zig and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"zig\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("pub fn main() void {}", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_zig`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeZiggy() = runBlocking {
        // Smoke test: load ziggy and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ziggy\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ziggy`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeZiggySchema() = runBlocking {
        // Smoke test: load ziggy_schema and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"ziggy_schema\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("x", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_ziggy_schema`: its declared assertions produced no check and no skip marker")
    }

    @Test
    fun testSmokeZsh() = runBlocking {
        // Smoke test: load zsh and parse a simple snippet
        val config = MAPPER.readValue("{\"language\":\"zsh\"}", ProcessConfig::class.java)
        val result = TreeSitterLanguagePack.process("echo hello", config)
        org.junit.jupiter.api.Assumptions.assumeTrue(false, "alef rendered no runnable expectation for fixture `smoke_zsh`: its declared assertions produced no check and no skip marker")
    }

}
