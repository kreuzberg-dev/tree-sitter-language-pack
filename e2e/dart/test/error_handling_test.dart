// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:71b5b8a1158709e3d3e705cc2de2c61850c1f4e26b215fd99d50f198241a9f9a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import 'package:test/test.dart';
import 'package:tree_sitter_language_pack/tree_sitter_language_pack.dart';

// E2e tests for category: error-handling
void main() {
  test('Detect language from empty content returns null', () {
    final result = detectLanguageFromContent('');
    expect(result, isEmpty);
  });

  test('Detect language from empty extension returns null', () {
    final result = detectLanguageFromExtension('');
    expect(result, isEmpty);
  });

  test('Detect language from empty path returns null', () {
    final result = detectLanguageFromPath('');
    expect(result, isEmpty);
  });

  test('Parsing with empty language name should error', () {
    expect(() => parseString('', 'hello'), throwsA(isA<Exception>()));
  });

  test('Extract patterns with unknown language returns error', () {
    expect(() => extractPatterns('x = 1', '{"language":"nonexistent_language_xyz","patterns":{}}'), throwsA(isA<Exception>()));
  });

  test('Parsing an empty string should still produce a tree.', () {
    final tree = parseString('javascript', '');
  });

  test('Parsing invalid syntax should produce a tree with error nodes.', () {
    final tree = parseString('javascript', 'function function function @@@ %%%');
    // method_result assertions not yet implemented for Dart
  });

  test('Loading a nonexistent language should produce an error.', () {
    expect(() => parseString('nonexistent_xyz', ''), throwsA(isA<Exception>()));
  });

  test('Process empty source code succeeds with zero metrics', () {
    final result = process('', '{"language":"python"}');
    expect(result.metrics.total_lines, equals(0));
  });

  test('Run query with invalid S-expression syntax produces error', () {
    final tree = parseString('python', 'x = 1');
    // method_result assertions not yet implemented for Dart
  });

  test('parse_string() returns error with empty language name', () {
    expect(() => parseString('', 'x = 1'), throwsA(isA<Exception>()));
  });

  test('process() returns error when config language is unknown', () {
    expect(() => process('x = 1', '{"language":"nonexistent_xyz"}'), throwsA(isA<Exception>()));
  });

}
