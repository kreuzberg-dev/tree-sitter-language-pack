// Code generated by e2e-generator. DO NOT EDIT.

package io.github.treesitter.languagepack.e2e;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

class LanguageDetectionTest {

  @Test
  void detect_content_bash_shebang() {
    // detect_language_from_content recognizes #!/bin/bash shebang
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromContent("#!/bin/bash\necho hi");
      assertEquals("bash", detectResult, "Expected language 'bash' detected from content");
    }
  }

  @Test
  void detect_content_no_shebang() {
    // detect_language_from_content returns None when no shebang present
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromContent("no shebang here");
      assertNull(detectResult, "Expected no language detected from content");
    }
  }

  @Test
  void detect_content_python_shebang() {
    // detect_language_from_content recognizes #!/usr/bin/env python3 shebang
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromContent("#!/usr/bin/env python3\npass");
      assertEquals("python", detectResult, "Expected language 'python' detected from content");
    }
  }

  @Test
  void detect_ext_cpp() {
    // detect_language_from_extension recognizes .cpp as cpp
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("cpp");
      assertEquals("cpp", detectResult, "Expected language 'cpp' detected from extension");
    }
  }

  @Test
  void detect_ext_go() {
    // detect_language_from_extension recognizes .go as go
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("go");
      assertEquals("go", detectResult, "Expected language 'go' detected from extension");
    }
  }

  @Test
  void detect_ext_java() {
    // detect_language_from_extension recognizes .java as java
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("java");
      assertEquals("java", detectResult, "Expected language 'java' detected from extension");
    }
  }

  @Test
  void detect_ext_javascript() {
    // detect_language_from_extension recognizes .js as javascript
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("js");
      assertEquals(
          "javascript", detectResult, "Expected language 'javascript' detected from extension");
    }
  }

  @Test
  void detect_ext_php() {
    // detect_language_from_extension recognizes .php as php
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("php");
      assertEquals("php", detectResult, "Expected language 'php' detected from extension");
    }
  }

  @Test
  void detect_ext_python() {
    // detect_language_from_extension recognizes .py as python
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("py");
      assertEquals("python", detectResult, "Expected language 'python' detected from extension");
    }
  }

  @Test
  void detect_ext_ruby() {
    // detect_language_from_extension recognizes .rb as ruby
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("rb");
      assertEquals("ruby", detectResult, "Expected language 'ruby' detected from extension");
    }
  }

  @Test
  void detect_ext_rust() {
    // detect_language_from_extension recognizes .rs as rust
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("rs");
      assertEquals("rust", detectResult, "Expected language 'rust' detected from extension");
    }
  }

  @Test
  void detect_ext_typescript() {
    // detect_language_from_extension recognizes .ts as typescript
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("ts");
      assertEquals(
          "typescript", detectResult, "Expected language 'typescript' detected from extension");
    }
  }

  @Test
  void detect_ext_unknown() {
    // detect_language_from_extension returns None for unknown extension .xyz
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromExtension("xyz");
      assertNull(detectResult, "Expected no language detected for extension");
    }
  }

  @Test
  void detect_path_dotfile() {
    // detect_language_from_path returns None for dotfile .gitignore (no standard extension)
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath(".gitignore");
      assertNull(detectResult, "Expected no language detected for path");
    }
  }

  @Test
  void detect_path_go_nested() {
    // detect_language_from_path extracts extension from nested path lib/server.go
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("lib/server.go");
      assertEquals("go", detectResult, "Expected language 'go' detected from path");
    }
  }

  @Test
  void detect_path_java_root() {
    // detect_language_from_path recognizes Main.java in root directory
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("Main.java");
      assertEquals("java", detectResult, "Expected language 'java' detected from path");
    }
  }

  @Test
  void detect_path_js_root() {
    // detect_language_from_path recognizes app.js in root directory
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("app.js");
      assertEquals("javascript", detectResult, "Expected language 'javascript' detected from path");
    }
  }

  @Test
  void detect_path_nested() {
    // detect_language_from_path extracts extension from nested path src/main.py
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("src/main.py");
      assertEquals("python", detectResult, "Expected language 'python' detected from path");
    }
  }

  @Test
  void detect_path_no_extension() {
    // detect_language_from_path returns None for extensionless file Makefile
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("Makefile");
      assertNull(detectResult, "Expected no language detected for path");
    }
  }

  @Test
  void detect_path_rust_src() {
    // detect_language_from_path extracts extension from nested path src/main.rs
    try (var registry = Helpers.createRegistry()) {
      var detectResult = registry.detectLanguageFromPath("src/main.rs");
      assertEquals("rust", detectResult, "Expected language 'rust' detected from path");
    }
  }
}
