// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9cb84fcaf9ff41f6c650f49b4e175bdb20001f9eb079dd7f27702e97a002ba24
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// Issues & docs: https://github.com/kreuzberg-dev/alef
import gleeunit
import gleeunit/should
import tree_sitter_language_pack

import gleam/int
import gleam/list
import gleam/string

// JavaScript with multiple exports, verify export count
pub fn process_javascript_exports_count_test() {
  let result = tree_sitter_language_pack.process("export function greet() { return 'hi'; }\nexport const VERSION = '1.0';\nexport default class App {}\n", "{\"language\":\"javascript\"}")
  result |> should.be_ok()
  result.language |> should.equal("javascript")
  result.exports |> list.length |> int.is_at_least(2) |> should.equal(True)
}

// JavaScript with exports, verify export count
pub fn process_javascript_exports_detail_test() {
  let result = tree_sitter_language_pack.process("export function greet(name) {\n  return `Hello ${name}`;\n}\n\nexport const VERSION = '1.0';\n", "{\"language\":\"javascript\"}")
  result |> should.be_ok()
  result.language |> should.equal("javascript")
  result.exports |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Python comprehensive source with all feature extraction enabled
pub fn process_python_all_features_test() {
  let result = tree_sitter_language_pack.process("import os\nfrom pathlib import Path\n\n# Configuration\nMY_CONST = 42\n\ndef process_file(path):\n    \"\"\"Process a file and return contents.\"\"\"\n    with open(path) as f:\n        return f.read()\n\nclass FileProcessor:\n    def __init__(self, base_dir):\n        self.base_dir = base_dir\n", "{\"comments\":true,\"docstrings\":true,\"imports\":true,\"language\":\"python\",\"structure\":true,\"symbols\":true}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.structure |> list.length |> int.is_at_least(2) |> should.equal(True)
  result.imports |> list.length |> int.is_at_least(1) |> should.equal(True)
  result.comments |> list.length |> int.is_at_least(1) |> should.equal(True)
  result.metrics.total_lines |> int.is_at_least(10) |> should.equal(True)
}

// Python with comments, verify comment count
pub fn process_python_comments_test() {
  let result = tree_sitter_language_pack.process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", "{\"comments\":true,\"language\":\"python\"}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.comments |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Python with function docstring, verify docstring count
pub fn process_python_docstrings_test() {
  let result = tree_sitter_language_pack.process("def greet(name):\n    \"\"\"Say hello to someone.\"\"\"\n    return f\"Hello {name}\"\n", "{\"docstrings\":true,\"language\":\"python\"}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.metrics.total_lines |> int.is_at_least(3) |> should.equal(True)
}

// Python with multiple imports, verify imports contain specific source
pub fn process_python_imports_detail_test() {
  let result = tree_sitter_language_pack.process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", "{\"language\":\"python\"}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.imports |> list.length |> int.is_at_least(2) |> should.equal(True)
  result.import_sources |> string.contains("os") |> should.equal(True)
}

// Python code with metrics assertions
pub fn process_python_metrics_detail_test() {
  let result = tree_sitter_language_pack.process("# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n", "{\"language\":\"python\"}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.metrics.code_lines |> int.is_at_least(4) |> should.equal(True)
  result.metrics.comment_lines |> int.is_at_least(1) |> should.equal(True)
  result.metrics.max_depth |> int.is_at_least(1) |> should.equal(True)
}

// Python with class and functions, verify symbol count
pub fn process_python_symbols_test() {
  let result = tree_sitter_language_pack.process("MY_CONST = 42\ndef helper(): pass\nclass Widget: pass\n", "{\"language\":\"python\",\"symbols\":true}")
  result |> should.be_ok()
  result.language |> should.equal("python")
  result.symbols |> list.length |> int.is_at_least(1) |> should.equal(True)
}

// Rust struct with name, verify structure name contains value
pub fn process_rust_structure_name_test() {
  let result = tree_sitter_language_pack.process("pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n", "{\"language\":\"rust\"}")
  result |> should.be_ok()
  result.language |> should.equal("rust")
  result.structure |> list.length |> int.is_at_least(1) |> should.equal(True)
  result.structure_names |> string.contains("MyConfig") |> should.equal(True)
}

