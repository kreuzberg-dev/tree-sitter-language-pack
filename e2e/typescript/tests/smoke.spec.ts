// Code generated by e2e-generator. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import {
	availableLanguages,
	hasLanguage,
	getLanguagePtr,
	process,
	detectLanguageFromExtension,
	detectLanguageFromPath,
	detectLanguageFromContent,
	extensionAmbiguity,
	getHighlightsQuery,
} from "./helpers";

describe("smoke", () => {
	it("smoke_abl", () => {
		// Smoke test: load abl and parse a simple snippet
		if (!hasLanguage("abl")) {
			console.log("Skipping: language 'abl' not available");
			return;
		}
		const ptr = getLanguagePtr("abl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_actionscript", () => {
		// Smoke test: load actionscript and parse a simple snippet
		if (!hasLanguage("actionscript")) {
			console.log("Skipping: language 'actionscript' not available");
			return;
		}
		const ptr = getLanguagePtr("actionscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ada", () => {
		// Smoke test: load ada and parse a simple snippet
		if (!hasLanguage("ada")) {
			console.log("Skipping: language 'ada' not available");
			return;
		}
		const ptr = getLanguagePtr("ada");
		expect(ptr).toBeTruthy();
	});

	it("smoke_agda", () => {
		// Smoke test: load agda and parse a simple snippet
		if (!hasLanguage("agda")) {
			console.log("Skipping: language 'agda' not available");
			return;
		}
		const ptr = getLanguagePtr("agda");
		expect(ptr).toBeTruthy();
	});

	it("smoke_al", () => {
		// Smoke test: load al and parse a simple snippet
		if (!hasLanguage("al")) {
			console.log("Skipping: language 'al' not available");
			return;
		}
		const ptr = getLanguagePtr("al");
		expect(ptr).toBeTruthy();
	});

	it("smoke_angular", () => {
		// Smoke test: load angular and parse a simple snippet
		if (!hasLanguage("angular")) {
			console.log("Skipping: language 'angular' not available");
			return;
		}
		const ptr = getLanguagePtr("angular");
		expect(ptr).toBeTruthy();
	});

	it("smoke_apex", () => {
		// Smoke test: load apex and parse a simple snippet
		if (!hasLanguage("apex")) {
			console.log("Skipping: language 'apex' not available");
			return;
		}
		const ptr = getLanguagePtr("apex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_arduino", () => {
		// Smoke test: load arduino and parse a simple snippet
		if (!hasLanguage("arduino")) {
			console.log("Skipping: language 'arduino' not available");
			return;
		}
		const ptr = getLanguagePtr("arduino");
		expect(ptr).toBeTruthy();
	});

	it("smoke_asciidoc", () => {
		// Smoke test: load asciidoc and parse a simple snippet
		if (!hasLanguage("asciidoc")) {
			console.log("Skipping: language 'asciidoc' not available");
			return;
		}
		const ptr = getLanguagePtr("asciidoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_asm", () => {
		// Smoke test: load asm and parse a simple snippet
		if (!hasLanguage("asm")) {
			console.log("Skipping: language 'asm' not available");
			return;
		}
		const ptr = getLanguagePtr("asm");
		expect(ptr).toBeTruthy();
	});

	it("smoke_astro", () => {
		// Smoke test: load astro and parse a simple snippet
		if (!hasLanguage("astro")) {
			console.log("Skipping: language 'astro' not available");
			return;
		}
		const ptr = getLanguagePtr("astro");
		expect(ptr).toBeTruthy();
	});

	it("smoke_awk", () => {
		// Smoke test: load awk and parse a simple snippet
		if (!hasLanguage("awk")) {
			console.log("Skipping: language 'awk' not available");
			return;
		}
		const ptr = getLanguagePtr("awk");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bash", () => {
		// Smoke test: load bash and parse a simple snippet
		if (!hasLanguage("bash")) {
			console.log("Skipping: language 'bash' not available");
			return;
		}
		const ptr = getLanguagePtr("bash");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bass", () => {
		// Smoke test: load bass and parse a simple snippet
		if (!hasLanguage("bass")) {
			console.log("Skipping: language 'bass' not available");
			return;
		}
		const ptr = getLanguagePtr("bass");
		expect(ptr).toBeTruthy();
	});

	it("smoke_batch", () => {
		// Smoke test: load batch and parse a simple snippet
		if (!hasLanguage("batch")) {
			console.log("Skipping: language 'batch' not available");
			return;
		}
		const ptr = getLanguagePtr("batch");
		expect(ptr).toBeTruthy();
	});

	it("smoke_beancount", () => {
		// Smoke test: load beancount and parse a simple snippet
		if (!hasLanguage("beancount")) {
			console.log("Skipping: language 'beancount' not available");
			return;
		}
		const ptr = getLanguagePtr("beancount");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bibtex", () => {
		// Smoke test: load bibtex and parse a simple snippet
		if (!hasLanguage("bibtex")) {
			console.log("Skipping: language 'bibtex' not available");
			return;
		}
		const ptr = getLanguagePtr("bibtex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bicep", () => {
		// Smoke test: load bicep and parse a simple snippet
		if (!hasLanguage("bicep")) {
			console.log("Skipping: language 'bicep' not available");
			return;
		}
		const ptr = getLanguagePtr("bicep");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bitbake", () => {
		// Smoke test: load bitbake and parse a simple snippet
		if (!hasLanguage("bitbake")) {
			console.log("Skipping: language 'bitbake' not available");
			return;
		}
		const ptr = getLanguagePtr("bitbake");
		expect(ptr).toBeTruthy();
	});

	it("smoke_blade", () => {
		// Smoke test: load blade and parse a simple snippet
		if (!hasLanguage("blade")) {
			console.log("Skipping: language 'blade' not available");
			return;
		}
		const ptr = getLanguagePtr("blade");
		expect(ptr).toBeTruthy();
	});

	it("smoke_brightscript", () => {
		// Smoke test: load brightscript and parse a simple snippet
		if (!hasLanguage("brightscript")) {
			console.log("Skipping: language 'brightscript' not available");
			return;
		}
		const ptr = getLanguagePtr("brightscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_bsl", () => {
		// Smoke test: load bsl and parse a simple snippet
		if (!hasLanguage("bsl")) {
			console.log("Skipping: language 'bsl' not available");
			return;
		}
		const ptr = getLanguagePtr("bsl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_c", () => {
		// Smoke test: load c and parse a simple snippet
		if (!hasLanguage("c")) {
			console.log("Skipping: language 'c' not available");
			return;
		}
		const ptr = getLanguagePtr("c");
		expect(ptr).toBeTruthy();
	});

	it("smoke_c3", () => {
		// Smoke test: load c3 and parse a simple snippet
		if (!hasLanguage("c3")) {
			console.log("Skipping: language 'c3' not available");
			return;
		}
		const ptr = getLanguagePtr("c3");
		expect(ptr).toBeTruthy();
	});

	it("smoke_caddy", () => {
		// Smoke test: load caddy and parse a simple snippet
		if (!hasLanguage("caddy")) {
			console.log("Skipping: language 'caddy' not available");
			return;
		}
		const ptr = getLanguagePtr("caddy");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cairo", () => {
		// Smoke test: load cairo and parse a simple snippet
		if (!hasLanguage("cairo")) {
			console.log("Skipping: language 'cairo' not available");
			return;
		}
		const ptr = getLanguagePtr("cairo");
		expect(ptr).toBeTruthy();
	});

	it("smoke_capnp", () => {
		// Smoke test: load capnp and parse a simple snippet
		if (!hasLanguage("capnp")) {
			console.log("Skipping: language 'capnp' not available");
			return;
		}
		const ptr = getLanguagePtr("capnp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cedar", () => {
		// Smoke test: load cedar and parse a simple snippet
		if (!hasLanguage("cedar")) {
			console.log("Skipping: language 'cedar' not available");
			return;
		}
		const ptr = getLanguagePtr("cedar");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cedarschema", () => {
		// Smoke test: load cedarschema and parse a simple snippet
		if (!hasLanguage("cedarschema")) {
			console.log("Skipping: language 'cedarschema' not available");
			return;
		}
		const ptr = getLanguagePtr("cedarschema");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cel", () => {
		// Smoke test: load cel and parse a simple snippet
		if (!hasLanguage("cel")) {
			console.log("Skipping: language 'cel' not available");
			return;
		}
		const ptr = getLanguagePtr("cel");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cfml", () => {
		// Smoke test: load cfml and parse a simple snippet
		if (!hasLanguage("cfml")) {
			console.log("Skipping: language 'cfml' not available");
			return;
		}
		const ptr = getLanguagePtr("cfml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_chatito", () => {
		// Smoke test: load chatito and parse a simple snippet
		if (!hasLanguage("chatito")) {
			console.log("Skipping: language 'chatito' not available");
			return;
		}
		const ptr = getLanguagePtr("chatito");
		expect(ptr).toBeTruthy();
	});

	it("smoke_chuck", () => {
		// Smoke test: load chuck and parse a simple snippet
		if (!hasLanguage("chuck")) {
			console.log("Skipping: language 'chuck' not available");
			return;
		}
		const ptr = getLanguagePtr("chuck");
		expect(ptr).toBeTruthy();
	});

	it("smoke_circom", () => {
		// Smoke test: load circom and parse a simple snippet
		if (!hasLanguage("circom")) {
			console.log("Skipping: language 'circom' not available");
			return;
		}
		const ptr = getLanguagePtr("circom");
		expect(ptr).toBeTruthy();
	});

	it("smoke_clarity", () => {
		// Smoke test: load clarity and parse a simple snippet
		if (!hasLanguage("clarity")) {
			console.log("Skipping: language 'clarity' not available");
			return;
		}
		const ptr = getLanguagePtr("clarity");
		expect(ptr).toBeTruthy();
	});

	it("smoke_clojure", () => {
		// Smoke test: load clojure and parse a simple snippet
		if (!hasLanguage("clojure")) {
			console.log("Skipping: language 'clojure' not available");
			return;
		}
		const ptr = getLanguagePtr("clojure");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cmake", () => {
		// Smoke test: load cmake and parse a simple snippet
		if (!hasLanguage("cmake")) {
			console.log("Skipping: language 'cmake' not available");
			return;
		}
		const ptr = getLanguagePtr("cmake");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cobol", () => {
		// Smoke test: load cobol and parse a simple snippet
		if (!hasLanguage("cobol")) {
			console.log("Skipping: language 'cobol' not available");
			return;
		}
		const ptr = getLanguagePtr("cobol");
		expect(ptr).toBeTruthy();
	});

	it("smoke_comment", () => {
		// Smoke test: load comment and parse a simple snippet
		if (!hasLanguage("comment")) {
			console.log("Skipping: language 'comment' not available");
			return;
		}
		const ptr = getLanguagePtr("comment");
		expect(ptr).toBeTruthy();
	});

	it("smoke_commonlisp", () => {
		// Smoke test: load commonlisp and parse a simple snippet
		if (!hasLanguage("commonlisp")) {
			console.log("Skipping: language 'commonlisp' not available");
			return;
		}
		const ptr = getLanguagePtr("commonlisp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cooklang", () => {
		// Smoke test: load cooklang and parse a simple snippet
		if (!hasLanguage("cooklang")) {
			console.log("Skipping: language 'cooklang' not available");
			return;
		}
		const ptr = getLanguagePtr("cooklang");
		expect(ptr).toBeTruthy();
	});

	it("smoke_corn", () => {
		// Smoke test: load corn and parse a simple snippet
		if (!hasLanguage("corn")) {
			console.log("Skipping: language 'corn' not available");
			return;
		}
		const ptr = getLanguagePtr("corn");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cpon", () => {
		// Smoke test: load cpon and parse a simple snippet
		if (!hasLanguage("cpon")) {
			console.log("Skipping: language 'cpon' not available");
			return;
		}
		const ptr = getLanguagePtr("cpon");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cpp", () => {
		// Smoke test: load cpp and parse a simple snippet
		if (!hasLanguage("cpp")) {
			console.log("Skipping: language 'cpp' not available");
			return;
		}
		const ptr = getLanguagePtr("cpp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_crystal", () => {
		// Smoke test: load crystal and parse a simple snippet
		if (!hasLanguage("crystal")) {
			console.log("Skipping: language 'crystal' not available");
			return;
		}
		const ptr = getLanguagePtr("crystal");
		expect(ptr).toBeTruthy();
	});

	it("smoke_csharp", () => {
		// Smoke test: load csharp and parse a simple snippet
		if (!hasLanguage("csharp")) {
			console.log("Skipping: language 'csharp' not available");
			return;
		}
		const ptr = getLanguagePtr("csharp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_css", () => {
		// Smoke test: load css and parse a simple snippet
		if (!hasLanguage("css")) {
			console.log("Skipping: language 'css' not available");
			return;
		}
		const ptr = getLanguagePtr("css");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cst", () => {
		// Smoke test: load cst and parse a simple snippet
		if (!hasLanguage("cst")) {
			console.log("Skipping: language 'cst' not available");
			return;
		}
		const ptr = getLanguagePtr("cst");
		expect(ptr).toBeTruthy();
	});

	it("smoke_csv", () => {
		// Smoke test: load csv and parse a simple snippet
		if (!hasLanguage("csv")) {
			console.log("Skipping: language 'csv' not available");
			return;
		}
		const ptr = getLanguagePtr("csv");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cuda", () => {
		// Smoke test: load cuda and parse a simple snippet
		if (!hasLanguage("cuda")) {
			console.log("Skipping: language 'cuda' not available");
			return;
		}
		const ptr = getLanguagePtr("cuda");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cue", () => {
		// Smoke test: load cue and parse a simple snippet
		if (!hasLanguage("cue")) {
			console.log("Skipping: language 'cue' not available");
			return;
		}
		const ptr = getLanguagePtr("cue");
		expect(ptr).toBeTruthy();
	});

	it("smoke_cylc", () => {
		// Smoke test: load cylc and parse a simple snippet
		if (!hasLanguage("cylc")) {
			console.log("Skipping: language 'cylc' not available");
			return;
		}
		const ptr = getLanguagePtr("cylc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_d", () => {
		// Smoke test: load d and parse a simple snippet
		if (!hasLanguage("d")) {
			console.log("Skipping: language 'd' not available");
			return;
		}
		const ptr = getLanguagePtr("d");
		expect(ptr).toBeTruthy();
	});

	it("smoke_dart", () => {
		// Smoke test: load dart and parse a simple snippet
		if (!hasLanguage("dart")) {
			console.log("Skipping: language 'dart' not available");
			return;
		}
		const ptr = getLanguagePtr("dart");
		expect(ptr).toBeTruthy();
	});

	it("smoke_desktop", () => {
		// Smoke test: load desktop and parse a simple snippet
		if (!hasLanguage("desktop")) {
			console.log("Skipping: language 'desktop' not available");
			return;
		}
		const ptr = getLanguagePtr("desktop");
		expect(ptr).toBeTruthy();
	});

	it("smoke_devicetree", () => {
		// Smoke test: load devicetree and parse a simple snippet
		if (!hasLanguage("devicetree")) {
			console.log("Skipping: language 'devicetree' not available");
			return;
		}
		const ptr = getLanguagePtr("devicetree");
		expect(ptr).toBeTruthy();
	});

	it("smoke_dhall", () => {
		// Smoke test: load dhall and parse a simple snippet
		if (!hasLanguage("dhall")) {
			console.log("Skipping: language 'dhall' not available");
			return;
		}
		const ptr = getLanguagePtr("dhall");
		expect(ptr).toBeTruthy();
	});

	it("smoke_diff", () => {
		// Smoke test: load diff and parse a simple snippet
		if (!hasLanguage("diff")) {
			console.log("Skipping: language 'diff' not available");
			return;
		}
		const ptr = getLanguagePtr("diff");
		expect(ptr).toBeTruthy();
	});

	it("smoke_djot", () => {
		// Smoke test: load djot and parse a simple snippet
		if (!hasLanguage("djot")) {
			console.log("Skipping: language 'djot' not available");
			return;
		}
		const ptr = getLanguagePtr("djot");
		expect(ptr).toBeTruthy();
	});

	it("smoke_dockerfile", () => {
		// Smoke test: load dockerfile and parse a simple snippet
		if (!hasLanguage("dockerfile")) {
			console.log("Skipping: language 'dockerfile' not available");
			return;
		}
		const ptr = getLanguagePtr("dockerfile");
		expect(ptr).toBeTruthy();
	});

	it("smoke_dot", () => {
		// Smoke test: load dot and parse a simple snippet
		if (!hasLanguage("dot")) {
			console.log("Skipping: language 'dot' not available");
			return;
		}
		const ptr = getLanguagePtr("dot");
		expect(ptr).toBeTruthy();
	});

	it("smoke_doxygen", () => {
		// Smoke test: load doxygen and parse a simple snippet
		if (!hasLanguage("doxygen")) {
			console.log("Skipping: language 'doxygen' not available");
			return;
		}
		const ptr = getLanguagePtr("doxygen");
		expect(ptr).toBeTruthy();
	});

	it("smoke_dtd", () => {
		// Smoke test: load dtd and parse a simple snippet
		if (!hasLanguage("dtd")) {
			console.log("Skipping: language 'dtd' not available");
			return;
		}
		const ptr = getLanguagePtr("dtd");
		expect(ptr).toBeTruthy();
	});

	it("smoke_earthfile", () => {
		// Smoke test: load earthfile and parse a simple snippet
		if (!hasLanguage("earthfile")) {
			console.log("Skipping: language 'earthfile' not available");
			return;
		}
		const ptr = getLanguagePtr("earthfile");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ebnf", () => {
		// Smoke test: load ebnf and parse a simple snippet
		if (!hasLanguage("ebnf")) {
			console.log("Skipping: language 'ebnf' not available");
			return;
		}
		const ptr = getLanguagePtr("ebnf");
		expect(ptr).toBeTruthy();
	});

	it("smoke_editorconfig", () => {
		// Smoke test: load editorconfig and parse a simple snippet
		if (!hasLanguage("editorconfig")) {
			console.log("Skipping: language 'editorconfig' not available");
			return;
		}
		const ptr = getLanguagePtr("editorconfig");
		expect(ptr).toBeTruthy();
	});

	it("smoke_eds", () => {
		// Smoke test: load eds and parse a simple snippet
		if (!hasLanguage("eds")) {
			console.log("Skipping: language 'eds' not available");
			return;
		}
		const ptr = getLanguagePtr("eds");
		expect(ptr).toBeTruthy();
	});

	it("smoke_eex", () => {
		// Smoke test: load eex and parse a simple snippet
		if (!hasLanguage("eex")) {
			console.log("Skipping: language 'eex' not available");
			return;
		}
		const ptr = getLanguagePtr("eex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_elisp", () => {
		// Smoke test: load elisp and parse a simple snippet
		if (!hasLanguage("elisp")) {
			console.log("Skipping: language 'elisp' not available");
			return;
		}
		const ptr = getLanguagePtr("elisp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_elixir", () => {
		// Smoke test: load elixir and parse a simple snippet
		if (!hasLanguage("elixir")) {
			console.log("Skipping: language 'elixir' not available");
			return;
		}
		const ptr = getLanguagePtr("elixir");
		expect(ptr).toBeTruthy();
	});

	it("smoke_elm", () => {
		// Smoke test: load elm and parse a simple snippet
		if (!hasLanguage("elm")) {
			console.log("Skipping: language 'elm' not available");
			return;
		}
		const ptr = getLanguagePtr("elm");
		expect(ptr).toBeTruthy();
	});

	it("smoke_elsa", () => {
		// Smoke test: load elsa and parse a simple snippet
		if (!hasLanguage("elsa")) {
			console.log("Skipping: language 'elsa' not available");
			return;
		}
		const ptr = getLanguagePtr("elsa");
		expect(ptr).toBeTruthy();
	});

	it("smoke_elvish", () => {
		// Smoke test: load elvish and parse a simple snippet
		if (!hasLanguage("elvish")) {
			console.log("Skipping: language 'elvish' not available");
			return;
		}
		const ptr = getLanguagePtr("elvish");
		expect(ptr).toBeTruthy();
	});

	it("smoke_embeddedtemplate", () => {
		// Smoke test: load embeddedtemplate and parse a simple snippet
		if (!hasLanguage("embeddedtemplate")) {
			console.log("Skipping: language 'embeddedtemplate' not available");
			return;
		}
		const ptr = getLanguagePtr("embeddedtemplate");
		expect(ptr).toBeTruthy();
	});

	it("smoke_enforce", () => {
		// Smoke test: load enforce and parse a simple snippet
		if (!hasLanguage("enforce")) {
			console.log("Skipping: language 'enforce' not available");
			return;
		}
		const ptr = getLanguagePtr("enforce");
		expect(ptr).toBeTruthy();
	});

	it("smoke_erlang", () => {
		// Smoke test: load erlang and parse a simple snippet
		if (!hasLanguage("erlang")) {
			console.log("Skipping: language 'erlang' not available");
			return;
		}
		const ptr = getLanguagePtr("erlang");
		expect(ptr).toBeTruthy();
	});

	it("smoke_facility", () => {
		// Smoke test: load facility and parse a simple snippet
		if (!hasLanguage("facility")) {
			console.log("Skipping: language 'facility' not available");
			return;
		}
		const ptr = getLanguagePtr("facility");
		expect(ptr).toBeTruthy();
	});

	it("smoke_faust", () => {
		// Smoke test: load faust and parse a simple snippet
		if (!hasLanguage("faust")) {
			console.log("Skipping: language 'faust' not available");
			return;
		}
		const ptr = getLanguagePtr("faust");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fennel", () => {
		// Smoke test: load fennel and parse a simple snippet
		if (!hasLanguage("fennel")) {
			console.log("Skipping: language 'fennel' not available");
			return;
		}
		const ptr = getLanguagePtr("fennel");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fidl", () => {
		// Smoke test: load fidl and parse a simple snippet
		if (!hasLanguage("fidl")) {
			console.log("Skipping: language 'fidl' not available");
			return;
		}
		const ptr = getLanguagePtr("fidl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_firrtl", () => {
		// Smoke test: load firrtl and parse a simple snippet
		if (!hasLanguage("firrtl")) {
			console.log("Skipping: language 'firrtl' not available");
			return;
		}
		const ptr = getLanguagePtr("firrtl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fish", () => {
		// Smoke test: load fish and parse a simple snippet
		if (!hasLanguage("fish")) {
			console.log("Skipping: language 'fish' not available");
			return;
		}
		const ptr = getLanguagePtr("fish");
		expect(ptr).toBeTruthy();
	});

	it("smoke_foam", () => {
		// Smoke test: load foam and parse a simple snippet
		if (!hasLanguage("foam")) {
			console.log("Skipping: language 'foam' not available");
			return;
		}
		const ptr = getLanguagePtr("foam");
		expect(ptr).toBeTruthy();
	});

	it("smoke_forth", () => {
		// Smoke test: load forth and parse a simple snippet
		if (!hasLanguage("forth")) {
			console.log("Skipping: language 'forth' not available");
			return;
		}
		const ptr = getLanguagePtr("forth");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fortran", () => {
		// Smoke test: load fortran and parse a simple snippet
		if (!hasLanguage("fortran")) {
			console.log("Skipping: language 'fortran' not available");
			return;
		}
		const ptr = getLanguagePtr("fortran");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fsharp", () => {
		// Smoke test: load fsharp and parse a simple snippet
		if (!hasLanguage("fsharp")) {
			console.log("Skipping: language 'fsharp' not available");
			return;
		}
		const ptr = getLanguagePtr("fsharp");
		expect(ptr).toBeTruthy();
	});

	it("smoke_fsharp_signature", () => {
		// Smoke test: load fsharp_signature and parse a simple snippet
		if (!hasLanguage("fsharp_signature")) {
			console.log("Skipping: language 'fsharp_signature' not available");
			return;
		}
		const ptr = getLanguagePtr("fsharp_signature");
		expect(ptr).toBeTruthy();
	});

	it("smoke_func", () => {
		// Smoke test: load func and parse a simple snippet
		if (!hasLanguage("func")) {
			console.log("Skipping: language 'func' not available");
			return;
		}
		const ptr = getLanguagePtr("func");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gap", () => {
		// Smoke test: load gap and parse a simple snippet
		if (!hasLanguage("gap")) {
			console.log("Skipping: language 'gap' not available");
			return;
		}
		const ptr = getLanguagePtr("gap");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gdscript", () => {
		// Smoke test: load gdscript and parse a simple snippet
		if (!hasLanguage("gdscript")) {
			console.log("Skipping: language 'gdscript' not available");
			return;
		}
		const ptr = getLanguagePtr("gdscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gdshader", () => {
		// Smoke test: load gdshader and parse a simple snippet
		if (!hasLanguage("gdshader")) {
			console.log("Skipping: language 'gdshader' not available");
			return;
		}
		const ptr = getLanguagePtr("gdshader");
		expect(ptr).toBeTruthy();
	});

	it("smoke_git_config", () => {
		// Smoke test: load git_config and parse a simple snippet
		if (!hasLanguage("git_config")) {
			console.log("Skipping: language 'git_config' not available");
			return;
		}
		const ptr = getLanguagePtr("git_config");
		expect(ptr).toBeTruthy();
	});

	it("smoke_git_rebase", () => {
		// Smoke test: load git_rebase and parse a simple snippet
		if (!hasLanguage("git_rebase")) {
			console.log("Skipping: language 'git_rebase' not available");
			return;
		}
		const ptr = getLanguagePtr("git_rebase");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gitattributes", () => {
		// Smoke test: load gitattributes and parse a simple snippet
		if (!hasLanguage("gitattributes")) {
			console.log("Skipping: language 'gitattributes' not available");
			return;
		}
		const ptr = getLanguagePtr("gitattributes");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gitcommit", () => {
		// Smoke test: load gitcommit and parse a simple snippet
		if (!hasLanguage("gitcommit")) {
			console.log("Skipping: language 'gitcommit' not available");
			return;
		}
		const ptr = getLanguagePtr("gitcommit");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gitignore", () => {
		// Smoke test: load gitignore and parse a simple snippet
		if (!hasLanguage("gitignore")) {
			console.log("Skipping: language 'gitignore' not available");
			return;
		}
		const ptr = getLanguagePtr("gitignore");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gleam", () => {
		// Smoke test: load gleam and parse a simple snippet
		if (!hasLanguage("gleam")) {
			console.log("Skipping: language 'gleam' not available");
			return;
		}
		const ptr = getLanguagePtr("gleam");
		expect(ptr).toBeTruthy();
	});

	it("smoke_glimmer", () => {
		// Smoke test: load glimmer and parse a simple snippet
		if (!hasLanguage("glimmer")) {
			console.log("Skipping: language 'glimmer' not available");
			return;
		}
		const ptr = getLanguagePtr("glimmer");
		expect(ptr).toBeTruthy();
	});

	it("smoke_glsl", () => {
		// Smoke test: load glsl and parse a simple snippet
		if (!hasLanguage("glsl")) {
			console.log("Skipping: language 'glsl' not available");
			return;
		}
		const ptr = getLanguagePtr("glsl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gn", () => {
		// Smoke test: load gn and parse a simple snippet
		if (!hasLanguage("gn")) {
			console.log("Skipping: language 'gn' not available");
			return;
		}
		const ptr = getLanguagePtr("gn");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gnuplot", () => {
		// Smoke test: load gnuplot and parse a simple snippet
		if (!hasLanguage("gnuplot")) {
			console.log("Skipping: language 'gnuplot' not available");
			return;
		}
		const ptr = getLanguagePtr("gnuplot");
		expect(ptr).toBeTruthy();
	});

	it("smoke_go", () => {
		// Smoke test: load go and parse a simple snippet
		if (!hasLanguage("go")) {
			console.log("Skipping: language 'go' not available");
			return;
		}
		const ptr = getLanguagePtr("go");
		expect(ptr).toBeTruthy();
	});

	it("smoke_godot_resource", () => {
		// Smoke test: load godot_resource and parse a simple snippet
		if (!hasLanguage("godot_resource")) {
			console.log("Skipping: language 'godot_resource' not available");
			return;
		}
		const ptr = getLanguagePtr("godot_resource");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gomod", () => {
		// Smoke test: load gomod and parse a simple snippet
		if (!hasLanguage("gomod")) {
			console.log("Skipping: language 'gomod' not available");
			return;
		}
		const ptr = getLanguagePtr("gomod");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gosum", () => {
		// Smoke test: load gosum and parse a simple snippet
		if (!hasLanguage("gosum")) {
			console.log("Skipping: language 'gosum' not available");
			return;
		}
		const ptr = getLanguagePtr("gosum");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gotmpl", () => {
		// Smoke test: load gotmpl and parse a simple snippet
		if (!hasLanguage("gotmpl")) {
			console.log("Skipping: language 'gotmpl' not available");
			return;
		}
		const ptr = getLanguagePtr("gotmpl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gowork", () => {
		// Smoke test: load gowork and parse a simple snippet
		if (!hasLanguage("gowork")) {
			console.log("Skipping: language 'gowork' not available");
			return;
		}
		const ptr = getLanguagePtr("gowork");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gpg", () => {
		// Smoke test: load gpg and parse a simple snippet
		if (!hasLanguage("gpg")) {
			console.log("Skipping: language 'gpg' not available");
			return;
		}
		const ptr = getLanguagePtr("gpg");
		expect(ptr).toBeTruthy();
	});

	it("smoke_graphql", () => {
		// Smoke test: load graphql and parse a simple snippet
		if (!hasLanguage("graphql")) {
			console.log("Skipping: language 'graphql' not available");
			return;
		}
		const ptr = getLanguagePtr("graphql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_groovy", () => {
		// Smoke test: load groovy and parse a simple snippet
		if (!hasLanguage("groovy")) {
			console.log("Skipping: language 'groovy' not available");
			return;
		}
		const ptr = getLanguagePtr("groovy");
		expect(ptr).toBeTruthy();
	});

	it("smoke_gstlaunch", () => {
		// Smoke test: load gstlaunch and parse a simple snippet
		if (!hasLanguage("gstlaunch")) {
			console.log("Skipping: language 'gstlaunch' not available");
			return;
		}
		const ptr = getLanguagePtr("gstlaunch");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hack", () => {
		// Smoke test: load hack and parse a simple snippet
		if (!hasLanguage("hack")) {
			console.log("Skipping: language 'hack' not available");
			return;
		}
		const ptr = getLanguagePtr("hack");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hare", () => {
		// Smoke test: load hare and parse a simple snippet
		if (!hasLanguage("hare")) {
			console.log("Skipping: language 'hare' not available");
			return;
		}
		const ptr = getLanguagePtr("hare");
		expect(ptr).toBeTruthy();
	});

	it("smoke_haskell", () => {
		// Smoke test: load haskell and parse a simple snippet
		if (!hasLanguage("haskell")) {
			console.log("Skipping: language 'haskell' not available");
			return;
		}
		const ptr = getLanguagePtr("haskell");
		expect(ptr).toBeTruthy();
	});

	it("smoke_haxe", () => {
		// Smoke test: load haxe and parse a simple snippet
		if (!hasLanguage("haxe")) {
			console.log("Skipping: language 'haxe' not available");
			return;
		}
		const ptr = getLanguagePtr("haxe");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hcl", () => {
		// Smoke test: load hcl and parse a simple snippet
		if (!hasLanguage("hcl")) {
			console.log("Skipping: language 'hcl' not available");
			return;
		}
		const ptr = getLanguagePtr("hcl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_heex", () => {
		// Smoke test: load heex and parse a simple snippet
		if (!hasLanguage("heex")) {
			console.log("Skipping: language 'heex' not available");
			return;
		}
		const ptr = getLanguagePtr("heex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hjson", () => {
		// Smoke test: load hjson and parse a simple snippet
		if (!hasLanguage("hjson")) {
			console.log("Skipping: language 'hjson' not available");
			return;
		}
		const ptr = getLanguagePtr("hjson");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hlsl", () => {
		// Smoke test: load hlsl and parse a simple snippet
		if (!hasLanguage("hlsl")) {
			console.log("Skipping: language 'hlsl' not available");
			return;
		}
		const ptr = getLanguagePtr("hlsl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hocon", () => {
		// Smoke test: load hocon and parse a simple snippet
		if (!hasLanguage("hocon")) {
			console.log("Skipping: language 'hocon' not available");
			return;
		}
		const ptr = getLanguagePtr("hocon");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hoon", () => {
		// Smoke test: load hoon and parse a simple snippet
		if (!hasLanguage("hoon")) {
			console.log("Skipping: language 'hoon' not available");
			return;
		}
		const ptr = getLanguagePtr("hoon");
		expect(ptr).toBeTruthy();
	});

	it("smoke_html", () => {
		// Smoke test: load html and parse a simple snippet
		if (!hasLanguage("html")) {
			console.log("Skipping: language 'html' not available");
			return;
		}
		const ptr = getLanguagePtr("html");
		expect(ptr).toBeTruthy();
	});

	it("smoke_htmldjango", () => {
		// Smoke test: load htmldjango and parse a simple snippet
		if (!hasLanguage("htmldjango")) {
			console.log("Skipping: language 'htmldjango' not available");
			return;
		}
		const ptr = getLanguagePtr("htmldjango");
		expect(ptr).toBeTruthy();
	});

	it("smoke_http", () => {
		// Smoke test: load http and parse a simple snippet
		if (!hasLanguage("http")) {
			console.log("Skipping: language 'http' not available");
			return;
		}
		const ptr = getLanguagePtr("http");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hurl", () => {
		// Smoke test: load hurl and parse a simple snippet
		if (!hasLanguage("hurl")) {
			console.log("Skipping: language 'hurl' not available");
			return;
		}
		const ptr = getLanguagePtr("hurl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_hyprlang", () => {
		// Smoke test: load hyprlang and parse a simple snippet
		if (!hasLanguage("hyprlang")) {
			console.log("Skipping: language 'hyprlang' not available");
			return;
		}
		const ptr = getLanguagePtr("hyprlang");
		expect(ptr).toBeTruthy();
	});

	it("smoke_idris", () => {
		// Smoke test: load idris and parse a simple snippet
		if (!hasLanguage("idris")) {
			console.log("Skipping: language 'idris' not available");
			return;
		}
		const ptr = getLanguagePtr("idris");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ini", () => {
		// Smoke test: load ini and parse a simple snippet
		if (!hasLanguage("ini")) {
			console.log("Skipping: language 'ini' not available");
			return;
		}
		const ptr = getLanguagePtr("ini");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ispc", () => {
		// Smoke test: load ispc and parse a simple snippet
		if (!hasLanguage("ispc")) {
			console.log("Skipping: language 'ispc' not available");
			return;
		}
		const ptr = getLanguagePtr("ispc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_jai", () => {
		// Smoke test: load jai and parse a simple snippet
		if (!hasLanguage("jai")) {
			console.log("Skipping: language 'jai' not available");
			return;
		}
		const ptr = getLanguagePtr("jai");
		expect(ptr).toBeTruthy();
	});

	it("smoke_janet", () => {
		// Smoke test: load janet and parse a simple snippet
		if (!hasLanguage("janet")) {
			console.log("Skipping: language 'janet' not available");
			return;
		}
		const ptr = getLanguagePtr("janet");
		expect(ptr).toBeTruthy();
	});

	it("smoke_java", () => {
		// Smoke test: load java and parse a simple snippet
		if (!hasLanguage("java")) {
			console.log("Skipping: language 'java' not available");
			return;
		}
		const ptr = getLanguagePtr("java");
		expect(ptr).toBeTruthy();
	});

	it("smoke_javadoc", () => {
		// Smoke test: load javadoc and parse a simple snippet
		if (!hasLanguage("javadoc")) {
			console.log("Skipping: language 'javadoc' not available");
			return;
		}
		const ptr = getLanguagePtr("javadoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_javascript", () => {
		// Smoke test: load javascript and parse a simple snippet
		if (!hasLanguage("javascript")) {
			console.log("Skipping: language 'javascript' not available");
			return;
		}
		const ptr = getLanguagePtr("javascript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_jinja2", () => {
		// Smoke test: load jinja2 and parse a simple snippet
		if (!hasLanguage("jinja2")) {
			console.log("Skipping: language 'jinja2' not available");
			return;
		}
		const ptr = getLanguagePtr("jinja2");
		expect(ptr).toBeTruthy();
	});

	it("smoke_jq", () => {
		// Smoke test: load jq and parse a simple snippet
		if (!hasLanguage("jq")) {
			console.log("Skipping: language 'jq' not available");
			return;
		}
		const ptr = getLanguagePtr("jq");
		expect(ptr).toBeTruthy();
	});

	it("smoke_jsdoc", () => {
		// Smoke test: load jsdoc and parse a simple snippet
		if (!hasLanguage("jsdoc")) {
			console.log("Skipping: language 'jsdoc' not available");
			return;
		}
		const ptr = getLanguagePtr("jsdoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_json", () => {
		// Smoke test: load json and parse a simple snippet
		if (!hasLanguage("json")) {
			console.log("Skipping: language 'json' not available");
			return;
		}
		const ptr = getLanguagePtr("json");
		expect(ptr).toBeTruthy();
	});

	it("smoke_json5", () => {
		// Smoke test: load json5 and parse a simple snippet
		if (!hasLanguage("json5")) {
			console.log("Skipping: language 'json5' not available");
			return;
		}
		const ptr = getLanguagePtr("json5");
		expect(ptr).toBeTruthy();
	});

	it("smoke_jsonnet", () => {
		// Smoke test: load jsonnet and parse a simple snippet
		if (!hasLanguage("jsonnet")) {
			console.log("Skipping: language 'jsonnet' not available");
			return;
		}
		const ptr = getLanguagePtr("jsonnet");
		expect(ptr).toBeTruthy();
	});

	it("smoke_julia", () => {
		// Smoke test: load julia and parse a simple snippet
		if (!hasLanguage("julia")) {
			console.log("Skipping: language 'julia' not available");
			return;
		}
		const ptr = getLanguagePtr("julia");
		expect(ptr).toBeTruthy();
	});

	it("smoke_just", () => {
		// Smoke test: load just and parse a simple snippet
		if (!hasLanguage("just")) {
			console.log("Skipping: language 'just' not available");
			return;
		}
		const ptr = getLanguagePtr("just");
		expect(ptr).toBeTruthy();
	});

	it("smoke_kcl", () => {
		// Smoke test: load kcl and parse a simple snippet
		if (!hasLanguage("kcl")) {
			console.log("Skipping: language 'kcl' not available");
			return;
		}
		const ptr = getLanguagePtr("kcl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_kconfig", () => {
		// Smoke test: load kconfig and parse a simple snippet
		if (!hasLanguage("kconfig")) {
			console.log("Skipping: language 'kconfig' not available");
			return;
		}
		const ptr = getLanguagePtr("kconfig");
		expect(ptr).toBeTruthy();
	});

	it("smoke_kdl", () => {
		// Smoke test: load kdl and parse a simple snippet
		if (!hasLanguage("kdl")) {
			console.log("Skipping: language 'kdl' not available");
			return;
		}
		const ptr = getLanguagePtr("kdl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_kotlin", () => {
		// Smoke test: load kotlin and parse a simple snippet
		if (!hasLanguage("kotlin")) {
			console.log("Skipping: language 'kotlin' not available");
			return;
		}
		const ptr = getLanguagePtr("kotlin");
		expect(ptr).toBeTruthy();
	});

	it("smoke_latex", () => {
		// Smoke test: load latex and parse a simple snippet
		if (!hasLanguage("latex")) {
			console.log("Skipping: language 'latex' not available");
			return;
		}
		const ptr = getLanguagePtr("latex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_lean", () => {
		// Smoke test: load lean and parse a simple snippet
		if (!hasLanguage("lean")) {
			console.log("Skipping: language 'lean' not available");
			return;
		}
		const ptr = getLanguagePtr("lean");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ledger", () => {
		// Smoke test: load ledger and parse a simple snippet
		if (!hasLanguage("ledger")) {
			console.log("Skipping: language 'ledger' not available");
			return;
		}
		const ptr = getLanguagePtr("ledger");
		expect(ptr).toBeTruthy();
	});

	it("smoke_less", () => {
		// Smoke test: load less and parse a simple snippet
		if (!hasLanguage("less")) {
			console.log("Skipping: language 'less' not available");
			return;
		}
		const ptr = getLanguagePtr("less");
		expect(ptr).toBeTruthy();
	});

	it("smoke_linkerscript", () => {
		// Smoke test: load linkerscript and parse a simple snippet
		if (!hasLanguage("linkerscript")) {
			console.log("Skipping: language 'linkerscript' not available");
			return;
		}
		const ptr = getLanguagePtr("linkerscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_liquid", () => {
		// Smoke test: load liquid and parse a simple snippet
		if (!hasLanguage("liquid")) {
			console.log("Skipping: language 'liquid' not available");
			return;
		}
		const ptr = getLanguagePtr("liquid");
		expect(ptr).toBeTruthy();
	});

	it("smoke_llvm", () => {
		// Smoke test: load llvm and parse a simple snippet
		if (!hasLanguage("llvm")) {
			console.log("Skipping: language 'llvm' not available");
			return;
		}
		const ptr = getLanguagePtr("llvm");
		expect(ptr).toBeTruthy();
	});

	it("smoke_lua", () => {
		// Smoke test: load lua and parse a simple snippet
		if (!hasLanguage("lua")) {
			console.log("Skipping: language 'lua' not available");
			return;
		}
		const ptr = getLanguagePtr("lua");
		expect(ptr).toBeTruthy();
	});

	it("smoke_luadoc", () => {
		// Smoke test: load luadoc and parse a simple snippet
		if (!hasLanguage("luadoc")) {
			console.log("Skipping: language 'luadoc' not available");
			return;
		}
		const ptr = getLanguagePtr("luadoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_luap", () => {
		// Smoke test: load luap and parse a simple snippet
		if (!hasLanguage("luap")) {
			console.log("Skipping: language 'luap' not available");
			return;
		}
		const ptr = getLanguagePtr("luap");
		expect(ptr).toBeTruthy();
	});

	it("smoke_luau", () => {
		// Smoke test: load luau and parse a simple snippet
		if (!hasLanguage("luau")) {
			console.log("Skipping: language 'luau' not available");
			return;
		}
		const ptr = getLanguagePtr("luau");
		expect(ptr).toBeTruthy();
	});

	it("smoke_magik", () => {
		// Smoke test: load magik and parse a simple snippet
		if (!hasLanguage("magik")) {
			console.log("Skipping: language 'magik' not available");
			return;
		}
		const ptr = getLanguagePtr("magik");
		expect(ptr).toBeTruthy();
	});

	it("smoke_make", () => {
		// Smoke test: load make and parse a simple snippet
		if (!hasLanguage("make")) {
			console.log("Skipping: language 'make' not available");
			return;
		}
		const ptr = getLanguagePtr("make");
		expect(ptr).toBeTruthy();
	});

	it("smoke_markdown", () => {
		// Smoke test: load markdown and parse a simple snippet
		if (!hasLanguage("markdown")) {
			console.log("Skipping: language 'markdown' not available");
			return;
		}
		const ptr = getLanguagePtr("markdown");
		expect(ptr).toBeTruthy();
	});

	it("smoke_markdown_inline", () => {
		// Smoke test: load markdown_inline and parse a simple snippet
		if (!hasLanguage("markdown_inline")) {
			console.log("Skipping: language 'markdown_inline' not available");
			return;
		}
		const ptr = getLanguagePtr("markdown_inline");
		expect(ptr).toBeTruthy();
	});

	it("smoke_matlab", () => {
		// Smoke test: load matlab and parse a simple snippet
		if (!hasLanguage("matlab")) {
			console.log("Skipping: language 'matlab' not available");
			return;
		}
		const ptr = getLanguagePtr("matlab");
		expect(ptr).toBeTruthy();
	});

	it("smoke_mermaid", () => {
		// Smoke test: load mermaid and parse a simple snippet
		if (!hasLanguage("mermaid")) {
			console.log("Skipping: language 'mermaid' not available");
			return;
		}
		const ptr = getLanguagePtr("mermaid");
		expect(ptr).toBeTruthy();
	});

	it("smoke_meson", () => {
		// Smoke test: load meson and parse a simple snippet
		if (!hasLanguage("meson")) {
			console.log("Skipping: language 'meson' not available");
			return;
		}
		const ptr = getLanguagePtr("meson");
		expect(ptr).toBeTruthy();
	});

	it("smoke_mlir", () => {
		// Smoke test: load mlir and parse a simple snippet
		if (!hasLanguage("mlir")) {
			console.log("Skipping: language 'mlir' not available");
			return;
		}
		const ptr = getLanguagePtr("mlir");
		expect(ptr).toBeTruthy();
	});

	it("smoke_mojo", () => {
		// Smoke test: load mojo and parse a simple snippet
		if (!hasLanguage("mojo")) {
			console.log("Skipping: language 'mojo' not available");
			return;
		}
		const ptr = getLanguagePtr("mojo");
		expect(ptr).toBeTruthy();
	});

	it("smoke_move", () => {
		// Smoke test: load move and parse a simple snippet
		if (!hasLanguage("move")) {
			console.log("Skipping: language 'move' not available");
			return;
		}
		const ptr = getLanguagePtr("move");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nasm", () => {
		// Smoke test: load nasm and parse a simple snippet
		if (!hasLanguage("nasm")) {
			console.log("Skipping: language 'nasm' not available");
			return;
		}
		const ptr = getLanguagePtr("nasm");
		expect(ptr).toBeTruthy();
	});

	it("smoke_netlinx", () => {
		// Smoke test: load netlinx and parse a simple snippet
		if (!hasLanguage("netlinx")) {
			console.log("Skipping: language 'netlinx' not available");
			return;
		}
		const ptr = getLanguagePtr("netlinx");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nginx", () => {
		// Smoke test: load nginx and parse a simple snippet
		if (!hasLanguage("nginx")) {
			console.log("Skipping: language 'nginx' not available");
			return;
		}
		const ptr = getLanguagePtr("nginx");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nickel", () => {
		// Smoke test: load nickel and parse a simple snippet
		if (!hasLanguage("nickel")) {
			console.log("Skipping: language 'nickel' not available");
			return;
		}
		const ptr = getLanguagePtr("nickel");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nim", () => {
		// Smoke test: load nim and parse a simple snippet
		if (!hasLanguage("nim")) {
			console.log("Skipping: language 'nim' not available");
			return;
		}
		const ptr = getLanguagePtr("nim");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ninja", () => {
		// Smoke test: load ninja and parse a simple snippet
		if (!hasLanguage("ninja")) {
			console.log("Skipping: language 'ninja' not available");
			return;
		}
		const ptr = getLanguagePtr("ninja");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nix", () => {
		// Smoke test: load nix and parse a simple snippet
		if (!hasLanguage("nix")) {
			console.log("Skipping: language 'nix' not available");
			return;
		}
		const ptr = getLanguagePtr("nix");
		expect(ptr).toBeTruthy();
	});

	it("smoke_norg", () => {
		// Smoke test: load norg and parse a simple snippet
		if (!hasLanguage("norg")) {
			console.log("Skipping: language 'norg' not available");
			return;
		}
		const ptr = getLanguagePtr("norg");
		expect(ptr).toBeTruthy();
	});

	it("smoke_norg_meta", () => {
		// Smoke test: load norg_meta and parse a simple snippet
		if (!hasLanguage("norg_meta")) {
			console.log("Skipping: language 'norg_meta' not available");
			return;
		}
		const ptr = getLanguagePtr("norg_meta");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nqc", () => {
		// Smoke test: load nqc and parse a simple snippet
		if (!hasLanguage("nqc")) {
			console.log("Skipping: language 'nqc' not available");
			return;
		}
		const ptr = getLanguagePtr("nqc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_nushell", () => {
		// Smoke test: load nushell and parse a simple snippet
		if (!hasLanguage("nushell")) {
			console.log("Skipping: language 'nushell' not available");
			return;
		}
		const ptr = getLanguagePtr("nushell");
		expect(ptr).toBeTruthy();
	});

	it("smoke_objc", () => {
		// Smoke test: load objc and parse a simple snippet
		if (!hasLanguage("objc")) {
			console.log("Skipping: language 'objc' not available");
			return;
		}
		const ptr = getLanguagePtr("objc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ocaml", () => {
		// Smoke test: load ocaml and parse a simple snippet
		if (!hasLanguage("ocaml")) {
			console.log("Skipping: language 'ocaml' not available");
			return;
		}
		const ptr = getLanguagePtr("ocaml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ocaml_interface", () => {
		// Smoke test: load ocaml_interface and parse a simple snippet
		if (!hasLanguage("ocaml_interface")) {
			console.log("Skipping: language 'ocaml_interface' not available");
			return;
		}
		const ptr = getLanguagePtr("ocaml_interface");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ocamllex", () => {
		// Smoke test: load ocamllex and parse a simple snippet
		if (!hasLanguage("ocamllex")) {
			console.log("Skipping: language 'ocamllex' not available");
			return;
		}
		const ptr = getLanguagePtr("ocamllex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_odin", () => {
		// Smoke test: load odin and parse a simple snippet
		if (!hasLanguage("odin")) {
			console.log("Skipping: language 'odin' not available");
			return;
		}
		const ptr = getLanguagePtr("odin");
		expect(ptr).toBeTruthy();
	});

	it("smoke_openscad", () => {
		// Smoke test: load openscad and parse a simple snippet
		if (!hasLanguage("openscad")) {
			console.log("Skipping: language 'openscad' not available");
			return;
		}
		const ptr = getLanguagePtr("openscad");
		expect(ptr).toBeTruthy();
	});

	it("smoke_org", () => {
		// Smoke test: load org and parse a simple snippet
		if (!hasLanguage("org")) {
			console.log("Skipping: language 'org' not available");
			return;
		}
		const ptr = getLanguagePtr("org");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pascal", () => {
		// Smoke test: load pascal and parse a simple snippet
		if (!hasLanguage("pascal")) {
			console.log("Skipping: language 'pascal' not available");
			return;
		}
		const ptr = getLanguagePtr("pascal");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pem", () => {
		// Smoke test: load pem and parse a simple snippet
		if (!hasLanguage("pem")) {
			console.log("Skipping: language 'pem' not available");
			return;
		}
		const ptr = getLanguagePtr("pem");
		expect(ptr).toBeTruthy();
	});

	it("smoke_perl", () => {
		// Smoke test: load perl and parse a simple snippet
		if (!hasLanguage("perl")) {
			console.log("Skipping: language 'perl' not available");
			return;
		}
		const ptr = getLanguagePtr("perl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pgn", () => {
		// Smoke test: load pgn and parse a simple snippet
		if (!hasLanguage("pgn")) {
			console.log("Skipping: language 'pgn' not available");
			return;
		}
		const ptr = getLanguagePtr("pgn");
		expect(ptr).toBeTruthy();
	});

	it("smoke_php", () => {
		// Smoke test: load php and parse a simple snippet
		if (!hasLanguage("php")) {
			console.log("Skipping: language 'php' not available");
			return;
		}
		const ptr = getLanguagePtr("php");
		expect(ptr).toBeTruthy();
	});

	it("smoke_phpdoc", () => {
		// Smoke test: load phpdoc and parse a simple snippet
		if (!hasLanguage("phpdoc")) {
			console.log("Skipping: language 'phpdoc' not available");
			return;
		}
		const ptr = getLanguagePtr("phpdoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pkl", () => {
		// Smoke test: load pkl and parse a simple snippet
		if (!hasLanguage("pkl")) {
			console.log("Skipping: language 'pkl' not available");
			return;
		}
		const ptr = getLanguagePtr("pkl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_po", () => {
		// Smoke test: load po and parse a simple snippet
		if (!hasLanguage("po")) {
			console.log("Skipping: language 'po' not available");
			return;
		}
		const ptr = getLanguagePtr("po");
		expect(ptr).toBeTruthy();
	});

	it("smoke_poe_filter", () => {
		// Smoke test: load poe_filter and parse a simple snippet
		if (!hasLanguage("poe_filter")) {
			console.log("Skipping: language 'poe_filter' not available");
			return;
		}
		const ptr = getLanguagePtr("poe_filter");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pony", () => {
		// Smoke test: load pony and parse a simple snippet
		if (!hasLanguage("pony")) {
			console.log("Skipping: language 'pony' not available");
			return;
		}
		const ptr = getLanguagePtr("pony");
		expect(ptr).toBeTruthy();
	});

	it("smoke_postscript", () => {
		// Smoke test: load postscript and parse a simple snippet
		if (!hasLanguage("postscript")) {
			console.log("Skipping: language 'postscript' not available");
			return;
		}
		const ptr = getLanguagePtr("postscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_powershell", () => {
		// Smoke test: load powershell and parse a simple snippet
		if (!hasLanguage("powershell")) {
			console.log("Skipping: language 'powershell' not available");
			return;
		}
		const ptr = getLanguagePtr("powershell");
		expect(ptr).toBeTruthy();
	});

	it("smoke_printf", () => {
		// Smoke test: load printf and parse a simple snippet
		if (!hasLanguage("printf")) {
			console.log("Skipping: language 'printf' not available");
			return;
		}
		const ptr = getLanguagePtr("printf");
		expect(ptr).toBeTruthy();
	});

	it("smoke_prisma", () => {
		// Smoke test: load prisma and parse a simple snippet
		if (!hasLanguage("prisma")) {
			console.log("Skipping: language 'prisma' not available");
			return;
		}
		const ptr = getLanguagePtr("prisma");
		expect(ptr).toBeTruthy();
	});

	it("smoke_prolog", () => {
		// Smoke test: load prolog and parse a simple snippet
		if (!hasLanguage("prolog")) {
			console.log("Skipping: language 'prolog' not available");
			return;
		}
		const ptr = getLanguagePtr("prolog");
		expect(ptr).toBeTruthy();
	});

	it("smoke_promql", () => {
		// Smoke test: load promql and parse a simple snippet
		if (!hasLanguage("promql")) {
			console.log("Skipping: language 'promql' not available");
			return;
		}
		const ptr = getLanguagePtr("promql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_properties", () => {
		// Smoke test: load properties and parse a simple snippet
		if (!hasLanguage("properties")) {
			console.log("Skipping: language 'properties' not available");
			return;
		}
		const ptr = getLanguagePtr("properties");
		expect(ptr).toBeTruthy();
	});

	it("smoke_proto", () => {
		// Smoke test: load proto and parse a simple snippet
		if (!hasLanguage("proto")) {
			console.log("Skipping: language 'proto' not available");
			return;
		}
		const ptr = getLanguagePtr("proto");
		expect(ptr).toBeTruthy();
	});

	it("smoke_prql", () => {
		// Smoke test: load prql and parse a simple snippet
		if (!hasLanguage("prql")) {
			console.log("Skipping: language 'prql' not available");
			return;
		}
		const ptr = getLanguagePtr("prql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_psv", () => {
		// Smoke test: load psv and parse a simple snippet
		if (!hasLanguage("psv")) {
			console.log("Skipping: language 'psv' not available");
			return;
		}
		const ptr = getLanguagePtr("psv");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pug", () => {
		// Smoke test: load pug and parse a simple snippet
		if (!hasLanguage("pug")) {
			console.log("Skipping: language 'pug' not available");
			return;
		}
		const ptr = getLanguagePtr("pug");
		expect(ptr).toBeTruthy();
	});

	it("smoke_puppet", () => {
		// Smoke test: load puppet and parse a simple snippet
		if (!hasLanguage("puppet")) {
			console.log("Skipping: language 'puppet' not available");
			return;
		}
		const ptr = getLanguagePtr("puppet");
		expect(ptr).toBeTruthy();
	});

	it("smoke_purescript", () => {
		// Smoke test: load purescript and parse a simple snippet
		if (!hasLanguage("purescript")) {
			console.log("Skipping: language 'purescript' not available");
			return;
		}
		const ptr = getLanguagePtr("purescript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_pymanifest", () => {
		// Smoke test: load pymanifest and parse a simple snippet
		if (!hasLanguage("pymanifest")) {
			console.log("Skipping: language 'pymanifest' not available");
			return;
		}
		const ptr = getLanguagePtr("pymanifest");
		expect(ptr).toBeTruthy();
	});

	it("smoke_python", () => {
		// Smoke test: load python and parse a simple snippet
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const ptr = getLanguagePtr("python");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ql", () => {
		// Smoke test: load ql and parse a simple snippet
		if (!hasLanguage("ql")) {
			console.log("Skipping: language 'ql' not available");
			return;
		}
		const ptr = getLanguagePtr("ql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_qmldir", () => {
		// Smoke test: load qmldir and parse a simple snippet
		if (!hasLanguage("qmldir")) {
			console.log("Skipping: language 'qmldir' not available");
			return;
		}
		const ptr = getLanguagePtr("qmldir");
		expect(ptr).toBeTruthy();
	});

	it("smoke_qmljs", () => {
		// Smoke test: load qmljs and parse a simple snippet
		if (!hasLanguage("qmljs")) {
			console.log("Skipping: language 'qmljs' not available");
			return;
		}
		const ptr = getLanguagePtr("qmljs");
		expect(ptr).toBeTruthy();
	});

	it("smoke_query", () => {
		// Smoke test: load query and parse a simple snippet
		if (!hasLanguage("query")) {
			console.log("Skipping: language 'query' not available");
			return;
		}
		const ptr = getLanguagePtr("query");
		expect(ptr).toBeTruthy();
	});

	it("smoke_r", () => {
		// Smoke test: load r and parse a simple snippet
		if (!hasLanguage("r")) {
			console.log("Skipping: language 'r' not available");
			return;
		}
		const ptr = getLanguagePtr("r");
		expect(ptr).toBeTruthy();
	});

	it("smoke_racket", () => {
		// Smoke test: load racket and parse a simple snippet
		if (!hasLanguage("racket")) {
			console.log("Skipping: language 'racket' not available");
			return;
		}
		const ptr = getLanguagePtr("racket");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rasi", () => {
		// Smoke test: load rasi and parse a simple snippet
		if (!hasLanguage("rasi")) {
			console.log("Skipping: language 'rasi' not available");
			return;
		}
		const ptr = getLanguagePtr("rasi");
		expect(ptr).toBeTruthy();
	});

	it("smoke_razor", () => {
		// Smoke test: load razor and parse a simple snippet
		if (!hasLanguage("razor")) {
			console.log("Skipping: language 'razor' not available");
			return;
		}
		const ptr = getLanguagePtr("razor");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rbs", () => {
		// Smoke test: load rbs and parse a simple snippet
		if (!hasLanguage("rbs")) {
			console.log("Skipping: language 'rbs' not available");
			return;
		}
		const ptr = getLanguagePtr("rbs");
		expect(ptr).toBeTruthy();
	});

	it("smoke_re2c", () => {
		// Smoke test: load re2c and parse a simple snippet
		if (!hasLanguage("re2c")) {
			console.log("Skipping: language 're2c' not available");
			return;
		}
		const ptr = getLanguagePtr("re2c");
		expect(ptr).toBeTruthy();
	});

	it("smoke_readline", () => {
		// Smoke test: load readline and parse a simple snippet
		if (!hasLanguage("readline")) {
			console.log("Skipping: language 'readline' not available");
			return;
		}
		const ptr = getLanguagePtr("readline");
		expect(ptr).toBeTruthy();
	});

	it("smoke_regex", () => {
		// Smoke test: load regex and parse a simple snippet
		if (!hasLanguage("regex")) {
			console.log("Skipping: language 'regex' not available");
			return;
		}
		const ptr = getLanguagePtr("regex");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rego", () => {
		// Smoke test: load rego and parse a simple snippet
		if (!hasLanguage("rego")) {
			console.log("Skipping: language 'rego' not available");
			return;
		}
		const ptr = getLanguagePtr("rego");
		expect(ptr).toBeTruthy();
	});

	it("smoke_requirements", () => {
		// Smoke test: load requirements and parse a simple snippet
		if (!hasLanguage("requirements")) {
			console.log("Skipping: language 'requirements' not available");
			return;
		}
		const ptr = getLanguagePtr("requirements");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rescript", () => {
		// Smoke test: load rescript and parse a simple snippet
		if (!hasLanguage("rescript")) {
			console.log("Skipping: language 'rescript' not available");
			return;
		}
		const ptr = getLanguagePtr("rescript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_robot", () => {
		// Smoke test: load robot and parse a simple snippet
		if (!hasLanguage("robot")) {
			console.log("Skipping: language 'robot' not available");
			return;
		}
		const ptr = getLanguagePtr("robot");
		expect(ptr).toBeTruthy();
	});

	it("smoke_roc", () => {
		// Smoke test: load roc and parse a simple snippet
		if (!hasLanguage("roc")) {
			console.log("Skipping: language 'roc' not available");
			return;
		}
		const ptr = getLanguagePtr("roc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ron", () => {
		// Smoke test: load ron and parse a simple snippet
		if (!hasLanguage("ron")) {
			console.log("Skipping: language 'ron' not available");
			return;
		}
		const ptr = getLanguagePtr("ron");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rst", () => {
		// Smoke test: load rst and parse a simple snippet
		if (!hasLanguage("rst")) {
			console.log("Skipping: language 'rst' not available");
			return;
		}
		const ptr = getLanguagePtr("rst");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rtf", () => {
		// Smoke test: load rtf and parse a simple snippet
		if (!hasLanguage("rtf")) {
			console.log("Skipping: language 'rtf' not available");
			return;
		}
		const ptr = getLanguagePtr("rtf");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ruby", () => {
		// Smoke test: load ruby and parse a simple snippet
		if (!hasLanguage("ruby")) {
			console.log("Skipping: language 'ruby' not available");
			return;
		}
		const ptr = getLanguagePtr("ruby");
		expect(ptr).toBeTruthy();
	});

	it("smoke_rust", () => {
		// Smoke test: load rust and parse a simple snippet
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const ptr = getLanguagePtr("rust");
		expect(ptr).toBeTruthy();
	});

	it("smoke_scala", () => {
		// Smoke test: load scala and parse a simple snippet
		if (!hasLanguage("scala")) {
			console.log("Skipping: language 'scala' not available");
			return;
		}
		const ptr = getLanguagePtr("scala");
		expect(ptr).toBeTruthy();
	});

	it("smoke_scheme", () => {
		// Smoke test: load scheme and parse a simple snippet
		if (!hasLanguage("scheme")) {
			console.log("Skipping: language 'scheme' not available");
			return;
		}
		const ptr = getLanguagePtr("scheme");
		expect(ptr).toBeTruthy();
	});

	it("smoke_scss", () => {
		// Smoke test: load scss and parse a simple snippet
		if (!hasLanguage("scss")) {
			console.log("Skipping: language 'scss' not available");
			return;
		}
		const ptr = getLanguagePtr("scss");
		expect(ptr).toBeTruthy();
	});

	it("smoke_slang", () => {
		// Smoke test: load slang and parse a simple snippet
		if (!hasLanguage("slang")) {
			console.log("Skipping: language 'slang' not available");
			return;
		}
		const ptr = getLanguagePtr("slang");
		expect(ptr).toBeTruthy();
	});

	it("smoke_smali", () => {
		// Smoke test: load smali and parse a simple snippet
		if (!hasLanguage("smali")) {
			console.log("Skipping: language 'smali' not available");
			return;
		}
		const ptr = getLanguagePtr("smali");
		expect(ptr).toBeTruthy();
	});

	it("smoke_smalltalk", () => {
		// Smoke test: load smalltalk and parse a simple snippet
		if (!hasLanguage("smalltalk")) {
			console.log("Skipping: language 'smalltalk' not available");
			return;
		}
		const ptr = getLanguagePtr("smalltalk");
		expect(ptr).toBeTruthy();
	});

	it("smoke_smithy", () => {
		// Smoke test: load smithy and parse a simple snippet
		if (!hasLanguage("smithy")) {
			console.log("Skipping: language 'smithy' not available");
			return;
		}
		const ptr = getLanguagePtr("smithy");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sml", () => {
		// Smoke test: load sml and parse a simple snippet
		if (!hasLanguage("sml")) {
			console.log("Skipping: language 'sml' not available");
			return;
		}
		const ptr = getLanguagePtr("sml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_snakemake", () => {
		// Smoke test: load snakemake and parse a simple snippet
		if (!hasLanguage("snakemake")) {
			console.log("Skipping: language 'snakemake' not available");
			return;
		}
		const ptr = getLanguagePtr("snakemake");
		expect(ptr).toBeTruthy();
	});

	it("smoke_solidity", () => {
		// Smoke test: load solidity and parse a simple snippet
		if (!hasLanguage("solidity")) {
			console.log("Skipping: language 'solidity' not available");
			return;
		}
		const ptr = getLanguagePtr("solidity");
		expect(ptr).toBeTruthy();
	});

	it("smoke_souffle", () => {
		// Smoke test: load souffle and parse a simple snippet
		if (!hasLanguage("souffle")) {
			console.log("Skipping: language 'souffle' not available");
			return;
		}
		const ptr = getLanguagePtr("souffle");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sourcepawn", () => {
		// Smoke test: load sourcepawn and parse a simple snippet
		if (!hasLanguage("sourcepawn")) {
			console.log("Skipping: language 'sourcepawn' not available");
			return;
		}
		const ptr = getLanguagePtr("sourcepawn");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sparql", () => {
		// Smoke test: load sparql and parse a simple snippet
		if (!hasLanguage("sparql")) {
			console.log("Skipping: language 'sparql' not available");
			return;
		}
		const ptr = getLanguagePtr("sparql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sql", () => {
		// Smoke test: load sql and parse a simple snippet
		if (!hasLanguage("sql")) {
			console.log("Skipping: language 'sql' not available");
			return;
		}
		const ptr = getLanguagePtr("sql");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sql_bigquery", () => {
		// Smoke test: load sql_bigquery and parse a simple snippet
		if (!hasLanguage("sql_bigquery")) {
			console.log("Skipping: language 'sql_bigquery' not available");
			return;
		}
		const ptr = getLanguagePtr("sql_bigquery");
		expect(ptr).toBeTruthy();
	});

	it("smoke_squirrel", () => {
		// Smoke test: load squirrel and parse a simple snippet
		if (!hasLanguage("squirrel")) {
			console.log("Skipping: language 'squirrel' not available");
			return;
		}
		const ptr = getLanguagePtr("squirrel");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ssh_config", () => {
		// Smoke test: load ssh_config and parse a simple snippet
		if (!hasLanguage("ssh_config")) {
			console.log("Skipping: language 'ssh_config' not available");
			return;
		}
		const ptr = getLanguagePtr("ssh_config");
		expect(ptr).toBeTruthy();
	});

	it("smoke_stan", () => {
		// Smoke test: load stan and parse a simple snippet
		if (!hasLanguage("stan")) {
			console.log("Skipping: language 'stan' not available");
			return;
		}
		const ptr = getLanguagePtr("stan");
		expect(ptr).toBeTruthy();
	});

	it("smoke_starlark", () => {
		// Smoke test: load starlark and parse a simple snippet
		if (!hasLanguage("starlark")) {
			console.log("Skipping: language 'starlark' not available");
			return;
		}
		const ptr = getLanguagePtr("starlark");
		expect(ptr).toBeTruthy();
	});

	it("smoke_superhtml", () => {
		// Smoke test: load superhtml and parse a simple snippet
		if (!hasLanguage("superhtml")) {
			console.log("Skipping: language 'superhtml' not available");
			return;
		}
		const ptr = getLanguagePtr("superhtml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_svelte", () => {
		// Smoke test: load svelte and parse a simple snippet
		if (!hasLanguage("svelte")) {
			console.log("Skipping: language 'svelte' not available");
			return;
		}
		const ptr = getLanguagePtr("svelte");
		expect(ptr).toBeTruthy();
	});

	it("smoke_sway", () => {
		// Smoke test: load sway and parse a simple snippet
		if (!hasLanguage("sway")) {
			console.log("Skipping: language 'sway' not available");
			return;
		}
		const ptr = getLanguagePtr("sway");
		expect(ptr).toBeTruthy();
	});

	it("smoke_swift", () => {
		// Smoke test: load swift and parse a simple snippet
		if (!hasLanguage("swift")) {
			console.log("Skipping: language 'swift' not available");
			return;
		}
		const ptr = getLanguagePtr("swift");
		expect(ptr).toBeTruthy();
	});

	it("smoke_systemverilog", () => {
		// Smoke test: load systemverilog and parse a simple snippet
		if (!hasLanguage("systemverilog")) {
			console.log("Skipping: language 'systemverilog' not available");
			return;
		}
		const ptr = getLanguagePtr("systemverilog");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tablegen", () => {
		// Smoke test: load tablegen and parse a simple snippet
		if (!hasLanguage("tablegen")) {
			console.log("Skipping: language 'tablegen' not available");
			return;
		}
		const ptr = getLanguagePtr("tablegen");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tact", () => {
		// Smoke test: load tact and parse a simple snippet
		if (!hasLanguage("tact")) {
			console.log("Skipping: language 'tact' not available");
			return;
		}
		const ptr = getLanguagePtr("tact");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tcl", () => {
		// Smoke test: load tcl and parse a simple snippet
		if (!hasLanguage("tcl")) {
			console.log("Skipping: language 'tcl' not available");
			return;
		}
		const ptr = getLanguagePtr("tcl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_teal", () => {
		// Smoke test: load teal and parse a simple snippet
		if (!hasLanguage("teal")) {
			console.log("Skipping: language 'teal' not available");
			return;
		}
		const ptr = getLanguagePtr("teal");
		expect(ptr).toBeTruthy();
	});

	it("smoke_templ", () => {
		// Smoke test: load templ and parse a simple snippet
		if (!hasLanguage("templ")) {
			console.log("Skipping: language 'templ' not available");
			return;
		}
		const ptr = getLanguagePtr("templ");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tera", () => {
		// Smoke test: load tera and parse a simple snippet
		if (!hasLanguage("tera")) {
			console.log("Skipping: language 'tera' not available");
			return;
		}
		const ptr = getLanguagePtr("tera");
		expect(ptr).toBeTruthy();
	});

	it("smoke_terraform", () => {
		// Smoke test: load terraform and parse a simple snippet
		if (!hasLanguage("terraform")) {
			console.log("Skipping: language 'terraform' not available");
			return;
		}
		const ptr = getLanguagePtr("terraform");
		expect(ptr).toBeTruthy();
	});

	it("smoke_test", () => {
		// Smoke test: load test and parse a simple snippet
		if (!hasLanguage("test")) {
			console.log("Skipping: language 'test' not available");
			return;
		}
		const ptr = getLanguagePtr("test");
		expect(ptr).toBeTruthy();
	});

	it("smoke_textproto", () => {
		// Smoke test: load textproto and parse a simple snippet
		if (!hasLanguage("textproto")) {
			console.log("Skipping: language 'textproto' not available");
			return;
		}
		const ptr = getLanguagePtr("textproto");
		expect(ptr).toBeTruthy();
	});

	it("smoke_thrift", () => {
		// Smoke test: load thrift and parse a simple snippet
		if (!hasLanguage("thrift")) {
			console.log("Skipping: language 'thrift' not available");
			return;
		}
		const ptr = getLanguagePtr("thrift");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tlaplus", () => {
		// Smoke test: load tlaplus and parse a simple snippet
		if (!hasLanguage("tlaplus")) {
			console.log("Skipping: language 'tlaplus' not available");
			return;
		}
		const ptr = getLanguagePtr("tlaplus");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tmux", () => {
		// Smoke test: load tmux and parse a simple snippet
		if (!hasLanguage("tmux")) {
			console.log("Skipping: language 'tmux' not available");
			return;
		}
		const ptr = getLanguagePtr("tmux");
		expect(ptr).toBeTruthy();
	});

	it("smoke_todotxt", () => {
		// Smoke test: load todotxt and parse a simple snippet
		if (!hasLanguage("todotxt")) {
			console.log("Skipping: language 'todotxt' not available");
			return;
		}
		const ptr = getLanguagePtr("todotxt");
		expect(ptr).toBeTruthy();
	});

	it("smoke_toml", () => {
		// Smoke test: load toml and parse a simple snippet
		if (!hasLanguage("toml")) {
			console.log("Skipping: language 'toml' not available");
			return;
		}
		const ptr = getLanguagePtr("toml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tsv", () => {
		// Smoke test: load tsv and parse a simple snippet
		if (!hasLanguage("tsv")) {
			console.log("Skipping: language 'tsv' not available");
			return;
		}
		const ptr = getLanguagePtr("tsv");
		expect(ptr).toBeTruthy();
	});

	it("smoke_tsx", () => {
		// Smoke test: load tsx and parse a simple snippet
		if (!hasLanguage("tsx")) {
			console.log("Skipping: language 'tsx' not available");
			return;
		}
		const ptr = getLanguagePtr("tsx");
		expect(ptr).toBeTruthy();
	});

	it("smoke_turtle", () => {
		// Smoke test: load turtle and parse a simple snippet
		if (!hasLanguage("turtle")) {
			console.log("Skipping: language 'turtle' not available");
			return;
		}
		const ptr = getLanguagePtr("turtle");
		expect(ptr).toBeTruthy();
	});

	it("smoke_twig", () => {
		// Smoke test: load twig and parse a simple snippet
		if (!hasLanguage("twig")) {
			console.log("Skipping: language 'twig' not available");
			return;
		}
		const ptr = getLanguagePtr("twig");
		expect(ptr).toBeTruthy();
	});

	it("smoke_typescript", () => {
		// Smoke test: load typescript and parse a simple snippet
		if (!hasLanguage("typescript")) {
			console.log("Skipping: language 'typescript' not available");
			return;
		}
		const ptr = getLanguagePtr("typescript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_typespec", () => {
		// Smoke test: load typespec and parse a simple snippet
		if (!hasLanguage("typespec")) {
			console.log("Skipping: language 'typespec' not available");
			return;
		}
		const ptr = getLanguagePtr("typespec");
		expect(ptr).toBeTruthy();
	});

	it("smoke_typoscript", () => {
		// Smoke test: load typoscript and parse a simple snippet
		if (!hasLanguage("typoscript")) {
			console.log("Skipping: language 'typoscript' not available");
			return;
		}
		const ptr = getLanguagePtr("typoscript");
		expect(ptr).toBeTruthy();
	});

	it("smoke_typst", () => {
		// Smoke test: load typst and parse a simple snippet
		if (!hasLanguage("typst")) {
			console.log("Skipping: language 'typst' not available");
			return;
		}
		const ptr = getLanguagePtr("typst");
		expect(ptr).toBeTruthy();
	});

	it("smoke_udev", () => {
		// Smoke test: load udev and parse a simple snippet
		if (!hasLanguage("udev")) {
			console.log("Skipping: language 'udev' not available");
			return;
		}
		const ptr = getLanguagePtr("udev");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ungrammar", () => {
		// Smoke test: load ungrammar and parse a simple snippet
		if (!hasLanguage("ungrammar")) {
			console.log("Skipping: language 'ungrammar' not available");
			return;
		}
		const ptr = getLanguagePtr("ungrammar");
		expect(ptr).toBeTruthy();
	});

	it("smoke_uxntal", () => {
		// Smoke test: load uxntal and parse a simple snippet
		if (!hasLanguage("uxntal")) {
			console.log("Skipping: language 'uxntal' not available");
			return;
		}
		const ptr = getLanguagePtr("uxntal");
		expect(ptr).toBeTruthy();
	});

	it("smoke_v", () => {
		// Smoke test: load v and parse a simple snippet
		if (!hasLanguage("v")) {
			console.log("Skipping: language 'v' not available");
			return;
		}
		const ptr = getLanguagePtr("v");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vb", () => {
		// Smoke test: load vb and parse a simple snippet
		if (!hasLanguage("vb")) {
			console.log("Skipping: language 'vb' not available");
			return;
		}
		const ptr = getLanguagePtr("vb");
		expect(ptr).toBeTruthy();
	});

	it("smoke_verilog", () => {
		// Smoke test: load verilog and parse a simple snippet
		if (!hasLanguage("verilog")) {
			console.log("Skipping: language 'verilog' not available");
			return;
		}
		const ptr = getLanguagePtr("verilog");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vhdl", () => {
		// Smoke test: load vhdl and parse a simple snippet
		if (!hasLanguage("vhdl")) {
			console.log("Skipping: language 'vhdl' not available");
			return;
		}
		const ptr = getLanguagePtr("vhdl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vhs", () => {
		// Smoke test: load vhs and parse a simple snippet
		if (!hasLanguage("vhs")) {
			console.log("Skipping: language 'vhs' not available");
			return;
		}
		const ptr = getLanguagePtr("vhs");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vim", () => {
		// Smoke test: load vim and parse a simple snippet
		if (!hasLanguage("vim")) {
			console.log("Skipping: language 'vim' not available");
			return;
		}
		const ptr = getLanguagePtr("vim");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vimdoc", () => {
		// Smoke test: load vimdoc and parse a simple snippet
		if (!hasLanguage("vimdoc")) {
			console.log("Skipping: language 'vimdoc' not available");
			return;
		}
		const ptr = getLanguagePtr("vimdoc");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vrl", () => {
		// Smoke test: load vrl and parse a simple snippet
		if (!hasLanguage("vrl")) {
			console.log("Skipping: language 'vrl' not available");
			return;
		}
		const ptr = getLanguagePtr("vrl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_vue", () => {
		// Smoke test: load vue and parse a simple snippet
		if (!hasLanguage("vue")) {
			console.log("Skipping: language 'vue' not available");
			return;
		}
		const ptr = getLanguagePtr("vue");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wast", () => {
		// Smoke test: load wast and parse a simple snippet
		if (!hasLanguage("wast")) {
			console.log("Skipping: language 'wast' not available");
			return;
		}
		const ptr = getLanguagePtr("wast");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wat", () => {
		// Smoke test: load wat and parse a simple snippet
		if (!hasLanguage("wat")) {
			console.log("Skipping: language 'wat' not available");
			return;
		}
		const ptr = getLanguagePtr("wat");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wgsl", () => {
		// Smoke test: load wgsl and parse a simple snippet
		if (!hasLanguage("wgsl")) {
			console.log("Skipping: language 'wgsl' not available");
			return;
		}
		const ptr = getLanguagePtr("wgsl");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wgsl_bevy", () => {
		// Smoke test: load wgsl_bevy and parse a simple snippet
		if (!hasLanguage("wgsl_bevy")) {
			console.log("Skipping: language 'wgsl_bevy' not available");
			return;
		}
		const ptr = getLanguagePtr("wgsl_bevy");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wit", () => {
		// Smoke test: load wit and parse a simple snippet
		if (!hasLanguage("wit")) {
			console.log("Skipping: language 'wit' not available");
			return;
		}
		const ptr = getLanguagePtr("wit");
		expect(ptr).toBeTruthy();
	});

	it("smoke_wolfram", () => {
		// Smoke test: load wolfram and parse a simple snippet
		if (!hasLanguage("wolfram")) {
			console.log("Skipping: language 'wolfram' not available");
			return;
		}
		const ptr = getLanguagePtr("wolfram");
		expect(ptr).toBeTruthy();
	});

	it("smoke_x86asm", () => {
		// Smoke test: load x86asm and parse a simple snippet
		if (!hasLanguage("x86asm")) {
			console.log("Skipping: language 'x86asm' not available");
			return;
		}
		const ptr = getLanguagePtr("x86asm");
		expect(ptr).toBeTruthy();
	});

	it("smoke_xcompose", () => {
		// Smoke test: load xcompose and parse a simple snippet
		if (!hasLanguage("xcompose")) {
			console.log("Skipping: language 'xcompose' not available");
			return;
		}
		const ptr = getLanguagePtr("xcompose");
		expect(ptr).toBeTruthy();
	});

	it("smoke_xml", () => {
		// Smoke test: load xml and parse a simple snippet
		if (!hasLanguage("xml")) {
			console.log("Skipping: language 'xml' not available");
			return;
		}
		const ptr = getLanguagePtr("xml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_yaml", () => {
		// Smoke test: load yaml and parse a simple snippet
		if (!hasLanguage("yaml")) {
			console.log("Skipping: language 'yaml' not available");
			return;
		}
		const ptr = getLanguagePtr("yaml");
		expect(ptr).toBeTruthy();
	});

	it("smoke_yuck", () => {
		// Smoke test: load yuck and parse a simple snippet
		if (!hasLanguage("yuck")) {
			console.log("Skipping: language 'yuck' not available");
			return;
		}
		const ptr = getLanguagePtr("yuck");
		expect(ptr).toBeTruthy();
	});

	it("smoke_zig", () => {
		// Smoke test: load zig and parse a simple snippet
		if (!hasLanguage("zig")) {
			console.log("Skipping: language 'zig' not available");
			return;
		}
		const ptr = getLanguagePtr("zig");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ziggy", () => {
		// Smoke test: load ziggy and parse a simple snippet
		if (!hasLanguage("ziggy")) {
			console.log("Skipping: language 'ziggy' not available");
			return;
		}
		const ptr = getLanguagePtr("ziggy");
		expect(ptr).toBeTruthy();
	});

	it("smoke_ziggy_schema", () => {
		// Smoke test: load ziggy_schema and parse a simple snippet
		if (!hasLanguage("ziggy_schema")) {
			console.log("Skipping: language 'ziggy_schema' not available");
			return;
		}
		const ptr = getLanguagePtr("ziggy_schema");
		expect(ptr).toBeTruthy();
	});

	it("smoke_zsh", () => {
		// Smoke test: load zsh and parse a simple snippet
		if (!hasLanguage("zsh")) {
			console.log("Skipping: language 'zsh' not available");
			return;
		}
		const ptr = getLanguagePtr("zsh");
		expect(ptr).toBeTruthy();
	});
});
