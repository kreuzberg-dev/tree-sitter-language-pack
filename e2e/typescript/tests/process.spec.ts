// Code generated by e2e-generator. DO NOT EDIT.

import { describe, it, expect } from "vitest";
import {
	availableLanguages,
	hasLanguage,
	getLanguagePtr,
	process,
	detectLanguageFromExtension,
	detectLanguageFromPath,
	detectLanguageFromContent,
	extensionAmbiguity,
	getHighlightsQuery,
} from "./helpers";

describe("process", () => {
	it("c_function_process", () => {
		// Intel: C function with include
		if (!hasLanguage("c")) {
			console.log("Skipping: language 'c' not available");
			return;
		}
		const intel = process('#include <stdio.h>\n\nint main() {\n    printf("hello");\n    return 0;\n}\n', {
			language: "c",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("c");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("config_all_python", () => {
		// Intel: process with all features enabled
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			'# A comment\ndef greet(name):\n    """Say hello."""\n    return f\'Hi {name}\'\n\nimport os\n',
			{
				language: "python",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("config_minimal_python", () => {
		// Intel: process with minimal config - only metrics
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def hello():\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(2);
	});

	it("go_function_process", () => {
		// Intel: extract structure from Go function definition
		if (!hasLanguage("go")) {
			console.log("Skipping: language 'go' not available");
			return;
		}
		const intel = process('package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n', {
			language: "go",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("go");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("go_function_process_detail", () => {
		// Intel: extract structure from Go function definition
		if (!hasLanguage("go")) {
			console.log("Skipping: language 'go' not available");
			return;
		}
		const intel = process('package main\n\nimport "fmt"\n\nfunc main() {\n\tfmt.Println("hello")\n}\n', {
			language: "go",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("go");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("java_class_process", () => {
		// Intel: Java class with methods and imports
		if (!hasLanguage("java")) {
			console.log("Skipping: language 'java' not available");
			return;
		}
		const intel = process(
			'import java.util.List;\n\npublic class Greeter {\n    public String greet(String name) {\n        return "Hello " + name;\n    }\n}\n',
			{
				language: "java",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("java");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Class")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("javascript_multi_import_process", () => {
		// Intel: detect multiple imports and function in JavaScript
		if (!hasLanguage("javascript")) {
			console.log("Skipping: language 'javascript' not available");
			return;
		}
		const intel = process(
			"import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n",
			{
				language: "javascript",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("javascript");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(2);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("javascript_multi_import_process_detail", () => {
		// Intel: detect multiple imports and function in JavaScript
		if (!hasLanguage("javascript")) {
			console.log("Skipping: language 'javascript' not available");
			return;
		}
		const intel = process(
			"import fs from 'fs';\nimport path from 'path';\n\nfunction process(input) {\n    return input.trim();\n}\n",
			{
				language: "javascript",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("javascript");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(2);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("process_javascript_exports_detail", () => {
		// JavaScript with exports, verify export count
		if (!hasLanguage("javascript")) {
			console.log("Skipping: language 'javascript' not available");
			return;
		}
		const intel = process(
			"export function greet(name) {\n  return \`Hello \${name}\`;\n}\n\nexport const VERSION = '1.0';\n",
			{
				language: "javascript",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("javascript");
		expect((intel.exports || []).length).toBeGreaterThanOrEqual(1);
	});

	it("process_python_comments", () => {
		// Python with comments, verify comment count
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("# This is a comment\n# Another comment\ndef hello():\n    # inline comment\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.comments || []).length).toBeGreaterThanOrEqual(1);
	});

	it("process_python_imports_detail", () => {
		// Python with multiple imports, verify imports contain specific source
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(2);
		expect((intel.imports || []).some((i: any) => (i.source || "").includes("os"))).toBe(true);
	});

	it("process_python_metrics_detail", () => {
		// Python code with metrics assertions
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			"# module docstring\nimport os\n\ndef hello():\n    # greeting\n    print('hello')\n\ndef world():\n    print('world')\n",
			{
				language: "python",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("python");
		expect((intel.metrics || {}).codeLines || 0).toBeGreaterThanOrEqual(4);
		expect((intel.metrics || {}).commentLines || 0).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).maxDepth || 0).toBeGreaterThanOrEqual(1);
	});

	it("process_rust_structure_name", () => {
		// Rust struct with name, verify structure name contains value
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const intel = process(
			"pub struct MyConfig {\n    pub name: String,\n    pub value: i32,\n}\n\nimpl MyConfig {\n    pub fn new() -> Self {\n        Self { name: String::new(), value: 0 }\n    }\n}\n",
			{
				language: "rust",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("rust");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => (s.name || "").includes("MyConfig"))).toBe(true);
	});

	it("python_chunking_medium", () => {
		// Intel: Python code with medium chunk size
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			"def first():\n    x = 1\n    return x\n\ndef second():\n    y = 2\n    return y\n\ndef third():\n    z = 3\n    return z\n",
			{ language: "python", chunkMaxSize: 50 },
		);
		const chunks = intel.chunks || [];
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(3);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
		expect((chunks || []).length).toBeGreaterThanOrEqual(2);
	});

	it("python_chunking_process_detail", () => {
		// Intel: chunk multi-function Python source into multiple pieces
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			"def alpha():\n    pass\n\ndef beta():\n    pass\n\ndef gamma():\n    pass\n\ndef delta():\n    pass\n",
			{ language: "python", chunkMaxSize: 30 },
		);
		const chunks = intel.chunks || [];
		expect(intel.language).toBe("python");
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(8);
		expect((chunks || []).length).toBeGreaterThanOrEqual(2);
	});

	it("python_class_with_methods_process", () => {
		// Intel: extract nested structure from Python class with methods
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			"class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n",
			{
				language: "python",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Class")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("python_class_with_methods_process_detail", () => {
		// Intel: extract nested structure from Python class with methods
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process(
			"class Calculator:\n    def add(self, a, b):\n        return a + b\n\n    def subtract(self, a, b):\n        return a - b\n",
			{
				language: "python",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Class")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(6);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("python_error_diagnostics", () => {
		// Intel: Python code with syntax errors should report diagnostics
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def broken(\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.metrics || {}).errorCount || 0).toBe(1);
		expect((intel.diagnostics || []).length).toBeGreaterThan(0);
	});

	it("python_function_process", () => {
		// Intel: extract structure from Python function definition
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def greet(name):\n    return f'Hello, {name}!'\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(2);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("python_function_process_detail", () => {
		// Intel: extract structure from Python function definition
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def greet(name):\n    return f'Hello, {name}!'\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(2);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("python_malformed_code_process", () => {
		// Intel: detect diagnostics in malformed Python code
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def broken(\n    return\nclass", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.diagnostics || []).length).toBeGreaterThan(0);
	});

	it("python_malformed_code_process_detail", () => {
		// Intel: detect diagnostics in malformed Python code
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("def broken(\n    return\nclass", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.diagnostics || []).length).toBeGreaterThan(0);
	});

	it("python_multi_import_process", () => {
		// Intel: detect multiple Python imports
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(3);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(5);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("python_multi_import_process_detail", () => {
		// Intel: detect multiple Python imports
		if (!hasLanguage("python")) {
			console.log("Skipping: language 'python' not available");
			return;
		}
		const intel = process("import os\nimport sys\nfrom pathlib import Path\n\ndef main():\n    pass\n", {
			language: "python",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("python");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(3);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(5);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("ruby_class_process", () => {
		// Intel: Ruby class with method
		if (!hasLanguage("ruby")) {
			console.log("Skipping: language 'ruby' not available");
			return;
		}
		const intel = process("require 'json'\n\nclass Greeter\n  def greet(name)\n    \"Hello #{name}\"\n  end\nend\n", {
			language: "ruby",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("ruby");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Class")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("rust_chunking_process", () => {
		// Intel: chunk multi-function Rust source into pieces
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const intel = process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", {
			language: "rust",
			chunkMaxSize: 30,
		});
		const chunks = intel.chunks || [];
		expect(intel.language).toBe("rust");
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((chunks || []).length).toBeGreaterThanOrEqual(2);
	});

	it("rust_chunking_process_detail", () => {
		// Intel: chunk multi-function Rust source into pieces
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const intel = process("fn alpha() {}\n\nfn beta() {}\n\nfn gamma() {}\n\nfn delta() {}\n", {
			language: "rust",
			chunkMaxSize: 30,
		});
		const chunks = intel.chunks || [];
		expect(intel.language).toBe("rust");
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(7);
		expect((chunks || []).length).toBeGreaterThanOrEqual(2);
	});

	it("rust_function_process", () => {
		// Intel: extract structure from Rust function definition
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const intel = process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", {
			language: "rust",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("rust");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(3);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("rust_function_process_detail", () => {
		// Intel: extract structure from Rust function definition
		if (!hasLanguage("rust")) {
			console.log("Skipping: language 'rust' not available");
			return;
		}
		const intel = process("fn add(a: i32, b: i32) -> i32 {\n    a + b\n}\n", {
			language: "rust",
			structure: true,
			imports: true,
			exports: true,
			comments: true,
			docstrings: true,
			symbols: true,
			diagnostics: true,
		});
		expect(intel.language).toBe("rust");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(3);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("typescript_function_process", () => {
		// Intel: extract structure from TypeScript function
		if (!hasLanguage("typescript")) {
			console.log("Skipping: language 'typescript' not available");
			return;
		}
		const intel = process(
			"import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return \`Hello, \${name}!\`;\n}\n",
			{
				language: "typescript",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("typescript");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(5);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});

	it("typescript_function_process_detail", () => {
		// Intel: extract structure from TypeScript function
		if (!hasLanguage("typescript")) {
			console.log("Skipping: language 'typescript' not available");
			return;
		}
		const intel = process(
			"import { readFile } from 'fs';\n\nfunction greet(name: string): string {\n    return \`Hello, \${name}!\`;\n}\n",
			{
				language: "typescript",
				structure: true,
				imports: true,
				exports: true,
				comments: true,
				docstrings: true,
				symbols: true,
				diagnostics: true,
			},
		);
		expect(intel.language).toBe("typescript");
		expect((intel.structure || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.structure || []).some((s: any) => s.kind === "Function")).toBe(true);
		expect((intel.imports || []).length).toBeGreaterThanOrEqual(1);
		expect((intel.metrics || {}).totalLines || 0).toBeGreaterThanOrEqual(5);
		expect((intel.metrics || {}).errorCount || 0).toBe(0);
	});
});
