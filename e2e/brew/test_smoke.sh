#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: smoke
set -euo pipefail

test_smoke_abl() {
    # Smoke test: load abl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_actionscript() {
    # Smoke test: load actionscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ada() {
    # Smoke test: load ada and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_agda() {
    # Smoke test: load agda and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_al() {
    # Smoke test: load al and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_angular() {
    # Smoke test: load angular and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_apex() {
    # Smoke test: load apex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_arduino() {
    # Smoke test: load arduino and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_asciidoc() {
    # Smoke test: load asciidoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_asm() {
    # Smoke test: load asm and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_astro() {
    # Smoke test: load astro and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_awk() {
    # Smoke test: load awk and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bash() {
    # Smoke test: load bash and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bass() {
    # Smoke test: load bass and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_batch() {
    # Smoke test: load batch and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_beancount() {
    # Smoke test: load beancount and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bibtex() {
    # Smoke test: load bibtex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bicep() {
    # Smoke test: load bicep and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bitbake() {
    # Smoke test: load bitbake and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_blade() {
    # Smoke test: load blade and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_brightscript() {
    # Smoke test: load brightscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_bsl() {
    # Smoke test: load bsl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_c() {
    # Smoke test: load c and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_c3() {
    # Smoke test: load c3 and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_caddy() {
    # Smoke test: load caddy and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cairo() {
    # Smoke test: load cairo and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_capnp() {
    # Smoke test: load capnp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cedar() {
    # Smoke test: load cedar and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cedarschema() {
    # Smoke test: load cedarschema and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cel() {
    # Smoke test: load cel and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cfml() {
    # Smoke test: load cfml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_chatito() {
    # Smoke test: load chatito and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_chuck() {
    # Smoke test: load chuck and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_circom() {
    # Smoke test: load circom and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_clarity() {
    # Smoke test: load clarity and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_clojure() {
    # Smoke test: load clojure and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cmake() {
    # Smoke test: load cmake and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cobol() {
    # Smoke test: load cobol and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_comment() {
    # Smoke test: load comment and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_commonlisp() {
    # Smoke test: load commonlisp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cooklang() {
    # Smoke test: load cooklang and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_corn() {
    # Smoke test: load corn and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cpon() {
    # Smoke test: load cpon and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cpp() {
    # Smoke test: load cpp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_crystal() {
    # Smoke test: load crystal and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_csharp() {
    # Smoke test: load csharp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_css() {
    # Smoke test: load css and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cst() {
    # Smoke test: load cst and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_csv() {
    # Smoke test: load csv and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cuda() {
    # Smoke test: load cuda and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cue() {
    # Smoke test: load cue and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_cylc() {
    # Smoke test: load cylc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_d() {
    # Smoke test: load d and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_dart() {
    # Smoke test: load dart and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_desktop() {
    # Smoke test: load desktop and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_devicetree() {
    # Smoke test: load devicetree and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_dhall() {
    # Smoke test: load dhall and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_diff() {
    # Smoke test: load diff and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_djot() {
    # Smoke test: load djot and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_dockerfile() {
    # Smoke test: load dockerfile and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_dot() {
    # Smoke test: load dot and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_doxygen() {
    # Smoke test: load doxygen and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_dtd() {
    # Smoke test: load dtd and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_earthfile() {
    # Smoke test: load earthfile and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ebnf() {
    # Smoke test: load ebnf and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_editorconfig() {
    # Smoke test: load editorconfig and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_eds() {
    # Smoke test: load eds and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_eex() {
    # Smoke test: load eex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_elisp() {
    # Smoke test: load elisp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_elixir() {
    # Smoke test: load elixir and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_elm() {
    # Smoke test: load elm and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_elsa() {
    # Smoke test: load elsa and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_elvish() {
    # Smoke test: load elvish and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_embeddedtemplate() {
    # Smoke test: load embeddedtemplate and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_enforce() {
    # Smoke test: load enforce and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_erlang() {
    # Smoke test: load erlang and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_facility() {
    # Smoke test: load facility and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_faust() {
    # Smoke test: load faust and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fennel() {
    # Smoke test: load fennel and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fidl() {
    # Smoke test: load fidl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_firrtl() {
    # Smoke test: load firrtl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fish() {
    # Smoke test: load fish and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_foam() {
    # Smoke test: load foam and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_forth() {
    # Smoke test: load forth and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fortran() {
    # Smoke test: load fortran and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fsharp() {
    # Smoke test: load fsharp and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_fsharp_signature() {
    # Smoke test: load fsharp_signature and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_func() {
    # Smoke test: load func and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gap() {
    # Smoke test: load gap and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gdscript() {
    # Smoke test: load gdscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gdshader() {
    # Smoke test: load gdshader and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_git_config() {
    # Smoke test: load git_config and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_git_rebase() {
    # Smoke test: load git_rebase and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gitattributes() {
    # Smoke test: load gitattributes and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gitcommit() {
    # Smoke test: load gitcommit and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gitignore() {
    # Smoke test: load gitignore and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gleam() {
    # Smoke test: load gleam and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_glimmer() {
    # Smoke test: load glimmer and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_glsl() {
    # Smoke test: load glsl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gn() {
    # Smoke test: load gn and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gnuplot() {
    # Smoke test: load gnuplot and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_go() {
    # Smoke test: load go and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_godot_resource() {
    # Smoke test: load godot_resource and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gomod() {
    # Smoke test: load gomod and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gosum() {
    # Smoke test: load gosum and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gotmpl() {
    # Smoke test: load gotmpl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gowork() {
    # Smoke test: load gowork and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gpg() {
    # Smoke test: load gpg and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_graphql() {
    # Smoke test: load graphql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gren() {
    # Smoke test: load gren and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_groovy() {
    # Smoke test: load groovy and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_gstlaunch() {
    # Smoke test: load gstlaunch and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hack() {
    # Smoke test: load hack and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hare() {
    # Smoke test: load hare and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_haskell() {
    # Smoke test: load haskell and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_haxe() {
    # Smoke test: load haxe and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hcl() {
    # Smoke test: load hcl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_heex() {
    # Smoke test: load heex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hjson() {
    # Smoke test: load hjson and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hlsl() {
    # Smoke test: load hlsl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hocon() {
    # Smoke test: load hocon and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hoon() {
    # Smoke test: load hoon and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_html() {
    # Smoke test: load html and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_htmldjango() {
    # Smoke test: load htmldjango and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_http() {
    # Smoke test: load http and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hurl() {
    # Smoke test: load hurl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_hyprlang() {
    # Smoke test: load hyprlang and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_idris() {
    # Smoke test: load idris and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ini() {
    # Smoke test: load ini and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ispc() {
    # Smoke test: load ispc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_jai() {
    # Smoke test: load jai and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_janet() {
    # Smoke test: load janet and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_java() {
    # Smoke test: load java and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_javadoc() {
    # Smoke test: load javadoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_javascript() {
    # Smoke test: load javascript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_jinja2() {
    # Smoke test: load jinja2 and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_jq() {
    # Smoke test: load jq and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_jsdoc() {
    # Smoke test: load jsdoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_json() {
    # Smoke test: load json and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_json5() {
    # Smoke test: load json5 and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_jsonnet() {
    # Smoke test: load jsonnet and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_julia() {
    # Smoke test: load julia and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_just() {
    # Smoke test: load just and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_kcl() {
    # Smoke test: load kcl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_kconfig() {
    # Smoke test: load kconfig and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_kdl() {
    # Smoke test: load kdl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_kotlin() {
    # Smoke test: load kotlin and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_latex() {
    # Smoke test: load latex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_lean() {
    # Smoke test: load lean and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ledger() {
    # Smoke test: load ledger and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_less() {
    # Smoke test: load less and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_linkerscript() {
    # Smoke test: load linkerscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_liquid() {
    # Smoke test: load liquid and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_llvm() {
    # Smoke test: load llvm and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_lua() {
    # Smoke test: load lua and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_luadoc() {
    # Smoke test: load luadoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_luap() {
    # Smoke test: load luap and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_luau() {
    # Smoke test: load luau and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_magik() {
    # Smoke test: load magik and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_make() {
    # Smoke test: load make and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_markdown() {
    # Smoke test: load markdown and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_markdown_inline() {
    # Smoke test: load markdown_inline and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_matlab() {
    # Smoke test: load matlab and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_mermaid() {
    # Smoke test: load mermaid and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_meson() {
    # Smoke test: load meson and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_mlir() {
    # Smoke test: load mlir and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_mojo() {
    # Smoke test: load mojo and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_move() {
    # Smoke test: load move and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nasm() {
    # Smoke test: load nasm and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_netlinx() {
    # Smoke test: load netlinx and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nginx() {
    # Smoke test: load nginx and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nickel() {
    # Smoke test: load nickel and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nim() {
    # Smoke test: load nim and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ninja() {
    # Smoke test: load ninja and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nix() {
    # Smoke test: load nix and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_norg() {
    # Smoke test: load norg and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_norg_meta() {
    # Smoke test: load norg_meta and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nqc() {
    # Smoke test: load nqc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_nushell() {
    # Smoke test: load nushell and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_objc() {
    # Smoke test: load objc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ocaml() {
    # Smoke test: load ocaml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ocaml_interface() {
    # Smoke test: load ocaml_interface and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ocamllex() {
    # Smoke test: load ocamllex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_odin() {
    # Smoke test: load odin and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_openscad() {
    # Smoke test: load openscad and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_org() {
    # Smoke test: load org and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pascal() {
    # Smoke test: load pascal and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pem() {
    # Smoke test: load pem and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_perl() {
    # Smoke test: load perl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pgn() {
    # Smoke test: load pgn and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_php() {
    # Smoke test: load php and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_phpdoc() {
    # Smoke test: load phpdoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pkl() {
    # Smoke test: load pkl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_po() {
    # Smoke test: load po and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_poe_filter() {
    # Smoke test: load poe_filter and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pony() {
    # Smoke test: load pony and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_postscript() {
    # Smoke test: load postscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_powershell() {
    # Smoke test: load powershell and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_printf() {
    # Smoke test: load printf and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_prisma() {
    # Smoke test: load prisma and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_prolog() {
    # Smoke test: load prolog and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_promql() {
    # Smoke test: load promql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_properties() {
    # Smoke test: load properties and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_proto() {
    # Smoke test: load proto and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_prql() {
    # Smoke test: load prql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_psv() {
    # Smoke test: load psv and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pug() {
    # Smoke test: load pug and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_puppet() {
    # Smoke test: load puppet and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_purescript() {
    # Smoke test: load purescript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_pymanifest() {
    # Smoke test: load pymanifest and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_python() {
    # Smoke test: load python and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ql() {
    # Smoke test: load ql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_qmldir() {
    # Smoke test: load qmldir and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_qmljs() {
    # Smoke test: load qmljs and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_query() {
    # Smoke test: load query and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_r() {
    # Smoke test: load r and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_racket() {
    # Smoke test: load racket and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rasi() {
    # Smoke test: load rasi and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_razor() {
    # Smoke test: load razor and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rbs() {
    # Smoke test: load rbs and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_re2c() {
    # Smoke test: load re2c and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_readline() {
    # Smoke test: load readline and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_regex() {
    # Smoke test: load regex and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rego() {
    # Smoke test: load rego and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_requirements() {
    # Smoke test: load requirements and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rescript() {
    # Smoke test: load rescript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_robot() {
    # Smoke test: load robot and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_roc() {
    # Smoke test: load roc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ron() {
    # Smoke test: load ron and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rst() {
    # Smoke test: load rst and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rtf() {
    # Smoke test: load rtf and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ruby() {
    # Smoke test: load ruby and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_rust() {
    # Smoke test: load rust and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_scala() {
    # Smoke test: load scala and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_scheme() {
    # Smoke test: load scheme and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_scss() {
    # Smoke test: load scss and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_slang() {
    # Smoke test: load slang and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_smali() {
    # Smoke test: load smali and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_smalltalk() {
    # Smoke test: load smalltalk and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_smithy() {
    # Smoke test: load smithy and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sml() {
    # Smoke test: load sml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_snakemake() {
    # Smoke test: load snakemake and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_solidity() {
    # Smoke test: load solidity and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_souffle() {
    # Smoke test: load souffle and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sourcepawn() {
    # Smoke test: load sourcepawn and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sparql() {
    # Smoke test: load sparql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sql() {
    # Smoke test: load sql and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sql_bigquery() {
    # Smoke test: load sql_bigquery and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_squirrel() {
    # Smoke test: load squirrel and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ssh_config() {
    # Smoke test: load ssh_config and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_stan() {
    # Smoke test: load stan and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_starlark() {
    # Smoke test: load starlark and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_superhtml() {
    # Smoke test: load superhtml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_svelte() {
    # Smoke test: load svelte and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_sway() {
    # Smoke test: load sway and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_swift() {
    # Smoke test: load swift and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_systemverilog() {
    # Smoke test: load systemverilog and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tablegen() {
    # Smoke test: load tablegen and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tact() {
    # Smoke test: load tact and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tcl() {
    # Smoke test: load tcl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_teal() {
    # Smoke test: load teal and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_templ() {
    # Smoke test: load templ and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tera() {
    # Smoke test: load tera and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_terraform() {
    # Smoke test: load terraform and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_test() {
    # Smoke test: load test and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_textproto() {
    # Smoke test: load textproto and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_thrift() {
    # Smoke test: load thrift and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tlaplus() {
    # Smoke test: load tlaplus and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tmux() {
    # Smoke test: load tmux and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_todotxt() {
    # Smoke test: load todotxt and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_toml() {
    # Smoke test: load toml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tsv() {
    # Smoke test: load tsv and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_tsx() {
    # Smoke test: load tsx and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_turtle() {
    # Smoke test: load turtle and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_twig() {
    # Smoke test: load twig and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_typescript() {
    # Smoke test: load typescript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_typespec() {
    # Smoke test: load typespec and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_typoscript() {
    # Smoke test: load typoscript and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_typst() {
    # Smoke test: load typst and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_udev() {
    # Smoke test: load udev and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ungrammar() {
    # Smoke test: load ungrammar and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_uxntal() {
    # Smoke test: load uxntal and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_v() {
    # Smoke test: load v and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vb() {
    # Smoke test: load vb and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_verilog() {
    # Smoke test: load verilog and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vhdl() {
    # Smoke test: load vhdl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vhs() {
    # Smoke test: load vhs and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vim() {
    # Smoke test: load vim and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vimdoc() {
    # Smoke test: load vimdoc and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vrl() {
    # Smoke test: load vrl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_vue() {
    # Smoke test: load vue and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wast() {
    # Smoke test: load wast and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wat() {
    # Smoke test: load wat and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wgsl() {
    # Smoke test: load wgsl and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wgsl_bevy() {
    # Smoke test: load wgsl_bevy and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wit() {
    # Smoke test: load wit and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_wolfram() {
    # Smoke test: load wolfram and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_x86asm() {
    # Smoke test: load x86asm and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_xcompose() {
    # Smoke test: load xcompose and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_xml() {
    # Smoke test: load xml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_yaml() {
    # Smoke test: load yaml and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_yuck() {
    # Smoke test: load yuck and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_zig() {
    # Smoke test: load zig and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ziggy() {
    # Smoke test: load ziggy and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_ziggy_schema() {
    # Smoke test: load ziggy_schema and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

test_smoke_zsh() {
    # Smoke test: load zsh and parse a simple snippet
    local output
    output=$(tree_sitter_language_pack process)

    local method_result_root_child_count
    method_result_root_child_count=$(tree_sitter_language_pack tree-root-child-count "$output")
    [ "$method_result_root_child_count" -ge 1 ] || exit 1
}

run_tests_smoke() {
    run_test test_smoke_abl
    run_test test_smoke_actionscript
    run_test test_smoke_ada
    run_test test_smoke_agda
    run_test test_smoke_al
    run_test test_smoke_angular
    run_test test_smoke_apex
    run_test test_smoke_arduino
    run_test test_smoke_asciidoc
    run_test test_smoke_asm
    run_test test_smoke_astro
    run_test test_smoke_awk
    run_test test_smoke_bash
    run_test test_smoke_bass
    run_test test_smoke_batch
    run_test test_smoke_beancount
    run_test test_smoke_bibtex
    run_test test_smoke_bicep
    run_test test_smoke_bitbake
    run_test test_smoke_blade
    run_test test_smoke_brightscript
    run_test test_smoke_bsl
    run_test test_smoke_c
    run_test test_smoke_c3
    run_test test_smoke_caddy
    run_test test_smoke_cairo
    run_test test_smoke_capnp
    run_test test_smoke_cedar
    run_test test_smoke_cedarschema
    run_test test_smoke_cel
    run_test test_smoke_cfml
    run_test test_smoke_chatito
    run_test test_smoke_chuck
    run_test test_smoke_circom
    run_test test_smoke_clarity
    run_test test_smoke_clojure
    run_test test_smoke_cmake
    run_test test_smoke_cobol
    run_test test_smoke_comment
    run_test test_smoke_commonlisp
    run_test test_smoke_cooklang
    run_test test_smoke_corn
    run_test test_smoke_cpon
    run_test test_smoke_cpp
    run_test test_smoke_crystal
    run_test test_smoke_csharp
    run_test test_smoke_css
    run_test test_smoke_cst
    run_test test_smoke_csv
    run_test test_smoke_cuda
    run_test test_smoke_cue
    run_test test_smoke_cylc
    run_test test_smoke_d
    run_test test_smoke_dart
    run_test test_smoke_desktop
    run_test test_smoke_devicetree
    run_test test_smoke_dhall
    run_test test_smoke_diff
    run_test test_smoke_djot
    run_test test_smoke_dockerfile
    run_test test_smoke_dot
    run_test test_smoke_doxygen
    run_test test_smoke_dtd
    run_test test_smoke_earthfile
    run_test test_smoke_ebnf
    run_test test_smoke_editorconfig
    run_test test_smoke_eds
    run_test test_smoke_eex
    run_test test_smoke_elisp
    run_test test_smoke_elixir
    run_test test_smoke_elm
    run_test test_smoke_elsa
    run_test test_smoke_elvish
    run_test test_smoke_embeddedtemplate
    run_test test_smoke_enforce
    run_test test_smoke_erlang
    run_test test_smoke_facility
    run_test test_smoke_faust
    run_test test_smoke_fennel
    run_test test_smoke_fidl
    run_test test_smoke_firrtl
    run_test test_smoke_fish
    run_test test_smoke_foam
    run_test test_smoke_forth
    run_test test_smoke_fortran
    run_test test_smoke_fsharp
    run_test test_smoke_fsharp_signature
    run_test test_smoke_func
    run_test test_smoke_gap
    run_test test_smoke_gdscript
    run_test test_smoke_gdshader
    run_test test_smoke_git_config
    run_test test_smoke_git_rebase
    run_test test_smoke_gitattributes
    run_test test_smoke_gitcommit
    run_test test_smoke_gitignore
    run_test test_smoke_gleam
    run_test test_smoke_glimmer
    run_test test_smoke_glsl
    run_test test_smoke_gn
    run_test test_smoke_gnuplot
    run_test test_smoke_go
    run_test test_smoke_godot_resource
    run_test test_smoke_gomod
    run_test test_smoke_gosum
    run_test test_smoke_gotmpl
    run_test test_smoke_gowork
    run_test test_smoke_gpg
    run_test test_smoke_graphql
    run_test test_smoke_gren
    run_test test_smoke_groovy
    run_test test_smoke_gstlaunch
    run_test test_smoke_hack
    run_test test_smoke_hare
    run_test test_smoke_haskell
    run_test test_smoke_haxe
    run_test test_smoke_hcl
    run_test test_smoke_heex
    run_test test_smoke_hjson
    run_test test_smoke_hlsl
    run_test test_smoke_hocon
    run_test test_smoke_hoon
    run_test test_smoke_html
    run_test test_smoke_htmldjango
    run_test test_smoke_http
    run_test test_smoke_hurl
    run_test test_smoke_hyprlang
    run_test test_smoke_idris
    run_test test_smoke_ini
    run_test test_smoke_ispc
    run_test test_smoke_jai
    run_test test_smoke_janet
    run_test test_smoke_java
    run_test test_smoke_javadoc
    run_test test_smoke_javascript
    run_test test_smoke_jinja2
    run_test test_smoke_jq
    run_test test_smoke_jsdoc
    run_test test_smoke_json
    run_test test_smoke_json5
    run_test test_smoke_jsonnet
    run_test test_smoke_julia
    run_test test_smoke_just
    run_test test_smoke_kcl
    run_test test_smoke_kconfig
    run_test test_smoke_kdl
    run_test test_smoke_kotlin
    run_test test_smoke_latex
    run_test test_smoke_lean
    run_test test_smoke_ledger
    run_test test_smoke_less
    run_test test_smoke_linkerscript
    run_test test_smoke_liquid
    run_test test_smoke_llvm
    run_test test_smoke_lua
    run_test test_smoke_luadoc
    run_test test_smoke_luap
    run_test test_smoke_luau
    run_test test_smoke_magik
    run_test test_smoke_make
    run_test test_smoke_markdown
    run_test test_smoke_markdown_inline
    run_test test_smoke_matlab
    run_test test_smoke_mermaid
    run_test test_smoke_meson
    run_test test_smoke_mlir
    run_test test_smoke_mojo
    run_test test_smoke_move
    run_test test_smoke_nasm
    run_test test_smoke_netlinx
    run_test test_smoke_nginx
    run_test test_smoke_nickel
    run_test test_smoke_nim
    run_test test_smoke_ninja
    run_test test_smoke_nix
    run_test test_smoke_norg
    run_test test_smoke_norg_meta
    run_test test_smoke_nqc
    run_test test_smoke_nushell
    run_test test_smoke_objc
    run_test test_smoke_ocaml
    run_test test_smoke_ocaml_interface
    run_test test_smoke_ocamllex
    run_test test_smoke_odin
    run_test test_smoke_openscad
    run_test test_smoke_org
    run_test test_smoke_pascal
    run_test test_smoke_pem
    run_test test_smoke_perl
    run_test test_smoke_pgn
    run_test test_smoke_php
    run_test test_smoke_phpdoc
    run_test test_smoke_pkl
    run_test test_smoke_po
    run_test test_smoke_poe_filter
    run_test test_smoke_pony
    run_test test_smoke_postscript
    run_test test_smoke_powershell
    run_test test_smoke_printf
    run_test test_smoke_prisma
    run_test test_smoke_prolog
    run_test test_smoke_promql
    run_test test_smoke_properties
    run_test test_smoke_proto
    run_test test_smoke_prql
    run_test test_smoke_psv
    run_test test_smoke_pug
    run_test test_smoke_puppet
    run_test test_smoke_purescript
    run_test test_smoke_pymanifest
    run_test test_smoke_python
    run_test test_smoke_ql
    run_test test_smoke_qmldir
    run_test test_smoke_qmljs
    run_test test_smoke_query
    run_test test_smoke_r
    run_test test_smoke_racket
    run_test test_smoke_rasi
    run_test test_smoke_razor
    run_test test_smoke_rbs
    run_test test_smoke_re2c
    run_test test_smoke_readline
    run_test test_smoke_regex
    run_test test_smoke_rego
    run_test test_smoke_requirements
    run_test test_smoke_rescript
    run_test test_smoke_robot
    run_test test_smoke_roc
    run_test test_smoke_ron
    run_test test_smoke_rst
    run_test test_smoke_rtf
    run_test test_smoke_ruby
    run_test test_smoke_rust
    run_test test_smoke_scala
    run_test test_smoke_scheme
    run_test test_smoke_scss
    run_test test_smoke_slang
    run_test test_smoke_smali
    run_test test_smoke_smalltalk
    run_test test_smoke_smithy
    run_test test_smoke_sml
    run_test test_smoke_snakemake
    run_test test_smoke_solidity
    run_test test_smoke_souffle
    run_test test_smoke_sourcepawn
    run_test test_smoke_sparql
    run_test test_smoke_sql
    run_test test_smoke_sql_bigquery
    run_test test_smoke_squirrel
    run_test test_smoke_ssh_config
    run_test test_smoke_stan
    run_test test_smoke_starlark
    run_test test_smoke_superhtml
    run_test test_smoke_svelte
    run_test test_smoke_sway
    run_test test_smoke_swift
    run_test test_smoke_systemverilog
    run_test test_smoke_tablegen
    run_test test_smoke_tact
    run_test test_smoke_tcl
    run_test test_smoke_teal
    run_test test_smoke_templ
    run_test test_smoke_tera
    run_test test_smoke_terraform
    run_test test_smoke_test
    run_test test_smoke_textproto
    run_test test_smoke_thrift
    run_test test_smoke_tlaplus
    run_test test_smoke_tmux
    run_test test_smoke_todotxt
    run_test test_smoke_toml
    run_test test_smoke_tsv
    run_test test_smoke_tsx
    run_test test_smoke_turtle
    run_test test_smoke_twig
    run_test test_smoke_typescript
    run_test test_smoke_typespec
    run_test test_smoke_typoscript
    run_test test_smoke_typst
    run_test test_smoke_udev
    run_test test_smoke_ungrammar
    run_test test_smoke_uxntal
    run_test test_smoke_v
    run_test test_smoke_vb
    run_test test_smoke_verilog
    run_test test_smoke_vhdl
    run_test test_smoke_vhs
    run_test test_smoke_vim
    run_test test_smoke_vimdoc
    run_test test_smoke_vrl
    run_test test_smoke_vue
    run_test test_smoke_wast
    run_test test_smoke_wat
    run_test test_smoke_wgsl
    run_test test_smoke_wgsl_bevy
    run_test test_smoke_wit
    run_test test_smoke_wolfram
    run_test test_smoke_x86asm
    run_test test_smoke_xcompose
    run_test test_smoke_xml
    run_test test_smoke_yaml
    run_test test_smoke_yuck
    run_test test_smoke_zig
    run_test test_smoke_ziggy
    run_test test_smoke_ziggy_schema
    run_test test_smoke_zsh
}
