#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:975aee80e787d5784565356ea79fdbc3bcf331796330baaa0ad24bd4656dbcce
# To regenerate: alef generate
# To verify freshness: alef verify --exit-code
# Issues & docs: https://github.com/kreuzberg-dev/alef
# E2e tests for category: process
set -euo pipefail

test_process_javascript_exports_count() {
    # JavaScript with multiple exports, verify export count
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'javascript' 'language'
    local count_exports
    count_exports=$(echo "$output" | jq '.exports | length')
    assert_count_min "$count_exports" 2 'exports'
}

test_process_javascript_exports_detail() {
    # JavaScript with exports, verify export count
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'javascript' 'language'
    local count_exports
    count_exports=$(echo "$output" | jq '.exports | length')
    assert_count_min "$count_exports" 1 'exports'
}

test_process_python_all_features() {
    # Python comprehensive source with all feature extraction enabled
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local count_structure
    count_structure=$(echo "$output" | jq '.structure | length')
    assert_count_min "$count_structure" 2 'structure'
    local count_imports
    count_imports=$(echo "$output" | jq '.imports | length')
    assert_count_min "$count_imports" 1 'imports'
    local count_comments
    count_comments=$(echo "$output" | jq '.comments | length')
    assert_count_min "$count_comments" 1 'comments'
    local val_metrics_total_lines
    val_metrics_total_lines=$(echo "$output" | jq -r '.metrics.total_lines')
    assert_greater_than_or_equal "$val_metrics_total_lines" '10' 'metrics.total_lines'
}

test_process_python_comments() {
    # Python with comments, verify comment count
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local count_comments
    count_comments=$(echo "$output" | jq '.comments | length')
    assert_count_min "$count_comments" 1 'comments'
}

test_process_python_docstrings() {
    # Python with function docstring, verify docstring count
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local val_metrics_total_lines
    val_metrics_total_lines=$(echo "$output" | jq -r '.metrics.total_lines')
    assert_greater_than_or_equal "$val_metrics_total_lines" '3' 'metrics.total_lines'
}

test_process_python_imports_detail() {
    # Python with multiple imports, verify imports contain specific source
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local count_imports
    count_imports=$(echo "$output" | jq '.imports | length')
    assert_count_min "$count_imports" 2 'imports'
    local val_import_sources
    val_import_sources=$(echo "$output" | jq -r '.import_sources')
    assert_contains "$val_import_sources" 'os' 'import_sources'
}

test_process_python_metrics_detail() {
    # Python code with metrics assertions
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local val_metrics_code_lines
    val_metrics_code_lines=$(echo "$output" | jq -r '.metrics.code_lines')
    assert_greater_than_or_equal "$val_metrics_code_lines" '4' 'metrics.code_lines'
    local val_metrics_comment_lines
    val_metrics_comment_lines=$(echo "$output" | jq -r '.metrics.comment_lines')
    assert_greater_than_or_equal "$val_metrics_comment_lines" '1' 'metrics.comment_lines'
    local val_metrics_max_depth
    val_metrics_max_depth=$(echo "$output" | jq -r '.metrics.max_depth')
    assert_greater_than_or_equal "$val_metrics_max_depth" '1' 'metrics.max_depth'
}

test_process_python_symbols() {
    # Python with class and functions, verify symbol count
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'python' 'language'
    local count_symbols
    count_symbols=$(echo "$output" | jq '.symbols | length')
    assert_count_min "$count_symbols" 1 'symbols'
}

test_process_rust_structure_name() {
    # Rust struct with name, verify structure name contains value
    local output
    output=$(tree_sitter_language_pack process)

    local val_language
    val_language=$(echo "$output" | jq -r '.language')
    assert_equals "$val_language" 'rust' 'language'
    local count_structure
    count_structure=$(echo "$output" | jq '.structure | length')
    assert_count_min "$count_structure" 1 'structure'
    local val_structure_names
    val_structure_names=$(echo "$output" | jq -r '.structure_names')
    assert_contains "$val_structure_names" 'MyConfig' 'structure_names'
}

run_tests_process() {
    run_test test_process_javascript_exports_count
    run_test test_process_javascript_exports_detail
    run_test test_process_python_all_features
    run_test test_process_python_comments
    run_test test_process_python_docstrings
    run_test test_process_python_imports_detail
    run_test test_process_python_metrics_detail
    run_test test_process_python_symbols
    run_test test_process_rust_structure_name
}
