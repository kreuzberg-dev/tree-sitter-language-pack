# Code generated by e2e-generator. DO NOT EDIT.

import pytest
from tree_sitter_language_pack import (
    get_parser,
    has_language,
)


@pytest.mark.skipif(not has_language("abl"), reason="Language 'abl' not available")
def test_smoke_abl():
    """Smoke test: load abl and parse a simple snippet"""
    parser = get_parser("abl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("actionscript"), reason="Language 'actionscript' not available")
def test_smoke_actionscript():
    """Smoke test: load actionscript and parse a simple snippet"""
    parser = get_parser("actionscript")
    tree = parser.parse(b"var x:int = 1;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ada"), reason="Language 'ada' not available")
def test_smoke_ada():
    """Smoke test: load ada and parse a simple snippet"""
    parser = get_parser("ada")
    tree = parser.parse(b"procedure Main is begin null; end Main;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("agda"), reason="Language 'agda' not available")
def test_smoke_agda():
    """Smoke test: load agda and parse a simple snippet"""
    parser = get_parser("agda")
    tree = parser.parse(b"module Main where")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("al"), reason="Language 'al' not available")
def test_smoke_al():
    """Smoke test: load al and parse a simple snippet"""
    parser = get_parser("al")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("angular"), reason="Language 'angular' not available")
def test_smoke_angular():
    """Smoke test: load angular and parse a simple snippet"""
    parser = get_parser("angular")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("apex"), reason="Language 'apex' not available")
def test_smoke_apex():
    """Smoke test: load apex and parse a simple snippet"""
    parser = get_parser("apex")
    tree = parser.parse(b"public class Main {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("arduino"), reason="Language 'arduino' not available")
def test_smoke_arduino():
    """Smoke test: load arduino and parse a simple snippet"""
    parser = get_parser("arduino")
    tree = parser.parse(b"void setup() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("asciidoc"), reason="Language 'asciidoc' not available")
def test_smoke_asciidoc():
    """Smoke test: load asciidoc and parse a simple snippet"""
    parser = get_parser("asciidoc")
    tree = parser.parse(b"= Title\n\nParagraph.")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("asm"), reason="Language 'asm' not available")
def test_smoke_asm():
    """Smoke test: load asm and parse a simple snippet"""
    parser = get_parser("asm")
    tree = parser.parse(b"mov eax, 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("astro"), reason="Language 'astro' not available")
def test_smoke_astro():
    """Smoke test: load astro and parse a simple snippet"""
    parser = get_parser("astro")
    tree = parser.parse(b"---\n---\n<p>hello</p>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("awk"), reason="Language 'awk' not available")
def test_smoke_awk():
    """Smoke test: load awk and parse a simple snippet"""
    parser = get_parser("awk")
    tree = parser.parse(b'BEGIN { print "hello" }')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bash"), reason="Language 'bash' not available")
def test_smoke_bash():
    """Smoke test: load bash and parse a simple snippet"""
    parser = get_parser("bash")
    tree = parser.parse(b"echo hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bass"), reason="Language 'bass' not available")
def test_smoke_bass():
    """Smoke test: load bass and parse a simple snippet"""
    parser = get_parser("bass")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("batch"), reason="Language 'batch' not available")
def test_smoke_batch():
    """Smoke test: load batch and parse a simple snippet"""
    parser = get_parser("batch")
    tree = parser.parse(b"@echo off\necho hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("beancount"), reason="Language 'beancount' not available")
def test_smoke_beancount():
    """Smoke test: load beancount and parse a simple snippet"""
    parser = get_parser("beancount")
    tree = parser.parse(b"2024-01-01 open Assets:Bank USD")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bibtex"), reason="Language 'bibtex' not available")
def test_smoke_bibtex():
    """Smoke test: load bibtex and parse a simple snippet"""
    parser = get_parser("bibtex")
    tree = parser.parse(b"@article{key, title={A}}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bicep"), reason="Language 'bicep' not available")
def test_smoke_bicep():
    """Smoke test: load bicep and parse a simple snippet"""
    parser = get_parser("bicep")
    tree = parser.parse(b"param name string")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bitbake"), reason="Language 'bitbake' not available")
def test_smoke_bitbake():
    """Smoke test: load bitbake and parse a simple snippet"""
    parser = get_parser("bitbake")
    tree = parser.parse(b'DESCRIPTION = "hello"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("blade"), reason="Language 'blade' not available")
def test_smoke_blade():
    """Smoke test: load blade and parse a simple snippet"""
    parser = get_parser("blade")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("brightscript"), reason="Language 'brightscript' not available")
def test_smoke_brightscript():
    """Smoke test: load brightscript and parse a simple snippet"""
    parser = get_parser("brightscript")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("bsl"), reason="Language 'bsl' not available")
def test_smoke_bsl():
    """Smoke test: load bsl and parse a simple snippet"""
    parser = get_parser("bsl")
    tree = parser.parse(b"Procedure Main() EndProcedure")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("c"), reason="Language 'c' not available")
def test_smoke_c():
    """Smoke test: load c and parse a simple snippet"""
    parser = get_parser("c")
    tree = parser.parse(b"int main() { return 0; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("c3"), reason="Language 'c3' not available")
def test_smoke_c3():
    """Smoke test: load c3 and parse a simple snippet"""
    parser = get_parser("c3")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("caddy"), reason="Language 'caddy' not available")
def test_smoke_caddy():
    """Smoke test: load caddy and parse a simple snippet"""
    parser = get_parser("caddy")
    tree = parser.parse(b':8080 {\n\trespond "Hello"\n}')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cairo"), reason="Language 'cairo' not available")
def test_smoke_cairo():
    """Smoke test: load cairo and parse a simple snippet"""
    parser = get_parser("cairo")
    tree = parser.parse(b"fn main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("capnp"), reason="Language 'capnp' not available")
def test_smoke_capnp():
    """Smoke test: load capnp and parse a simple snippet"""
    parser = get_parser("capnp")
    tree = parser.parse(b"@0xabcdef1234567890;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cedar"), reason="Language 'cedar' not available")
def test_smoke_cedar():
    """Smoke test: load cedar and parse a simple snippet"""
    parser = get_parser("cedar")
    tree = parser.parse(b"permit(principal, action, resource);")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cedarschema"), reason="Language 'cedarschema' not available")
def test_smoke_cedarschema():
    """Smoke test: load cedarschema and parse a simple snippet"""
    parser = get_parser("cedarschema")
    tree = parser.parse(b"entity User;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cel"), reason="Language 'cel' not available")
def test_smoke_cel():
    """Smoke test: load cel and parse a simple snippet"""
    parser = get_parser("cel")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cfml"), reason="Language 'cfml' not available")
def test_smoke_cfml():
    """Smoke test: load cfml and parse a simple snippet"""
    parser = get_parser("cfml")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("chatito"), reason="Language 'chatito' not available")
def test_smoke_chatito():
    """Smoke test: load chatito and parse a simple snippet"""
    parser = get_parser("chatito")
    tree = parser.parse(b"%[greeting]\n    hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("chuck"), reason="Language 'chuck' not available")
def test_smoke_chuck():
    """Smoke test: load chuck and parse a simple snippet"""
    parser = get_parser("chuck")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("circom"), reason="Language 'circom' not available")
def test_smoke_circom():
    """Smoke test: load circom and parse a simple snippet"""
    parser = get_parser("circom")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("clarity"), reason="Language 'clarity' not available")
def test_smoke_clarity():
    """Smoke test: load clarity and parse a simple snippet"""
    parser = get_parser("clarity")
    tree = parser.parse(b"(define-public (hello) (ok true))")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("clojure"), reason="Language 'clojure' not available")
def test_smoke_clojure():
    """Smoke test: load clojure and parse a simple snippet"""
    parser = get_parser("clojure")
    tree = parser.parse(b"(def x 1)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cmake"), reason="Language 'cmake' not available")
def test_smoke_cmake():
    """Smoke test: load cmake and parse a simple snippet"""
    parser = get_parser("cmake")
    tree = parser.parse(b"cmake_minimum_required(VERSION 3.0)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cobol"), reason="Language 'cobol' not available")
def test_smoke_cobol():
    """Smoke test: load cobol and parse a simple snippet"""
    parser = get_parser("cobol")
    tree = parser.parse(b"       IDENTIFICATION DIVISION.\n       PROGRAM-ID. HELLO.")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("comment"), reason="Language 'comment' not available")
def test_smoke_comment():
    """Smoke test: load comment and parse a simple snippet"""
    parser = get_parser("comment")
    tree = parser.parse(b"TODO: fix this")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("commonlisp"), reason="Language 'commonlisp' not available")
def test_smoke_commonlisp():
    """Smoke test: load commonlisp and parse a simple snippet"""
    parser = get_parser("commonlisp")
    tree = parser.parse(b'(defun hello () (print "hello"))')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cooklang"), reason="Language 'cooklang' not available")
def test_smoke_cooklang():
    """Smoke test: load cooklang and parse a simple snippet"""
    parser = get_parser("cooklang")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("corn"), reason="Language 'corn' not available")
def test_smoke_corn():
    """Smoke test: load corn and parse a simple snippet"""
    parser = get_parser("corn")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cpon"), reason="Language 'cpon' not available")
def test_smoke_cpon():
    """Smoke test: load cpon and parse a simple snippet"""
    parser = get_parser("cpon")
    tree = parser.parse(b'{"key": 1}')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cpp"), reason="Language 'cpp' not available")
def test_smoke_cpp():
    """Smoke test: load cpp and parse a simple snippet"""
    parser = get_parser("cpp")
    tree = parser.parse(b"int main() { return 0; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("crystal"), reason="Language 'crystal' not available")
def test_smoke_crystal():
    """Smoke test: load crystal and parse a simple snippet"""
    parser = get_parser("crystal")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("csharp"), reason="Language 'csharp' not available")
def test_smoke_csharp():
    """Smoke test: load csharp and parse a simple snippet"""
    parser = get_parser("csharp")
    tree = parser.parse(b"class Main {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("css"), reason="Language 'css' not available")
def test_smoke_css():
    """Smoke test: load css and parse a simple snippet"""
    parser = get_parser("css")
    tree = parser.parse(b"body { color: red; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cst"), reason="Language 'cst' not available")
def test_smoke_cst():
    """Smoke test: load cst and parse a simple snippet"""
    parser = get_parser("cst")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("csv"), reason="Language 'csv' not available")
def test_smoke_csv():
    """Smoke test: load csv and parse a simple snippet"""
    parser = get_parser("csv")
    tree = parser.parse(b"a,b,c\n1,2,3")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cuda"), reason="Language 'cuda' not available")
def test_smoke_cuda():
    """Smoke test: load cuda and parse a simple snippet"""
    parser = get_parser("cuda")
    tree = parser.parse(b"__global__ void kernel() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cue"), reason="Language 'cue' not available")
def test_smoke_cue():
    """Smoke test: load cue and parse a simple snippet"""
    parser = get_parser("cue")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("cylc"), reason="Language 'cylc' not available")
def test_smoke_cylc():
    """Smoke test: load cylc and parse a simple snippet"""
    parser = get_parser("cylc")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("d"), reason="Language 'd' not available")
def test_smoke_d():
    """Smoke test: load d and parse a simple snippet"""
    parser = get_parser("d")
    tree = parser.parse(b"void main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("dart"), reason="Language 'dart' not available")
def test_smoke_dart():
    """Smoke test: load dart and parse a simple snippet"""
    parser = get_parser("dart")
    tree = parser.parse(b"void main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("desktop"), reason="Language 'desktop' not available")
def test_smoke_desktop():
    """Smoke test: load desktop and parse a simple snippet"""
    parser = get_parser("desktop")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("devicetree"), reason="Language 'devicetree' not available")
def test_smoke_devicetree():
    """Smoke test: load devicetree and parse a simple snippet"""
    parser = get_parser("devicetree")
    tree = parser.parse(b"/dts-v1/;\n/ { };")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("dhall"), reason="Language 'dhall' not available")
def test_smoke_dhall():
    """Smoke test: load dhall and parse a simple snippet"""
    parser = get_parser("dhall")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("diff"), reason="Language 'diff' not available")
def test_smoke_diff():
    """Smoke test: load diff and parse a simple snippet"""
    parser = get_parser("diff")
    tree = parser.parse(b"--- a/file\n+++ b/file\n@@ -1 +1 @@\n-old\n+new")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("djot"), reason="Language 'djot' not available")
def test_smoke_djot():
    """Smoke test: load djot and parse a simple snippet"""
    parser = get_parser("djot")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("dockerfile"), reason="Language 'dockerfile' not available")
def test_smoke_dockerfile():
    """Smoke test: load dockerfile and parse a simple snippet"""
    parser = get_parser("dockerfile")
    tree = parser.parse(b"FROM alpine")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("dot"), reason="Language 'dot' not available")
def test_smoke_dot():
    """Smoke test: load dot and parse a simple snippet"""
    parser = get_parser("dot")
    tree = parser.parse(b"digraph G { A -> B; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("doxygen"), reason="Language 'doxygen' not available")
def test_smoke_doxygen():
    """Smoke test: load doxygen and parse a simple snippet"""
    parser = get_parser("doxygen")
    tree = parser.parse(b"/** @brief A function */")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("dtd"), reason="Language 'dtd' not available")
def test_smoke_dtd():
    """Smoke test: load dtd and parse a simple snippet"""
    parser = get_parser("dtd")
    tree = parser.parse(b"<!ELEMENT note (body)>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("earthfile"), reason="Language 'earthfile' not available")
def test_smoke_earthfile():
    """Smoke test: load earthfile and parse a simple snippet"""
    parser = get_parser("earthfile")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ebnf"), reason="Language 'ebnf' not available")
def test_smoke_ebnf():
    """Smoke test: load ebnf and parse a simple snippet"""
    parser = get_parser("ebnf")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("editorconfig"), reason="Language 'editorconfig' not available")
def test_smoke_editorconfig():
    """Smoke test: load editorconfig and parse a simple snippet"""
    parser = get_parser("editorconfig")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("eds"), reason="Language 'eds' not available")
def test_smoke_eds():
    """Smoke test: load eds and parse a simple snippet"""
    parser = get_parser("eds")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("eex"), reason="Language 'eex' not available")
def test_smoke_eex():
    """Smoke test: load eex and parse a simple snippet"""
    parser = get_parser("eex")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("elisp"), reason="Language 'elisp' not available")
def test_smoke_elisp():
    """Smoke test: load elisp and parse a simple snippet"""
    parser = get_parser("elisp")
    tree = parser.parse(b'(defun hello () (message "hello"))')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("elixir"), reason="Language 'elixir' not available")
def test_smoke_elixir():
    """Smoke test: load elixir and parse a simple snippet"""
    parser = get_parser("elixir")
    tree = parser.parse(b'IO.puts("hello")')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("elm"), reason="Language 'elm' not available")
def test_smoke_elm():
    """Smoke test: load elm and parse a simple snippet"""
    parser = get_parser("elm")
    tree = parser.parse(b"module Main exposing (..)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("elsa"), reason="Language 'elsa' not available")
def test_smoke_elsa():
    """Smoke test: load elsa and parse a simple snippet"""
    parser = get_parser("elsa")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("elvish"), reason="Language 'elvish' not available")
def test_smoke_elvish():
    """Smoke test: load elvish and parse a simple snippet"""
    parser = get_parser("elvish")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("embeddedtemplate"), reason="Language 'embeddedtemplate' not available")
def test_smoke_embeddedtemplate():
    """Smoke test: load embeddedtemplate and parse a simple snippet"""
    parser = get_parser("embeddedtemplate")
    tree = parser.parse(b"<%= value %>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("enforce"), reason="Language 'enforce' not available")
def test_smoke_enforce():
    """Smoke test: load enforce and parse a simple snippet"""
    parser = get_parser("enforce")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("erlang"), reason="Language 'erlang' not available")
def test_smoke_erlang():
    """Smoke test: load erlang and parse a simple snippet"""
    parser = get_parser("erlang")
    tree = parser.parse(b"main() -> ok.")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("facility"), reason="Language 'facility' not available")
def test_smoke_facility():
    """Smoke test: load facility and parse a simple snippet"""
    parser = get_parser("facility")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("faust"), reason="Language 'faust' not available")
def test_smoke_faust():
    """Smoke test: load faust and parse a simple snippet"""
    parser = get_parser("faust")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fennel"), reason="Language 'fennel' not available")
def test_smoke_fennel():
    """Smoke test: load fennel and parse a simple snippet"""
    parser = get_parser("fennel")
    tree = parser.parse(b"(fn hello [] (print :hello))")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fidl"), reason="Language 'fidl' not available")
def test_smoke_fidl():
    """Smoke test: load fidl and parse a simple snippet"""
    parser = get_parser("fidl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("firrtl"), reason="Language 'firrtl' not available")
def test_smoke_firrtl():
    """Smoke test: load firrtl and parse a simple snippet"""
    parser = get_parser("firrtl")
    tree = parser.parse(b"circuit Main :")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fish"), reason="Language 'fish' not available")
def test_smoke_fish():
    """Smoke test: load fish and parse a simple snippet"""
    parser = get_parser("fish")
    tree = parser.parse(b"echo hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("foam"), reason="Language 'foam' not available")
def test_smoke_foam():
    """Smoke test: load foam and parse a simple snippet"""
    parser = get_parser("foam")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("forth"), reason="Language 'forth' not available")
def test_smoke_forth():
    """Smoke test: load forth and parse a simple snippet"""
    parser = get_parser("forth")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fortran"), reason="Language 'fortran' not available")
def test_smoke_fortran():
    """Smoke test: load fortran and parse a simple snippet"""
    parser = get_parser("fortran")
    tree = parser.parse(b"program main\nend program main")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fsharp"), reason="Language 'fsharp' not available")
def test_smoke_fsharp():
    """Smoke test: load fsharp and parse a simple snippet"""
    parser = get_parser("fsharp")
    tree = parser.parse(b"let x = 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("fsharp_signature"), reason="Language 'fsharp_signature' not available")
def test_smoke_fsharp_signature():
    """Smoke test: load fsharp_signature and parse a simple snippet"""
    parser = get_parser("fsharp_signature")
    tree = parser.parse(b"val x: int")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("func"), reason="Language 'func' not available")
def test_smoke_func():
    """Smoke test: load func and parse a simple snippet"""
    parser = get_parser("func")
    tree = parser.parse(b"() recv_internal() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gap"), reason="Language 'gap' not available")
def test_smoke_gap():
    """Smoke test: load gap and parse a simple snippet"""
    parser = get_parser("gap")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gdscript"), reason="Language 'gdscript' not available")
def test_smoke_gdscript():
    """Smoke test: load gdscript and parse a simple snippet"""
    parser = get_parser("gdscript")
    tree = parser.parse(b"extends Node\nfunc _ready():\n\tpass")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gdshader"), reason="Language 'gdshader' not available")
def test_smoke_gdshader():
    """Smoke test: load gdshader and parse a simple snippet"""
    parser = get_parser("gdshader")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("git_config"), reason="Language 'git_config' not available")
def test_smoke_git_config():
    """Smoke test: load git_config and parse a simple snippet"""
    parser = get_parser("git_config")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("git_rebase"), reason="Language 'git_rebase' not available")
def test_smoke_git_rebase():
    """Smoke test: load git_rebase and parse a simple snippet"""
    parser = get_parser("git_rebase")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gitattributes"), reason="Language 'gitattributes' not available")
def test_smoke_gitattributes():
    """Smoke test: load gitattributes and parse a simple snippet"""
    parser = get_parser("gitattributes")
    tree = parser.parse(b"*.txt text")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gitcommit"), reason="Language 'gitcommit' not available")
def test_smoke_gitcommit():
    """Smoke test: load gitcommit and parse a simple snippet"""
    parser = get_parser("gitcommit")
    tree = parser.parse(b"feat: add feature\n\nBody text")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gitignore"), reason="Language 'gitignore' not available")
def test_smoke_gitignore():
    """Smoke test: load gitignore and parse a simple snippet"""
    parser = get_parser("gitignore")
    tree = parser.parse(b"*.o\n*.log")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gleam"), reason="Language 'gleam' not available")
def test_smoke_gleam():
    """Smoke test: load gleam and parse a simple snippet"""
    parser = get_parser("gleam")
    tree = parser.parse(b"pub fn main() { }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("glimmer"), reason="Language 'glimmer' not available")
def test_smoke_glimmer():
    """Smoke test: load glimmer and parse a simple snippet"""
    parser = get_parser("glimmer")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("glsl"), reason="Language 'glsl' not available")
def test_smoke_glsl():
    """Smoke test: load glsl and parse a simple snippet"""
    parser = get_parser("glsl")
    tree = parser.parse(b"void main() { gl_Position = vec4(0.0); }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gn"), reason="Language 'gn' not available")
def test_smoke_gn():
    """Smoke test: load gn and parse a simple snippet"""
    parser = get_parser("gn")
    tree = parser.parse(b'group("hello") {}')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gnuplot"), reason="Language 'gnuplot' not available")
def test_smoke_gnuplot():
    """Smoke test: load gnuplot and parse a simple snippet"""
    parser = get_parser("gnuplot")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("go"), reason="Language 'go' not available")
def test_smoke_go():
    """Smoke test: load go and parse a simple snippet"""
    parser = get_parser("go")
    tree = parser.parse(b"package main")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("godot_resource"), reason="Language 'godot_resource' not available")
def test_smoke_godot_resource():
    """Smoke test: load godot_resource and parse a simple snippet"""
    parser = get_parser("godot_resource")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gomod"), reason="Language 'gomod' not available")
def test_smoke_gomod():
    """Smoke test: load gomod and parse a simple snippet"""
    parser = get_parser("gomod")
    tree = parser.parse(b"module example.com/hello\n\ngo 1.21")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gosum"), reason="Language 'gosum' not available")
def test_smoke_gosum():
    """Smoke test: load gosum and parse a simple snippet"""
    parser = get_parser("gosum")
    tree = parser.parse(b"example.com/pkg v1.0.0 h1:abc=")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gotmpl"), reason="Language 'gotmpl' not available")
def test_smoke_gotmpl():
    """Smoke test: load gotmpl and parse a simple snippet"""
    parser = get_parser("gotmpl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gowork"), reason="Language 'gowork' not available")
def test_smoke_gowork():
    """Smoke test: load gowork and parse a simple snippet"""
    parser = get_parser("gowork")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gpg"), reason="Language 'gpg' not available")
def test_smoke_gpg():
    """Smoke test: load gpg and parse a simple snippet"""
    parser = get_parser("gpg")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("graphql"), reason="Language 'graphql' not available")
def test_smoke_graphql():
    """Smoke test: load graphql and parse a simple snippet"""
    parser = get_parser("graphql")
    tree = parser.parse(b"type Query { hello: String }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("groovy"), reason="Language 'groovy' not available")
def test_smoke_groovy():
    """Smoke test: load groovy and parse a simple snippet"""
    parser = get_parser("groovy")
    tree = parser.parse(b"def x = 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("gstlaunch"), reason="Language 'gstlaunch' not available")
def test_smoke_gstlaunch():
    """Smoke test: load gstlaunch and parse a simple snippet"""
    parser = get_parser("gstlaunch")
    tree = parser.parse(b"fakesrc ! fakesink")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hack"), reason="Language 'hack' not available")
def test_smoke_hack():
    """Smoke test: load hack and parse a simple snippet"""
    parser = get_parser("hack")
    tree = parser.parse(b"<?hh\nfunction main(): void {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hare"), reason="Language 'hare' not available")
def test_smoke_hare():
    """Smoke test: load hare and parse a simple snippet"""
    parser = get_parser("hare")
    tree = parser.parse(b"export fn main() void = void;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("haskell"), reason="Language 'haskell' not available")
def test_smoke_haskell():
    """Smoke test: load haskell and parse a simple snippet"""
    parser = get_parser("haskell")
    tree = parser.parse(b'main = putStrLn "hello"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("haxe"), reason="Language 'haxe' not available")
def test_smoke_haxe():
    """Smoke test: load haxe and parse a simple snippet"""
    parser = get_parser("haxe")
    tree = parser.parse(b"class Main { static function main() {} }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hcl"), reason="Language 'hcl' not available")
def test_smoke_hcl():
    """Smoke test: load hcl and parse a simple snippet"""
    parser = get_parser("hcl")
    tree = parser.parse(b'variable "name" { type = string }')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("heex"), reason="Language 'heex' not available")
def test_smoke_heex():
    """Smoke test: load heex and parse a simple snippet"""
    parser = get_parser("heex")
    tree = parser.parse(b"<%= @greeting %>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hjson"), reason="Language 'hjson' not available")
def test_smoke_hjson():
    """Smoke test: load hjson and parse a simple snippet"""
    parser = get_parser("hjson")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hlsl"), reason="Language 'hlsl' not available")
def test_smoke_hlsl():
    """Smoke test: load hlsl and parse a simple snippet"""
    parser = get_parser("hlsl")
    tree = parser.parse(b"float4 main() : SV_Target { return 0; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hocon"), reason="Language 'hocon' not available")
def test_smoke_hocon():
    """Smoke test: load hocon and parse a simple snippet"""
    parser = get_parser("hocon")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hoon"), reason="Language 'hoon' not available")
def test_smoke_hoon():
    """Smoke test: load hoon and parse a simple snippet"""
    parser = get_parser("hoon")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("html"), reason="Language 'html' not available")
def test_smoke_html():
    """Smoke test: load html and parse a simple snippet"""
    parser = get_parser("html")
    tree = parser.parse(b"<p>hello</p>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("htmldjango"), reason="Language 'htmldjango' not available")
def test_smoke_htmldjango():
    """Smoke test: load htmldjango and parse a simple snippet"""
    parser = get_parser("htmldjango")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("http"), reason="Language 'http' not available")
def test_smoke_http():
    """Smoke test: load http and parse a simple snippet"""
    parser = get_parser("http")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hurl"), reason="Language 'hurl' not available")
def test_smoke_hurl():
    """Smoke test: load hurl and parse a simple snippet"""
    parser = get_parser("hurl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("hyprlang"), reason="Language 'hyprlang' not available")
def test_smoke_hyprlang():
    """Smoke test: load hyprlang and parse a simple snippet"""
    parser = get_parser("hyprlang")
    tree = parser.parse(b"general { border_size = 1 }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("idris"), reason="Language 'idris' not available")
def test_smoke_idris():
    """Smoke test: load idris and parse a simple snippet"""
    parser = get_parser("idris")
    tree = parser.parse(b"module Main")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ini"), reason="Language 'ini' not available")
def test_smoke_ini():
    """Smoke test: load ini and parse a simple snippet"""
    parser = get_parser("ini")
    tree = parser.parse(b"[section]\nkey = value")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ispc"), reason="Language 'ispc' not available")
def test_smoke_ispc():
    """Smoke test: load ispc and parse a simple snippet"""
    parser = get_parser("ispc")
    tree = parser.parse(b"export void main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("jai"), reason="Language 'jai' not available")
def test_smoke_jai():
    """Smoke test: load jai and parse a simple snippet"""
    parser = get_parser("jai")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("janet"), reason="Language 'janet' not available")
def test_smoke_janet():
    """Smoke test: load janet and parse a simple snippet"""
    parser = get_parser("janet")
    tree = parser.parse(b'(print "hello")')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("java"), reason="Language 'java' not available")
def test_smoke_java():
    """Smoke test: load java and parse a simple snippet"""
    parser = get_parser("java")
    tree = parser.parse(b"class Main {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("javadoc"), reason="Language 'javadoc' not available")
def test_smoke_javadoc():
    """Smoke test: load javadoc and parse a simple snippet"""
    parser = get_parser("javadoc")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("javascript"), reason="Language 'javascript' not available")
def test_smoke_javascript():
    """Smoke test: load javascript and parse a simple snippet"""
    parser = get_parser("javascript")
    tree = parser.parse(b"console.log('hello');")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("jinja2"), reason="Language 'jinja2' not available")
def test_smoke_jinja2():
    """Smoke test: load jinja2 and parse a simple snippet"""
    parser = get_parser("jinja2")
    tree = parser.parse(b"{{ variable }}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("jq"), reason="Language 'jq' not available")
def test_smoke_jq():
    """Smoke test: load jq and parse a simple snippet"""
    parser = get_parser("jq")
    tree = parser.parse(b".[] | select(.key)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("jsdoc"), reason="Language 'jsdoc' not available")
def test_smoke_jsdoc():
    """Smoke test: load jsdoc and parse a simple snippet"""
    parser = get_parser("jsdoc")
    tree = parser.parse(b"/** @param {string} name */")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("json"), reason="Language 'json' not available")
def test_smoke_json():
    """Smoke test: load json and parse a simple snippet"""
    parser = get_parser("json")
    tree = parser.parse(b'{"key": "value"}')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("json5"), reason="Language 'json5' not available")
def test_smoke_json5():
    """Smoke test: load json5 and parse a simple snippet"""
    parser = get_parser("json5")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("jsonnet"), reason="Language 'jsonnet' not available")
def test_smoke_jsonnet():
    """Smoke test: load jsonnet and parse a simple snippet"""
    parser = get_parser("jsonnet")
    tree = parser.parse(b"{ key: 'value' }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("julia"), reason="Language 'julia' not available")
def test_smoke_julia():
    """Smoke test: load julia and parse a simple snippet"""
    parser = get_parser("julia")
    tree = parser.parse(b"function main() end")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("just"), reason="Language 'just' not available")
def test_smoke_just():
    """Smoke test: load just and parse a simple snippet"""
    parser = get_parser("just")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("kcl"), reason="Language 'kcl' not available")
def test_smoke_kcl():
    """Smoke test: load kcl and parse a simple snippet"""
    parser = get_parser("kcl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("kconfig"), reason="Language 'kconfig' not available")
def test_smoke_kconfig():
    """Smoke test: load kconfig and parse a simple snippet"""
    parser = get_parser("kconfig")
    tree = parser.parse(b'config FOO\n\tbool "Enable foo"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("kdl"), reason="Language 'kdl' not available")
def test_smoke_kdl():
    """Smoke test: load kdl and parse a simple snippet"""
    parser = get_parser("kdl")
    tree = parser.parse(b'node "value"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("kotlin"), reason="Language 'kotlin' not available")
def test_smoke_kotlin():
    """Smoke test: load kotlin and parse a simple snippet"""
    parser = get_parser("kotlin")
    tree = parser.parse(b"fun main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("latex"), reason="Language 'latex' not available")
def test_smoke_latex():
    """Smoke test: load latex and parse a simple snippet"""
    parser = get_parser("latex")
    tree = parser.parse(b"\\documentclass{article}\n\\begin{document}\nHello\n\\end{document}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("lean"), reason="Language 'lean' not available")
def test_smoke_lean():
    """Smoke test: load lean and parse a simple snippet"""
    parser = get_parser("lean")
    tree = parser.parse(b"def main : IO Unit := pure ()")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ledger"), reason="Language 'ledger' not available")
def test_smoke_ledger():
    """Smoke test: load ledger and parse a simple snippet"""
    parser = get_parser("ledger")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("less"), reason="Language 'less' not available")
def test_smoke_less():
    """Smoke test: load less and parse a simple snippet"""
    parser = get_parser("less")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("linkerscript"), reason="Language 'linkerscript' not available")
def test_smoke_linkerscript():
    """Smoke test: load linkerscript and parse a simple snippet"""
    parser = get_parser("linkerscript")
    tree = parser.parse(b"SECTIONS { .text : { *(.text) } }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("liquid"), reason="Language 'liquid' not available")
def test_smoke_liquid():
    """Smoke test: load liquid and parse a simple snippet"""
    parser = get_parser("liquid")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("llvm"), reason="Language 'llvm' not available")
def test_smoke_llvm():
    """Smoke test: load llvm and parse a simple snippet"""
    parser = get_parser("llvm")
    tree = parser.parse(b"define i32 @main() { ret i32 0 }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("lua"), reason="Language 'lua' not available")
def test_smoke_lua():
    """Smoke test: load lua and parse a simple snippet"""
    parser = get_parser("lua")
    tree = parser.parse(b"print('hello')")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("luadoc"), reason="Language 'luadoc' not available")
def test_smoke_luadoc():
    """Smoke test: load luadoc and parse a simple snippet"""
    parser = get_parser("luadoc")
    tree = parser.parse(b"---@param name string")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("luap"), reason="Language 'luap' not available")
def test_smoke_luap():
    """Smoke test: load luap and parse a simple snippet"""
    parser = get_parser("luap")
    tree = parser.parse(b"[a-z]+")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("luau"), reason="Language 'luau' not available")
def test_smoke_luau():
    """Smoke test: load luau and parse a simple snippet"""
    parser = get_parser("luau")
    tree = parser.parse(b"local x: number = 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("magik"), reason="Language 'magik' not available")
def test_smoke_magik():
    """Smoke test: load magik and parse a simple snippet"""
    parser = get_parser("magik")
    tree = parser.parse(b"_method object.hello\n_endmethod")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("make"), reason="Language 'make' not available")
def test_smoke_make():
    """Smoke test: load make and parse a simple snippet"""
    parser = get_parser("make")
    tree = parser.parse(b"all:\n\techo hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("markdown"), reason="Language 'markdown' not available")
def test_smoke_markdown():
    """Smoke test: load markdown and parse a simple snippet"""
    parser = get_parser("markdown")
    tree = parser.parse(b"# Hello\n\nWorld")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("markdown_inline"), reason="Language 'markdown_inline' not available")
def test_smoke_markdown_inline():
    """Smoke test: load markdown_inline and parse a simple snippet"""
    parser = get_parser("markdown_inline")
    tree = parser.parse(b"**bold** and *italic*")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("matlab"), reason="Language 'matlab' not available")
def test_smoke_matlab():
    """Smoke test: load matlab and parse a simple snippet"""
    parser = get_parser("matlab")
    tree = parser.parse(b"function y = hello(x)\ny = x;\nend")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("mermaid"), reason="Language 'mermaid' not available")
def test_smoke_mermaid():
    """Smoke test: load mermaid and parse a simple snippet"""
    parser = get_parser("mermaid")
    tree = parser.parse(b"graph TD\nA --> B")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("meson"), reason="Language 'meson' not available")
def test_smoke_meson():
    """Smoke test: load meson and parse a simple snippet"""
    parser = get_parser("meson")
    tree = parser.parse(b"project('hello', 'c')")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("mlir"), reason="Language 'mlir' not available")
def test_smoke_mlir():
    """Smoke test: load mlir and parse a simple snippet"""
    parser = get_parser("mlir")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("mojo"), reason="Language 'mojo' not available")
def test_smoke_mojo():
    """Smoke test: load mojo and parse a simple snippet"""
    parser = get_parser("mojo")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("move"), reason="Language 'move' not available")
def test_smoke_move():
    """Smoke test: load move and parse a simple snippet"""
    parser = get_parser("move")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nasm"), reason="Language 'nasm' not available")
def test_smoke_nasm():
    """Smoke test: load nasm and parse a simple snippet"""
    parser = get_parser("nasm")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("netlinx"), reason="Language 'netlinx' not available")
def test_smoke_netlinx():
    """Smoke test: load netlinx and parse a simple snippet"""
    parser = get_parser("netlinx")
    tree = parser.parse(b"PROGRAM_NAME='hello'")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nginx"), reason="Language 'nginx' not available")
def test_smoke_nginx():
    """Smoke test: load nginx and parse a simple snippet"""
    parser = get_parser("nginx")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nickel"), reason="Language 'nickel' not available")
def test_smoke_nickel():
    """Smoke test: load nickel and parse a simple snippet"""
    parser = get_parser("nickel")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nim"), reason="Language 'nim' not available")
def test_smoke_nim():
    """Smoke test: load nim and parse a simple snippet"""
    parser = get_parser("nim")
    tree = parser.parse(b'echo "hello"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ninja"), reason="Language 'ninja' not available")
def test_smoke_ninja():
    """Smoke test: load ninja and parse a simple snippet"""
    parser = get_parser("ninja")
    tree = parser.parse(b"rule cc\n  command = cc $in -o $out")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nix"), reason="Language 'nix' not available")
def test_smoke_nix():
    """Smoke test: load nix and parse a simple snippet"""
    parser = get_parser("nix")
    tree = parser.parse(b"{ pkgs ? import <nixpkgs> {} }: pkgs.hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("norg"), reason="Language 'norg' not available")
def test_smoke_norg():
    """Smoke test: load norg and parse a simple snippet"""
    parser = get_parser("norg")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("norg_meta"), reason="Language 'norg_meta' not available")
def test_smoke_norg_meta():
    """Smoke test: load norg_meta and parse a simple snippet"""
    parser = get_parser("norg_meta")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nqc"), reason="Language 'nqc' not available")
def test_smoke_nqc():
    """Smoke test: load nqc and parse a simple snippet"""
    parser = get_parser("nqc")
    tree = parser.parse(b"task main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("nushell"), reason="Language 'nushell' not available")
def test_smoke_nushell():
    """Smoke test: load nushell and parse a simple snippet"""
    parser = get_parser("nushell")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("objc"), reason="Language 'objc' not available")
def test_smoke_objc():
    """Smoke test: load objc and parse a simple snippet"""
    parser = get_parser("objc")
    tree = parser.parse(b"@interface Main @end")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ocaml"), reason="Language 'ocaml' not available")
def test_smoke_ocaml():
    """Smoke test: load ocaml and parse a simple snippet"""
    parser = get_parser("ocaml")
    tree = parser.parse(b'let () = print_endline "hello"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ocaml_interface"), reason="Language 'ocaml_interface' not available")
def test_smoke_ocaml_interface():
    """Smoke test: load ocaml_interface and parse a simple snippet"""
    parser = get_parser("ocaml_interface")
    tree = parser.parse(b"val x : int")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ocamllex"), reason="Language 'ocamllex' not available")
def test_smoke_ocamllex():
    """Smoke test: load ocamllex and parse a simple snippet"""
    parser = get_parser("ocamllex")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("odin"), reason="Language 'odin' not available")
def test_smoke_odin():
    """Smoke test: load odin and parse a simple snippet"""
    parser = get_parser("odin")
    tree = parser.parse(b"package main")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("openscad"), reason="Language 'openscad' not available")
def test_smoke_openscad():
    """Smoke test: load openscad and parse a simple snippet"""
    parser = get_parser("openscad")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("org"), reason="Language 'org' not available")
def test_smoke_org():
    """Smoke test: load org and parse a simple snippet"""
    parser = get_parser("org")
    tree = parser.parse(b"* Hello\nWorld")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pascal"), reason="Language 'pascal' not available")
def test_smoke_pascal():
    """Smoke test: load pascal and parse a simple snippet"""
    parser = get_parser("pascal")
    tree = parser.parse(b"program Hello; begin end.")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pem"), reason="Language 'pem' not available")
def test_smoke_pem():
    """Smoke test: load pem and parse a simple snippet"""
    parser = get_parser("pem")
    tree = parser.parse(b"-----BEGIN CERTIFICATE-----\ndata\n-----END CERTIFICATE-----")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("perl"), reason="Language 'perl' not available")
def test_smoke_perl():
    """Smoke test: load perl and parse a simple snippet"""
    parser = get_parser("perl")
    tree = parser.parse(b"print 'hello';")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pgn"), reason="Language 'pgn' not available")
def test_smoke_pgn():
    """Smoke test: load pgn and parse a simple snippet"""
    parser = get_parser("pgn")
    tree = parser.parse(b"1. e4 e5 *")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("php"), reason="Language 'php' not available")
def test_smoke_php():
    """Smoke test: load php and parse a simple snippet"""
    parser = get_parser("php")
    tree = parser.parse(b"<?php echo 'hello'; ?>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("phpdoc"), reason="Language 'phpdoc' not available")
def test_smoke_phpdoc():
    """Smoke test: load phpdoc and parse a simple snippet"""
    parser = get_parser("phpdoc")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pkl"), reason="Language 'pkl' not available")
def test_smoke_pkl():
    """Smoke test: load pkl and parse a simple snippet"""
    parser = get_parser("pkl")
    tree = parser.parse(b'name = "hello"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("po"), reason="Language 'po' not available")
def test_smoke_po():
    """Smoke test: load po and parse a simple snippet"""
    parser = get_parser("po")
    tree = parser.parse(b'msgid "hello"\nmsgstr "world"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("poe_filter"), reason="Language 'poe_filter' not available")
def test_smoke_poe_filter():
    """Smoke test: load poe_filter and parse a simple snippet"""
    parser = get_parser("poe_filter")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pony"), reason="Language 'pony' not available")
def test_smoke_pony():
    """Smoke test: load pony and parse a simple snippet"""
    parser = get_parser("pony")
    tree = parser.parse(b"actor Main\n  new create(env: Env) => None")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("postscript"), reason="Language 'postscript' not available")
def test_smoke_postscript():
    """Smoke test: load postscript and parse a simple snippet"""
    parser = get_parser("postscript")
    tree = parser.parse(b"/hello { (Hello) show } def")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("powershell"), reason="Language 'powershell' not available")
def test_smoke_powershell():
    """Smoke test: load powershell and parse a simple snippet"""
    parser = get_parser("powershell")
    tree = parser.parse(b"Write-Host 'hello'")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("printf"), reason="Language 'printf' not available")
def test_smoke_printf():
    """Smoke test: load printf and parse a simple snippet"""
    parser = get_parser("printf")
    tree = parser.parse(b"%d %s")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("prisma"), reason="Language 'prisma' not available")
def test_smoke_prisma():
    """Smoke test: load prisma and parse a simple snippet"""
    parser = get_parser("prisma")
    tree = parser.parse(b"model User { id Int @id }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("prolog"), reason="Language 'prolog' not available")
def test_smoke_prolog():
    """Smoke test: load prolog and parse a simple snippet"""
    parser = get_parser("prolog")
    tree = parser.parse(b"hello :- write('hello'), nl.")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("promql"), reason="Language 'promql' not available")
def test_smoke_promql():
    """Smoke test: load promql and parse a simple snippet"""
    parser = get_parser("promql")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("properties"), reason="Language 'properties' not available")
def test_smoke_properties():
    """Smoke test: load properties and parse a simple snippet"""
    parser = get_parser("properties")
    tree = parser.parse(b"key=value")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("proto"), reason="Language 'proto' not available")
def test_smoke_proto():
    """Smoke test: load proto and parse a simple snippet"""
    parser = get_parser("proto")
    tree = parser.parse(b'syntax = "proto3";')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("prql"), reason="Language 'prql' not available")
def test_smoke_prql():
    """Smoke test: load prql and parse a simple snippet"""
    parser = get_parser("prql")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("psv"), reason="Language 'psv' not available")
def test_smoke_psv():
    """Smoke test: load psv and parse a simple snippet"""
    parser = get_parser("psv")
    tree = parser.parse(b"a|b|c\n1|2|3")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pug"), reason="Language 'pug' not available")
def test_smoke_pug():
    """Smoke test: load pug and parse a simple snippet"""
    parser = get_parser("pug")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("puppet"), reason="Language 'puppet' not available")
def test_smoke_puppet():
    """Smoke test: load puppet and parse a simple snippet"""
    parser = get_parser("puppet")
    tree = parser.parse(b"notify { 'hello': }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("purescript"), reason="Language 'purescript' not available")
def test_smoke_purescript():
    """Smoke test: load purescript and parse a simple snippet"""
    parser = get_parser("purescript")
    tree = parser.parse(b"module Main where")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("pymanifest"), reason="Language 'pymanifest' not available")
def test_smoke_pymanifest():
    """Smoke test: load pymanifest and parse a simple snippet"""
    parser = get_parser("pymanifest")
    tree = parser.parse(b"include *.txt")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("python"), reason="Language 'python' not available")
def test_smoke_python():
    """Smoke test: load python and parse a simple snippet"""
    parser = get_parser("python")
    tree = parser.parse(b"print('hello')")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ql"), reason="Language 'ql' not available")
def test_smoke_ql():
    """Smoke test: load ql and parse a simple snippet"""
    parser = get_parser("ql")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("qmldir"), reason="Language 'qmldir' not available")
def test_smoke_qmldir():
    """Smoke test: load qmldir and parse a simple snippet"""
    parser = get_parser("qmldir")
    tree = parser.parse(b"module Example")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("qmljs"), reason="Language 'qmljs' not available")
def test_smoke_qmljs():
    """Smoke test: load qmljs and parse a simple snippet"""
    parser = get_parser("qmljs")
    tree = parser.parse(b"import QtQuick 2.0\nItem {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("query"), reason="Language 'query' not available")
def test_smoke_query():
    """Smoke test: load query and parse a simple snippet"""
    parser = get_parser("query")
    tree = parser.parse(b"(identifier) @name")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("r"), reason="Language 'r' not available")
def test_smoke_r():
    """Smoke test: load r and parse a simple snippet"""
    parser = get_parser("r")
    tree = parser.parse(b"print('hello')")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("racket"), reason="Language 'racket' not available")
def test_smoke_racket():
    """Smoke test: load racket and parse a simple snippet"""
    parser = get_parser("racket")
    tree = parser.parse(b"#lang racket\n(define x 1)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rasi"), reason="Language 'rasi' not available")
def test_smoke_rasi():
    """Smoke test: load rasi and parse a simple snippet"""
    parser = get_parser("rasi")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("razor"), reason="Language 'razor' not available")
def test_smoke_razor():
    """Smoke test: load razor and parse a simple snippet"""
    parser = get_parser("razor")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rbs"), reason="Language 'rbs' not available")
def test_smoke_rbs():
    """Smoke test: load rbs and parse a simple snippet"""
    parser = get_parser("rbs")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("re2c"), reason="Language 're2c' not available")
def test_smoke_re2c():
    """Smoke test: load re2c and parse a simple snippet"""
    parser = get_parser("re2c")
    tree = parser.parse(b"/*!re2c\n  [a-z]+ { return; }\n*/")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("readline"), reason="Language 'readline' not available")
def test_smoke_readline():
    """Smoke test: load readline and parse a simple snippet"""
    parser = get_parser("readline")
    tree = parser.parse(b"set editing-mode vi")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("regex"), reason="Language 'regex' not available")
def test_smoke_regex():
    """Smoke test: load regex and parse a simple snippet"""
    parser = get_parser("regex")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rego"), reason="Language 'rego' not available")
def test_smoke_rego():
    """Smoke test: load rego and parse a simple snippet"""
    parser = get_parser("rego")
    tree = parser.parse(b"package main\ndefault allow = false")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("requirements"), reason="Language 'requirements' not available")
def test_smoke_requirements():
    """Smoke test: load requirements and parse a simple snippet"""
    parser = get_parser("requirements")
    tree = parser.parse(b"flask>=2.0")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rescript"), reason="Language 'rescript' not available")
def test_smoke_rescript():
    """Smoke test: load rescript and parse a simple snippet"""
    parser = get_parser("rescript")
    tree = parser.parse(b"let x = 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("robot"), reason="Language 'robot' not available")
def test_smoke_robot():
    """Smoke test: load robot and parse a simple snippet"""
    parser = get_parser("robot")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("roc"), reason="Language 'roc' not available")
def test_smoke_roc():
    """Smoke test: load roc and parse a simple snippet"""
    parser = get_parser("roc")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ron"), reason="Language 'ron' not available")
def test_smoke_ron():
    """Smoke test: load ron and parse a simple snippet"""
    parser = get_parser("ron")
    tree = parser.parse(b'(key: "value")')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rst"), reason="Language 'rst' not available")
def test_smoke_rst():
    """Smoke test: load rst and parse a simple snippet"""
    parser = get_parser("rst")
    tree = parser.parse(b"Hello\n=====\n\nWorld")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rtf"), reason="Language 'rtf' not available")
def test_smoke_rtf():
    """Smoke test: load rtf and parse a simple snippet"""
    parser = get_parser("rtf")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ruby"), reason="Language 'ruby' not available")
def test_smoke_ruby():
    """Smoke test: load ruby and parse a simple snippet"""
    parser = get_parser("ruby")
    tree = parser.parse(b"puts 'hello'")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("rust"), reason="Language 'rust' not available")
def test_smoke_rust():
    """Smoke test: load rust and parse a simple snippet"""
    parser = get_parser("rust")
    tree = parser.parse(b"fn main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("scala"), reason="Language 'scala' not available")
def test_smoke_scala():
    """Smoke test: load scala and parse a simple snippet"""
    parser = get_parser("scala")
    tree = parser.parse(b"object Main")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("scheme"), reason="Language 'scheme' not available")
def test_smoke_scheme():
    """Smoke test: load scheme and parse a simple snippet"""
    parser = get_parser("scheme")
    tree = parser.parse(b"(define x 1)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("scss"), reason="Language 'scss' not available")
def test_smoke_scss():
    """Smoke test: load scss and parse a simple snippet"""
    parser = get_parser("scss")
    tree = parser.parse(b"$color: red;\nbody { color: $color; }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("slang"), reason="Language 'slang' not available")
def test_smoke_slang():
    """Smoke test: load slang and parse a simple snippet"""
    parser = get_parser("slang")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("smali"), reason="Language 'smali' not available")
def test_smoke_smali():
    """Smoke test: load smali and parse a simple snippet"""
    parser = get_parser("smali")
    tree = parser.parse(b".class public LMain;\n.super Ljava/lang/Object;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("smalltalk"), reason="Language 'smalltalk' not available")
def test_smoke_smalltalk():
    """Smoke test: load smalltalk and parse a simple snippet"""
    parser = get_parser("smalltalk")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("smithy"), reason="Language 'smithy' not available")
def test_smoke_smithy():
    """Smoke test: load smithy and parse a simple snippet"""
    parser = get_parser("smithy")
    tree = parser.parse(b"namespace example\nstring MyString")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sml"), reason="Language 'sml' not available")
def test_smoke_sml():
    """Smoke test: load sml and parse a simple snippet"""
    parser = get_parser("sml")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("snakemake"), reason="Language 'snakemake' not available")
def test_smoke_snakemake():
    """Smoke test: load snakemake and parse a simple snippet"""
    parser = get_parser("snakemake")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("solidity"), reason="Language 'solidity' not available")
def test_smoke_solidity():
    """Smoke test: load solidity and parse a simple snippet"""
    parser = get_parser("solidity")
    tree = parser.parse(b"pragma solidity ^0.8.0;\ncontract Main {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("souffle"), reason="Language 'souffle' not available")
def test_smoke_souffle():
    """Smoke test: load souffle and parse a simple snippet"""
    parser = get_parser("souffle")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sourcepawn"), reason="Language 'sourcepawn' not available")
def test_smoke_sourcepawn():
    """Smoke test: load sourcepawn and parse a simple snippet"""
    parser = get_parser("sourcepawn")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sparql"), reason="Language 'sparql' not available")
def test_smoke_sparql():
    """Smoke test: load sparql and parse a simple snippet"""
    parser = get_parser("sparql")
    tree = parser.parse(b"SELECT ?s WHERE { ?s ?p ?o }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sql"), reason="Language 'sql' not available")
def test_smoke_sql():
    """Smoke test: load sql and parse a simple snippet"""
    parser = get_parser("sql")
    tree = parser.parse(b"SELECT 1;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sql_bigquery"), reason="Language 'sql_bigquery' not available")
def test_smoke_sql_bigquery():
    """Smoke test: load sql_bigquery and parse a simple snippet"""
    parser = get_parser("sql_bigquery")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("squirrel"), reason="Language 'squirrel' not available")
def test_smoke_squirrel():
    """Smoke test: load squirrel and parse a simple snippet"""
    parser = get_parser("squirrel")
    tree = parser.parse(b"function main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ssh_config"), reason="Language 'ssh_config' not available")
def test_smoke_ssh_config():
    """Smoke test: load ssh_config and parse a simple snippet"""
    parser = get_parser("ssh_config")
    tree = parser.parse(b"Host example\n  HostName example.com")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("stan"), reason="Language 'stan' not available")
def test_smoke_stan():
    """Smoke test: load stan and parse a simple snippet"""
    parser = get_parser("stan")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("starlark"), reason="Language 'starlark' not available")
def test_smoke_starlark():
    """Smoke test: load starlark and parse a simple snippet"""
    parser = get_parser("starlark")
    tree = parser.parse(b"def hello(): pass")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("superhtml"), reason="Language 'superhtml' not available")
def test_smoke_superhtml():
    """Smoke test: load superhtml and parse a simple snippet"""
    parser = get_parser("superhtml")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("svelte"), reason="Language 'svelte' not available")
def test_smoke_svelte():
    """Smoke test: load svelte and parse a simple snippet"""
    parser = get_parser("svelte")
    tree = parser.parse(b"<script>let x = 1;</script>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("sway"), reason="Language 'sway' not available")
def test_smoke_sway():
    """Smoke test: load sway and parse a simple snippet"""
    parser = get_parser("sway")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("swift"), reason="Language 'swift' not available")
def test_smoke_swift():
    """Smoke test: load swift and parse a simple snippet"""
    parser = get_parser("swift")
    tree = parser.parse(b'print("hello")')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("systemverilog"), reason="Language 'systemverilog' not available")
def test_smoke_systemverilog():
    """Smoke test: load systemverilog and parse a simple snippet"""
    parser = get_parser("systemverilog")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tablegen"), reason="Language 'tablegen' not available")
def test_smoke_tablegen():
    """Smoke test: load tablegen and parse a simple snippet"""
    parser = get_parser("tablegen")
    tree = parser.parse(b"def Hello : Base {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tact"), reason="Language 'tact' not available")
def test_smoke_tact():
    """Smoke test: load tact and parse a simple snippet"""
    parser = get_parser("tact")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tcl"), reason="Language 'tcl' not available")
def test_smoke_tcl():
    """Smoke test: load tcl and parse a simple snippet"""
    parser = get_parser("tcl")
    tree = parser.parse(b"puts hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("teal"), reason="Language 'teal' not available")
def test_smoke_teal():
    """Smoke test: load teal and parse a simple snippet"""
    parser = get_parser("teal")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("templ"), reason="Language 'templ' not available")
def test_smoke_templ():
    """Smoke test: load templ and parse a simple snippet"""
    parser = get_parser("templ")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tera"), reason="Language 'tera' not available")
def test_smoke_tera():
    """Smoke test: load tera and parse a simple snippet"""
    parser = get_parser("tera")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("terraform"), reason="Language 'terraform' not available")
def test_smoke_terraform():
    """Smoke test: load terraform and parse a simple snippet"""
    parser = get_parser("terraform")
    tree = parser.parse(b'resource "null_resource" "main" {}')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("test"), reason="Language 'test' not available")
def test_smoke_test():
    """Smoke test: load test and parse a simple snippet"""
    parser = get_parser("test")
    tree = parser.parse(b"===========\nTest\n===========\n---\n(node)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("textproto"), reason="Language 'textproto' not available")
def test_smoke_textproto():
    """Smoke test: load textproto and parse a simple snippet"""
    parser = get_parser("textproto")
    tree = parser.parse(b'key: "value"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("thrift"), reason="Language 'thrift' not available")
def test_smoke_thrift():
    """Smoke test: load thrift and parse a simple snippet"""
    parser = get_parser("thrift")
    tree = parser.parse(b"service HelloService {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tlaplus"), reason="Language 'tlaplus' not available")
def test_smoke_tlaplus():
    """Smoke test: load tlaplus and parse a simple snippet"""
    parser = get_parser("tlaplus")
    tree = parser.parse(b"---- MODULE Main ----\n====")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tmux"), reason="Language 'tmux' not available")
def test_smoke_tmux():
    """Smoke test: load tmux and parse a simple snippet"""
    parser = get_parser("tmux")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("todotxt"), reason="Language 'todotxt' not available")
def test_smoke_todotxt():
    """Smoke test: load todotxt and parse a simple snippet"""
    parser = get_parser("todotxt")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("toml"), reason="Language 'toml' not available")
def test_smoke_toml():
    """Smoke test: load toml and parse a simple snippet"""
    parser = get_parser("toml")
    tree = parser.parse(b'key = "value"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tsv"), reason="Language 'tsv' not available")
def test_smoke_tsv():
    """Smoke test: load tsv and parse a simple snippet"""
    parser = get_parser("tsv")
    tree = parser.parse(b"a\tb\tc\n1\t2\t3")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("tsx"), reason="Language 'tsx' not available")
def test_smoke_tsx():
    """Smoke test: load tsx and parse a simple snippet"""
    parser = get_parser("tsx")
    tree = parser.parse(b"const App = () => <div />;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("turtle"), reason="Language 'turtle' not available")
def test_smoke_turtle():
    """Smoke test: load turtle and parse a simple snippet"""
    parser = get_parser("turtle")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("twig"), reason="Language 'twig' not available")
def test_smoke_twig():
    """Smoke test: load twig and parse a simple snippet"""
    parser = get_parser("twig")
    tree = parser.parse(b"{{ variable }}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("typescript"), reason="Language 'typescript' not available")
def test_smoke_typescript():
    """Smoke test: load typescript and parse a simple snippet"""
    parser = get_parser("typescript")
    tree = parser.parse(b"const x: number = 42;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("typespec"), reason="Language 'typespec' not available")
def test_smoke_typespec():
    """Smoke test: load typespec and parse a simple snippet"""
    parser = get_parser("typespec")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("typoscript"), reason="Language 'typoscript' not available")
def test_smoke_typoscript():
    """Smoke test: load typoscript and parse a simple snippet"""
    parser = get_parser("typoscript")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("typst"), reason="Language 'typst' not available")
def test_smoke_typst():
    """Smoke test: load typst and parse a simple snippet"""
    parser = get_parser("typst")
    tree = parser.parse(b"#let x = 1")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("udev"), reason="Language 'udev' not available")
def test_smoke_udev():
    """Smoke test: load udev and parse a simple snippet"""
    parser = get_parser("udev")
    tree = parser.parse(b'ACTION=="add", KERNEL=="sd*"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ungrammar"), reason="Language 'ungrammar' not available")
def test_smoke_ungrammar():
    """Smoke test: load ungrammar and parse a simple snippet"""
    parser = get_parser("ungrammar")
    tree = parser.parse(b"Root = Item*\nItem = 'token'")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("uxntal"), reason="Language 'uxntal' not available")
def test_smoke_uxntal():
    """Smoke test: load uxntal and parse a simple snippet"""
    parser = get_parser("uxntal")
    tree = parser.parse(b"|0100 LIT 01")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("v"), reason="Language 'v' not available")
def test_smoke_v():
    """Smoke test: load v and parse a simple snippet"""
    parser = get_parser("v")
    tree = parser.parse(b"fn main() {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vb"), reason="Language 'vb' not available")
def test_smoke_vb():
    """Smoke test: load vb and parse a simple snippet"""
    parser = get_parser("vb")
    tree = parser.parse(b"Module Main\nEnd Module")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("verilog"), reason="Language 'verilog' not available")
def test_smoke_verilog():
    """Smoke test: load verilog and parse a simple snippet"""
    parser = get_parser("verilog")
    tree = parser.parse(b"module main; endmodule")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vhdl"), reason="Language 'vhdl' not available")
def test_smoke_vhdl():
    """Smoke test: load vhdl and parse a simple snippet"""
    parser = get_parser("vhdl")
    tree = parser.parse(b"entity main is end main;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vhs"), reason="Language 'vhs' not available")
def test_smoke_vhs():
    """Smoke test: load vhs and parse a simple snippet"""
    parser = get_parser("vhs")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vim"), reason="Language 'vim' not available")
def test_smoke_vim():
    """Smoke test: load vim and parse a simple snippet"""
    parser = get_parser("vim")
    tree = parser.parse(b"echo 'hello'")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vimdoc"), reason="Language 'vimdoc' not available")
def test_smoke_vimdoc():
    """Smoke test: load vimdoc and parse a simple snippet"""
    parser = get_parser("vimdoc")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vrl"), reason="Language 'vrl' not available")
def test_smoke_vrl():
    """Smoke test: load vrl and parse a simple snippet"""
    parser = get_parser("vrl")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("vue"), reason="Language 'vue' not available")
def test_smoke_vue():
    """Smoke test: load vue and parse a simple snippet"""
    parser = get_parser("vue")
    tree = parser.parse(b"<template><div>hello</div></template>")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wast"), reason="Language 'wast' not available")
def test_smoke_wast():
    """Smoke test: load wast and parse a simple snippet"""
    parser = get_parser("wast")
    tree = parser.parse(b"(module)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wat"), reason="Language 'wat' not available")
def test_smoke_wat():
    """Smoke test: load wat and parse a simple snippet"""
    parser = get_parser("wat")
    tree = parser.parse(b"(module)")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wgsl"), reason="Language 'wgsl' not available")
def test_smoke_wgsl():
    """Smoke test: load wgsl and parse a simple snippet"""
    parser = get_parser("wgsl")
    tree = parser.parse(b"@vertex fn main() -> @builtin(position) vec4f { return vec4f(); }")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wgsl_bevy"), reason="Language 'wgsl_bevy' not available")
def test_smoke_wgsl_bevy():
    """Smoke test: load wgsl_bevy and parse a simple snippet"""
    parser = get_parser("wgsl_bevy")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wit"), reason="Language 'wit' not available")
def test_smoke_wit():
    """Smoke test: load wit and parse a simple snippet"""
    parser = get_parser("wit")
    tree = parser.parse(b"package example:pkg;")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("wolfram"), reason="Language 'wolfram' not available")
def test_smoke_wolfram():
    """Smoke test: load wolfram and parse a simple snippet"""
    parser = get_parser("wolfram")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("x86asm"), reason="Language 'x86asm' not available")
def test_smoke_x86asm():
    """Smoke test: load x86asm and parse a simple snippet"""
    parser = get_parser("x86asm")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("xcompose"), reason="Language 'xcompose' not available")
def test_smoke_xcompose():
    """Smoke test: load xcompose and parse a simple snippet"""
    parser = get_parser("xcompose")
    tree = parser.parse(b'<Multi_key> <a> : "a"')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("xml"), reason="Language 'xml' not available")
def test_smoke_xml():
    """Smoke test: load xml and parse a simple snippet"""
    parser = get_parser("xml")
    tree = parser.parse(b'<?xml version="1.0"?>\n<root>hello</root>')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("yaml"), reason="Language 'yaml' not available")
def test_smoke_yaml():
    """Smoke test: load yaml and parse a simple snippet"""
    parser = get_parser("yaml")
    tree = parser.parse(b"key: value")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("yuck"), reason="Language 'yuck' not available")
def test_smoke_yuck():
    """Smoke test: load yuck and parse a simple snippet"""
    parser = get_parser("yuck")
    tree = parser.parse(b'(defwidget main [] (label :text "hi"))')
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("zig"), reason="Language 'zig' not available")
def test_smoke_zig():
    """Smoke test: load zig and parse a simple snippet"""
    parser = get_parser("zig")
    tree = parser.parse(b"pub fn main() void {}")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ziggy"), reason="Language 'ziggy' not available")
def test_smoke_ziggy():
    """Smoke test: load ziggy and parse a simple snippet"""
    parser = get_parser("ziggy")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("ziggy_schema"), reason="Language 'ziggy_schema' not available")
def test_smoke_ziggy_schema():
    """Smoke test: load ziggy_schema and parse a simple snippet"""
    parser = get_parser("ziggy_schema")
    tree = parser.parse(b"x")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"


@pytest.mark.skipif(not has_language("zsh"), reason="Language 'zsh' not available")
def test_smoke_zsh():
    """Smoke test: load zsh and parse a simple snippet"""
    parser = get_parser("zsh")
    tree = parser.parse(b"echo hello")
    assert tree is not None, "Parse tree should not be None"
    root = tree.root_node
    assert root.child_count >= 1, f"Root should have at least 1 child(ren), got {root.child_count}"
