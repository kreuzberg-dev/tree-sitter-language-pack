// Generated by alef. Do not edit by hand.
// alef:hash:ee78d1f1b0a8a77b9c0e6f93a09770f387b8a8be90a319d211c4f1fc52a3f49a
//
// JNI shim for the Android AAR. Every `pub unsafe extern "system" fn`
// here matches one `external fun native*` in the paired Kotlin Bridge object.

#![allow(non_snake_case)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::missing_safety_doc)]
#![allow(unused_imports)]
#![allow(unused_variables)]
#![allow(unused_mut)]
#![allow(dead_code)]
#![allow(clippy::let_unit_value)]
#![allow(clippy::unused_unit)]
#![allow(clippy::let_and_return)]
#![allow(missing_docs)]
#![allow(unsafe_op_in_unsafe_fn, unsafe_attr_outside_unsafe)]

use futures_util::StreamExt;
use futures_util::stream::BoxStream;
use jni::objects::{JClass, JObject, JString};
use jni::sys::{jboolean, jbyteArray, jlong, jstring};
use jni::{AttachGuard, Env, EnvUnowned};
use std::sync::Mutex;
use std::sync::OnceLock;
use tokio::runtime::Runtime;

use core_crate::*;
use tree_sitter_language_pack as core_crate;

const ERROR_CLASS: &str = "io/xberg/tslp/android/TreeSitterLanguagePackBridgeException";
fn runtime() -> &'static Runtime {
    static RT: OnceLock<Runtime> = OnceLock::new();
    RT.get_or_init(|| Runtime::new().expect("create tokio runtime"))
}

fn jstring_to_string(env: &mut Env<'_>, s: JString) -> std::result::Result<String, jni::errors::Error> {
    s.try_to_string(env)
}

fn string_to_jstring(env: &mut Env<'_>, s: impl AsRef<str>) -> jstring {
    match env.new_string(s.as_ref()) {
        Ok(o) => o.into_raw(),
        Err(_) => std::ptr::null_mut(),
    }
}

fn jni_call_string_method(
    env: &mut Env<'_>,
    obj: JObject,
    method_name: &str,
    method_sig: &str,
) -> std::result::Result<String, jni::errors::Error> {
    use std::str::FromStr;
    let class = env.get_object_class(&obj)?;
    let name_jni = jni::strings::JNIString::from(method_name);
    let sig_runtime = jni::signature::RuntimeMethodSignature::from_str(method_sig)?;
    let sig = sig_runtime.method_signature();
    let method_id = env.get_method_id(&class, name_jni, sig)?;
    // SAFETY: method_id is valid from the preceding get_method_id call, and the method exists on the class.
    let result = unsafe { env.call_method_unchecked(&obj, method_id, jni::signature::ReturnType::Object, &[])? }.l()?;
    // SAFETY: JNI return type guaranteed a String, so the raw jstring pointer is valid.
    let jstring = unsafe { JString::from_raw(env, result.into_raw()) };
    jstring_to_string(env, jstring)
}

fn throw_jni_error(env: &mut Env<'_>, msg: &str) {
    // If the error class cannot be found (misconfigured AAR), fall back to a
    // generic RuntimeException so the caller always gets *some* exception rather
    // than a silent null/zero return that looks like a valid result.
    let class_jni = jni::strings::JNIString::from(ERROR_CLASS);
    let msg_jni = jni::strings::JNIString::from(msg);
    if env.throw_new(&class_jni, &msg_jni).is_err() {
        let fallback = jni::strings::JNIString::from("java/lang/RuntimeException");
        let _ = env.throw_new(&fallback, &msg_jni);
    }
}

fn run_or_throw<T, F>(env: &mut Env<'_>, f: F) -> Option<T>
where
    F: FnOnce(&mut Env<'_>) -> T,
{
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| f(env))) {
        Ok(v) => Some(v),
        Err(payload) => {
            let msg = payload
                .downcast_ref::<String>()
                .cloned()
                .or_else(|| payload.downcast_ref::<&str>().map(|s| (*s).to_string()))
                .unwrap_or_else(|| "panic in native code".to_string());
            throw_jni_error(env, &format!("native panic: {msg}"));
            None
        }
    }
}

/// Call a trait-bridge dispatcher's `dispatch(String, String) -> String` entry
/// point. Used by the generated plugin bridges to route trait method calls to
/// the JVM host object as JSON.
#[allow(dead_code)]
fn jni_dispatch_json(
    env: &mut Env<'_>,
    obj: &JObject,
    method: &str,
    args_json: &str,
) -> std::result::Result<String, jni::errors::Error> {
    use std::str::FromStr;
    let class = env.get_object_class(obj)?;
    let name_jni = jni::strings::JNIString::from("dispatch");
    let sig_runtime =
        jni::signature::RuntimeMethodSignature::from_str("(Ljava/lang/String;Ljava/lang/String;)Ljava/lang/String;")?;
    let sig = sig_runtime.method_signature();
    let method_id = env.get_method_id(&class, name_jni, sig)?;
    let method_j = env.new_string(method)?;
    let args_j = env.new_string(args_json)?;
    // SAFETY: method_id comes from get_method_id on this object's class and the
    // argument values match the looked-up signature.
    let call = unsafe {
        env.call_method_unchecked(
            obj,
            method_id,
            jni::signature::ReturnType::Object,
            &[
                jni::objects::JValue::Object(&method_j).as_jni(),
                jni::objects::JValue::Object(&args_j).as_jni(),
            ],
        )
    };
    let result = match call.and_then(|v| v.l()) {
        Ok(o) => o,
        Err(e) => {
            // A pending Java exception must be cleared before further JNI calls.
            if env.exception_check() {
                env.exception_clear();
            }
            return Err(e);
        }
    };
    // SAFETY: the dispatcher's `dispatch` method returns a Java String.
    let jstr = unsafe { JString::from_raw(env, result.into_raw()) };
    jstring_to_string(env, jstr)
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeDetectLanguageFromExtension(
    mut env: EnvUnowned,
    _class: JClass,
    ext: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let ext = match jstring_to_string(env, ext) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::detect_language_from_extension(&ext);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeDetectLanguageFromPath(
    mut env: EnvUnowned,
    _class: JClass,
    path: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let path = match jstring_to_string(env, path) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::detect_language_from_path(&path);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeDetectLanguageFromContent(
    mut env: EnvUnowned,
    _class: JClass,
    content: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let content = match jstring_to_string(env, content) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::detect_language_from_content(&content);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetHighlightsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_highlights_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetInjectionsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_injections_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetLocalsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_locals_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetTagsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_tags_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetIndentsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_indents_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetFoldsQuery(
    mut env: EnvUnowned,
    _class: JClass,
    language: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let language = match jstring_to_string(env, language) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::get_folds_query(&language);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    name: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let name = match jstring_to_string(env, name) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let result = core_crate::get_language(&name);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                0
            }
            Ok(v) => v.into_raw() as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeGetParser(
    mut env: EnvUnowned,
    _class: JClass,
    name: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let name = match jstring_to_string(env, name) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let result = core_crate::get_parser(&name);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                0
            }
            Ok(v) => Box::into_raw(Box::new(v)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeDetectLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    path: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let path = match jstring_to_string(env, path) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let v = core_crate::detect_language(&path);
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeAvailableLanguages(
    mut env: EnvUnowned,
    _class: JClass,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let v = core_crate::available_languages();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeHasLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    name: JString,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let name = match jstring_to_string(env, name) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return false;
            }
        };
        let v = core_crate::has_language(&name);
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageCount(
    mut env: EnvUnowned,
    _class: JClass,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let v = core_crate::language_count();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcess(
    mut env: EnvUnowned,
    _class: JClass,
    source: JString,
    config: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let source = match jstring_to_string(env, source) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let config_str = match jstring_to_string(env, config) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return std::ptr::null_mut();
            }
        };
        let config: core_crate::ProcessConfig = match serde_json::from_str(&config_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let result = core_crate::process(&source, &config);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                std::ptr::null_mut()
            }
            Ok(v) => {
                let s = match serde_json::to_string(&v) {
                    Ok(s) => s,
                    Err(e) => {
                        throw_jni_error(env, &format!("serialize: {e}"));
                        return std::ptr::null_mut();
                    }
                };
                string_to_jstring(env, s)
            }
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativePrefetch(
    mut env: EnvUnowned,
    _class: JClass,
    languages: JString,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        let languages_str = match jstring_to_string(env, languages) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return ();
            }
        };
        let languages: Vec<String> = match serde_json::from_str(&languages_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("deserialize: {e}"));
                return ();
            }
        };
        let languages_refs: Vec<&str> = languages.iter().map(String::as_str).collect();
        let result = core_crate::prefetch(&languages_refs);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                ()
            }
            Ok(v) => {}
        }
    }) else {
        return ();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserSetMaxSourceBytes(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return ();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return ();
            }
        };
        let max_bytes: Option<usize> = if req_str.is_empty() {
            None
        } else {
            match serde_json::from_str(&req_str) {
                Ok(v) => Some(v),
                Err(e) => {
                    throw_jni_error(env, &format!("request deserialize: {e}"));
                    return ();
                }
            }
        };
        let v = client.set_max_source_bytes(max_bytes);
    }) else {
        return ();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserSetParseTimeoutMs(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return ();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return ();
            }
        };
        let timeout_ms: Option<u64> = if req_str.is_empty() {
            None
        } else {
            match serde_json::from_str(&req_str) {
                Ok(v) => Some(v),
                Err(e) => {
                    throw_jni_error(env, &format!("request deserialize: {e}"));
                    return ();
                }
            }
        };
        let v = client.set_parse_timeout_ms(timeout_ms);
    }) else {
        return ();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserSetLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return ();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return ();
            }
        };
        let name: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let result = client.set_language(&name);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                ()
            }
            Ok(v) => {}
        }
    }) else {
        return ();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserParse(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let source: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let v = client.parse(&source);
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserParseBytes(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    source: jbyteArray,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let source_jarr = unsafe { jni::objects::JByteArray::from_raw(env, source) };
        let source: Vec<u8> = match env.convert_byte_array(&source_jarr) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let v = client.parse_bytes(&source);
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeParserReset(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return ();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::Parser = unsafe { &mut *(handle as *mut core_crate::Parser) };
        let v = client.reset();
    }) else {
        return ();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeFreeParser(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(()) = run_or_throw(env, |_env| {
        if handle == 0 {
            return;
        }
        // SAFETY: `handle` was allocated by the matching constructor shim and
        // ownership is transferred back here for drop via Box::from_raw.
        unsafe {
            let _ = Box::from_raw(handle as *mut core_crate::Parser);
        }
    }) else {
        return;
    };
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeRootNode(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Tree = unsafe { &*(handle as *const core_crate::Tree) };
        let v = client.root_node();
        Box::into_raw(Box::new(v)) as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeWalk(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Tree = unsafe { &*(handle as *const core_crate::Tree) };
        let v = client.walk();
        Box::into_raw(Box::new(v)) as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeFreeTree(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(()) = run_or_throw(env, |_env| {
        if handle == 0 {
            return;
        }
        // SAFETY: `handle` was allocated by the matching constructor shim and
        // ownership is transferred back here for drop via Box::from_raw.
        unsafe {
            let _ = Box::from_raw(handle as *mut core_crate::Tree);
        }
    }) else {
        return;
    };
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeKind(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.kind();
        string_to_jstring(env, v)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeKindId(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jni::sys::jshort {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.kind_id();
        v as jni::sys::jshort
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeStartByte(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.start_byte();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeEndByte(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.end_byte();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeByteRange(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.byte_range();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeStartPosition(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.start_position();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeEndPosition(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.end_position();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeIsNamed(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.is_named();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeIsError(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.is_error();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeIsMissing(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.is_missing();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeIsExtra(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.is_extra();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeHasError(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.has_error();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeParent(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.parent();
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeChild(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let index: u32 = match serde_json::from_str(&req_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("request deserialize: {e}"));
                return 0;
            }
        };
        let v = client.child(index);
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeChildCount(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.child_count();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeNamedChild(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let index: u32 = match serde_json::from_str(&req_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("request deserialize: {e}"));
                return 0;
            }
        };
        let v = client.named_child(index);
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeNamedChildCount(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.named_child_count();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeChildByFieldName(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let name: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let v = client.child_by_field_name(&name);
        match v {
            None => 0i64,
            Some(inner) => Box::into_raw(Box::new(inner)) as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeToSexp(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.to_sexp();
        string_to_jstring(env, v)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeNodeWalk(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::Node = unsafe { &*(handle as *const core_crate::Node) };
        let v = client.walk();
        Box::into_raw(Box::new(v)) as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeFreeNode(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(()) = run_or_throw(env, |_env| {
        if handle == 0 {
            return;
        }
        // SAFETY: `handle` was allocated by the matching constructor shim and
        // ownership is transferred back here for drop via Box::from_raw.
        unsafe {
            let _ = Box::from_raw(handle as *mut core_crate::Node);
        }
    }) else {
        return;
    };
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeCursorNode(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::TreeCursor = unsafe { &*(handle as *const core_crate::TreeCursor) };
        let v = client.node();
        Box::into_raw(Box::new(v)) as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeCursorGotoFirstChild(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::TreeCursor = unsafe { &mut *(handle as *mut core_crate::TreeCursor) };
        let v = client.goto_first_child();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeCursorGotoParent(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::TreeCursor = unsafe { &mut *(handle as *mut core_crate::TreeCursor) };
        let v = client.goto_parent();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeCursorGotoNextSibling(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &mut core_crate::TreeCursor = unsafe { &mut *(handle as *mut core_crate::TreeCursor) };
        let v = client.goto_next_sibling();
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeTreeCursorFieldName(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::TreeCursor = unsafe { &*(handle as *const core_crate::TreeCursor) };
        let v = client.field_name();
        match v {
            None => std::ptr::null_mut(),
            Some(s) => string_to_jstring(env, s),
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeFreeTreeCursor(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(()) = run_or_throw(env, |_env| {
        if handle == 0 {
            return;
        }
        // SAFETY: `handle` was allocated by the matching constructor shim and
        // ownership is transferred back here for drop via Box::from_raw.
        unsafe {
            let _ = Box::from_raw(handle as *mut core_crate::TreeCursor);
        }
    }) else {
        return;
    };
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryGetLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return 0;
            }
        };
        let name: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let result = client.get_language(&name);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                0
            }
            Ok(v) => v.into_raw() as jlong,
        }
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryAvailableLanguages(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let v = client.available_languages();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryHasParser(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return false;
            }
        };
        let name: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let v = client.has_parser(&name);
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryHasLanguage(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jboolean {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return false;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                return false;
            }
        };
        let name: String = match serde_json::from_str(&req_str) {
            Ok(s) => s,
            Err(_) => req_str,
        };
        let v = client.has_language(&name);
        v
    }) else {
        return false;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryLanguageCount(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) -> jlong {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return 0;
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let v = client.language_count();
        v as jlong
    }) else {
        return 0;
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeLanguageRegistryProcess(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
    request_json: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        if handle == 0 {
            throw_jni_error(env, "native handle must not be zero");
            return std::ptr::null_mut();
        }
        // SAFETY: handle was allocated by the matching constructor shim and remains
        // valid until nativeFree is called. The Kotlin AutoCloseable.close() guarantee
        // ensures the handle outlives this call.
        let client: &core_crate::LanguageRegistry = unsafe { &*(handle as *const core_crate::LanguageRegistry) };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid request_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let req_map: serde_json::Map<String, serde_json::Value> = match serde_json::from_str(&req_str) {
            Ok(m) => m,
            Err(e) => {
                throw_jni_error(env, &format!("param deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let source: String = match req_map
            .get("source")
            .and_then(|v| serde_json::from_value(v.clone()).ok())
        {
            Some(v) => v,
            None => {
                throw_jni_error(env, "missing param: source");
                return std::ptr::null_mut();
            }
        };
        let config: core_crate::ProcessConfig = match req_map
            .get("config")
            .and_then(|v| serde_json::from_value(v.clone()).ok())
        {
            Some(v) => v,
            None => {
                throw_jni_error(env, "missing param: config");
                return std::ptr::null_mut();
            }
        };
        let result = client.process(&source, &config);
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                std::ptr::null_mut()
            }
            Ok(v) => {
                let s = match serde_json::to_string(&v) {
                    Ok(s) => s,
                    Err(e) => {
                        throw_jni_error(env, &format!("serialize: {e}"));
                        return std::ptr::null_mut();
                    }
                };
                string_to_jstring(env, s)
            }
        }
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeFreeLanguageRegistry(
    mut env: EnvUnowned,
    _class: JClass,
    handle: jlong,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(()) = run_or_throw(env, |_env| {
        if handle == 0 {
            return;
        }
        // SAFETY: `handle` was allocated by the matching constructor shim and
        // ownership is transferred back here for drop via Box::from_raw.
        unsafe {
            let _ = Box::from_raw(handle as *mut core_crate::LanguageRegistry);
        }
    }) else {
        return;
    };
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcessConfigWithChunking(
    mut env: EnvUnowned,
    _class: JClass,
    self_json: JString,
    request_json: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        // Value types have no native handle: the Kotlin data class owns its fields, so the
        // receiver is rebuilt here from the JSON the caller serialised out of `this`.
        let self_str = match jstring_to_string(env, self_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid self_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let mut client: core_crate::ProcessConfig = match serde_json::from_str(&self_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("ProcessConfig deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid request_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let req_map: serde_json::Map<String, serde_json::Value> = match serde_json::from_str(&req_str) {
            Ok(m) => m,
            Err(e) => {
                throw_jni_error(env, &format!("param deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let max_size: usize = match req_map
            .get("max_size")
            .and_then(|v| serde_json::from_value(v.clone()).ok())
        {
            Some(v) => v,
            None => {
                throw_jni_error(env, "missing param: max_size");
                return std::ptr::null_mut();
            }
        };
        let v = client.with_chunking(max_size);
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcessConfigAll(
    mut env: EnvUnowned,
    _class: JClass,
    self_json: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        // Value types have no native handle: the Kotlin data class owns its fields, so the
        // receiver is rebuilt here from the JSON the caller serialised out of `this`.
        let self_str = match jstring_to_string(env, self_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid self_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let mut client: core_crate::ProcessConfig = match serde_json::from_str(&self_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("ProcessConfig deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let v = client.all();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcessConfigMinimal(
    mut env: EnvUnowned,
    _class: JClass,
    self_json: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        // Value types have no native handle: the Kotlin data class owns its fields, so the
        // receiver is rebuilt here from the JSON the caller serialised out of `this`.
        let self_str = match jstring_to_string(env, self_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid self_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let mut client: core_crate::ProcessConfig = match serde_json::from_str(&self_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("ProcessConfig deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let v = client.minimal();
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcessConfigWithDataExtraction(
    mut env: EnvUnowned,
    _class: JClass,
    self_json: JString,
    request_json: JString,
) -> jstring {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        // Value types have no native handle: the Kotlin data class owns its fields, so the
        // receiver is rebuilt here from the JSON the caller serialised out of `this`.
        let self_str = match jstring_to_string(env, self_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid self_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let mut client: core_crate::ProcessConfig = match serde_json::from_str(&self_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("ProcessConfig deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let req_str = match jstring_to_string(env, request_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid request_json: {e}"));
                return std::ptr::null_mut();
            }
        };
        let req_map: serde_json::Map<String, serde_json::Value> = match serde_json::from_str(&req_str) {
            Ok(m) => m,
            Err(e) => {
                throw_jni_error(env, &format!("param deserialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        let enabled: bool = match req_map
            .get("enabled")
            .and_then(|v| serde_json::from_value(v.clone()).ok())
        {
            Some(v) => v,
            None => {
                throw_jni_error(env, "missing param: enabled");
                return std::ptr::null_mut();
            }
        };
        let v = client.with_data_extraction(enabled);
        let s = match serde_json::to_string(&v) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("serialize: {e}"));
                return std::ptr::null_mut();
            }
        };
        string_to_jstring(env, s)
    }) else {
        return std::ptr::null_mut();
    };
    __jni_result
}
#[unsafe(no_mangle)]
pub unsafe extern "system" fn Java_io_xberg_tslp_android_TreeSitterLanguagePackBridge_nativeProcessConfigValidate(
    mut env: EnvUnowned,
    _class: JClass,
    self_json: JString,
) {
    // SAFETY: env is a valid EnvUnowned passed by the JVM for this native call frame.
    let mut __jni_attach_guard = unsafe { jni::AttachGuard::from_unowned(env.as_raw()) };
    let env = __jni_attach_guard.borrow_env_mut();
    let Some(__jni_result) = run_or_throw(env, |env| {
        // Value types have no native handle: the Kotlin data class owns its fields, so the
        // receiver is rebuilt here from the JSON the caller serialised out of `this`.
        let self_str = match jstring_to_string(env, self_json) {
            Ok(s) => s,
            Err(e) => {
                throw_jni_error(env, &format!("invalid self_json: {e}"));
                return ();
            }
        };
        let mut client: core_crate::ProcessConfig = match serde_json::from_str(&self_str) {
            Ok(v) => v,
            Err(e) => {
                throw_jni_error(env, &format!("ProcessConfig deserialize: {e}"));
                return ();
            }
        };
        let result = client.validate();
        match result {
            Err(e) => {
                throw_jni_error(env, &format!("{e}"));
                ()
            }
            Ok(v) => {}
        }
    }) else {
        return ();
    };
    __jni_result
}
