// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:06c26c046c94c60e0bc6e7463423ba1ac8391b7766ee69d98e0de98dd42eaadf
// alef:handle-abi:1
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_mut, noop_method_call)]
#![allow(unsafe_op_in_unsafe_fn, unsafe_attr_outside_unsafe)]
#![allow(
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::type_complexity,
    clippy::clone_on_copy,
    clippy::collapsible_if
)]
#![allow(
    clippy::missing_safety_doc,
    clippy::doc_lazy_continuation,
    clippy::doc_overindented_list_items
)]

use std::cell::RefCell;
use std::ffi::{CStr, CString, c_char};

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
    static LAST_RETURN_LENGTHS: RefCell<std::collections::BTreeMap<&'static str, usize>> =
        const { RefCell::new(std::collections::BTreeMap::new()) };
}

#[repr(i32)]
pub enum AlefFfiErrorCode {
    TsPackAlefNone = 0,
    TsPackAlefConversion = 1,
    TsPackAlefUnknown = 2,
    TsPackAlefPanic = 3,
    TsPackAlefInvalidHandle = 4,
    TreeSitterLanguagePackErrorLanguageNotFound = 100,
    TreeSitterLanguagePackErrorDynamicLoad = 101,
    TreeSitterLanguagePackErrorNullLanguagePointer = 102,
    TreeSitterLanguagePackErrorParserSetup = 103,
    TreeSitterLanguagePackErrorLockPoisoned = 104,
    TreeSitterLanguagePackErrorConfig = 105,
    TreeSitterLanguagePackErrorParseFailed = 106,
    TreeSitterLanguagePackErrorParseTimeout = 107,
    TreeSitterLanguagePackErrorQueryError = 108,
    TreeSitterLanguagePackErrorInvalidRange = 109,
    TreeSitterLanguagePackErrorDownload = 110,
    TreeSitterLanguagePackErrorChecksumMismatch = 111,
    TreeSitterLanguagePackErrorCacheLock = 112,
}

const ALEF_FFI_CONVERSION_ERROR: i32 = AlefFfiErrorCode::TsPackAlefConversion as i32;
const ALEF_FFI_UNKNOWN_ERROR: i32 = AlefFfiErrorCode::TsPackAlefUnknown as i32;
const ALEF_FFI_PANIC_ERROR: i32 = AlefFfiErrorCode::TsPackAlefPanic as i32;
const ALEF_INVALID_HANDLE_ERROR: i32 = AlefFfiErrorCode::TsPackAlefInvalidHandle as i32;

fn alef_ffi_error_code(error: &dyn std::any::Any) -> i32 {
    if let Some(error) = error.downcast_ref::<tree_sitter_language_pack::error::Error>() {
        #[allow(unreachable_patterns)]
        return match error {
            tree_sitter_language_pack::error::Error::LanguageNotFound(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorLanguageNotFound as i32
            }
            tree_sitter_language_pack::error::Error::DynamicLoad(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorDynamicLoad as i32
            }
            tree_sitter_language_pack::error::Error::NullLanguagePointer(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorNullLanguagePointer as i32
            }
            tree_sitter_language_pack::error::Error::ParserSetup(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorParserSetup as i32
            }
            tree_sitter_language_pack::error::Error::LockPoisoned(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorLockPoisoned as i32
            }
            tree_sitter_language_pack::error::Error::Config(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorConfig as i32
            }
            tree_sitter_language_pack::error::Error::ParseFailed => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorParseFailed as i32
            }
            tree_sitter_language_pack::error::Error::ParseTimeout { .. } => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorParseTimeout as i32
            }
            tree_sitter_language_pack::error::Error::QueryError(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorQueryError as i32
            }
            tree_sitter_language_pack::error::Error::InvalidRange(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorInvalidRange as i32
            }
            tree_sitter_language_pack::error::Error::Download(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorDownload as i32
            }
            tree_sitter_language_pack::error::Error::ChecksumMismatch { .. } => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorChecksumMismatch as i32
            }
            tree_sitter_language_pack::error::Error::CacheLock(..) => {
                AlefFfiErrorCode::TreeSitterLanguagePackErrorCacheLock as i32
            }
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    ALEF_FFI_UNKNOWN_ERROR
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

fn set_panic_error() {
    set_last_error(ALEF_FFI_PANIC_ERROR, "Rust panic contained at FFI boundary");
}

fn catch_ffi_panic<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    clear_last_error();
    catch_ffi_panic_preserving_error(fallback, body)
}

fn catch_ffi_panic_preserving_error<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(body)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call). Unlike `catch_ffi_panic` (which always
            // clears first), this wrapper's whole contract is to leave that in place, so the
            // generic panic marker is stamped only when nothing more specific already is.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            fallback
        }
    }
}

fn set_last_return_len(function: &'static str, len: usize) {
    LAST_RETURN_LENGTHS.with_borrow_mut(|lengths| {
        lengths.insert(function, len);
    });
}

fn last_return_len(function: &'static str) -> usize {
    LAST_RETURN_LENGTHS.with_borrow(|lengths| lengths.get(function).copied().unwrap_or(0))
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// This function does not allocate and returns no owned pointer.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_last_error_code() -> i32 {
    catch_ffi_panic_preserving_error(ALEF_FFI_PANIC_ERROR, || LAST_ERROR_CODE.with_borrow(|c| *c))
}

/// Return the last error message. The pointer is borrowed and valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// The returned pointer is borrowed from thread-local storage and must NOT be freed.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_last_error_context() -> *const c_char {
    catch_ffi_panic_preserving_error(std::ptr::null(), || {
        LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
    })
}

type AlefHandle = u64;
const HANDLE_INDEX_MASK: u64 = u32::MAX as u64;

#[derive(Debug)]
enum HandleError {
    InvalidZero,
    UnknownSlot,
    StaleGeneration,
    WrongType,
    RegistryPoisoned,
    HandleBusy,
    AliasedHandle,
}

impl std::fmt::Display for HandleError {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let message = match self {
            Self::InvalidZero => "zero is not a valid handle",
            Self::UnknownSlot => "handle refers to an unknown slot",
            Self::StaleGeneration => "handle generation is stale",
            Self::WrongType => "handle has the wrong type",
            Self::RegistryPoisoned => "handle registry lock is poisoned",
            Self::HandleBusy => "handle is currently in use",
            Self::AliasedHandle => "the same handle was passed more than once",
        };
        formatter.write_str(message)
    }
}

type ErasedHandleValue = std::sync::Arc<std::sync::Mutex<Box<dyn std::any::Any + Send>>>;

struct HandleSlot {
    generation: u32,
    value: Option<ErasedHandleValue>,
}

#[derive(Default)]
struct HandleRegistry {
    slots: Vec<HandleSlot>,
}

impl HandleRegistry {
    fn insert<T: std::any::Any + Send>(&mut self, value: T) -> AlefHandle {
        let entry = std::sync::Arc::new(std::sync::Mutex::new(Box::new(value) as Box<dyn std::any::Any + Send>));
        if let Some((index, slot)) = self.slots.iter_mut().enumerate().find(|(_, slot)| slot.value.is_none()) {
            slot.value = Some(entry);
            return encode_handle(index, slot.generation);
        }
        self.slots.push(HandleSlot {
            generation: 1,
            value: Some(entry),
        });
        encode_handle(self.slots.len() - 1, 1)
    }

    fn get(&self, handle: AlefHandle) -> Result<ErasedHandleValue, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        slot.value.clone().ok_or(HandleError::StaleGeneration)
    }

    fn remove<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<(), HandleError> {
        drop(self.take::<T>(handle)?);
        Ok(())
    }

    fn take<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<T, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get_mut(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        let value = slot.value.as_ref().ok_or(HandleError::StaleGeneration)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if !guard.is::<T>() {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        let value = slot.value.take().ok_or(HandleError::StaleGeneration)?;
        let mutex = std::sync::Arc::try_unwrap(value).map_err(|value| {
            slot.value = Some(value);
            HandleError::HandleBusy
        })?;
        slot.generation = next_generation(slot.generation);
        let boxed = mutex.into_inner().map_err(|_| HandleError::RegistryPoisoned)?;
        boxed
            .downcast::<T>()
            .map(|value| *value)
            .map_err(|_| HandleError::WrongType)
    }
}

static HANDLE_REGISTRY: std::sync::OnceLock<std::sync::Mutex<HandleRegistry>> = std::sync::OnceLock::new();

fn handle_registry() -> &'static std::sync::Mutex<HandleRegistry> {
    HANDLE_REGISTRY.get_or_init(|| std::sync::Mutex::new(HandleRegistry::default()))
}

fn insert_handle<T: std::any::Any + Send>(value: T) -> Result<AlefHandle, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    Ok(registry.insert(value))
}

struct SerializedHandle<T: 'static> {
    json: String,
    marker: std::marker::PhantomData<fn() -> T>,
}

fn insert_serialized_handle<T: serde::Serialize + 'static>(value: &T) -> Result<AlefHandle, String> {
    let json = serde_json::to_string(value).map_err(|error| error.to_string())?;
    insert_handle(SerializedHandle::<T> {
        json,
        marker: std::marker::PhantomData,
    })
    .map_err(|error| error.to_string())
}

fn with_handle<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_ref::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

struct HandleRequest {
    handle: AlefHandle,
    expected_type: std::any::TypeId,
}

fn acquire_handles(requests: &[HandleRequest]) -> Result<Vec<(AlefHandle, ErasedHandleValue)>, HandleError> {
    let mut ordered = requests.iter().collect::<Vec<_>>();
    ordered.sort_by_key(|request| request.handle);
    if ordered.windows(2).any(|pair| pair[0].handle == pair[1].handle) {
        return Err(HandleError::AliasedHandle);
    }
    let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let mut values = Vec::with_capacity(ordered.len());
    for request in ordered {
        let value = registry.get(request.handle)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if guard.as_ref().type_id() != request.expected_type {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        values.push((request.handle, value));
    }
    Ok(values)
}

fn locked_handle_ptr<T: std::any::Any + Send>(
    guards: &mut [(AlefHandle, std::sync::MutexGuard<'_, Box<dyn std::any::Any + Send>>)],
    handle: AlefHandle,
) -> Result<*mut T, HandleError> {
    let (_, guard) = guards
        .iter_mut()
        .find(|(candidate, _)| *candidate == handle)
        .ok_or(HandleError::UnknownSlot)?;
    guard
        .downcast_mut::<T>()
        .map(|value| value as *mut T)
        .ok_or(HandleError::WrongType)
}

fn with_handle_mut<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&mut T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let mut guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_mut::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

fn remove_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<(), HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.remove::<T>(handle)
}

fn take_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<T, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.take::<T>(handle)
}

fn encode_handle(index: usize, generation: u32) -> AlefHandle {
    ((generation as u64) << 32) | ((index as u64) + 1)
}

fn decode_handle(handle: AlefHandle) -> Result<(usize, u32), HandleError> {
    if handle == 0 {
        return Err(HandleError::InvalidZero);
    }
    let encoded_index = handle & HANDLE_INDEX_MASK;
    if encoded_index == 0 {
        return Err(HandleError::UnknownSlot);
    }
    Ok(((encoded_index - 1) as usize, (handle >> 32) as u32))
}

fn next_generation(generation: u32) -> u32 {
    let next = generation.wrapping_add(1);
    if next == 0 { 1 } else { next }
}

fn set_handle_error(error: &HandleError) {
    set_last_error(ALEF_INVALID_HANDLE_ERROR, &error.to_string());
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_free_string(ptr: *mut c_char) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by CString::into_raw; caller ensures no aliases.
            unsafe {
                drop(CString::from_raw(ptr));
            }
        }
    })
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_version() -> *const c_char {
    catch_ffi_panic(std::ptr::null(), || {
        static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
        VERSION.as_ptr() as *const c_char
    })
}

#[allow(dead_code)]
fn comment_kind_from_i32_rs(v: i32) -> Option<tree_sitter_language_pack::CommentKind> {
    match v {
        0 => Some(tree_sitter_language_pack::CommentKind::Line),
        1 => Some(tree_sitter_language_pack::CommentKind::Block),
        2 => Some(tree_sitter_language_pack::CommentKind::Doc),
        _ => None,
    }
}

#[allow(dead_code)]
fn data_node_kind_from_i32_rs(v: i32) -> Option<tree_sitter_language_pack::DataNodeKind> {
    match v {
        0 => Some(tree_sitter_language_pack::DataNodeKind::KeyValue),
        1 => Some(tree_sitter_language_pack::DataNodeKind::Element),
        2 => Some(tree_sitter_language_pack::DataNodeKind::Sequence),
        _ => None,
    }
}

#[allow(dead_code)]
fn diagnostic_severity_from_i32_rs(v: i32) -> Option<tree_sitter_language_pack::DiagnosticSeverity> {
    match v {
        0 => Some(tree_sitter_language_pack::DiagnosticSeverity::Error),
        1 => Some(tree_sitter_language_pack::DiagnosticSeverity::Warning),
        2 => Some(tree_sitter_language_pack::DiagnosticSeverity::Info),
        _ => None,
    }
}

#[allow(dead_code)]
fn export_kind_from_i32_rs(v: i32) -> Option<tree_sitter_language_pack::ExportKind> {
    match v {
        0 => Some(tree_sitter_language_pack::ExportKind::Named),
        1 => Some(tree_sitter_language_pack::ExportKind::Default),
        2 => Some(tree_sitter_language_pack::ExportKind::ReExport),
        _ => None,
    }
}

/// Create a `ByteRange` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_byte_range_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ByteRange>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ByteRange` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ByteRange, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ByteRange` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ByteRange>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `start` field from a `ByteRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_start(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ByteRange, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end` field from a `ByteRange`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_byte_range_end(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ByteRange, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ChunkContext` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_chunk_context_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ChunkContext>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChunkContext` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChunkContext` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ChunkContext>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
            match CString::new(obj.language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `chunk_index` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_chunk_index(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| obj.chunk_index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_chunks` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_total_chunks(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| obj.total_chunks);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `node_types` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_node_types(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
                match serde_json::to_string(&obj.node_types) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `context_path` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_context_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
                match serde_json::to_string(&obj.context_path) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `symbols_defined` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_symbols_defined(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
                match serde_json::to_string(&obj.symbols_defined) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `comments` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_comments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
                match serde_json::to_string(&obj.comments) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `docstrings` field from a `ChunkContext`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_docstrings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| {
                match serde_json::to_string(&obj.docstrings) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `has_error_nodes` field from a `ChunkContext`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_chunk_context_has_error_nodes(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<tree_sitter_language_pack::ChunkContext, _>(handle, |obj| obj.has_error_nodes as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `CodeChunk` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_code_chunk_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::CodeChunk>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CodeChunk` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CodeChunk` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::CodeChunk>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `content` field from a `CodeChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| {
            match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `start_byte` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_start_byte(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| obj.start_byte);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end_byte` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_end_byte(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| obj.end_byte);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `start_line` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_start_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| obj.start_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end_line` field from a `CodeChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_end_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| obj.end_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `metadata` field from a `CodeChunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_chunk_context_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_code_chunk_metadata(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CodeChunk, _>(handle, |obj| {
            match insert_handle(obj.metadata.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `CommentInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_comment_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::CommentInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CommentInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::CommentInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CommentInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::CommentInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `text` field from a `CommentInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::CommentInfo, _>(handle, |obj| {
            match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `kind` field from a `CommentInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_comment_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CommentInfo, _>(handle, |obj| {
            match insert_handle(obj.kind.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `CommentInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::CommentInfo, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `associated_node` field from a `CommentInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_info_associated_node(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::CommentInfo, _>(handle, |obj| match &obj.associated_node {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DataAttribute` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_data_attribute_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::DataAttribute>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DataAttribute` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DataAttribute, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DataAttribute` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DataAttribute>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `DataAttribute`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataAttribute, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `value` field from a `DataAttribute`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataAttribute, _>(handle, |obj| {
            match CString::new(obj.value.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `span` field from a `DataAttribute`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_attribute_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::DataAttribute, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `DataNode` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_data_node_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::DataNode>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DataNode` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DataNode, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DataNode` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DataNode>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `kind` field from a `DataNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_data_node_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| {
            match insert_handle(obj.kind.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `key` field from a `DataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| match &obj.key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `value` field from a `DataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_value(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| match &obj.value {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `attributes` field from a `DataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_attributes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| {
            match serde_json::to_string(&obj.attributes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `children` field from a `DataNode`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| {
            match serde_json::to_string(&obj.children) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `span` field from a `DataNode`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::DataNode, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Diagnostic` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_diagnostic_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::Diagnostic>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Diagnostic` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::Diagnostic, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Diagnostic` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Diagnostic>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `message` field from a `Diagnostic`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_message(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::Diagnostic, _>(handle, |obj| {
            match CString::new(obj.message.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `severity` field from a `Diagnostic`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_diagnostic_severity_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Diagnostic, _>(handle, |obj| {
            match insert_handle(obj.severity.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `Diagnostic`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Diagnostic, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `DocSection` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_doc_section_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::DocSection>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocSection` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DocSection, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocSection` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DocSection>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `kind` field from a `DocSection`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_kind(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DocSection, _>(handle, |obj| {
            match CString::new(obj.kind.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `name` field from a `DocSection`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DocSection, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `DocSection`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_doc_section_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DocSection, _>(handle, |obj| {
            match CString::new(obj.description.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DocstringInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_docstring_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::DocstringInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocstringInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocstringInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DocstringInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `text` field from a `DocstringInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, |obj| {
            match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `format` field from a `DocstringInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_docstring_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, |obj| {
            match insert_handle(obj.format.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `DocstringInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `associated_item` field from a `DocstringInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_associated_item(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, |obj| match &obj.associated_item {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `parsed_sections` field from a `DocstringInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_info_parsed_sections(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::DocstringInfo, _>(handle, |obj| {
            match serde_json::to_string(&obj.parsed_sections) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "download")]
/// Free a `DownloadManager` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DownloadManager>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_new(version: *const std::ffi::c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        let version_rs = if version.is_null() {
            String::new()
        } else {
            // SAFETY: null check above guarantees version is a valid pointer.
            unsafe { std::ffi::CStr::from_ptr(version) }
                .to_string_lossy()
                .into_owned()
        };
        let result = tree_sitter_language_pack::DownloadManager::new(&version_rs);
        match result {
            Ok(value) => match insert_handle(value) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(1, &e.to_string());
                0
            }
        }
    })
}

/// List languages that are already downloaded and cached.
///
/// Derived from the on-disk cache filenames, one canonical name per file, plus
/// every alias that resolves to it (via `aliases_for`) â so
/// this list agrees with the user-facing
/// `LanguageRegistry::available_languages` (crate::LanguageRegistry::available_languages)
/// about which names are "available"; both report `"shell"` once `bash` is
/// cached, for example. A previous version reported canonical names only,
/// which could never agree with `available_languages()`. See #107.
///
/// Returns an empty list if the cache directory does not exist. If it exists
/// but cannot be read (e.g. a permission error), also returns an empty list â
/// changing this to a `Result` would be a breaking change across every
/// language binding â but logs a `tracing::warn!` so the failure is not
/// silently indistinguishable from "nothing installed".
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_installed_languages(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::DownloadManager>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::DownloadManager>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.installed_languages();
        match serde_json::to_string(&result) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Download the platform bundle and extract every library file it contains.
///
/// Unlike `Self.ensure_languages`, this does not check the manifest language list
/// against archive contents â it simply extracts all `.so`/`.dylib`/`.dll` files
/// from the bundle. Languages in the manifest that are missing from the archive
/// are silently ignored rather than returning an error.
///
/// Returns the number of library files extracted (including those already cached).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_download_all_best_effort(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::DownloadManager>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::DownloadManager>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.download_all_best_effort();
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Remove all cached parser libraries.
///
/// Acquires the cross-process lock so `clean_cache` cannot race a concurrent
/// downloader (avoids Windows sharing-violation errors against an in-flight
/// bundle write). The `.download.lock` file itself is **not** removed â it is
/// permanent infrastructure; deleting it could allow a concurrent process that
/// already opened the file to continue holding a stale lock handle while a new
/// process opens a fresh inode, breaking the mutual-exclusion guarantee.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_manager_clean_cache(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::DownloadManager>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::DownloadManager>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.clean_cache();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Create a `ExportInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_export_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ExportInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ExportInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ExportInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExportInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ExportInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `ExportInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ExportInfo, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `kind` field from a `ExportInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_export_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ExportInfo, _>(handle, |obj| {
            match insert_handle(obj.kind.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `ExportInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_info_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ExportInfo, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `FileMetrics` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_file_metrics_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::FileMetrics>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FileMetrics` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FileMetrics` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::FileMetrics>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `total_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_total_lines(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.total_lines);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `code_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_code_lines(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.code_lines);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `comment_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_comment_lines(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.comment_lines);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `blank_lines` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_blank_lines(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.blank_lines);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_bytes` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_total_bytes(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.total_bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `node_count` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_node_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.node_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `error_count` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_error_count(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.error_count);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_depth` field from a `FileMetrics`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_file_metrics_max_depth(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::FileMetrics, _>(handle, |obj| obj.max_depth);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImportInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_import_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ImportInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImportInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImportInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ImportInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `source` field from a `ImportInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_source(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, |obj| {
            match CString::new(obj.source.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `items` field from a `ImportInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_items(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, |obj| {
                match serde_json::to_string(&obj.items) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `alias` field from a `ImportInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_alias(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, |obj| match &obj.alias {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `is_wildcard` field from a `ImportInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_is_wildcard(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, |obj| obj.is_wildcard as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `ImportInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_import_info_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ImportInfo, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Free a `Language` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Language>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Free a `LanguageRegistry` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::LanguageRegistry>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_new() -> AlefHandle {
    catch_ffi_panic(0, || {
        let result = tree_sitter_language_pack::LanguageRegistry::new();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get a tree-sitter `Language` by name.
///
/// Resolves aliases (e.g., `"shell"` -> `"bash"`, `"makefile"` -> `"make"`),
/// then looks up the language in the static table. When the `dynamic-loading`
/// feature is enabled, falls back to loading a shared library on demand.
/// \note Returns `Error.LanguageNotFound` if the name (after alias resolution)
/// does not match any known grammar.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_get_language(
    this: AlefHandle,
    name: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = obj.get_language(&name_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// List all available language names, sorted and deduplicated.
///
/// Includes statically compiled languages, dynamically loadable languages
/// (if the `dynamic-loading` feature is enabled), and all configured aliases.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_available_languages(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.available_languages();
        match serde_json::to_string(&result) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Check whether this language can be parsed right now, without downloading.
///
/// Resolves aliases, then answers from exactly the lookup
/// `get_language` (Self::get_language) performs: the statically compiled
/// table, the already-loaded dynamic grammars, and the parser shared
/// libraries present in the primary and extra (download-cache) library
/// directories. It never performs network I/O.
///
/// A previous implementation consulted only the statically compiled table.
/// That table is empty in every build that does not set `TSLP_LANGUAGES`,
/// so the function answered `false` for languages this registry parses
/// perfectly well.
///
/// Contrast `has_language` (Self::has_language), which is also `true` for a
/// grammar that is merely *known to the manifest* and would have to be
/// downloaded first. The pair distinguishes "we can parse it offline, now"
/// from "we recognise the name".
///
/// The first `true` answer for a dynamic grammar loads its shared library:
/// loading is the only way to know the grammar is usable, since a truncated
/// or wrong-architecture library exists on disk but cannot parse. Loads are
/// cached process-wide, so repeat calls are cheap.
///
/// ```no_run
/// use tree_sitter_language_pack::{detect_language_from_extension, LanguageRegistry};
///
/// let registry = LanguageRegistry::new();
/// // Extension detection uses the static ext table for all 371 grammars.
/// let lang = detect_language_from_extension("feature"); // always returns Some("gherkin")
/// // Parser availability depends on what is compiled in or cached on disk.
/// let can_parse = lang.map(|name| registry.has_parser(name)).unwrap_or(false);
/// ```
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_has_parser(this: AlefHandle, name: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = obj.has_parser(&name_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Check whether a language is available by name or alias.
///
/// Returns `true` if the language can be loaded, either from the static
/// table or from a dynamic library on disk.
///
/// Every branch is one more way to answer `true`, so they are ordered
/// cheapest-first and the filesystem is only consulted once every in-memory
/// source has said no. Probing the loaded-grammar map and the manifest ahead
/// of the `stat` calls is what keeps this off the syscall path for the
/// languages a process actually uses.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_has_language(
    this: AlefHandle,
    name: *const std::ffi::c_char,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = obj.has_language(&name_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the total number of available languages (including aliases).
///
/// Counts the same set `available_languages` (Self::available_languages)
/// lists, without materialising or sorting it.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_language_count(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.language_count()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Parse source code and extract file intelligence based on config in a single pass.
/// \note Returns `Error.InvalidRange` if the config is invalid (see
/// `ProcessConfig::validate` (crate::ProcessConfig::validate)) or if the source
/// exceeds the configured
/// `max_source_bytes` (crate::ProcessConfig::max_source_bytes);
/// `Error.LanguageNotFound` if the language is unknown; or
/// `Error.ParseFailed` if parsing produces no tree.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_process(
    this: AlefHandle,
    source: *const std::ffi::c_char,
    config: AlefHandle,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<tree_sitter_language_pack::LanguageRegistry>(),
            }),
            Some(HandleRequest {
                handle: config,
                expected_type: std::any::TypeId::of::<tree_sitter_language_pack::ProcessConfig>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::LanguageRegistry>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if source.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'source'");
            return 0;
        }
        // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
        let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'source'");
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr =
            match locked_handle_ptr::<tree_sitter_language_pack::ProcessConfig>(&mut __alef_guards, config) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = obj.process(&source_rs, &config_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_registry_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = tree_sitter_language_pack::LanguageRegistry::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `Node` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Node>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Return the node's kind name (e.g. `"function_definition"`).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_kind(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.kind();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the node's numeric kind ID.
///
/// Tree-sitter assigns a stable `u16` ID to every node kind in a grammar
/// (e.g. `"function_definition" â 42`). Comparing `kind_id()` is cheaper
/// than comparing the string `kind()` (Self::kind) in tight AST loops.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_kind_id(this: AlefHandle) -> u16 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.kind_id()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the inclusive start byte offset of this node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_start_byte(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.start_byte()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the exclusive end byte offset of this node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_end_byte(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.end_byte()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the node's byte range as a `ByteRange`.
///
/// Callers should slice their own source bytes â this is a zero-copy
/// text accessor.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_byte_range(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.byte_range();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the start `Point` (row, column).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_start_position(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.start_position();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the end `Point` (row, column).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_end_position(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.end_position();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// True when this node is named (not punctuation/whitespace).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_named(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_named();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// True when this is an error node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_error(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_error();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// True when this is a missing-token node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_missing(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_missing();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// True when this is an "extra" node (e.g. a comment).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_is_extra(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.is_extra();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// True when this node or any descendant is an error.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_has_error(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.has_error();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return this node's parent, if any.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_parent(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.parent();
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the i-th child of this node, if any.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child(this: AlefHandle, index: u32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let index_rs = index;

        let result = obj.child(index_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Total number of children (including unnamed).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child_count(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.child_count()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the i-th named child of this node, if any.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_named_child(this: AlefHandle, index: u32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let index_rs = index;

        let result = obj.named_child(index_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Number of named children of this node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_named_child_count(this: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        obj.named_child_count()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Look up a child by its grammar-defined field name.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_child_by_field_name(
    this: AlefHandle,
    name: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = obj.child_by_field_name(&name_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the S-expression form of this node's subtree.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_to_sexp(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.to_sexp();
        match CString::new(result.to_string()) {
            Ok(cs) => cs.into_raw(),
            Err(_) => {
                set_last_error(
                    ALEF_FFI_CONVERSION_ERROR,
                    "FFI field value contains an interior NUL byte",
                );
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return a `TreeCursor` positioned at this node.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_node_walk(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Node>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Node>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.walk();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `PackConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_pack_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::PackConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PackConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::PackConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PackConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::PackConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `cache_dir` field from a `PackConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_cache_dir(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::PackConfig, _>(handle, |obj| match &obj.cache_dir {
            Some(val) => match CString::new(val.to_string_lossy().to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI path value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `languages` field from a `PackConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_languages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::PackConfig, _>(handle, |obj| match &obj.languages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `groups` field from a `PackConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_pack_config_groups(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::PackConfig, _>(handle, |obj| match &obj.groups {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a `Parser` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Parser>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_new() -> AlefHandle {
    catch_ffi_panic(0, || {
        let result = tree_sitter_language_pack::Parser::new();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Refuse to parse sources longer than `max_bytes`.
///
/// `None` (the default) means no limit. Over-limit input makes
/// `parse` (Self::parse) and `parse_bytes` (Self::parse_bytes) return
/// `None` after emitting a `WARN`; input is never silently truncated.
///
/// See `RECOMMENDED_MAX_SOURCE_BYTES` (crate::process_config::RECOMMENDED_MAX_SOURCE_BYTES).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_set_max_source_bytes(this: AlefHandle, max_bytes: usize) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        // Optional numeric primitive: max value of type = None
        let max_bytes_rs = if max_bytes == usize::MAX { None } else { Some(max_bytes) };

        obj.set_max_source_bytes(max_bytes_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Cancel a parse that exceeds `timeout_ms` milliseconds of wall clock.
///
/// `None` (the default) means no budget. Cancellation runs through
/// tree-sitter's parse progress callback, so it is granular to that
/// callback's interval rather than exact.
///
/// See `RECOMMENDED_PARSE_TIMEOUT_MS` (crate::process_config::RECOMMENDED_PARSE_TIMEOUT_MS).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_set_parse_timeout_ms(this: AlefHandle, timeout_ms: u64) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        // Optional numeric primitive: max value of type = None
        let timeout_ms_rs = if timeout_ms == u64::MAX { None } else { Some(timeout_ms) };

        obj.set_parse_timeout_ms(timeout_ms_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Configure the parser to use the language identified by name (e.g. `"python"`).
///
/// Resolves the language through the global registry â auto-downloading
/// if necessary, when the `download` feature is enabled.
/// \note Returns `Error.LanguageNotFound` if the language is not recognized,
/// or `Error.ParserSetup` if the language ABI is incompatible.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_set_language(this: AlefHandle, name: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return -1;
            }
        };

        let result = obj.set_language(&name_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Parse a UTF-8 source string.
///
/// Returns `None` if no language is set, if the parse was cancelled by the
/// configured timeout (Self::set_parse_timeout_ms), or if the source
/// exceeds the configured size limit (Self::set_max_source_bytes). Each
/// non-parse outcome is logged, so an empty result is never silent.
///
/// # Concurrency
///
/// Parsing runs fully in parallel across threads for almost every language â no lock is
/// taken. The exception is the small set of grammars whose external scanner keeps mutable
/// process-global state (currently just `properties`): parses of that language are
/// serialized against each other through an internal per-language lock, so that concurrent
/// threads can't corrupt shared scanner state. Every other language is unaffected by that
/// lock and never waits on it.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_parse(this: AlefHandle, source: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if source.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'source'");
            return 0;
        }
        // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
        let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'source'");
                return 0;
            }
        };

        let result = obj.parse(&source_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Parse a raw byte slice.
///
/// Same outcomes as `parse` (Self::parse), including the concurrency behaviour documented
/// there.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_parse_bytes(
    this: AlefHandle,
    source: *const u8,
    source_len: usize,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        if source.is_null() && source_len > 0 {
            set_last_error(1, "Null pointer passed for parameter 'source'");
            return 0;
        }
        // SAFETY: when source is null, source_len is 0 (checked above), so we use an empty slice; otherwise data is valid for len elements.
        let source_rs = if source.is_null() {
            Vec::new()
        } else {
            unsafe { std::slice::from_raw_parts(source, source_len) }.to_vec()
        };

        let result = obj.parse_bytes(&source_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Reset internal state. The next call to `parse` (Self::parse) will
/// not be incremental.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_reset(this: AlefHandle) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Parser>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Parser>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        obj.reset()
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_parser_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = tree_sitter_language_pack::Parser::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `Point` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_point_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::Point>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Point` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::Point, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Point` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Point>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `row` field from a `Point`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_row(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Point, _>(handle, |obj| obj.row);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `column` field from a `Point`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_point_column(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Point, _>(handle, |obj| obj.column);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ProcessConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_process_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ProcessConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProcessConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProcessConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ProcessConfig>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `ProcessConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| {
            match CString::new(obj.language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `structure` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_structure(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.structure as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `imports` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_imports(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.imports as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `exports` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_exports(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.exports as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `comments` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_comments(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.comments as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `docstrings` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_docstrings(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.docstrings as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `symbols` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_symbols(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.symbols as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `diagnostics` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_diagnostics(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.diagnostics as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `chunk_max_size` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_chunk_max_size(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| match &obj.chunk_max_size {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `data_extraction` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_data_extraction(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| obj.data_extraction as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_source_bytes` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_max_source_bytes(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| match &obj.max_source_bytes {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `parse_timeout_ms` field from a `ProcessConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_parse_timeout_ms(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<tree_sitter_language_pack::ProcessConfig, _>(handle, |obj| match &obj.parse_timeout_ms {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = tree_sitter_language_pack::ProcessConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Enable chunking with the given maximum chunk size in bytes.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_with_chunking(this: AlefHandle, max_size: usize) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let max_size_rs = max_size;

        let result = obj.with_chunking(max_size_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Enable every analysis feature, including data extraction.
///
/// Chunking is not an analysis feature and stays off; enable it with
/// `with_chunking` (Self::with_chunking).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_all(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let result = obj.all();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Disable all analysis features (only metrics computed).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_minimal(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let result = obj.minimal();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Enable or disable hierarchical data extraction for data-format files.
///
/// When `true`, `ProcessResult::data` (crate::ProcessResult::data) is
/// populated with a key/value tree for supported data-format languages.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_with_data_extraction(this: AlefHandle, enabled: i32) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        let enabled_rs = enabled != 0;

        let result = obj.with_data_extraction(enabled_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Reject source longer than `max_bytes` instead of parsing it.
///
/// Pass `None` to restore the default unbounded behaviour. See
/// `RECOMMENDED_MAX_SOURCE_BYTES` for a starting value.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_with_max_source_bytes(
    this: AlefHandle,
    max_bytes: usize,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        // Optional numeric primitive: max value of type = None
        let max_bytes_rs = if max_bytes == usize::MAX { None } else { Some(max_bytes) };

        let result = obj.with_max_source_bytes(max_bytes_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Cancel the parse if it exceeds `timeout_ms` milliseconds of wall clock.
///
/// Pass `None` to restore the default (no timeout). See
/// `RECOMMENDED_PARSE_TIMEOUT_MS` for a starting value.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_with_parse_timeout_ms(this: AlefHandle, timeout_ms: u64) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = Vec::new();
        if __alef_requests.iter().any(|request| request.handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        if __alef_values.iter().any(|(handle, _)| *handle == this) {
            set_handle_error(&HandleError::AliasedHandle);
            return 0;
        }
        let obj = match take_handle::<tree_sitter_language_pack::ProcessConfig>(this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };

        // Optional numeric primitive: max value of type = None
        let timeout_ms_rs = if timeout_ms == u64::MAX { None } else { Some(timeout_ms) };

        let result = obj.with_parse_timeout_ms(timeout_ms_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Check that the configured limits are usable before they drive a parse.
///
/// Called by `process` and
/// `LanguageRegistry::process` (crate::LanguageRegistry::process); call it
/// directly to reject a bad configuration early.
/// \note Returns `Error::InvalidRange` (crate::Error::InvalidRange) when
/// `chunk_max_size`, `max_source_bytes`, or `parse_timeout_ms` is `Some(0)`.
/// Zero is always a configuration mistake: `None` is how each of these is
/// disabled, so `Some(0)` could only mean "produce nothing".
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_config_validate(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::ProcessConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::ProcessConfig>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.validate();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Create a `ProcessResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_process_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::ProcessResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProcessResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProcessResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ProcessResult>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `language` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match CString::new(obj.language.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metrics` field from a `ProcessResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_file_metrics_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_metrics(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match insert_handle(obj.metrics.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `structure` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_structure(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.structure) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `imports` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_imports(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.imports) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `exports` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_exports(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.exports) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `comments` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_comments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.comments) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `docstrings` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_docstrings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.docstrings) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `symbols` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_symbols(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.symbols) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `diagnostics` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_diagnostics(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.diagnostics) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `chunks` field from a `ProcessResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_chunks(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| {
            match serde_json::to_string(&obj.chunks) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `ProcessResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_data_node_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process_result_data(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::ProcessResult, _>(handle, |obj| match &obj.data {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Span` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_span_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::Span>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Span` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::Span, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Span` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Span>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `start_byte` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_byte(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.start_byte);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end_byte` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_byte(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.end_byte);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `start_line` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.start_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `start_column` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_start_column(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.start_column);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end_line` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_line(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.end_line);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `end_column` field from a `Span`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_span_end_column(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::Span, _>(handle, |obj| obj.end_column);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `StructureItem` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_structure_item_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::StructureItem>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StructureItem` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StructureItem` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::StructureItem>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `kind` field from a `StructureItem`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_structure_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| {
            match insert_handle(obj.kind.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `name` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `visibility` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_visibility(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| match &obj.visibility {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `span` field from a `StructureItem`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `children` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_children(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| {
            match serde_json::to_string(&obj.children) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `decorators` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_decorators(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| {
            match serde_json::to_string(&obj.decorators) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `doc_comment` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_doc_comment(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| match &obj.doc_comment {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `signature` field from a `StructureItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_signature(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| match &obj.signature {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `body_span` field from a `StructureItem`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_item_body_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::StructureItem, _>(handle, |obj| match &obj.body_span {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SymbolInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `ts_pack_symbol_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<tree_sitter_language_pack::SymbolInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SymbolInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SymbolInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::SymbolInfo>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Get the `name` field from a `SymbolInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `kind` field from a `SymbolInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_symbol_kind_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_kind(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, |obj| {
            match insert_handle(obj.kind.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `span` field from a `SymbolInfo`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `ts_pack_span_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_span(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, |obj| {
            match insert_handle(obj.span.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `type_annotation` field from a `SymbolInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_type_annotation(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, |obj| match &obj.type_annotation {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `doc` field from a `SymbolInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `ts_pack_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_info_doc(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<tree_sitter_language_pack::SymbolInfo, _>(handle, |obj| match &obj.doc {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Free a `Tree` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::Tree>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Return the root `Node` of this tree.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_root_node(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Tree>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Tree>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.root_node();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return a `TreeCursor` positioned at the root.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_walk(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::Tree>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::Tree>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.walk();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Free a `TreeCursor` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::TreeCursor>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Return the `Node` at the cursor's current position.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_node(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::TreeCursor>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::TreeCursor>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.node();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Move the cursor to the first child of the current node.
/// Returns `true` if a child existed.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_first_child(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::TreeCursor>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::TreeCursor>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        let result = obj.goto_first_child();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Move the cursor to the parent of the current node.
/// Returns `true` if a parent existed.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_parent(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::TreeCursor>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::TreeCursor>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        let result = obj.goto_parent();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Move the cursor to the next sibling of the current node.
/// Returns `true` if a sibling existed.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_goto_next_sibling(this: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::TreeCursor>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::TreeCursor>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &mut *obj_ptr };

        let result = obj.goto_next_sibling();
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the field name for the current node, if any.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_tree_cursor_field_name(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::TreeCursor>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<tree_sitter_language_pack::TreeCursor>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.field_name();
        match result {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Convert an integer to a `CommentKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Line
            1 => 1, // Block
            2 => 2, // Doc
            _ => {
                set_last_error(1, "Invalid CommentKind variant");
                -1
            }
        }
    })
}

/// Convert a `CommentKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Line" => 0,
            "Block" => 1,
            "Doc" => 2,
            _ => {
                set_last_error(1, "Unknown CommentKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `DataNodeKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // KeyValue
            1 => 1, // Element
            2 => 2, // Sequence
            _ => {
                set_last_error(1, "Invalid DataNodeKind variant");
                -1
            }
        }
    })
}

/// Convert a `DataNodeKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "KeyValue" => 0,
            "Element" => 1,
            "Sequence" => 2,
            _ => {
                set_last_error(1, "Unknown DataNodeKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `DiagnosticSeverity` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Error
            1 => 1, // Warning
            2 => 2, // Info
            _ => {
                set_last_error(1, "Invalid DiagnosticSeverity variant");
                -1
            }
        }
    })
}

/// Convert a `DiagnosticSeverity` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Error" => 0,
            "Warning" => 1,
            "Info" => 2,
            _ => {
                set_last_error(1, "Unknown DiagnosticSeverity variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `DocstringFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // PythonTripleQuote
            1 => 1, // JSDoc
            2 => 2, // Rustdoc
            3 => 3, // GoDoc
            4 => 4, // JavaDoc
            5 => 5, // Other
            _ => {
                set_last_error(1, "Invalid DocstringFormat variant");
                -1
            }
        }
    })
}

/// Convert a `DocstringFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "PythonTripleQuote" => 0,
            "JSDoc" => 1,
            "Rustdoc" => 2,
            "GoDoc" => 3,
            "JavaDoc" => 4,
            "Other" => 5,
            _ => {
                set_last_error(1, "Unknown DocstringFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ExportKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Named
            1 => 1, // Default
            2 => 2, // ReExport
            _ => {
                set_last_error(1, "Invalid ExportKind variant");
                -1
            }
        }
    })
}

/// Convert a `ExportKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Named" => 0,
            "Default" => 1,
            "ReExport" => 2,
            _ => {
                set_last_error(1, "Unknown ExportKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `StructureKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0,   // Function
            1 => 1,   // Method
            2 => 2,   // Class
            3 => 3,   // Struct
            4 => 4,   // Interface
            5 => 5,   // Enum
            6 => 6,   // Module
            7 => 7,   // Trait
            8 => 8,   // Impl
            9 => 9,   // Namespace
            10 => 10, // Other
            _ => {
                set_last_error(1, "Invalid StructureKind variant");
                -1
            }
        }
    })
}

/// Convert a `StructureKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Function" => 0,
            "Method" => 1,
            "Class" => 2,
            "Struct" => 3,
            "Interface" => 4,
            "Enum" => 5,
            "Module" => 6,
            "Trait" => 7,
            "Impl" => 8,
            "Namespace" => 9,
            "Other" => 10,
            _ => {
                set_last_error(1, "Unknown StructureKind variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `SymbolKind` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Variable
            1 => 1, // Constant
            2 => 2, // Function
            3 => 3, // Class
            4 => 4, // Type
            5 => 5, // Interface
            6 => 6, // Enum
            7 => 7, // Module
            8 => 8, // Other
            _ => {
                set_last_error(1, "Invalid SymbolKind variant");
                -1
            }
        }
    })
}

/// Convert a `SymbolKind` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Variable" => 0,
            "Constant" => 1,
            "Function" => 2,
            "Class" => 3,
            "Type" => 4,
            "Interface" => 5,
            "Enum" => 6,
            "Module" => 7,
            "Other" => 8,
            _ => {
                set_last_error(1, "Unknown SymbolKind variant");
                -1
            }
        }
    })
}

/// Free a `CommentKind` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::CommentKind>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `CommentKind` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::CommentKind, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `CommentKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_comment_kind_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::CommentKind, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "CommentKind variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DataNodeKind` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DataNodeKind>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `DataNodeKind` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DataNodeKind, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `DataNodeKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_data_node_kind_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::DataNodeKind, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "DataNodeKind variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DiagnosticSeverity` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DiagnosticSeverity>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `DiagnosticSeverity` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DiagnosticSeverity, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `DiagnosticSeverity` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_diagnostic_severity_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::DiagnosticSeverity, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "DiagnosticSeverity variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocstringFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::DocstringFormat>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `DocstringFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::DocstringFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `DocstringFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_docstring_format_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::DocstringFormat, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "DocstringFormat variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ExportKind` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::ExportKind>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `ExportKind` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::ExportKind, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ExportKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_export_kind_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::ExportKind, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ExportKind variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StructureKind` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::StructureKind>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `StructureKind` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::StructureKind, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `StructureKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_structure_kind_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::StructureKind, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "StructureKind variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SymbolKind` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0 {
            if let Err(error) = remove_handle::<tree_sitter_language_pack::SymbolKind>(handle) {
                set_handle_error(&error);
            }
        }
    })
}

/// Serialize a `SymbolKind` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<tree_sitter_language_pack::SymbolKind, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `SymbolKind` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `ts_pack` function.
/// The returned string must be freed with `ts_pack_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_symbol_kind_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<tree_sitter_language_pack::SymbolKind, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "SymbolKind variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// List all available language names (sorted, deduplicated, includes aliases).
///
/// Returns names of both statically compiled and dynamically loadable languages,
/// plus any configured aliases.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::available_languages;
///
/// let langs = available_languages();
/// for name in &langs {
///     println!("{}", name);
/// }
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_available_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_available_languages", 0);
        let result = tree_sitter_language_pack::available_languages();
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_available_languages", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_available_languages", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("ts_pack_available_languages", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_available_languages` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_available_languages`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_available_languages_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_available_languages"))
}

/// Return the effective cache directory path.
///
/// This is `{base}/tree-sitter-language-pack/v{version}/libs/`, where `{base}` is
/// either the custom BASE directory set via `configure` / `init`
/// (`PackConfig::cache_dir`) or the platform default cache directory â both are
/// suffixed identically, so a custom `cache_dir` is never used as the final libs
/// path. The default resolves to `~/.cache/tree-sitter-language-pack/v{version}/libs/`
/// on a typical Unix system.
/// \note Returns an error if no cache directory can be resolved: `version` is somehow
/// invalid, or (with no custom `cache_dir` configured) the platform reports none
/// and `TREE_SITTER_LANGUAGE_PACK_CACHE_DIR` is unset. This crate no longer falls
/// back to the temporary directory for the latter case â see #101 H2.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::cache_dir;
///
/// let dir = cache_dir().unwrap();
/// println!("Cache directory: {dir}");
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_cache_dir() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_cache_dir", 0);
        let result = tree_sitter_language_pack::cache_dir();
        match result {
            Ok(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_cache_dir", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_cache_dir", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_cache_dir` on this thread.
/// Returns 0 when the primary call returned null or failed before producing a string. Enables safe
/// slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_cache_dir`.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_cache_dir_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_cache_dir"))
}

/// Delete all cached parser shared libraries.
///
/// Resets the cache registration so the next call to `get_language` or
/// a download function will re-register the (now empty) cache directory.
/// \note Returns an error if the cache directory cannot be removed.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::clean_cache;
///
/// clean_cache().unwrap();
/// println!("Cache cleared");
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_clean_cache() -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = tree_sitter_language_pack::clean_cache();
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Apply download configuration without downloading anything.
///
/// Use this to set a custom cache directory before the first call to
/// `get_language` or any download function. Changing the cache dir
/// after languages have been registered has no effect on already-loaded
/// languages.
///
/// `PackConfig::cache_dir` is a BASE directory, not the final libs path: this
/// crate appends `tree-sitter-language-pack/v{version}/libs` to it, the same
/// suffix applied to the platform default cache directory. In the example below,
/// files actually land under `/tmp/my-parsers/tree-sitter-language-pack/v{version}/libs/`,
/// never directly in `/tmp/my-parsers/`. Call `cache_dir` to read back the
/// resolved, fully-suffixed path.
/// \note Returns an error if the lock cannot be acquired.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use std::path::PathBuf;
/// use tree_sitter_language_pack::{PackConfig, configure};
///
/// let config = PackConfig {
///     cache_dir: Some(PathBuf::from("/tmp/my-parsers")),
///     languages: None,
///     groups: None,
/// };
/// configure(&config).unwrap();
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_configure(config: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::PackConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return -1;
        }
        let config_rs_ptr = match locked_handle_ptr::<tree_sitter_language_pack::PackConfig>(&mut __alef_guards, config)
        {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = tree_sitter_language_pack::configure(&config_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Detect language name from a file path or extension.
///
/// This compatibility alias matches the pre-Alef Python binding API.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language(path: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_detect_language", 0);
        if path.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'path'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
        let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::detect_language(&path_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_detect_language", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_detect_language", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_detect_language", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_detect_language` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_detect_language`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_len(_path: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_detect_language"))
}

/// Detect language name from file content using the shebang line (`#!`).
///
/// Inspects only the first line of `content`. If it begins with `#!`, the
/// interpreter name is extracted and mapped to a language name.
///
/// Handles common patterns:
/// - `#!/usr/bin/env python3` â `"python"`
/// - `#!/bin/bash` â `"bash"`
/// - `#!/usr/bin/env node` â `"javascript"`
///
/// The `-S` flag accepted by some `env` implementations is skipped automatically.
/// Version suffixes (e.g. `python3.11`, `ruby3.2`) are stripped before matching.
///
/// A leading UTF-8 BOM (`U+FEFF`) is skipped before the `#!` check, so a
/// BOM-prefixed script is still detected by its shebang.
///
/// Returns `None` when content does not start with `#!` (after stripping a
/// leading BOM), the shebang is malformed, or the interpreter is not recognised.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_content;
/// assert_eq!(detect_language_from_content("#!/usr/bin/env python3\npass"), Some("python"));
/// assert_eq!(detect_language_from_content("#!/bin/bash\necho hi"), Some("bash"));
/// assert_eq!(detect_language_from_content("no shebang here"), None);
/// assert_eq!(
///     detect_language_from_content("\u{FEFF}#!/usr/bin/env python3\npass"),
///     Some("python")
/// );
/// ```
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_content(
    content: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_detect_language_from_content", 0);
        if content.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'content'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees content is a valid pointer; string is valid UTF-8 from caller.
        let content_rs = match unsafe { CStr::from_ptr(content) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'content'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::detect_language_from_content(&content_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_detect_language_from_content", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_detect_language_from_content", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_detect_language_from_content", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by
/// `ts_pack_detect_language_from_content` on this thread. Returns 0 when the primary call returned null
/// or failed before producing a string. Enables safe slice construction in Zig and Java FFM Panama
/// without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_detect_language_from_content`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_content_len(_content: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_detect_language_from_content"))
}

/// Detect language name from a file extension (without leading dot).
///
/// Returns `None` for unrecognized extensions. The match is case-insensitive.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_extension;
/// assert_eq!(detect_language_from_extension("py"), Some("python"));
/// assert_eq!(detect_language_from_extension("RS"), Some("rust"));
/// assert_eq!(detect_language_from_extension("xyz"), None);
/// ```
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_extension(ext: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_detect_language_from_extension", 0);
        if ext.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'ext'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees ext is a valid pointer; string is valid UTF-8 from caller.
        let ext_rs = match unsafe { CStr::from_ptr(ext) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'ext'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::detect_language_from_extension(&ext_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_detect_language_from_extension", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_detect_language_from_extension", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_detect_language_from_extension", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by
/// `ts_pack_detect_language_from_extension` on this thread. Returns 0 when the primary call returned
/// null or failed before producing a string. Enables safe slice construction in Zig and Java FFM Panama
/// without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_detect_language_from_extension`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_extension_len(_ext: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_detect_language_from_extension"))
}

/// Detect language name from a file path.
///
/// Extracts the file extension and looks it up. Returns `None` if the
/// path has no extension or the extension is not recognized.
///
/// ```
/// use tree_sitter_language_pack::detect_language_from_path;
/// assert_eq!(detect_language_from_path("src/main.rs"), Some("rust"));
/// assert_eq!(detect_language_from_path("README.md"), Some("markdown"));
/// assert_eq!(detect_language_from_path("Makefile"), None);
/// ```
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_path(path: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_detect_language_from_path", 0);
        if path.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'path'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees path is a valid pointer; string is valid UTF-8 from caller.
        let path_rs = match unsafe { CStr::from_ptr(path) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'path'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::detect_language_from_path(&path_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_detect_language_from_path", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_detect_language_from_path", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_detect_language_from_path", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_detect_language_from_path`
/// on this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_detect_language_from_path`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_detect_language_from_path_len(_path: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_detect_language_from_path"))
}

/// Download specific languages to the local cache.
///
/// Returns the number of distinct languages available after the call. Already
/// compiled or cached languages are included in the count.
///
/// Aliases are resolved before counting, so `["shell", "bash"]` names one
/// language and returns 1.
/// \note Returns an error if any language is not available in the manifest or if
/// the download fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::download;
///
/// let count = download(&["python", "rust", "typescript"]).unwrap();
/// println!("Ensured {} languages", count);
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download(names: *const std::ffi::c_char) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if names.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'names'");
            return 0;
        }
        // SAFETY: null check above guarantees names is a valid pointer; string is valid UTF-8 from caller.
        let names_rs_str = match unsafe { CStr::from_ptr(names) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'names'");
                return 0;
            }
        };
        let names_rs = match serde_json::from_str::<Vec<String>>(names_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return 0;
            }
        };

        let result = tree_sitter_language_pack::download(&names_rs.iter().map(|s| s.as_str()).collect::<Vec<&str>>());
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Download all available languages from the remote manifest.
///
/// Downloads the platform bundle and extracts every library it contains.
/// Languages that appear in the manifest but are absent from the bundle
/// (e.g. grammars that failed to compile at release time) are silently
/// skipped â they are not treated as an error.
///
/// Returns the total number of languages now available (statically compiled
/// plus downloaded and cached).
/// \note Returns an error if the manifest cannot be fetched or the bundle download fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::download_all;
///
/// let count = download_all().unwrap();
/// println!("{} languages available", count);
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_all() -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = tree_sitter_language_pack::download_all();
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Download every language in a named group.
///
/// Groups are defined by the remote manifest, not by this crate, and let you
/// ensure a curated set of related grammars in one call instead of listing each
/// name to `download()`. Already-cached languages are skipped.
///
/// Call `manifest_groups` to discover the group names the manifest actually
/// defines. The published manifest currently defines a single group, `"all"`;
/// earlier revisions of this documentation advertised `"web"`, `"data"`, and
/// `"systems"`, which the manifest has never contained, so every call following
/// that example failed. Do not hardcode a group name without checking.
///
/// Returns the total number of languages now available (statically compiled
/// plus downloaded and cached).
/// \note Returns `Error.Download` if the manifest cannot be fetched, if the group
/// is unknown â the message lists the groups the manifest defines â or if any
/// constituent language fails to download.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::{download_group, manifest_groups};
///
/// let group = manifest_groups()?.into_iter().next().expect("manifest defines no groups");
/// let count = download_group(&group)?;
/// println!("{count} languages available");
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_download_group(name: *const std::ffi::c_char) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = tree_sitter_language_pack::download_group(&name_rs);
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return languages that are already downloaded and cached locally.
///
/// Does not perform any network requests. Returns an empty list if the
/// cache directory does not exist or cannot be read.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::downloaded_languages;
///
/// let langs = downloaded_languages();
/// println!("{} languages already cached", langs.len());
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_downloaded_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_downloaded_languages", 0);
        let result = tree_sitter_language_pack::downloaded_languages();
        {
            match serde_json::to_string(&result) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_downloaded_languages", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_downloaded_languages", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("ts_pack_downloaded_languages", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_downloaded_languages` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_downloaded_languages`.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_downloaded_languages_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_downloaded_languages"))
}

/// Get the folds query for a language, if bundled.
///
/// Returns the contents of `folds.scm` (used for code folding) as a static string,
/// or `None` if no folds query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_folds_query;
///
/// let query = get_folds_query("rust");
/// // Returns None for languages without bundled folds queries
/// let missing = get_folds_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_folds_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_folds_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_folds_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_folds_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_folds_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_folds_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_folds_query` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_folds_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_folds_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_folds_query"))
}

/// Get the highlights query for a language, if bundled.
///
/// Returns the contents of `highlights.scm` as a static string, or `None`
/// if no highlights query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_highlights_query;
///
/// // Returns Some(...) for languages with bundled queries
/// let query = get_highlights_query("python");
/// // Returns None for languages without bundled highlights queries
/// let missing = get_highlights_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_highlights_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_highlights_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_highlights_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_highlights_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_highlights_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_highlights_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_highlights_query` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_highlights_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_highlights_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_highlights_query"))
}

/// Get the indents query for a language, if bundled.
///
/// Returns the contents of `indents.scm` (used for auto-indentation) as a static
/// string, or `None` if no indents query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_indents_query;
///
/// let query = get_indents_query("objc");
/// // Returns None for languages without bundled indents queries
/// let missing = get_indents_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_indents_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_indents_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_indents_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_indents_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_indents_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_indents_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_indents_query` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_indents_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_indents_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_indents_query"))
}

/// Get the injections query for a language, if bundled.
///
/// Returns the contents of `injections.scm` as a static string, or `None`
/// if no injections query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_injections_query;
///
/// let query = get_injections_query("markdown");
/// // Returns None for languages without bundled injections queries
/// let missing = get_injections_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_injections_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_injections_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_injections_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_injections_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_injections_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_injections_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_injections_query` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_injections_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_injections_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_injections_query"))
}

/// Get a tree-sitter `Language` by name using the global registry.
///
/// Resolves language aliases (e.g., `"shell"` maps to `"bash"`).
/// When the `download` feature is enabled (default), automatically downloads
/// the parser from GitHub releases if not found locally.
/// \note Returns `Error.LanguageNotFound` if the language is not recognized,
/// or `Error.Download` if auto-download fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::{get_language, Parser};
///
/// let _lang = get_language("python")?;
/// let mut parser = Parser::new();
/// parser.set_language("python")?;
/// let tree = parser.parse("x = 1").expect("parse failed");
/// assert_eq!(tree.root_node().kind(), "module");
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_language(name: *const std::ffi::c_char) -> *const tree_sitter::ffi::TSLanguage {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return std::ptr::null();
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return std::ptr::null();
            }
        };

        let result = tree_sitter_language_pack::get_language(&name_rs);
        match result {
            Ok(val) => val.into_raw(),
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null()
        }
    }
}

/// Get the locals query for a language, if bundled.
///
/// Returns the contents of `locals.scm` as a static string, or `None`
/// if no locals query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_locals_query;
///
/// let query = get_locals_query("python");
/// // Returns None for languages without bundled locals queries
/// let missing = get_locals_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_locals_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_locals_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_locals_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_locals_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_locals_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_locals_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_locals_query` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_locals_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_locals_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_locals_query"))
}

/// Get a `Parser` pre-configured for the given language.
///
/// This is a convenience function that calls `get_language` and configures
/// a new parser in one step.
/// \note Returns `Error.LanguageNotFound` if the language is not recognized, or
/// `Error.ParserSetup` if the language cannot be applied to the parser.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_parser;
///
/// let mut parser = get_parser("rust")?;
/// let tree = parser.parse("fn main() {}").expect("parse failed");
/// assert!(!tree.root_node().has_error());
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_parser(name: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = tree_sitter_language_pack::get_parser(&name_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Get the tags query for a language, if bundled.
///
/// Returns the contents of `tags.scm` as a static string, or `None`
/// if no tags query is bundled for this language.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::get_tags_query;
///
/// let query = get_tags_query("rust");
/// // Returns None for languages without bundled tags queries
/// let missing = get_tags_query("nonexistent_lang");
/// assert!(missing.is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_tags_query(language: *const std::ffi::c_char) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_get_tags_query", 0);
        if language.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'language'");
            return std::ptr::null_mut();
        }
        // SAFETY: null check above guarantees language is a valid pointer; string is valid UTF-8 from caller.
        let language_rs = match unsafe { CStr::from_ptr(language) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'language'");
                return std::ptr::null_mut();
            }
        };

        let result = tree_sitter_language_pack::get_tags_query(&language_rs);
        match result {
            Some(val) => {
                let __alef_return = val.to_string();
                match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_get_tags_query", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_get_tags_query", 0);
                        std::ptr::null_mut()
                    }
                }
            }
            None => {
                set_last_return_len("ts_pack_get_tags_query", 0);
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_get_tags_query` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_get_tags_query`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_get_tags_query_len(_language: *const std::ffi::c_char) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_get_tags_query"))
}

/// Check if a language is available by name or alias.
///
/// Returns `true` if the language can be loaded (statically compiled,
/// dynamically available, or a known alias for one of these).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::has_language;
///
/// assert!(has_language("python"));
/// assert!(has_language("shell")); // alias for "bash"
/// assert!(!has_language("nonexistent_language"));
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_has_language(name: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = tree_sitter_language_pack::has_language(&name_rs);
        if result { 1 } else { 0 }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Initialize the language pack with the given configuration.
///
/// Applies any custom cache directory, then downloads all languages and groups
/// specified in the config. This is the recommended entry point when you want
/// to pre-warm the cache before use.
/// \note Returns an error if configuration cannot be applied or if downloads fail.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::{PackConfig, init};
///
/// let config = PackConfig {
///     cache_dir: None,
///     languages: Some(vec!["python".to_string(), "rust".to_string()]),
///     groups: None,
/// };
/// init(&config).unwrap();
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_init(config: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::PackConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return -1;
        }
        let config_rs_ptr = match locked_handle_ptr::<tree_sitter_language_pack::PackConfig>(&mut __alef_guards, config)
        {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = tree_sitter_language_pack::init(&config_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Return the number of available languages.
///
/// Includes statically compiled languages, dynamically loadable languages,
/// and aliases.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::language_count;
///
/// let count = language_count();
/// println!("{} languages available", count);
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_language_count() -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(tree_sitter_language_pack::language_count)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the names of every language group the remote manifest defines, sorted.
///
/// Group names are manifest data, not a compile-time constant of this crate, so
/// this is the only reliable way to learn what `download_group` and
/// `PackConfig.groups` accept. The published manifest currently defines just
/// `"all"`.
/// \note Returns `Error.Download` if the manifest cannot be fetched.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::manifest_groups;
///
/// for group in manifest_groups()? {
///     println!("{group}");
/// }
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_manifest_groups() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_manifest_groups", 0);
        let result = tree_sitter_language_pack::manifest_groups();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_manifest_groups", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_manifest_groups", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("ts_pack_manifest_groups", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_manifest_groups` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_manifest_groups`.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_manifest_groups_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_manifest_groups"))
}

/// Return all language names available in the remote manifest (371).
///
/// Fetches (and caches) the remote manifest to discover the full list of
/// downloadable languages. Use `downloaded_languages` to list what is
/// already cached locally.
/// \note Returns an error if the manifest cannot be fetched.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::manifest_languages;
///
/// let langs = manifest_languages().unwrap();
/// println!("{} languages available for download", langs.len());
/// \endcode
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_manifest_languages() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("ts_pack_manifest_languages", 0);
        let result = tree_sitter_language_pack::manifest_languages();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("ts_pack_manifest_languages", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("ts_pack_manifest_languages", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("ts_pack_manifest_languages", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `ts_pack_manifest_languages` on
/// this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `ts_pack_manifest_languages`.
#[cfg(feature = "download")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_manifest_languages_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("ts_pack_manifest_languages"))
}

/// Prefetch grammars: download any not already loadable from disk, then load every
/// requested language into the process registry so a subsequent hot loop only parses.
///
/// Unlike `download()`, this does not trust in-memory availability â it downloads
/// whenever a grammar is not actually loadable from disk (fixing the case where a
/// known-but-not-downloaded grammar is reported present), then resolves and caches
/// every requested language. Call it once, up front, before a parallel workload.
/// \note Returns `Error.Download` if a required grammar cannot be fetched, or
/// `Error.LanguageNotFound` if a requested name is unknown.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::prefetch;
///
/// prefetch(&["rust", "python", "go"])?;
/// # Ok::<(), tree_sitter_language_pack::Error>(())
/// \endcode
#[cfg(any(feature = "download", not(feature = "download")))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_prefetch(languages: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if languages.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'languages'");
            return -1;
        }
        // SAFETY: null check above guarantees languages is a valid pointer; string is valid UTF-8 from caller.
        let languages_rs_str = match unsafe { CStr::from_ptr(languages) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'languages'");
                return -1;
            }
        };
        let languages_rs = match serde_json::from_str::<Vec<String>>(languages_rs_str) {
            Ok(v) => v,
            Err(e) => {
                set_last_error(2, &e.to_string());
                return -1;
            }
        };

        let result =
            tree_sitter_language_pack::prefetch(&languages_rs.iter().map(|s| s.as_str()).collect::<Vec<&str>>());
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Process source code and extract file intelligence using the global registry.
///
/// Parses the source with tree-sitter and extracts metrics, structure, imports,
/// exports, comments, docstrings, symbols, diagnostics, and/or chunks based on
/// the flags set in `ProcessConfig`.
/// \note Returns `Error.InvalidRange` if the config carries a zero-valued limit or
/// the source exceeds `ProcessConfig.max_source_bytes`,
/// `Error.ParseTimeout` if the parse exceeds
/// `ProcessConfig.parse_timeout_ms`, `Error.LanguageNotFound` if the
/// language is unknown, or `Error.ParseFailed` if parsing yields no tree.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use tree_sitter_language_pack::{ProcessConfig, process};
///
/// let config = ProcessConfig::new("python").all();
/// let result = process("def hello(): pass", &config).unwrap();
/// println!("Language: {}", result.language);
/// println!("Lines: {}", result.metrics.total_lines);
/// println!("Structures: {}", result.structure.len());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn ts_pack_process(source: *const std::ffi::c_char, config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<tree_sitter_language_pack::ProcessConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if source.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'source'");
            return 0;
        }
        // SAFETY: null check above guarantees source is a valid pointer; string is valid UTF-8 from caller.
        let source_rs = match unsafe { CStr::from_ptr(source) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'source'");
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr =
            match locked_handle_ptr::<tree_sitter_language_pack::ProcessConfig>(&mut __alef_guards, config) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = tree_sitter_language_pack::process(&source_rs, &config_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}
