# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:be30954f1d5dcbc66a4117ab916d2bc97c04fdb21f60579d1bc54c936999e791
# To regenerate: alef generate
# To verify freshness: alef verify

[package]
name = "ts-pack-core-node"
version = "1.15.0"
edition = "2024"
license = "MIT"
description = "Pre-compiled tree-sitter grammars for 371 programming languages"
readme = false
keywords = ["language-pack", "parser", "syntax", "tree-sitter"]
categories = []

# `serde_json` is emitted unconditionally above so the manifest is stable
# across regens, but for umbrella crates with no JSON-marshalled return types
# it is genuinely unused. The conditional `async-trait` / `futures-util` deps
# are similarly flagged when the umbrella has trait-bridge / streaming
# adapters configured but no actual async-trait callsite in this binding.
[package.metadata.cargo-machete]
ignored = ["serde_json"]

[lib]
crate-type = ["cdylib"]

[features]
default = ["download"]
download = ["tree-sitter-language-pack/download"]

[dependencies]
napi = { version = "3", features = ["async"] }
napi-derive = "3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tree-sitter-language-pack = { version = "1.15.0", path = "../ts-pack-core", features = ["serde", "config", "download"] }

[build-dependencies]
napi-build = "2"

# Only the diagnostics lints are inherited explicitly; a blanket `[lints]
# workspace = true` would also apply `clippy::all = "deny"` to napi-derive
# macro expansions. ~keep
[lints.clippy]
print_stdout = "deny"
print_stderr = "deny"
dbg_macro = "deny"
