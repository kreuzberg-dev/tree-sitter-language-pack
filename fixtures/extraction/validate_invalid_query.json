{
	"id": "validate_extraction_invalid_query",
	"description": "Invalid query syntax fails validation",
	"category": "extraction",
	"tags": ["extraction", "validation", "error"],
	"call": "validate",
	"input": {
		"config": {
			"language": "python",
			"patterns": {
				"broken": {
					"query": "(this_is_not_a_valid_node @x",
					"capture_output": "Full"
				}
			}
		}
	},
	"assertions": [
		{
			"type": "error"
		}
	],
	"skip": {
		"languages": ["rust"],
		"reason": "Rust e2e generator does not yet support this pattern"
	}
}
