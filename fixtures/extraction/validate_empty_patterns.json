{
	"id": "validate_empty_patterns",
	"description": "validate_extraction() with empty patterns returns valid",
	"category": "extraction",
	"tags": ["extraction", "validate"],
	"call": "validate",
	"input": {
		"config": {
			"language": "python",
			"patterns": {}
		}
	},
	"assertions": [
		{
			"type": "not_error"
		},
		{
			"type": "equals",
			"field": "valid",
			"value": true
		}
	],
	"skip": {
		"languages": ["rust"],
		"reason": "Opaque return type not supported in Rust e2e"
	}
}
