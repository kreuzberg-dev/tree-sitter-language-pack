{
	"id": "validate_extraction_valid_config",
	"description": "Valid extraction config passes validation",
	"category": "extraction",
	"tags": [
		"extraction",
		"validation"
	],
	"call": "validate",
	"input": {
		"config": {
			"language": "python",
			"patterns": {
				"functions": {
					"query": "(function_definition name: (identifier) @name)",
					"capture_output": "Full"
				}
			}
		}
	},
	"assertions": [
		{
			"type": "not_error"
		},
		{
			"type": "equals",
			"field": "valid",
			"value": true
		}
	],
	"skip": {
		"languages": [
			"rust"
		],
		"reason": "Rust e2e generator does not yet support this pattern"
	}
}
